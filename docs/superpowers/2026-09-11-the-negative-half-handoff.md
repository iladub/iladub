# Handoff — the negative half: six `dec` criteria, one shape of work

**Topic:** negative-half
**Serves:** prog:criterion:dec:02 — and dec:09/12/13/15/17, the same work (spec § 1).
**Date:** 2026-09-11. **Loop shape:** originating then executing, in one fresh session; no
working figure reported. **Doc impact: none** (the spec declares its own).

Part 5 written first, before any fixture existed.

## 5. The next concrete action

### 5a. ASSERTED — execute spec § 2 in this session, then open the PR

The spec is `docs/superpowers/specs/2026-09-11-the-negative-half-design.md`. Its § 2 is
mechanical: five fixtures, six tests at node ids the manifest already names, six `prog:met`
flips with `prog:metOn "2026-09-11"`, four rationales corrected, the landscape cache
regenerated, two `.ttl` pins re-measured 149 → 154. The outcome is known; doing it is the work.

### 5b. PROPOSED — every proposed edge on these criteria fails A6 and stays proposed

Spec § 2 predicts M17 does not fire on dec:02→dec:01, dec:12→dec:11, dec:13→dec:11,
dec:15→dec:14 because each new oracle shares its shape FILE with its target. Refuted if
`tests/test_arc_manifest.py` reports M17 on any of them after the flip; then that edge is
asserted in the same change and the ablation grounds it. Runs at task time; § 2 below records
the result.

### 5c. PROPOSED — carried from the register-serves-the-arc plan-ran handoff § 5c

With this handoff on disk the arc line's `serves` first digit leaves zero (`1/3/…` over the
7-day window). The three-loop prediction of that spec's § 3.2 is met on the first loop.

## 1. Where the primaries are

| what | where | what to establish there |
| --- | --- | --- |
| the spec | `docs/superpowers/specs/2026-09-11-the-negative-half-design.md` | § 1 the selection and its measurements; § 2 the rule, the pin, the A6 prediction; § 3 what is not done |
| the fixtures | `tests/dec-confidence-leak.ttl`, `tests/promotion-unaccountable-leak.ttl`, `tests/risk-assessment-contextless-leak.ttl`, `tests/sensitivity-without-reads-leak.ttl`, `tests/permission-without-action-leak.ttl` | each header names the shape, the criterion and the oracle |
| the tests | the six node ids the manifest names (spec § 1 table), appended to `tests/test_vocab_shapes.py`, `test_boundary.py`, `test_risk.py`, `test_governance.py`, `test_hga_alignment.py` | each pins `(focus, resultPath, constraintComponent)` via a module-local `_violations` helper |
| the manifest | `tests/arc-manifest.ttl` — the six `dec` blocks, and the four `prog:dependencyRationale`s at the file's foot | `prog:met true`, `prog:metOn "2026-09-11"`, artifacts in dec:11's form; rationales now say *fails A6* |
| the cache | `docs/superpowers/arc-dependency-landscape.md` (regenerated by `scripts/arc_depends.py`) | `21 ready → 15 ready`; the six rows gone |
| the pins | `tests/test_artifact_terms.py`, `tests/test_artifact_declarations.py` | 149 → 154, dated comment |

## 2. What was measured this session (branch `the-negative-half` off `86340a5`)

- **RED then GREEN.** Five tests RED on the missing fixture file; dec:17's was green on the
  existing axioms (its RED is arm F6). All six GREEN after the fixtures.
- **The first pin was wrong, and the fixture caught it.** The spec's § 2 said "pinned by
  `sh:sourceShape`"; measured, that value on a property-constraint violation is the anonymous
  property shape (a BNode), so the test could name nothing. Re-pinned on `(focus node,
  sh:resultPath, sh:sourceConstraintComponent)`; the spec's rule stands, its named mechanism
  did not. Recorded here and in the helpers' docstrings; the spec is not rewritten.
- **Falsification, six arms, all RED then restored:** F1 confidence `1.5, 0.2 → 0.5`; F2 reviews +
  decidedBy added; F3 subject + context added; F4 reads added; F5 action added; F6 `dec.ttl:38`
  deleted. **F1 carries the measurement the pin exists for:** with the confidence in range the
  bare verdict is STILL `conforms == False` (dec:DecisionHolonShape's minimums fire on the same
  node), so a `not c` test would have stayed green with its subject removed.
- **5b HELD.** `tests/test_arc_manifest.py`: 28 passed on the flipped manifest — M17 did not
  fire; the four edges stay proposed with corrected rationales.
- **The population trap fired on schedule:** the two pin tests failed at 149 with the fixtures
  untracked and pass at 154 after `git add` — the count is of TRACKED files (PR #204 § 7).
- Strip after: `dec ▰▰▰▰ 17/17 … ready 9  serves 1/3/34` (was `11/17 … ready 15  serves 0/3/34`).
- Gates run locally: `test_arc_manifest` 28, `test_arc_landscape` + pins + `test_cockpit` +
  `test_doc_governance` + `test_residue_register_integrity` 70 (after the pin fix: 27 on the two
  pin files), the five oracle modules 33. `test_arc_ablation` was started in the background; § 4
  says whether it finished.

## 3. What was decided, and where that decision is recorded

- **Six criteria in one loop, `Serves:` naming dec:02 with the siblings in prose** — spec § 1,
  last paragraph. Reversible: nothing downstream reads the prose.
- **Pin on `(focus, path, component)`, not `sh:sourceShape`** — the helpers' docstrings and § 2
  above; nowhere else.
- **dec:17 names its four axiom lines as `prog:oracleArtifact`** — the manifest's comment above
  that block explains why they were withheld before and named now.
- **The four rationales are replaced, not appended** — spec § 2 (M18 permits one).

## 4. Unverified or assumed

- `tests/test_arc_ablation.py` — running in the background at the time of writing; its result
  is appended below if it finished before the PR, else CI is the run of record.
- No full-suite run (~61 minutes); CI on the PR is the run of record.
- The spec's `Doc impact: none` assumes the shapes' negative tests are not a released page;
  `mkdocs.yml` was not opened.

## 6. Appended before the PR — the ablation, the direct membrane run, and one deviation from spec § 3

- `tests/test_arc_ablation.py`: **9 passed in 40.48s** on the flipped manifest (§ 4's first
  item is resolved).
- **Spec § 3 said the register gains no row. It gains one — [[R209]].** Looking for the test
  that would have reported M17 on the flipped manifest, the loop found that **no test validates
  the tracked manifest through its membrane at all**: every `validate_manifest` /
  `environment_refusals` caller takes a leak fixture, and no other arc module calls pySHACL.
  So "5b HELD" in § 2 rests on a DIRECT run, not on the suite: `pyshacl.validate(manifest,
  arc-shapes.ttl, advanced=True)` → `Conforms: True`, `environment_refusals(manifest)` → `[]`.
  The row is raised, not fixed, because the gate is its own subject with its own falsification;
  the spec's sentence was wrong and is left as written (the spec is not rewritten under the
  loop; this section is the correction).
