# Handoff — the 92 cells are the right cells: bfs p5 label identity holds, 27/27

**Topic:** The maintainer ruled identity ahead of remedy. It was measured and it **holds**. Every
canton row's col-0 cell is **its own** canton name and its col-11 cell **its own** `en %`, with
exact word-sequence reconstruction on all **46** admitted rows. The +92 headline stands. Two new
rows were raised in passing, and the loop's own pre-registered prediction was **refuted**.

**Serves:** prog:criterion:etkl:05 — bfs. [[R44]] gates it through tab:02, tab:07 and tab:09.

**Date:** 2026-09-16. **Tree:** branch `identity-of-the-92-cells`, cut from `main` at `d85cff7`.
**No shipped file edited. No behaviour changed.** One instrument added, measurement only.

**Doc impact: none.**

---

## 5. The next concrete action

### 5a. ASSERTED — the ruling's question is answered; put the RESULT to the maintainer

Mechanical, and the outcome is known: `2026-09-16-identity-before-remedy-ruling.md` asked
whether the 92 cells are the right cells. **They are** — C1 27/27 on the canton rows, 19/19 on
the year rows, all three controls passing including a shift null that drives C1 to 0/27. The
ruling's falsifier resolves in the first direction it names: *the switch's value stands as
measured, and scope becomes the live question.*

**Scope is the maintainer's** (CLAUDE.md § 8, and the ruling defers it explicitly): blanket,
per-page, or per-document refusal of the decoration universe.

### 5b. PROPOSED — [[R241]] sits upstream of any remedy, and should be measured before scope is built on

**This is typed PROPOSED deliberately, and the previous loop's superseded § 5a is why.** That
section asserted *"nothing further needs measuring"* while its own § 3d said *"NOT shown"* one
hour apart. This loop will not repeat the shape, so: **the identity question is closed, and it
was a ROW-WISE question.** C1 proves every word lands in some column in source order. It does
**not** prove the interior column boundaries are semantically right.

[[R241]] makes that concrete rather than theoretical: **one 12-column universe spans two
different tables.** Col 3 is filled by the 27 canton rows only; col 9 by 18 year rows only. A
remedy designed on *"the switch carries 92 more cells"* would be built over a column universe
whose indices mean different things by row class.

**Why this may be wrong, stated before anyone builds on it:** the fusion **pre-exists the
switch** — the alternating 27/19 fill pattern is present under the decoration universe too, at
46 admitted rows either way — so it is not a cost of the switch and may be judged orthogonal to
the scope decision. If the maintainer rules it orthogonal, scope proceeds without it. **The
falsifying measurement, if it is run:** do the interior cells sit under the header their *source*
column carries, per table?

### 5c. ASSERTED — what NOT to do

- **Do not re-run the identity probe.** The reading rule and instrument were committed at
  `4d95ce5` **before** the figures existed, and all three controls passed. Re-measuring is a
  second loop of the same question.
- **Do not read C1's 27/27 as verifying the columns.** It is row-wise. *"Are alignment's 12
  columns the RIGHT 12"* is **untouched** by this loop and remains open exactly as PR #237 and
  PR #239 left it.
- **Do not use the document score as the oracle here** — [[R240]]; it is an ink-token ratio and
  moved not one digit while 92 cells appeared.
- **Do not cite C3 (final-cell match) as evidence of pairing.** The shift null passed it **2/27**
  on adjacent cantons sharing a growth rate. C1 is the load-bearing check.
- **Do not trust the `tab` rung's `file:line` citations or `FIRES n` counts** — [[R236]],
  [[R237]].

---

## 1. Where the primaries are

- **This loop's evidence** — `2026-09-16-r238-label-identity-evidence.md`. § 1 is the reading
  rule, **committed before the probe ran** (`4d95ce5`); § 1f is the prediction that was refuted;
  § 3 is the result; § 3d is the mis-specified check reported beside its repair; § 3f is what
  this does NOT establish.
- **The instrument** — `scripts/r238_label_identity.py`, § 8 class PROCEDURAL.
  `PYTHONPATH=src:. .venv/bin/python scripts/r238_label_identity.py`
- **The ruling this discharges** — `2026-09-16-identity-before-remedy-ruling.md`.
- **The prior loop** — PR #239 (the gate: +92 cells at zero score cost) and its evidence § 3d,
  the section whose "NOT shown" this loop closes.
- **The rows** — [[R238]] (`residues-open.md`, appended), [[R241]] and [[R242]] (new).

## 2. What was decided, and where it is recorded

| decision | recorded |
| --- | --- |
| **Identity HOLDS — 27/27 canton rows, 19/19 year rows** | evidence § 3c |
| C1 exact word-sequence reconstruction on all 46 admitted rows | evidence § 3c |
| Controls P, U and S all pass; S drives C1 to 0/27 | evidence § 3a |
| C3 passes 2/27 under the shift null — a final-cell check is too weak | evidence § 3a |
| C2 as pre-registered was **mis-specified**; both forms reported | evidence § 3d |
| **The pre-registered prediction is REFUTED** — long names carried whole | evidence § 3e |
| One 12-column universe spans two tables | [[R241]], evidence § 3f |
| Footnote markers fuse into the label cell | [[R242]], evidence § 3e |
| [[R238]] stays **open** — the switch is unbuilt | this handoff, § 4 |

## 3. Unverified or assumed

- **Whether alignment's 12 columns are the RIGHT 12** — untouched. C1 is row-wise. [[R241]].
- **Scope** — blanket vs per-page vs per-document: still UNRULED, deliberately.
- **The premise this corpus cannot falsify** — it holds no page whose decoration rectangle is
  both adopted *and* correct (PR #239 § 5b). Unchanged; attack it first if a blanket remedy is
  designed.
- **Why graincorp-capacity p0 straddles nothing** — still uninvestigated, five loops on.
- **What p5 region16 `DATAGRID_RESIDUE` holds** — still unanswered, six loops on.

## 4. What this session did, and what it cost

Merged PR #240 (the ruling), then ran the identity measurement it ordered, with the reading rule
and the instrument committed before the figures existed.

**The cost worth carrying: I pre-registered a check that was wrong, and the run would have
reported a false 24/27 had C1 not been beside it.** C2 compared a multi-word *cell* against the
line's first *word*, so three correct cells — `'Suisse 3'`, `'Appenzell Rh.-Ext.'`,
`'Appenzell Rh.-Int.'` — read as identity failures. Because C1 (total sequence equality) passed on
all three, the defect localised to the checker in one reading rather than becoming a false
finding about the page. **The lesson is not "pre-register less" — it is that a total check and a
pointwise check disagree informatively, and shipping both is what made the mis-specification
visible.** A probe carrying only C2 would have reported a defect that does not exist.

**A second, smaller one:** my first tally of the register used `grep -o "closed" | wc -l`, which
counts the word wherever it appears in prose (133) rather than in a status cell (71). Caught
before it reached a row's raise-time snapshot, which is the one place it would have been frozen.
