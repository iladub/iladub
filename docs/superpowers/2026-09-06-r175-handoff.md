# Handoff — R175 is closed; what remains of the 5-part fork is the two PROPOSED halves

**Topic:** `R175` closed. `R173`'s CI-visibility half and `R172`'s reading question are untouched.

**Written 2026-09-06**, in the loop that ran action **5b** of
`docs/superpowers/2026-09-05-r173-5a-handoff.md`.

**Part 5 was written FIRST**, before parts 1–4 and before the PR, per CLAUDE.md § *The handoff's
next action is TYPED*. It is typed per action.

**Doc impact: none.**

---

## 5. The next concrete action

The predecessor handoff offered three independent actions and this loop ran one. **The other two
are unchanged, still typed PROPOSED, and are still the menu** — do not re-derive them, read them
at `docs/superpowers/2026-09-05-r173-5a-handoff.md` § 5a and § 5c. Restated only as pointers:

### 5a. PROPOSED (carried forward, unchanged) — price R173's CI half

Make a skipped `pytest.mark.corpus` test LOUD. Still blocked on the same fork the predecessor
named: *loud in CI* (needs the corpus, and whether the third-party PDFs can be fetched there is
open in CLAUDE.md § Open items) versus *loud in the summary* (needs nothing). **Decide which
question you are answering before writing anything.**

**One thing this loop adds to it, and it cuts in the fork's favour:** the corpus-gated tests in
`tests/etkl/test_adoption_document.py` are now 5 of 13, down from 6 of 12, and the module's CI
coverage went 7 → 8. The remaining 5 are corpus tests *about apple*, not corpus-gated versions of
CI-covered claims. So the population 5a would make loud is smaller and more genuinely
corpus-bound than it was when R173 was raised — which makes the *summary* reading cheaper and the
*CI* reading less valuable than it looked.

### 5c. PROPOSED (carried forward, unchanged) — R172, the reading the red test caught

Content-diff apple p1's merged band: the 124 entries `assert_matrix_region` returns against the
56 cells now asserted. **The prediction is that the diff, not the count, decides** whether
`score=1.0, escalated=0` is a correct reading. Closing R172 is what turns
`test_an_adopted_page_never_scores_one_by_construction` green, one way or the other.

**This is the one with a live detector pointing at it**, and it is the only red line in the
module. If you want the loop with the most information already gathered for it, it is this one.

### What NOT to do

- **Do not "fix" `test_an_adopted_page_never_scores_one_by_construction`.** Red on purpose, R172's
  detector; its docstring and R172's row both say so. Unchanged by this loop.
- **Do not touch `currency_marker_escalating_pdf`.** `tests/etkl/test_unit_marker.py` and
  `tests/etkl/test_escalation_wiring.py` both name its SINGLE band in prose. The two-band document
  is a sibling, `currency_marker_escalating_with_note_pdf`, precisely so that stays true.
- **Do not add a second band to the note.** Two- and three-line notes were compiled and rejected
  with the measurement recorded — they book escalated tokens and break the residue equality.

---

## 1. Goal

Close R175: give the document-scope adoption pins a control that runs in the same CI run they do.

## 2. Where the primaries are

| primary | what to establish there |
| --- | --- |
| `docs/superpowers/2026-09-06-r175-the-second-band.md` | **This loop's measurement and result** — the four note shapes compiled and why three were rejected, the `compile.py:1276` mechanism, the falsification, and the before/after runs. |
| `docs/superpowers/residues-closed.md`, row **~~R175~~** | The closure and its evidence. |
| `docs/superpowers/2026-09-05-r173-5a-handoff.md` § 5 | **The live menu.** 5a and 5c are carried forward verbatim; this document does not restate them. |
| `tests/etkl/fixtures.py::currency_marker_escalating_with_note_pdf` | The fixture, and the measured region list in its docstring. |
| `tests/etkl/test_adoption_document.py` | The control (`…_on_the_adopting_page_is_untouched`) and its falsification block. |

## 3. What was decided, and where each decision is recorded

- **A sibling fixture, not an edit to `currency_marker_escalating_pdf`.** Recorded in the new
  fixture's docstring and in the evidence document § 3. Reason: two other modules name that
  fixture's single band in prose.
- **A ONE-line note, chosen so no pin needed re-baselining.** Recorded in the evidence document
  § 2 with the three rejected variants and their measured token counts. The multi-line variant is
  the stronger control in one respect and was still rejected — that trade is stated, not hidden.
- **The apple test was renamed, not deleted.** It had stopped being a control (apple adopts
  nothing, so it has no superseded region to control) while still carrying the name. Recorded in
  its new docstring and in R175's closed row.
- **The control asserts BOTH halves in one run** — that `_superseded(p)` is non-empty *and* that
  the queries agree. Recorded in the test body with a comment saying why both.

## 4. Unverified or assumed

- **The full suite was not run in this session.** Measured here: `test_adoption_document.py`
  (12 passed, 1 red by design), its corpus-free view (8 passed, 5 deselected),
  `test_unit_marker.py` + `test_escalation_wiring.py` (26 passed), and the register / governance /
  ownership / citation tests (20 passed). **The PR's CI is the record for everything else.**
- **The control is one-sided, deliberately.** It catches supersession being applied too WIDELY.
  The narrow stub — deleting the `dec:supersedes` edge — does not fail it, and
  `test_the_effective_reading_of_a_superseded_band_is_not_the_escalated_one` is what covers that
  direction. Stated in the docstring; not independently re-verified in this session.
- **The note band's `NON_TABLE` reason is `'fewer than 2 lines'`**, not "this is prose". If band
  segmentation ever merges the note into the table, the control vanishes — but it fails loudly
  (`assert others, "no unsuperseded band to use as a control"`) rather than passing vacuously.
- **The 60pt gap between table and note is not derived from anything.** It is comfortably clear of
  the 14pt line pitch and was measured to segment, but no oracle disposes it. It is fixture
  geometry, not pipeline geometry, so CLAUDE.md § 8's tuned-constant prohibition does not bite —
  said explicitly so the next reader does not have to decide it again.
- **`is_adoption_candidate`'s refusal on bfs p0/p4 is still observed, not explained** (carried
  from the predecessor handoff, untouched by this loop).
