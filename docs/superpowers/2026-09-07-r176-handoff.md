# Handoff — R176's fork is taken, its own prediction is refuted, and the score's denominator is repaired

**Topic:** [[R176]]. The fork the row demanded be taken *before anything is written* is taken, on
corpus measurement rather than on the two pages the row was raised from.

**Written 2026-09-07**, in the loop that ran action **5a** of
`docs/superpowers/2026-09-07-r172-handoff.md`.

**Part 5 was written FIRST**, before parts 1–4 and before the implementation, per CLAUDE.md
§ *The handoff's next action is TYPED*. It is typed per action.

**Doc impact: none.**

---

## 5. The next concrete action

### 5a. ASSERTED — the row-hierarchical site needs a fixture, and only a fixture

`src/iladub/etkl/compile.py:921` books a subset of its band by the same `data_cols` shape as the
matrix site, and **no corpus document reaches it** (measured: the census' `uri` column shows zero
`#rhtable` bands across all 7 documents). So this loop's corpus oracle is structurally blind to
it, and the ruling taken here — repair it, and pin it with a synthetic fixture — is recorded in
the spec § 3.2 and in the fixture's own docstring.

**Why ASSERTED:** the outcome is known before it is run. The site's accounting is the matrix
site's accounting with a different region type; the helper is the same call; the fixture is a
two-column row-grouped band. Nothing here can come back with a surprise — it is transcription
against a written invariant, which is exactly what an asserted next action is.

**What it is NOT:** it is not "verify the repair works" — O1 already does that on 45 corpus bands.
It is *coverage of the one branch O1 cannot see*.

### 5b. PROPOSED — the `Year: Month` orphan, and whether 3 words is the whole population

This loop makes who-wfa's dropped axis caption **visible** by booking it `escalated`; it does not
carry it. The row raised in the spec § 7 says so.

**The prediction, and it is the falsifiable part:** that the orphan population is *small and
caption-shaped* — that `Year: Month` is a band caption the matrix reader declines to place on
either axis, and that the general repair is a caption rule rather than a geometry change. **Three
words across three pages of one document is a sample of one document**, and the corpus has seven.
The reading that would sink it: orphans are not captions at all but ordinary ink the matrix
reader drops whenever a band's header block is ragged, in which case this is [[R166]]'s shape and
not a caption question.

**Run the cheap disposal first**: `scripts/unbooked_ink_fate.py` prints every orphan's text. Point
it at the corpus and read the words before designing anything. If they are all captions, 5b is a
small loop; if they are not, 5b is the wrong frame and the residue needs re-raising.

### 5c. PROPOSED (carried forward, unchanged) — price R173's CI half

Untouched by this loop, and now the only item of the old menu still standing. Read it at
`docs/superpowers/2026-09-05-r173-5a-handoff.md` § 5a, and the datum
`docs/superpowers/2026-09-06-r175-handoff.md` § 5a adds, and the one
`docs/superpowers/2026-09-07-r172-handoff.md` § 5b adds (the corpus-gated population grew by one,
with a test that pins a ruling rather than a number).

### What NOT to do

- **Do not re-run the fork.** It is taken, for (a), and the reason is structural rather than a
  preference: fork (b) asks an escalated band to partition ink it has just refused to partition,
  and at `compile.py:783` and `:1121` there is no region to consult at all. Spec § 1.3.
- **Do not expect apple p0/p1 to move.** R176 § 5a predicted they would stop reading `1.0`. They
  do not, and that is the loop's headline: their unbooked ink is 100% label-covered, so `1.0` was
  correct all along and is now provable from its own operands.
- **Do not add the helper to the `#htable` branches** (`compile.py:1110`, `:1045`, `:1102`). They
  already book every word of the band; a second call double-books. Measured: who-wfa p0 b3/b5,
  `ink == booked == 76`.
- **Do not re-baseline a synthetic `score == 1.0` pin that breaks.** Spec § 5's O5 rules that such
  a break is a *finding* — the fixture drops ink the old accounting was hiding — not a number to
  update.
- **Do not treat `scripts/unbooked_ink_fate.py`'s `EPS` as a precedent for `src/`.** It is derived
  from `holon.py`'s 2-dp bbox rounding and exists only because a probe reads the graph. The
  shipped helper compares `Word` objects and carries no tolerance at all.

---

## 1. Goal

Take [[R176]]'s fork — the one its row said must be taken *before anything is written* — and, if
the answer permits, repair the denominator in the same loop.

## 2. Where the primaries are

| primary | what to establish there |
| --- | --- |
| `docs/superpowers/specs/2026-09-07-the-denominator-that-moves-design.md` | **The whole argument.** § 1.1 the corpus asymmetry, § 1.2 the refutation, § 1.3 why fork (b) cannot be built, § 2 the § 8 classification and the one deliberate exception in it, § 3.2 the four-site table, § 5 the oracles with their MEASURED results. |
| `src/iladub/etkl/compile.py`, `_book_recovered_ink` + its four call sites | The repair. Each call site's comment names the emitter it MIRRORS, in `holon.py`, with line numbers — that mirroring is the correctness argument, not an implementation note. |
| `scripts/unbooked_ink_census.py` | The instrument that measures the defect and its absence. One run ≈ 12 min on 7 documents; prints the three-verdict table and every dirty band. |
| `scripts/unbooked_ink_fate.py` | The instrument that says where unbooked ink WENT (entry / label / orphan). This is the one [[R178]] must be run with first. |
| `tests/etkl/test_read_band_books_every_word.py` | The pins, one per assert-site, all four RED before the repair. Its module docstring carries the corpus table. |
| `docs/superpowers/residues-closed.md`, row **~~R176~~** | The closure, and the refutation of the row's own prediction. |
| `docs/superpowers/residues-open.md`, rows **R177**, **R178** | This loop's two deferrals, each with its disposal named. |

## 3. What was decided, and where each decision is recorded

- **The fork is taken for (a) — COUNT label ink — and the reason is structural, not a preference.**
  Fork (b) asks an escalated band to partition ink it has just refused to partition, and at
  `compile.py:783` and `:1212` there is no region to consult at all. Recorded in spec § 1.3 and in
  the closed row.
- **Label ink is booked ASSERTED when a carried structure holds it, ESCALATED otherwise** — a
  presence test, never an inference from absence. Recorded in `_book_recovered_ink`'s docstring as
  the § 8 classification, which also states the one place it deliberately does NOT lower to SPARQL
  and invites a reviewer to challenge that on its stated terms.
- **The `#htable` branches are excluded.** They already book every word; a second call double-books.
  Recorded in spec § 3.2, in `test_a_wholesale_branch_is_left_alone`, and in the handoff § 5 above.
- **The repair is strictly additive.** No existing branch's arithmetic was edited, so every
  currently-correct number stays correct by construction. Recorded in spec § 3.2.
- **One operand pin was re-baselined WITH a ruling** (`test_apple_adopts_nothing_because_the_page_asserts_outright`,
  `56 → 98`). Recorded in that test's own docstring, which argues the new assertion is *stronger*
  than the one it replaces rather than merely different.
- **The document score's rise to `0.71875` is labelled NOT-progress in the test that reports it**
  (`test_the_document_score_rises`) — 90 words moved into a numerator; the document read exactly
  what it read the day before.
- **`assert_record_region`'s "LabelCells are structural, not scored facts" comment is corrected in
  place**, saying what stopped being true and when.

## 4. Unverified or assumed

- **The full suite had not finished in this session when the PR was opened.** Measured here:
  `tests/etkl/test_read_band_books_every_word.py` (6 passed; all four site cases RED before the
  repair), the corpus census before and after, and the one corpus test the change moved. The
  `tests/etkl/` run was still in flight. **The PR's CI is the record for everything else** — and
  for the corpus-gated tests CI does not run, nothing is the record, which is [[R173]]'s open half.
- **`O3`'s who-wfa rows were predicted WRONG by this spec** and are corrected in place with the
  reason (two dirty bands per page, one summed). The code matched the corrected arithmetic
  exactly; nothing about the repair was surprising. Recorded in spec § 5.
- **Only 7 corpus documents, 24 asserted bands.** The row-hierarchical site is reached by none of
  them, so its repair rests entirely on `row_grouped_table_pdf` and on the claim that the site is
  the matrix site's shape with a different region type.
- **R178's population is 3 words in 1 document.** The row says explicitly that this cannot
  distinguish "axis caption" from "[[R166]] at a new position", and names the cheap print that can.
- **`Word` equality is assumed total** — that no two distinct words of one band share text and all
  four bounds. Physically impossible; asserted by nothing.
- **`is_adoption_candidate`'s refusal on bfs p0/p4 is still observed, not explained** (carried
  unchanged from the three predecessor handoffs; untouched here).
