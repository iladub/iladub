# Handoff — `R155` is REFUTED into the NEURAL class, and the code was REVERTED

**Topic:** `R155`, from `docs/superpowers/specs/2026-09-01-marked-content-is-not-a-label-design.md`.
The predecessor handoff graded this **PROPOSED** and ordered the population be enumerated before
anything was built. It was — and the enumeration refuted the row's own remedy, then three further
candidates were measured and refuted too. **The fourth was built, passed every page-0 oracle, and
destroyed bfs at document scope. It is reverted: `src/` and `tests/` are byte-identical to
`20cc5b8`.** Branch `r155-the-author-drew-one-label`.

**`R155` stays OPEN, reclassified NEURAL.** Nothing was closed this loop. That is the honest tally.

Part 5 is written first, per `CLAUDE.md` § "The handoff's next action is TYPED".

## 5. The next concrete action — TYPED

### PROPOSED — `R157`, and it is the finding worth acting on before `R155`

`R157`'s row (`docs/superpowers/residues-open.md`) is the primary; **open it, do not plan against
this paragraph.** The C3 design **named its own failure mode in a risk section**, measured it at *0
occurrences in 3613 boundaries* and *byte-identical cell streams on five of seven documents*, and
scoped every one of those figures to **page 0**. The risk was live on **page 6 of the same corpus**.

**Graded PROPOSED, and what must be run before anything is built:** the row proposes a convention
(*a claim of inertness for a per-page rule must be measured per-page*) and, tentatively, a lint. **No
one has measured how many existing claims in this register would fail that convention.** Measure that
first. If most page-scoped evidence here is legitimately page-scoped, the remedy is a convention on
*claims*, not a lint over *measurements*, and a lint would fire mostly on rows that are fine — the
shape `R139`'s census already refuted once (prescribed lint, recall 0/4).

### PROPOSED — `R155` itself, if anyone builds it

**Do not start by designing a proposer.** `R155`'s enriched row now carries a five-sided oracle
(spec §7). Two legs are new and both were discovered by killing a candidate: **O3** bfs p6 band 2
line 0 byte-identical (nine header cells) and **O4** apple p0 data rows byte-identical.
**O1 alone is satisfied by any number of wrong changes — C3 satisfied it while collapsing bfs 324 →
53 cells.**

**What must be run first:** WHO's is the **only confirmed live instance**. Nobody has read apple,
gstem, gcap or ons against their published PDFs looking for a second. Enumerate the defect before
building NEURAL machinery for it — that is `R144`'s recorded failure mode (a real oracle gap with no
live instance behind it).

### ASSERTED — the four refutations, which are this loop's actual product

- **C0 — the row's own scope is REFUTED.** It calls the remedy's population *"tiny — exactly one."*
  Enumerated over all seven documents, page 0, it is **4790**. One line of one band had been read as
  a corpus figure.
- **C1 — the straddling space glyph is REFUTED by WHO's own next line.** `b=125.51` and `b=168.29`
  separate `'Year: Month'` | `'Month'` | `'L'` — legitimate gutters, straddled by a space. 139 would
  fire corpus-wide.
- **C2 — the unbroken glyph chain is REFUTED twice**, and the second generalises: it fires on **23
  apple DATA rows** (253 boundaries, dissolving apple's table), and does **not** fire on WHO row 0 at
  all, because intra-word **kerning** gaps of 0.042/0.064pt and the inter-word gap of 2.758pt
  **differ only in magnitude**. Any predicate separating them is a tuned constant.
  **This upgrades `R155`'s own assertion from asserted to MEASURED, for the geometric half.**
- **C3 — marked content is REFUTED by bfs.** It was exact on WHO (row 0 = one mcid spanning
  `b=594.35`; row 1 = twelve mcids, all 39 boundaries between runs) and on cbh, and the page-0 census
  said 8 declines in 3613 with four documents emitting zero mcids. **bfs's producer tags the entire
  header block — both rows, 109 glyphs, nine labels — as one item `mcid=9`.** Page 6 went **324 → 53
  cells**; `['Grandes régions','Total','0-19 ans',…,'Rapport de']` became one blob. **`mcid`
  granularity is producer-dependent and guarantees nothing.**
- **C3′ — the conjunction was REFUSED, not merely refuted.** Same-mcid **and** straddling-space saves
  14 of bfs's 16 gutters and still welds `'Rapport de'`+`'Rapport de'`. Adding a third clause because
  the first two leave two boundaries wrong is corpus-fitting; the premise had already failed.

### ASSERTED — the one measurement that should change how the next loop reads scores

bfs's document score **ROSE `0.3464 → 0.9401`** under C3 while its `'fewer than 2 columns'` refusals
rose **24 → 34**. The tables collapsed so far they stopped qualifying as regions and left the
denominator. **A large score rise with more regions refused is a collapse wearing an improvement's
clothes** — and it is why `R154`'s closure row was right that score movement is never the oracle.

### PROPOSED — blocked on rulings, unchanged and NOT re-derived

`R156`, `R132`, `R127`, `R131`(b). Open `docs/superpowers/2026-08-31-r154-closed-handoff.md` § 5 and
`docs/superpowers/2026-08-30-four-rows-closed-handoff.md` § 5.

## 1. Goal

Close `R155`: stop the ruled re-extraction splitting a header label at a **word gap**, without
breaking the twelve real column labels one line below it, and without a tuned constant. **Not
achieved.** The goal was shown to be unreachable by any of the four constant-free routes tried, and
the residue was reclassified.

## 2. Where the primaries are

| primary | what to establish there |
| --- | --- |
| `docs/superpowers/specs/2026-09-01-marked-content-is-not-a-label-design.md` | **THE PRIMARY.** §2–§5 are the four refutations with their measurements; §6 is the methodological finding; §7 is the five-sided oracle; §8 is what is NOT established |
| `docs/superpowers/residues-open.md`, `R155` | The enriched row — reclassified NEURAL, carrying all four refutations and the oracle |
| `docs/superpowers/residues-open.md`, `R157` | The page-0-census finding |
| `src/iladub/etkl/geometry.py` | **UNCHANGED.** Byte-identical to `20cc5b8`; `_row_dividers` is still `R154`'s predicate and nothing else |
| git history on this branch | `51bdd23` is the built-then-reverted C3 implementation. Read it only to see what was tried; it is not a starting point |

## 3. What was decided, and where that decision is recorded

- **C3 is reverted rather than narrowed.** Recorded in spec §5.3 and the `R155` row. The reasoning:
  the premise (*"the author drew one label"*) is false in general, so a conjunction that patches 14
  of 16 symptoms restores nothing.
- **`R155` is reclassified NEURAL.** Recorded in the `R155` row and spec §7. Reversible — it rests on
  §4's kerning measurement, which is the falsifiable part.
- **`R157` raised.** Recorded in the register and spec §6.
- **Nothing was closed.** The tally snapshot on `R157` is `(41/147 closed)` — unchanged numerator.

## 4. Unverified or assumed

- **The full unit suite was never run to completion on a tree that survived.** It was started against
  the C3 build, reached 36% with no failures, and was killed when the revert made it moot. `src/` and
  `tests/` are now byte-identical to `20cc5b8`, whose suite `R154`'s handoff records as
  `3 failed → re-based, 1382 passed`; **that is inherited evidence, not evidence from this session.**
  Only `test_residue_register_integrity.py` and `test_doc_governance.py` were run here (10 passed).
- **`/StructTreeRoot` was never read.** C3 consumed `mcid` as an opaque grouping token. A route that
  reads the actual structure tree — where `<TH>` is distinguishable from `<P>` — is **not** refuted
  by §5, because it was never tried. This is the most promising unexplored direction and it is not in
  any residue row.
- **cbh's three prose notes are still split** (`'Dates are based | on Daily Transport…'`). C3
  repaired them; the revert un-repaired them. A genuine fidelity defect, unaddressed, and not
  currently anyone's residue.
- **The cbh page-0 score moved `0.90919 → 0.90878` under C3** and was never explained before the
  revert made it moot.
- **The C2 refutation rests on one reading of "abut".** `b.x0 - a.x1 <= COORD_EPS`. A different
  reading might make C2 fire on WHO row 0 — but it would not rescue C2, which is independently killed
  by apple.
- **No timing was taken for anything this session.** `R39` untouched.
- **No `plimslop` working-token figure exists for this session** — `preflight` reported *"unmeasured,
  no turn recorded for this project"*, and `plimslop reader override` shows no live per-session
  figure. The shape was logged as *originating*, twice.
