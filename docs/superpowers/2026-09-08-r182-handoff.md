# Handoff — R182 closes, and the loop's own suite refuted the loop's own ruling

**Topic:** [[R182]], **CLOSED**. `tab:LabelCoversProvenanceShape` ships, wired into the region
gate, corpus verdict diff **whole-file identical** on 27 pages.

**The part worth reading is not the shape.** The predecessor typed its next action **PROPOSED** on
two facts; both were measured, both surprised. But the second measurement was then
**over-generalised into a ruling that was false**, and this loop shipped code, edited two comments
and raised a residue against it before its own full suite refuted all four. The mechanism, the
enumeration that would have caught it, and why the corpus guard structurally could not, are in
spec § 2.3 — that section is the loop's real product.

**Written 2026-09-08**, in the loop that ran action **5a** of
`docs/superpowers/2026-09-07-r181-handoff.md`.

**Part 5 was written FIRST**, before parts 1–4, per CLAUDE.md § *The handoff's next action is TYPED*.
It is typed per action.

**Doc impact: none.**

---

## 5. The next concrete action

### 5a. ASSERTED — audit the OTHER emitters against the published domain axioms

Asserted, not proposed, because **the defect class is now demonstrated, not hypothesised**, and
the instrument that finds instances already exists and already runs.

This loop emitted `tab:hasBBox` on a node that the published ontology's own `rdfs:domain` says is
therefore a `tab:Cell` — a false assertion in a CC-BY graph (CLAUDE.md § 7), caught only because
`tests/etkl/test_closure_equiv.py` happens to exercise the full-RDFS leg on one page. **There is a
standing instrument for exactly this**: `scripts/probe_domain_range_agreement.py`, built for
[[R61]], which runs all 27 corpus pages and reports UNTYPED / DISAGREE / ONT-VISIBLE /
OUTSIDE-MEMBRANE. R61's row records **14 live DISAGREE nodes** on the data-grid path
(`tab:atColumn → tab:LeafColumn` ×12, `tab:onPage → tab:Cell` ×2) and says the row cannot close
because the ontology-vs-emitter modelling decision is unmade.

**This loop is the argument that the decision can no longer be deferred, and which way it goes.**
R61 has sat open reading as a tidy-up. It is not: it is the same defect this loop committed, in
production, on a different path — an emitter asserting a property whose published domain
contradicts the node's actual type. The action is to **run the probe, take R61's 14, and rule each
one** (fix the emitter, or fix the axiom) — with the ruling grounded on the § 2.3 argument, which
R61's row does not currently contain: *the published axioms bind consumers we do not run, so the
membrane's tolerance is not a defence.*

**THE CHECK WAS RUN, AND IT REFUTED THE PREDICTION — though not in the direction feared.** This
loop's two properties add nothing, as predicted (both are domained on `tab:HeaderSourceCell`, which
is the node's actual type). But the probe no longer reports R61's **14** live nodes. It reports
**32**:

```
$ PYTHONPATH=src python3 scripts/probe_domain_range_agreement.py
range  hasLabel -> LabelCell                     DISAGREE     18  YES       YES        tab.ttl
range  atColumn -> LeafColumn                    DISAGREE     12  YES       YES        tab.ttl
domain onPage -> Cell                            DISAGREE      2  YES       YES        tab.ttl
domain x0 -> BBox                                DISAGREE     12  no        no         tab.ttl
domain x1 -> BBox                                DISAGREE     12  no        no         tab.ttl
…
  UNTYPED/DISAGREE on a class an sh:sparql shape targets: 32   <-- the live hazard
```

**The 18 that moved are the ones R61's row calls inert, and the reason they moved is [[R179]].**
That row states, verbatim, that the other 42 are *"inert for shape reach: 18x `range hasLabel ->
tab:LabelCell` and 24x `domain x0/x1 -> tab:BBox`, whose classes no shape targets."* R179 shipped
`tab:LabelCellPhysicalShape` — `sh:targetClass tab:LabelCell` — on 2026-09-07, and 18 inert nodes
became live. **14 + 18 = 32.** Nothing recorded it, because nothing runs this probe: no test, no
workflow, exactly as R61's row says.

**Do not read that as 18 new defects.** R179 knew about this case and neutralised it in the shape
itself (`FILTER NOT EXISTS { $this a tab:EntryCell }`, its closure row records the full-closure
divergence it was fixing). So the honest statement is narrower and more useful: *the probe's live
count is now 32, of which 18 are neutralised by one explicit carve-out in one shape's SPARQL, and
that carve-out is the only thing standing between them and a refusal.* The action is to **run the
probe, and rule all 32** — 12 + 2 unhandled on the data-grid path, 18 handled by a carve-out that
nothing pins as load-bearing — with the § 2.3 argument in hand: *the published axioms bind
consumers we do not run, so neither the membrane's tolerance nor a shape's carve-out is a defence.*

### 5b. PROPOSED — [[R180]], carried forward untouched for a FOURTH loop

Nothing here touched it. Its fork, prediction and zero-population trap are stated correctly in
`2026-09-07-r179-handoff.md` § 5b; read them there, not any restatement.

**Four consecutive loops have declined it, and that is now the most informative thing about it.**
Its own row says the corpus population is **zero** — no document exercises the path — so it cannot
be settled by a corpus run, which is precisely why every loop that could pick something else has.
The honest next action is therefore **not "do R180"** but *"rule on whether a residue with no
population and no oracle is a residue"*. That is a maintainer question, not an engineering one, and
it should be put as one rather than deferred a fifth time.

### 5c. ASSERTED — [[R173]] gets a SECOND datum pointing the same way as R181's, and a stronger one

The predecessor asked for R181's datum to be recorded: the pin was CI-able, the oracle was not.
**This loop is the same datum with the oracle included**, which is the part R181 could not claim.
All five new pins run on the synthetic `caption_and_wrap_band` fixture with **no corpus at all** —
and so does the *disposal*: the falsifying evidence is `test_the_real_emission_conforms` failing
when the [[R181]] union is reverted, which needs no PDF. The corpus run here was a **guard** and
nothing turned on it.

So the row can now record: *"in a second consecutive case the pins were CI-able, and in this one
the falsifying oracle was too — both times the thing that made it possible was a fixture built for
an earlier loop."* That is a materially different claim from the four predecessors' *"corpus-gated
pins are unavoidable here"*. Read the row at `docs/superpowers/2026-09-05-r173-5a-handoff.md` § 5a.

### What NOT to do

- **Do not re-run the corpus driver to "confirm" this shape.** It ran here (§ 2) and returned
  **whole-file identical**; it is a guard. Exactly one of the 27 pages emits a
  `prov:wasDerivedFrom` on a label at all, so an identical diff is consistent with the shape
  working, doing nothing, and never being reached. The evidence with power is the union-revert
  falsification, and it is a 10-second test run.
- **Do NOT repeat this loop's own first ruling that "adding `tab:hasBBox` trips nothing".** It is
  true of the gate and the doc membrane and FALSE of the repo (§ 2.3). The two R19 comments this
  loop briefly "corrected" are restored, with the refutation added to them. A claim about what an
  inference does needs an enumeration of that inference's consumers, not two conforming
  validations.
- **Do not trust a whole-corpus verdict diff to falsify a claim about a closure.** It reported
  *whole-file identical* on the defective emission, because it runs through the membrane — the one
  leg blind to this by construction. Twice now the corpus run has been a guard mistaken for an
  oracle; this is the sharpest instance.
- **Do not treat `prov:wasDerivedFrom` on a `tab:LabelCell` and on a `tab:RepeatedHeaderRow` as
  one thing.** Same predicate, different subject class, different purpose (`tab:RepeatedHeaderRow` in `tab.ttl` vs this
  loop). Reusing the predicate is right; reusing a shape across them would not be.
- **Do not require `tab:sourceRegion` on every `tab:HeaderSourceCell`.** Every source cell now carries
  one *in practice*, but the shape is conditional on the derivation edge on purpose — a mandatory
  form would infer from absence (CLAUDE.md § 8). Spec § 5.
- **Do not read `test_the_real_emission_conforms` as the shape's only pin.** Under the three
  emission-removal falsifications it stays GREEN, correctly: remove the edge or the box and the
  constraint goes vacuous. Its falsifier is the union revert alone.

---

## 1. Goal

Run action 5a of `docs/superpowers/2026-09-07-r181-handoff.md` — measure the two facts that decide
whether [[R182]]'s shape is buildable at all — and, since both answers made it buildable, build it.

## 2. Where the primaries are

| primary | what to establish there |
| --- | --- |
| `docs/superpowers/specs/2026-09-08-the-box-must-cover-its-provenance-design.md` | **§ 2.3 first — it refutes § 2.1 and is the loop's real product.** Then § 0 the three-way gate classification; § 1 the first question answered NO, with the graph dump, and § 1.1 why the derivation was nonetheless in hand; § 2 the second question answered NO *at the gate and the membrane*, with § 2.1's over-reach left visible and § 2.2 the synthetic probe that could not answer it at all; § 3 the third question nobody asked — the shape is expressible and both engines agree; § 4 the oracle, the corrected account of what reverting actually does, the measured corpus edge census (1 page of 27, 8 edges), and why there is no tolerance; § 5 what is deliberately not done. |
| `src/iladub/etkl/rowrole.py`, `build_row_reading` + `emit_reading_evidence` | The emission, in three places: the `flat`/`at`/`derived` block that records the join's membership; `source_cells` entries widened from `(row, text)` to `(row, cell, derived_into)`; and the emitter writing `tab:sourceRegion` + `tab:sourcePage` on every source cell and `prov:wasDerivedFrom` from `-hl{idx}` to `-hsc{k}`. The docstring states why those two predicates and not `tab:hasBBox`/`tab:onPage`. |
| `vocab/shapes/tab-physical-shapes.ttl`, `tab:LabelCoversProvenanceShape` | The constraint, and the comment stating why it is conditional on the edge, why the source side walks `tab:sourceRegion` rather than `tab:hasBBox`, why there is no `EPS`, and why it is listed in `tiling._PHYSICAL_SHAPE_IRIS` too. |
| `vocab/ontology/tab.ttl`, `tab:sourceRegion` + `tab:sourcePage` | The two minted properties and, in their `rdfs:comment`s, the measured reason they are not `tab:hasBBox`/`tab:onPage`. Read alongside `tab:markerRegion` at  (`tab:markerRegion`), whose restored comment now cites this loop as its proof. |
| `tests/etkl/test_closure_equiv.py` | The instrument that caught it. Nothing was changed here; it is listed because it is the only thing in the suite that sees this defect class, and § 5a is about widening what it implies. |
| `tests/etkl/test_label_provenance_shape.py` | The shape's three pins: the real emission conforms, a shrunk box is refused, a label with no edge is unaffected. All CI-runnable. |
| `tests/etkl/test_rowrole_reading.py` + `test_rowrole_resolution.py`, last tests of each | The membership pin and the two emission pins, including the anti-leak pin (`test_an_unjoined_source_cell_claims_no_derivation`) without which deriving every label from every source cell would pass. |
| `docs/superpowers/residues-closed.md`, row **~~R182~~** | The closure, the two surprises, and the loop's own self-refutation recorded in place. |
| `docs/superpowers/residues-closed.md`, row **~~R183~~** | The residue raised and withdrawn inside this loop, kept struck as the record of the wrong claim and the enumeration that would have prevented it. |
| `docs/superpowers/residues-open.md`, row **R173** | R173's second CI-able datum — this time the falsifying oracle is CI-runnable too, which R181's was not. |

## 3. What was decided, and where each decision is recorded

- **There is no label→source edge; the shape needed a graph change first.** Spec § 1, with the
  dump. Recorded in the closed row.
- **`tab:hasBBox` on a `tab:HeaderSourceCell` trips nothing AT THE GATE OR THE MEMBRANE — and the
  ruling drawn from that ("the R19 mechanism is dead") is REFUTED.** Spec § 2 keeps the
  measurements, § 2.1 keeps the over-reach visible rather than silently corrected, § 2.3 carries the
  refutation, the mechanism, the enumeration that was skipped, and the published-axioms argument
  that outranks the test. Recorded in the closed row, in the restored `_emit_unit_markers` docstring
  and `tab:markerRegion`'s comment in `tab.ttl` comment (both now carry this loop as their evidence), and in [[R183]]'s
  withdrawn row.
- **The source cell's geometry rides `tab:sourceRegion` + `tab:sourcePage`, never `tab:hasBBox` /
  `tab:onPage`.** Recorded in both property definitions in `tab.ttl`, in the emitter docstring, in
  the shape's comment, and pinned by a test that asserts `tab:hasBBox` is ABSENT.
- **The derivation edge is `prov:wasDerivedFrom`, reused deliberately, and it is emitted from the
  label rather than the source cell.** Spec § 1, and the emitter's docstring.
- **The shape is conditional on the edge, never on its absence.** Spec § 5, and the shape's own
  comment in the `.ttl`.
- **There is no tolerance and there must not be one.** Spec § 4.3, and the shape's comment, which
  contrasts it with `unbooked_ink_fate.py`'s `EPS` and says why that one is needed and this one is
  not.
- **[[R183]] is WITHDRAWN inside the loop that raised it**, and kept struck in
  `residues-closed.md` rather than deleted — the record of a wrong claim caught before anything was
  built on it is worth more than a clean register.

## 4. Unverified or assumed

- **The corpus diff is a guard and proves nothing about generalisation.** Whole-file identical on
  27 pages, of which exactly one emits the new edge. Everything else is vacuous by construction.
- **`test_a_label_whose_box_misses_its_provenance_is_refused` doctors a committed graph**, it does
  not reproduce the defect through the pipeline. The pipeline-level falsification is the union
  revert, run separately (§ 5c); under it the gate refuses the whole reading and
  `resolve_header_row_roles` returns `None` — which is stronger than the doctored form and is not
  what that test asserts.
- **The identity map `at = {id(cell): k}`** assumes no `cells.SourceCell` object appears twice in
  one `header_rows`. That holds for every construction site reached today and is **not pinned by a
  test**; a future path that reused one cell object across two rows would mis-attribute its edge.
- **`tab:sourcePage` is taken from `cell.page`**, not from the region's `page` argument. They agree
  everywhere measured; nothing enforces that they must.
- **`tab:sourceRegion`/`tab:sourcePage` add no new domain/range disagreement — MEASURED**, not
  reasoned: `scripts/probe_domain_range_agreement.py` names neither property (§ 5a). What the same
  run did surface is that R61's live count is 14 no longer; it is 32.
- **Only ONE page of 27 emits the new edge**, measured on this emission (8 edges on
  graincorp-stem p0), not carried forward from R181's population. Everything else in the corpus
  guard is vacuous.
- **Only the loop-C / ruled-roles paths emit the edge.** `assert_record_region` and the transposed
  maker mint labels from a single cell with no join, so the shape is vacuously true there — correct,
  but it means those makers are unguarded by it, not guarded and passing.
- **`is_adoption_candidate`'s refusal on bfs p0/p4 is still observed, not explained** (carried
  unchanged from eight predecessor handoffs; untouched here).
