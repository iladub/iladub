# Handoff — R185 closes: the drain is a release act, and the release gate now passes

**Topic:** [[R185]], **CLOSED**. `scripts/release_gate.py` exits **0** for the first time since
2026-08-10, with both blocking documents still declaring `Doc impact: contradiction` verbatim and no
Evidence file edited — the row's own stated closure condition.

**The part worth reading is not the mechanism.** It is that this loop **withdrew one of R185's own
arguments**. The row refused arm 1 because it *"makes Evidence mutable in the one field the gate
reads."* That premise is false — nothing enforces Evidence immutability — so arm 1 had to be refused
on three other grounds, and the false premise became [[R186]].

**Written 2026-09-08**, in the loop that ran action **5a** of `docs/superpowers/2026-09-08-r61-handoff.md`.

**Part 5 was written FIRST**, before parts 1–4, per CLAUDE.md § *The handoff's next action is TYPED*.
It is typed per action.

**Doc impact: none.** (The spec carries the `increment`; this handoff asserts nothing new.)

---

## 5. The next concrete action

R185 was the release loop's first task and it is done. What remains is the rest of the release —
action **5b** of the R61 handoff, unchanged in substance and now with one item discharged.

### 5a. ASSERTED — run `RELEASE.md` steps 1 and 3, which are judgement, not machinery

Asserted because the outcome is not in doubt and doing it *is* the work:

- **Step 1, the promotion queue.** `test_doc_governance.py` reports it as a warning; this loop saw
  **5** queued wiki pages go past (`corpus-harness`, `data-grid`, `dimension-split`,
  `doc-governance`, `table-holon-compilation`). Which to promote is a judgement nobody has made.
  `RELEASE.md` says the queue is *"the visible, enumerable lag … not a blocker"*, so scoping it to
  **zero is a legitimate answer** — but it must be a decision, not an omission.
- **Step 3, the version bump.** Mechanical and guarded:
  `tests/test_smoke.py::test_version_single_source` holds `pyproject.toml`,
  `src/iladub/__init__.py` and `CITATION.cff` in lockstep. The last tag is `v0.0.3` (2026-08-02).

### 5b. PROPOSED — that the release is now one loop rather than two

The R61 handoff budgeted two loops and named four unverified items. Three are now discharged:
`mkdocs build --strict` passes (R61 loop), the contradiction gate passes (this loop), and the full
suite's result for this branch is in part 4. **One remains, and it is the one that fails *after* the
irreversible act:**

- **The PyPI Trusted Publisher** (`RELEASE.md` § One-time prerequisite). Manual, on pypi.org,
  and **nothing in this repo can verify it**. If it was never configured, the tag build deploys the
  site and *then* fails at the publish step with an OIDC error. **Confirm it with the account owner
  BEFORE tagging** — this is the single item that makes the difference between one loop and two, and
  it cannot be measured from here.

Graded PROPOSED because the one-loop claim rests entirely on that unverifiable item. If it is
unconfigured, the tag still half-succeeds and the recovery is a second loop.

### 5c. PROPOSED — the capability claim, carried forward UNCHANGED and still unmeasured

Carried verbatim from the R61 handoff § 5c, and **still not re-measured** — this loop touched no
reader and had no reason to run the corpus:

```
graincorp  0.9654      bfs   0.9401      WHO   0.9096      apple  0.6289
```

These four figures have now been quoted through **four** handoffs without re-measurement. That is
exactly the pattern CLAUDE.md § Deferred residues warns about. **Re-measure before any of them
reaches a published page** — a released page is an Assertion.

---

## 1. Where the primaries are

| what | where | what to establish there |
| --- | --- | --- |
| the ruling | `docs/superpowers/specs/2026-09-08-the-drain-is-a-release-act-design.md` | §3 — why arm 2, and the two corrections to R185's own framing |
| the closed row | [[R185]] in `docs/superpowers/residues-closed.md` | the full closure evidence and the three falsifications |
| the withdrawn premise | [[R186]] in `docs/superpowers/residues-open.md` | a three-arm fork, and the count nobody has taken |
| the register itself | `tests/docgov-drains.ttl` | its header explains why it is not in `docs/superpowers/` and not in the spec |
| the drain's contract | `vocab/shapes/doc-governance-shapes.ttl`, `dg:ContradictionDrainShape` | the five clauses, one negative test each |
| how to record one | `RELEASE.md` step 2 | including the one thing the gate cannot catch |
| the full-suite result | part 4, and the PR's final commit message | whether anything outside the docgov tree moved |

## 2. What changed

New: `tests/docgov-drains.ttl` (the register, 2 drains), five `docgov:` terms in
`vocab/internal/docgov.ttl`, `dg:ContradictionDrainShape` in `vocab/shapes/doc-governance-shapes.ttl`,
9 tests (2 gate + 8 shape, minus one renamed). Changed: one `FILTER NOT EXISTS` in
`vocab/queries/docgov-release-gate.rq`, one `g.parse` in `tests/docgov_extract.py`, `RELEASE.md`
step 2, and three re-measured census pins. `scripts/release_gate.py` is **unchanged** — the rule
stayed in the `.rq`, as its own docstring requires.

## 3. What was decided, and where that decision is recorded

- **Arm 2 wins; arms 1 and 3 are refused.** Recorded in spec §3, and in R185's closed row.
- **The drain register lives in `tests/`, not `docs/superpowers/`.** Recorded in spec §4.1 and in
  the register's own header. The reason is not aesthetic: `docs/superpowers/**` is Evidence with
  exactly two CLAUDE.md-named mutable exceptions, and a third would require editing the Contract.
- **The register load lives in `extract()`, not in the gate runner.** Recorded in the
  `DRAIN_REGISTER` comment (`tests/docgov_extract.py:29-34`). Both of the shape's `sh:sparql`
  constraints are joins against extracted doc facts and are unexpressible in a separate graph.
- **`dg:drains` declares NO `rdfs:range`.** Recorded in its `rdfs:comment`. Range-typing the target
  as `dg:Document` would, under the membrane's `inference="rdfs"`, drag a mistyped target into
  `DocumentShape` and bury the intended message. [[R182]] is the precedent.
- **A drain is not modelled as a `dec:DecisionHolon`.** Recorded in spec §6 as the upgrade path,
  with the reason it was not taken (`contradiction` declared twice in the repo's life).

## 4. Unverified or assumed

- **The full suite was launched detached and its result is in the PR / final commit message.** The
  R61 handoff's trap was respected: `pytest -q` was **not** run inside a tool call (the Bash
  background timeout maxes at 10 minutes against a ~60-minute suite). What returned green before the
  handoff was written: the docgov tree, the gate, the shapes, both declaration instruments, source
  ownership, source citations and register integrity — **81 passed**, re-run *after* the rebase onto
  merged `main`.
- **CI green is NOT full-suite evidence on this repo** ([[R173]]) — the corpus is gitignored, so
  corpus-gated tests skip in CI. This loop touches no reader, so its blast radius on corpus tests
  should be nil; *should be* is the assumption.
- **`scripts/release_gate.py` exiting 0 is not evidence the two wiki pages are correct.** It is
  evidence that a drain was recorded. The pages were read, and both call sites verified present
  (`compile.py:668`, `feed.py:619`), but *"the page no longer contradicts the spec"* is a human
  judgement — spec §3.3 says so, and the gate is designed not to pretend otherwise.
- **The PyPI Trusted Publisher is unverified and unverifiable from here** (§ 5b).
- **The four corpus scores in §5c are quoted, not measured — for the fourth handoff running.**
- **[[R186]]'s "nobody has counted" is literal.** This loop found one post-close Evidence edit by
  accident while weighing arm 1; it did not enumerate. Do not read "one" as the population.
- **A process note.** This loop branched off `main` while [[R185]]'s own row existed only in the
  unmerged PR #172, so the register edit failed with a `StopIteration` on a row that was genuinely
  absent. The fix was to merge #172 first and rebase — but the failure looked for a moment like a
  malformed row. **When a register edit cannot find a row the index promises, check which branch
  raised it before doubting the file.** The rebase also invalidated three freshly-measured census
  pins, which were re-run rather than carried: **measure after the rebase, not only before.**
