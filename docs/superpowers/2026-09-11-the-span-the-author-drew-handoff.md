# Handoff — the span the author drew: etkl:02's spec is written

**Topic:** the-span-the-author-drew
**Serves:** prog:criterion:etkl:02 — continues `2026-09-11-etkl-02-waits-on-r166-handoff.md` § 9b.
**Date:** 2026-09-11. **Loop shape:** spec, written by a fresh session in its first third. The
pre-flight logged `originating` at an estimated 30,000 working tokens (the harness had no recorded
turn to measure). **Doc impact: none.**

Part 5 was written first.

## 5. The next concrete action

### 5a. ASSERTED: execute `plans/2026-09-11-grid-donation.md` as written, in a fresh session

This is spec § 5, step 1. The spec does not change that plan. Its `same_ncols` still refuses
graincorp-capacity, correctly: graincorp belongs to the spanning arm (spec § 3.1, (d)), not to
equal-count donation. The spanning arm reuses the plan's evidence emitter, licence fact and seam, so
they have to ship before a plan for this spec can measure against them.

### 5b. PROPOSED: the plan for this spec opens by running spec § 3.1's prediction

After grid donation ships, the first step of this spec's plan is to measure what Layer A would carry
on graincorp p0: 27 data rows, 9 level-0 nodes over 16 columns, and no numeric label. It must also
measure the two seams named in § 3.1: which band the round-trip sees, and where the donor's ink is
booked. **Refuted in minutes** if the donor's ink cannot be conserved, and then Layer A needs its own
design pass before any task is written. Layer B's population is already measured: 0 on today's
compile, with 8 corpus negatives for its *childless* clause (spec § 3.2).

### 5c. For the maintainer at review: confirm one reading of the ruling

Spec § 6, first bullet. The spec reads the Split ruling as *the contract author names `capacity`,
licensed by the page's title and footer*. If the intent was that the **machine** derives the name
from the title, [[R212]] and a proposer input carrying the page's furniture become prerequisites.

## 1. Where the primaries are

| what | where |
| --- | --- |
| the spec | `docs/superpowers/specs/2026-09-11-the-span-the-author-drew-design.md` |
| the maintainer's ruling on the two unnamed measures | spec § 0 |
| the register | [[R211]]'s index line (✎ SPEC); new rows [[R212]], [[R213]], [[R214]], [[R215]] |
| the reshape and row-group probe | not committed; its figures are in spec § 2.4 and § 3.4 |
| the prior measurements | `2026-09-11-etkl-02-waits-on-r166-handoff.md` §§ 6-9 |

## 3. What was decided, and where it is recorded

- **The measures follow the maintainer's Split ruling:** the tonnage is named by the contract from
  the title and footer, and Y/N is quarantined. It is recorded in spec § 0, and nowhere else.
- **Two layers.** Compile gets spanning donation. The feed gets the key split, disposed by
  `marker_field`. Spec § 3 records it; it rests on an argument, not a measurement (§ 6).
- **Layer A's relation** is grid donation's clauses with (d) `drawn(D) ⊊ drawn(R)` in place of
  `same_ncols`, and no level-1 header nodes. Recorded in spec § 3.1.
- **Reshape is not built on:** it has no recipe for this pivot, and its oracle passes vacuously.
  Recorded in spec § 2.4 and [[R214]].
- **A deterministic "the one field whose constraint admits this value" rule is refused**, in spec
  § 3.3.
- **Sequence:** grid donation first, then one plan for both layers plus the capacity contract, then
  the maintainer's read. Recorded in spec § 5.

## 4. Unverified or assumed

- Everything in spec § 6.
- The working-token figure at the pre-flight is an estimate, not a measurement.
