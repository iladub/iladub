# Closed residues — detail

Full text of every CLOSED row, kept as the evidence of repair and as the other half of the tally's denominator. Struck ids (`~~R92~~`) are closed.

**This file is DETAIL. The index — and the conventions — live in [`residues.md`](residues.md). Find a row by its `R<n>` id.**

| # | Residue | Measured | Why deferred | What would close it |
| --- | --- | --- | --- | --- |
| ~~R1~~ | **CLOSED with R13 (Loop G attempt 2, 2026-07-30)** — it was the same defect | Loop F measured R1 IS R13; the header-confirmed split recovers `Date Loading Completed`/`Commodity`/`Total` as three columns | — | — |
| ~~R2~~ | **CLOSED for the ruled path (Loop F, 2026-07-29)** — padding space glyphs no longer split a contiguous number | Root cause was: source chars CONTIGUOUS (gaps −0.08…−0.03); padding space glyphs OVERLAPPING the digit run were joined in by `rule_aware_lines` | Fixed by a two-presence-test rule (a space glyph exists AND the glyphs it separates are apart). Two magnitude-based hypotheses were measured and REFUTED first — see the Loop F spec §2 | **Still open for the UNRULED path as R16** |
| ~~R4~~ | **CLOSED for the ruled hierarchical path (Loop H, 2026-07-30)** — de-fusion + arithmetic subtotal detection | The hrule veto in `group_wrapped` (author rules are the row delimiters — presence test, no constant) de-fuses every suppressed-key/subtotal line; `rows.detect_aggregation_rows` (justified PROCEDURAL: decidable exact Decimal arithmetic) confirms a sparse candidate iff its all-numeric measure equals the token-sum of the non-aggregation rows back to the previous confirmed same-or-outer-level aggregation (the label's COLUMN encodes the level; label TEXT is never read). Measured on GrainCorp: fused cells **NONE** (was 3 source lines in one record); **17** `tab:DetectedAggregationRow` with correct members incl. the 2-level nesting (`Jul 26 Total` = 4 port groups; `Aug 26 Total` = 21 members; `2025/26 Total` = 32) — score/cells unchanged 0.9496/509. Candidate classification must be STRICT (every token numeric): `'Jul 26 Total'` carries the numeric token `26`, and lenient classification collapsed detection 17→4 (pinned by `test_a_label_containing_digits_is_still_a_label`) | — | **Open narrower forms (final review measured (c')–(e)):** (a) **blank-total subtotals** stay ordinary rows — `Port Kembla Total` (`'-'`) is arithmetically unverifiable and vacuous `0==0` confirmation would mark any sparse row over blanks (honest refusal, measured); (b) **unruled suppressed-key documents** keep the fusion defect — the hrule veto is inert without hrules; (c) **the row-hier path** (`assert_row_hier_region`) is unwired — `subtotals_row_group_pdf`'s reconciling `Total` rows (10+20=30, 15+25=40) still compile as data records via that path; (c') **the record path too** — `totals_table_pdf` compiles via `assert_record_region` and still mints `['Total','220','240','460']` as a data record (measured by the final review); worse, that Total row is **DENSE** (label + every measure column), so the 2-cell sparsity candidate bar makes the dense-total shape structurally unreachable by the detector on *any* path — closing (c)/(c') needs a widened candidate definition, not just wiring; (d) **the false-positive direction is inherent**: a sparse reference row whose number coincidentally equals the running sum (`see p.250` over 100+150) confirms — arithmetic-only detection has no defense, named honestly; (e) **single-member confirmation is degenerate but legitimate** — a row repeating the one measure above it confirms as a 1-member "sum" (`Mackay Total` over one data row is the measured real case; not fixable without losing that recall). The row-group *hierarchy* (Month > Port `coversRow` tree) is its own future loop, now unblocked |
| ~~R13~~ | **CLOSED for the ruled path (Loop G attempt 2, 2026-07-30)** — rules coarser than the columns | Attempt 2: interior-gutter boundaries are CANDIDATES, confirmed only when the header region places char ink strictly on both sides within the author interval with no straddling glyph (`confirm-boundary.rq` AXIOM, no numeric literal). Measured: GrainCorp 15→**17** header labels (`Date Loading Completed` \| `Commodity` \| `Total` separate; cells 509, score 0.9496); attempt 1's counter-example compiles graph-ISOMORPHIC to main; the PLAIN-HIERARCHICAL crash class is closed by the membrane backstop (`REGION_TILING_FAILED` escalates in-band; the record/transposed paths were gated by Loop J, 2026-07-30). **Honest cost:** candidates are *generated* by `refine_rule_columns`' inherited constants (`gutter_pct`/`min_gutter_bins`), so R13's **recall** is bounded by generation, not by the AXIOM — in one of the counter-example's three intervals it was the generation threshold, not header confirmation, that prevented a split | **Remaining narrow form, honest by construction:** a genuinely unlabeled sub-column (header ink one side only, yet truly two columns) is indistinguishable from the counter-example and stays merged — asserting it would be exactly attempt 1's defect. No measured document exhibits it | The attempt-1 post-mortem section below stands as history; attempt 2 resolved it |
| ~~R19~~ | **CLOSED (Loop R41 Task 5, 2026-08-05)** — both physical shapes (`tab:EntryCellPhysicalShape`, `tab:WrappedCellShape`) now join `region_tiles`' CBD extraction (`_PHYSICAL_SHAPE_IRIS` in `src/iladub/etkl/tiling.py`), so a region defect expressible only in a physical shape now REFUSES at the gate (escalates) instead of raising at final validation | Loop R41 Task 3 (2026-08-05): once R41's `IndexError` was fixed, the apple document's p1#mtable4 region reached final validation and CRASHED with an `AssertionError` — exactly this row's pre-existing, previously-unreachable class, now REACHABLE for real on a shipped corpus document (a `ROUND_TRIP_FAIL` candidate carrying a bbox + empty `cellText`, RDFS-typing as `tab:Cell` via `tab:hasBBox`'s domain, tripping `tab:WrappedCellShape` outside the gate). François adjudicated: extend the loop rather than defer again. Loop R41 Task 5 (2026-08-05): `_PHYSICAL_SHAPE_IRIS = [TAB.EntryCellPhysicalShape, TAB.WrappedCellShape]` added to the gate's CBD extraction; apple now compiles end to end (score 0.0106, 3 pages, p1's mtable4 escalates `MATRIX_AMBIGUOUS` instead of crashing); battery `test_expected_verdict[financial/apple-fy2026q3-statements.pdf]` PASSES (Unadjudicated); stem score 0.9655 unchanged, CBH/stem pins all pass (13/13); measured healthy-gate cost **0.095 s/call**, *below* this row's stated pre-extension baseline (~0.26 s) — flagged as load-variance-suspect (a background pytest run may have still been executing during the timing probe), not presented as a clean speedup. **Fix round 1 (commit `78be731`):** the gate extension broke two `tests/etkl/test_row_groups.py` synthetic fixtures (`test_partial_derived_coverage_passes_the_row_tiling_shapes`, `test_two_confirmed_groups_over_the_same_member_rows_still_tile`) whose `_emit` helper stamped `tab:cellText`/`tab:hasBBox` on every `EntryCell` but never `tab:onPage`, now required (`minCount 1`, `xsd:integer`) by `tab:EntryCellPhysicalShape`; fixed by stamping `tab:onPage` in the helper — no shape, gate, or assertion touched; `test_row_groups.py` 22/22 green afterward | — | **The honest residual, named not hidden:** the alternate closure this row originally offered — dropping the bbox from `ROUND_TRIP_FAIL` candidates so they stop typing as `tab:Cell` — was not needed and was not done; a candidate typing as `tab:Cell` via `tab:hasBBox`'s domain inference now REFUSES at the gate rather than crashing at final validation, which is the honest direction (§7: never crash, always at worst escalate). **Root closure (loop-subclass, 2026-08-06):** the hazard's *mechanism* is now gone at the root — a node can no longer type as `tab:Cell` merely by carrying `tab:hasBBox`, since the membrane no longer materialises domain typing; pinned by `test_subclass_closure_drops_domain_typing`. Measured on the real apple document, this was an **active defect, not a latent one**: two `iladub:CandidateConcept` escalation nodes (`mtable4-cc0_2`, `mtable4-cc1_2`) carrying a bbox but no `cellText` were typing as `tab:Cell` under full closure and tripping `tab:WrappedCellShape`, suppressing apple p1's region 4 (8 cells) until this loop — see the subclass-only-closure spec §7. Nothing further to close |
| ~~R29~~ | **CLOSED (Loop M Task 3, 2026-08-02)** — page N-1's CONFIRMED header-block reading is now CARRIED onto a page the continuation AXIOM recognized, matched row-by-row by exact per-column text identity, and the redrawn block is recorded as `tab:RepeatedHeader` facts (never data) | Measured on the live stem via `compile_document`: pages 1-2 went from `REGION_TILING_FAILED` to **asserted** (p1 825 tokens / 0.9706, p2 741 / 0.9674), the document scores **0.9655** over **2152** cells in ONE chain of 3, and 6 `tab:RepeatedHeader` rows on pages 1-2 trace by `prov:wasDerivedFrom` to page 0's own header-source cells (`tests/test_corpus_stem.py::test_stem_document_stitches_three_pages`) | — | **Residual, measured and correct, NOT a reopening:** a continuation page compiled STANDALONE (`compile_tables` on page 1 alone) still escalates — standalone it really is a header block with no table under it, and no recognition has licensed anything. Pinned by `test_stem_continuation_pages_status`. The carriage is a document-level act by construction (see R33: it fires only where recognition AND — since loop O, 2026-08-03 — the continuation LICENCE allowed the pair; the case-3 exposure it used to inherit is closed for marked documents, and remains only for markless identical pages) |
| ~~R33~~ | **CLOSED FOR MARKED DOCUMENTS (Loop O, 2026-08-03) — the continuation LICENCE.** Recognition no longer stitches on its own: a recognized pair must ALSO be licensed by `vocab/queries/continuation-licence.rq` (the second continuation AXIOM — open-world derivation over presence facts, with **no numeric LITERAL**: no digit is ever read as data, the ordinal cancellation happening entirely at emission; digits in the query text are variable suffixes like `?n2` and clause labels in comments (the PREFIX IRI w3id also carries a digit)), and `compile_document` gates ALL of it behind that licence — the carried header reading, `tab:continuesTable`/`tab:continuesColumn`/`tab:inLogicalColumn`, the chain, the document-level arithmetic window, and the document-level row groups (the last two need no second guard: they iterate `chains` of length > 1). **The law (V4, François's call after V1–V3 were measured and rejected):** (a) **HEAD SIDE, STRICT** — every non-table block ABOVE page N's table has a TEXT-IDENTICAL counterpart on page N−1; (b) **TAILS, INVARIANT MODULO THE PRINTED PAGE ORDINAL** — every block BELOW EITHER page's table has a counterpart on the other page after cancelling WHOLE TOKENS equal to that block's own printed ordinal (cancelled at emission by `document._ordinal_normalized`, so the AXIOM stays digit-blind); blocks above page N−1's table are UNCONSTRAINED. That asymmetry is the asymmetry of the CUT — a document's opening furniture is legitimately drawn once — and a symmetric law refuses the stem's genuine stitch (measured, task 2). **Refusals are recorded, never discarded:** `DocumentReport.refused_licences`, the graph fact `cur tab:licenceRefused prev` (same direction `tab:continuesTable` would have had), and `tab:LicenceRefusalShape` making the two verdicts exclusive over one pair (negative test `tests/test_tab.py::test_licence_refusal_and_stitch_are_exclusive`). **Face by face:** face 1 (the false `tab:continuesTable` link) refused for a marked document; face 2 (the record-identity weld) was already closed in loop M task 4 by unconditional page-qualification, untouched here; face 3 (page N−1's confirmed header reading carried onto an unrelated table) is now bounded **structurally** — the carriage branch lies INSIDE the licensed arm, so a refused pair never reaches it; `tab:RepeatedHeader == []` on the refused fixture, though that fixture never engages carriage anyway (it compiles down the RECORD_TABLE path, a single-level header that never reaches loop L's header-stack law), so the bound is by construction, not by that measurement; faces 4–5 were MEASURED before the fix and are gone after it (next column). The original defect statement stands as history: its evidence is the REPEATED HEADER BLOCK, and two logically independent tables built from one template share that evidence exactly, so `continuation-of.rq` licenses a stitch between them and `tab:continuesTable` is asserted between tables that are not one table | **LOOP O, 2026-08-03 — faces 4–5 measured on REAL false-stitch compiles BEFORE the fix** (Task 1, both variants of `case3_with_subtotals_pdf`, through `compile_document`; page 0 one row Qty=0 / label `Alpha`, page 1 two rows + a `SUB` row confirming page-locally 100+150=250, differing per-page banners): **face 5 reproduces in BOTH directions.** *Fabrication* (matching labels) — one document-level `tab:DerivedRowGroup` keyed `"Alpha"` is derived (`groups_derived=1`) whose `tab:coversRow` set is `[p0#htable1-r0, p1#htable1-r0, p1#htable1-r1]`, i.e. **page 0's row — an unrelated, logically independent table's own row — is silently absorbed into page 1's subtotal group**. *Loss* (conflicting labels) — page 1's legitimately-earned page-local group is torn down unconditionally once the false chain forms (`groups_superseded=1`) and the document-level re-derivation REFUSES to replace it (`groups_derived=0`; `row-group-key-logical.rq`'s single-distinct-key guard fails on `Alpha`/`Beta`), so the group **vanishes with no replacement**. **Face 4 did NOT retract on this fixture** (`page_confirmed == document_confirmed == 1`, `retracted=0`) — by construction: page 0's Qty=0 keeps the widened walk-back sum unchanged so face 5 could be observed alone. Reported as a non-reproduction rather than re-engineered; the attribution was then VERIFIED fireable by the reviewer's parametrized probe (nonzero page-0 qty → `retracted=1` in 4/4 runs), so the mechanism is real and this fixture isolates it. **AFTER THE GATE (Task 3, same fixture, both variants):** `recognized=((0,1),)` **unchanged** (recognition still sees the template repeat — the licence is a second question, not a weakening of the first), `refused_licences=((0,1),)`, `tab:continuesTable=[]`, `tab:licenceRefused=[(p1,p0)]`, chains = two SINGLETONS, `arithmetic=()`, `tab:RepeatedHeader=[]`, 4 records on 4 distinct subjects — and page 1's `SUB` row **KEEPS its page-local confirmation**: the `groups_superseded=1` loss is gone because no document window forms. **The stem (the genuine case-2) is BYTE-IDENTICAL through the whole loop** — both pairs LICENSE (`refused_licences=()`, 0 `tab:licenceRefused` facts): score **0.9655**, **2152** cells, **one chain of 3**, **133** records / **585** grounded / **1265** quarantined, missing-year-key **0**, ledger **41/62/0/21**, 29,377 merged triples; nothing was tuned to reach it (the law is task 2's V4 at the round-2 token boundary, untouched by task 3). Pins: `tests/etkl/test_continuation_licence.py::test_marked_case3_does_not_stitch` (both variants) and `tests/etkl/test_document.py::test_template_pages_are_refused_by_the_continuation_licence` — the renamed, INVERTED former `test_template_pages_stitch_the_known_case3_false_positive`, whose simulated-unstitch block was deleted because loop O removes the stitch for real. **The historical measurement of the defect:** Loop M Task 2 review, 2026-08-02: the reviewer built a two-page PDF with the same `Store \| Item \| Qty` header on the same grid, DIFFERENT data on each page, and even DIFFERENT per-page banners — measured `recognized=((0,1),)` and `tab:continuesTable` asserted. The specimen's genuine case-2 stitch (`recognized=((0,1),(1,2))`) is indistinguishable from it on header evidence alone | Spec §2b defers taxonomy case 3 legitimately, and at Task 2 the consequence is INERT (recognition only; the pages still compile independently). It becomes ACTIVE with Task 3's carried header reading (page N-1's confirmed reading applied to an unrelated table) and Task 4's chain-walk (independent records merged into one identity space) — so it must be registered before those land, not after. **ACTIVATION MEASURED, 2026-08-02 (Task 4 review, F5)** — no longer future tense: the chain-walk shipped, and the fixture's two independent tables ARE now walked as one logical table, in one identity space. What that did, measured on the pinned fixture at the FEED: before record ids were page-qualified, 4 records collapsed onto **2** distinct subjects (`Alpha/Bolt/10` welded to `Gamma/Screw/30`); after, 4/4. So the false stitch's *identity* consequence was real and is now closed, while the false stitch itself stays open. **Note the dependency, and do not lose it when closing R33:** removing the false link is exactly the case that made the pages read independently again, which BEFORE Task 4's fix re-opened the same weld (measured: unstitched, 4 records → 2 subjects). It no longer does, because qualification became unconditional rather than chain-conditional (F1); the pin is `tests/etkl/test_document.py::test_template_pages_stitch_the_known_case3_false_positive`, which now asserts 4/4 subjects BOTH stitched and with the link removed | **THREE RESIDUALS REMAIN, and NONE reopens face 1.** **(1) Bare identical pages still stitch — invariant-CONSISTENT, not a defect.** Two markless byte-identical template pages carry NO page-invariance evidence that distinguishes them, and a fluent reader reads two indistinguishable bare tables on consecutive pages as one continued table; *vacuity is licence*, deliberately (query header; pinned by `test_bare_identical_still_stitches` and `test_bare_identical_evidence_licenses`). The law refuses on MARKS and invents none — closing this would mean asserting a distinction the document does not carry (§7). **This is the honest boundary of the closure: R33 is closed for MARKED documents, not for markless ones.** **(2) The narrowed permissive class of clause (b):** two tails differing ONLY by a STANDALONE token equal to each page's OWN printed ordinal still license, whether or not that token really is a page number. The honest worst example is `TOTAL 1` / `TOTAL 2` — the F1-motivating below-table subtotal shape in its simplest form — alongside `Rev 1` / `Rev 2` and `Note 1` / `Note 2`. **ADV-A narrows it sharply, and the narrowing is measured and PINNED (2026-08-03, `test_licence_law_licenses_a_standalone_ordinal_token_only_at_that_ordinal`, which pins both `TOTAL 1`/`TOTAL 2` and `Rev 1`/`Rev 2` in both directions):** the residual bites ONLY where the mark COINCIDES with that page's own ordinal — the same two tails at ordinals 5/6 **REFUSE**, and every non-coinciding number already refuses at the token boundary (`TOTAL 1000`/`TOTAL 2000`, `Lot 11`/`Lot 22`, `Subtotal 141`/`Subtotal 242`, `Store A1`/`Store A2` — all pinned). Reachability is therefore low, and the class is irreducible without READING the text (which is NEURAL, §8, and not what this AXIOM does). **(3) The licence has no compile verdict** (`licence_evidence`'s own docstring names this), so every non-paired band is emitted as a text block regardless of whether it is furniture or a second table: this is LIVE and HARMLESS on the shipped stem, where the chain head is `p0#htable2` so `p0#htable1`'s text is emitted as a prior-head `AboveTableBlock` and licenses only because prior-head blocks are unconstrained, but it is a GENUINE FALSE-REFUSAL class in the mirror position — a real last-page continuation followed by a second table or a new section below it emits `BelowTableBlock`s that clause (b) constrains, so the pair refuses (conservative: false refusal, never false stitch). **Also carried, and untested by construction:** a refused pair where one page asserted no table records the pair on `refused_licences` but asserts no `tab:licenceRefused` fact — it mirrors the stitch path's identical `if prev_uri is None or cur_uri is None: continue` guard, and no fixture in the repo produces a recognized-refused pair with a non-asserting page. **HISTORY — the closure route that was proposed, and how loop O actually took it:** the licence IS the body-side discriminator below, in its page-invariance form (it excludes page-invariant furniture by ANSWERING it across the pair rather than by naming it), and it is the reason the naive "runs to the last text line" form was never built. A **body-side presence discriminator**, which is available and is NOT text reading: a paginated table is cut BY the page edge, so page N-1's table runs to that page's LAST text line, whereas an independent table ends before it with a banner or whitespace after. Expressible as presence/ordering facts over lines already extracted (does any text line follow the earlier table's last body row?) — no threshold, no distance, so it belongs in the AXIOM as a further clause. If a document defeats the geometric form, the named fallback is NEURAL banner reading (propose → oracle dispose), never a tuned constant. Measured falsification of the naive form: a page-invariant footer line sits below the table band on every specimen page (573.87 vs table bottoms 456.51/569.07/510.75), so 'runs to the last text line' refuses the genuine stitch. The discriminator must FIRST exclude blocks that repeat on EVERY page (page-invariant furniture — the same repeated-block evidence the law already emits), THEN ask whether the earlier table's last body row ends the page.  **Fourth face — RESOLVED for a marked document by the licence (loop O; recorded here as history)** (Loop N Task 2 review, 2026-08-03): a wrong window does not merely fail to confirm — it DESTROYS page-confirmed facts. The constructed retraction case (`page_local_group_two_page_pdf` fixture, `test_retracted_witness_takes_its_derived_group`) shows a correct page-local subtotal (and its derived group, and the record identity built on it) retracted because a false stitch's document-level walk-back reaches rows that were never its operands — the exposure is subtractive as well as additive. Measured INERT on the real stem (0 retractions across p0/p1/p2); the pinned case-3 fixture itself carries no aggregation candidates, so nothing about a REAL false stitch is measured here — only the mechanism is demonstrated. **Fifth face — RESOLVED for a marked document by the licence, after being measured for real (loop O; history)** (Loop N Task 3 review, 2026-08-03): on a false chain WITH subtotals, record CONTENT itself (not just typing) is exposed, in two silent, opposite directions — a CONFLICTING page-2 label makes the derived key non-unique so NO group is derived at all (a page-local reading's key is silently lost), while a MATCHING label INJECTS page-1's key into unrelated page-2 rows (fabrication). Again unmeasured on any real false stitch at the time — the pinned case-3 fixture had no aggregation candidates. **Loop O task 1 built exactly that missing fixture** (`case3_with_subtotals_pdf`) and measured both directions on real compiles, which is what the measured column now quotes; the gate then removed them. |
| ~~R35~~ | **CLOSED (Loop N, 2026-08-03)** — the closure boundary was lifted from the PAGE to the LOGICAL TABLE, exactly as R35's own "what would close it" specified: `reconcile_chain_arithmetic` (task 2) confirms subtotals over the chain's row sequence in page order (PROCEDURAL, §8's second class — decidable exact arithmetic, only its holon changed), and `_derive_document_row_groups` + `_supersede_page_groups` (task 3) re-derive row groups and inject group keys from the document-confirmed result | Measured on the stem: p2 aggregations 0→**21** confirmed (bar ≥20), with cross-page operands (pinned: at least one; task-level probe measured all 21) (**57** cross-page `tab:aggregates` edges, chain `p0#htable2 -> p1#htable1 -> p2#htable1`); key coverage now reaches every page (missing `GC Fin Year` 112 (pre-loop: 50 p1 + 62 p2) → **0**); records 154→**133** (21 p2 subtotal rows stopped minting records); grounded 567 (pre-loop) → 546 (once those records left the denominator) → **585** (chain-wide keys); quarantined 1194→**1265**; record identity is now **ONE kind** — a group path, at most one page-scoped disambiguation suffix, never in a non-final position (`test_stem_record_identity_is_one_kind`) — not the three page-determined kinds R35 measured, with exactly **one** exception (a bare-discriminator id with no group label at all — R4-family, see R40 below); whole-graph SHACL conforms (29,377 triples, ~41–47 s); score `0.9654553611484971`, byte-identical to the pre-loop baseline; full suite 787 passed / 5 skipped / 0 failed | — | **Residual, measured and correct, NOT a reopening:** R33's fourth and fifth faces (above) describe what a FALSE chain does to this same machinery — **measured for real in loop O (2026-08-03) on `case3_with_subtotals_pdf`, then removed for marked documents by the continuation licence** (face 5 both directions; face 4 non-reproducing on that fixture by construction, its attribution verified by probe); R37–R40 (below) register what the closure itself left open — the retraction-semantics modelling choice, the headerLevel-after-retraction residual, the `row-group-nesting.rq` cost it now dominates, and the Grand-Total zero-member refusal |
| ~~R42~~ | **CLOSED (Loop Q Task 7, 2026-08-04)** — CBH's Excel-print house style, both stacked gaps closed to what actually measured, per loop Q's own design amendment (this row's prior "what would close it"): the repair re-homed at SECTION scope (Tasks 2-4, superseding loop P's band-scoped attempt below), section-key attribution injected as candidate concepts and used to prefix record identity (Task 5), and the three-arm naming cascade recovering the denormalized column name (Task 6) — all measured end-to-end on the real document in Task 7 | Corpus battery, 2026-08-04, `test_expected_verdict[ag-trade/cbh-stem-2026-08-03.pdf]` (measured outcome PASS as `Unadjudicated` — compiles without crash or hang, per spec §4's Unadjudicated gate; the low score is the adjudication evidence, not a test failure); the two producer strings are read from the tracked register itself, `tests/corpus-manifest.ttl`'s `cor:producer` facts (the `cbh-stem-2026-08-03` entry's `cor:producer "Microsoft® Excel® for Microsoft 365"` vs. the `graincorp-stem-2026-07-31` entry's `cor:producer "Microsoft: Print To PDF"`). **Structure adjudicated (François, 2026-08-04, measured on the page-0 render):** the document is ONE master table **dimension-split-as-denormalization** (spec §2b taxonomy case 3, in INTRA-page form) — per section: a bare key heading (`GERALDTON`/`KWINANA`/`ALBANY`/`ESPERANCE`), berth-notice furniture lines, an **identically repeated 3-line header stack** (loop M's repeated-header signature, intra-page), data rows carrying **values without the key**, and a per-section total (`374,904`, `737,289`) that is loop-H arithmetic-confirmable. Two STACKED gaps: (a) the borderless Excel 3-line header stack escalates `MERGE_AMBIGUOUS`; (b) the section KEY must be attributed to its rows and the denormalized column **NAME** is nowhere in the document. **Loop Q Task 7 close (2026-08-04), measured end-to-end (`tests/test_cbh_e2e.py`):** gap (a) — score 0.0698 → **0.9047** (0.9046563192904656); `repaired_bands = ((0,1),(0,3),(0,5),(0,7))` (all 4 escalated bands recognized, repaired via the ink-witness path, and adopted); chains **[4, 1]** (GERALDTON/KWINANA/ALBANY/ESPERANCE stitched into one 4-member logical table via `tab:continuesTable`, plus the unrelated stock/maintenance-notice table as a singleton) — **CLOSED**. Gap (b) — grounding: records=58, grounded=134, still-quarantined=775, every `GroundedNode` exactly one `wasPromotedBy`; 49 records section-prefixed and distinct across the four ports (Task 5's attribution, now measured on the real document, not just its unit fixture); the naming cascade (Task 6), fed the record-identity prefixes `['ALBANY','ESPERANCE','GERALDTON','KWINANA']`, resolves `port` via the **unique-admitting-field** AXIOM arm (`ambiguity_score=1`, one `iladub:PromotionDecision`, no LLM call — a raising proposer proved the short-circuit); the 8 notice-only `is_section_marker` texts carried on the same records measured disjoint from that marker set — **CLOSED for attribution + naming**. §4.2's GRAPH-level key form (`tab:DerivedRowGroup`) stays open separately as **R49** (not overclaimed here); the per-section printed totals (§4.1 arithmetic, `374,904`/`737,289` above) measured **zero** `tab:SectionTotal` facts on the real document — that residual is R47's (trailing-strip peel/weld) and R50's (LAST-row-only oracle), not this row's | — | **Not a reopening — two residuals this closure measured but did not itself close:** (1) §4.2's GRAPH-level key form stays open as **R49** (loop Q Task 5, 2026-08-04) — Task 7 shipped only the feed-level positional attribution this row's gap (b) needed to close; the graph-form deferral is R49's, not re-litigated here. (2) CBH's per-section printed totals (§4.1 arithmetic) did not confirm on the real document — Task 7 measured **zero** `tab:SectionTotal` facts on the real compile; see **R47** (the trailing-strip peel/weld mechanism, still unbuilt) and **R50** (the total-confirmation oracle's LAST-row-only scope). (3) The attribution/naming close itself rests on an unstated assumption — the section heading is drawn ABOVE its notices, true on CBH, unchecked — registered separately as **R54** (review round 1, 2026-08-04); the cascade's own disposal degrades a violation honestly (quarantine, never a false assert), so this is not a soundness gap in what closed, only a generalization one. For the record, the loop-P history this row previously carried in full (three reverted witness rounds, `1271156`, the `_build_ruled_band`-is-the-wrong-scope finding) is preserved verbatim in git history at this row's pre-close revision — not repeated here to keep the closed row legible |
| ~~R55~~ | **CLOSED (loop-quantity, 2026-08-06) — WITH ITS ATTRIBUTION CORRECTED.** This row originally claimed `transpose_is_coherent` failed **SOLELY** because row 2 typed `[Numeric, Text, Numeric, Text]` (the parenthesized negatives). Measured through the production seam (`compile.page_bands` — a hand-rolled band loop gives a different, wrong answer), that attribution is off by one link: fixing the parens alone changes **nothing** — band 4 still has `looks_transposed=True` and is still `transpose_is_coherent=False`, i.e. still escalates `TRANSPOSED`. The real chain: the `Currency`-vs-`Numeric` split is what makes **no column type-homogeneous**, which is what makes `looks_transposed` fire in the first place; the parens fail only the *second* gate (the coherence oracle), reached only once the first gate is cleared. Four-state measurement (design spec §2): **none (today)** — `looks_transposed`=True, `transpose_is_coherent`=False, escalates TRANSPOSED; **parens only** — True, False, unchanged (still escalates); **currency only** — False, not asked, takes the record path; **both** — False, not asked, asserts, 20 cells. Both fixes shipped together in loop-quantity for exactly this reason: the currency-family fix alone clears the region past the first gate, and the paren fix alone is inert without it. **Also discharges the unit-marker spec's §6.1** (the `$ 45,781`-mixed-with-`45,781` homogeneity question): answered by the declared `tab:Quantity` family (`tab:Numeric` and `tab:Currency` both `tab:inDatatypeFamily tab:Quantity`), which normalises the two forms to one type for homogeneity purposes | Loop-quantity Tasks 1/3/4, 2026-08-06: simulated through `compile.page_bands` before implementation (design spec §2 table), then re-measured live on apple p0 after implementation — band 4 asserts, 20 cells, page score 0.0000 → 0.1170, document score 0.0326975477 → 0.0606860158; corpus byte-identity held on stem/CBH/capacity/WHO (`tests/test_corpus_stem.py` + `tests/test_cbh_e2e.py`, 13 passed in 248.79 s) | — | Closed; nothing further. (R55's originally-registered "what would close it" — a `tab:AccountingNegative` clause retyping the parens as a signed Numeric — is NOT what shipped: the design instead adds `tab:ParenthesizedNumber` as an ABSTAINING wildcard, per design spec §3.1, because the form is format-identical to a footnote marker `(1)` and no grammar can separate them — abstention is the honest §7 reading, not a sign-flip) |
| ~~R58~~ | **CLOSED (loop-subclass, 2026-08-06)** — subclass-only closure replaces owlrl's full RDFS closure | Closure timing **1.311 s → 0.047 s** (~28×). The membrane's new dominant cost is rudof's n-triples parse (~0.58 s on an 8k-triple page) — carried forward as R60 below. Measured blast radius of what full closure was adding that subclass-only does not: 2,105 vacuous `rdf:type rdfs:Resource` triples, 207 ontology-node types (`Class`/`ObjectProperty`/`DatatypeProperty`/`Property`/`Ontology`), and 18 `tab:LabelCell` types via property domain — **none targeted by any shape**. `tab:Cell` loses nothing, because every cell is explicitly typed at emission | — | R58's mandated `sh:class` case (the final-review addendum: `tab:hasBBox rdfs:range tab:BBox` made `EntryCellPhysicalShape`'s `sh:class tab:BBox` unfalsifiable under full closure) shipped as `test_rudof_implements_sh_class_once_range_typing_is_gone` in `tests/etkl/test_closure_equiv.py`, with the fixture corrected mid-loop after a review found it confounded by unrelated MinCount violations |
| ~~R72~~ | **CLOSED (loop-data-grid, 2026-08-08)** — the score now consults the page-level grid gate: `denom == 0` scores 1.0 only when the page carries no table, and 0.0 when it does. Originally: **an empty reading scored a perfect 1.0** — `compile.py`'s `score = 1.0 if denom == 0 else asserted/denom` gives a page that asserts nothing AND escalates nothing a 1.0000, so the metric cannot distinguish *nothing to read* from *failed to read a table* | loop-data-grid, 2026-08-08: measured across the corpus, **11 of 27 pages** score a degenerate 1.0 — bfs p1-p3 and ons p0-p3/p5/p6 correctly (prose the grid gate discards), but **ons p7 and p8 are real tables that produced zero cells and scored 1.0**. The data-grid fallback now reads 276 cells on each and the score stays 1.0000 either way | Fixing the score changes every corpus number and the manifest floors; it needs its own loop and its own adjudication | Consult the grid gate's page-level verdict, which already separates the two cases (17 of 27 pages carry a table) and which the score does not read. Until then no reading improvement on an empty page can be expressed as a score |
| ~~R92~~ (raised at 12/82 closed) | **CLOSED 2026-08-12 — the 39 float-valued `xsd:decimal` emitters are converted, and a tripwire prevents the 40th.** The defect: `Literal(round(x, 2), datatype=XSD.decimal)` keeps a Python **float** as `.value` while claiming `xsd:decimal`; pySHACL's check is `isinstance(value, Decimal)` so it REFUSES, while rudof sees only the serialized lexical form and ADMITS (**rudof is spec-correct**). It was masked only because no shape typed a coordinate — and the *default* install was the refusing side (`pyshacl` core at `pyproject.toml:27`, `pyrudof` only an extra), with `ci.yml:22` always installing the admitting one, so no CI gate could have caught it | Live proof before the fix on graincorp-stem p0 (8668 triples, 586 `tab:BBox`): `sh:datatype xsd:decimal` on `tab:x0` alone gave **pySHACL `False` / rudof `True`**, and an n-triples round trip of the same graph flipped pySHACL back to `True`. Three independent agents measured the corpus-wide split at **13 of 27 pages**, 9384/9384 coordinate literals in the bad form | — | **DONE, with its evidence.** 39 sites converted to `Literal(Decimal(str(round(x, n))))`: `holon.py` 21, `datagrid.py` 6, `gridregion.py` 4, `ruledroles.py` 4, `compile.py` 4. **Behaviour is unchanged — a 27-page before/after census shows 0 diffs in score, asserted, escalated, triples and coordinate count, with float-valued coordinates 9384 → 0.** 35 of the 39 are provably byte-identical (200008 random values, 0 lexical or datatype mismatches); the other 4 are `compile.py:249-252`, the only **unrounded** site, which gained the `round(x, 2)` its 35 siblings already had. Tripwire: `tests/etkl/test_decimal_typing.py` — emitter invariant, both-engines-agree under a typed-coordinate shape (unit + real page), a non-vacuity guard, and a source lint against reintroduction. FALSIFIED: inverting `_bbox_node`'s four lines fails 3 of the 7 tests. **NOT closed by this, still true:** `sh:targetClass tab:BBox` under-covers (`datagrid.py:635-636` hangs coordinates on `tab:GridColumn`, contradicting `tab:x0 rdfs:domain tab:BBox`, `tab.ttl:111`); placement decides crash vs silent collapse (`tiling._PHYSICAL_SHAPE_IRIS` is a hardcoded allowlist); committed `.ttl` fixtures cannot detect this class at all (Turtle `220.0` parses to a Decimal-valued literal); and `tab:onPage`'s `xsd:integer` is safe only by type coincidence |
| ~~R93~~ (raised at 12/83 closed) | **CLOSED 2026-08-12 with R92** — `to_rdf.py:79,112` now mint `Literal(Decimal(str(round(cc.confidence, 6))))` | It was never live, and that was established by measurement before anything was changed: static import closure (`compile` and `feed` never reach `to_rdf`; `m4`, its only caller, reaches neither membrane), the literals go to `ExtractionGraph.propositions` which `m4.py:75` does not validate, and a probe over every validator seam recorded **zero** confidence hits. Converted anyway because `iladub:confidence` is one of only two `sh:datatype xsd:decimal` constraints in the shipped shapes (`iladub-shapes.ttl:25`), and validating the `#propositions` databook block against it is one obvious step away | — | Pinned by `test_to_rdf_mints_a_decimal_valued_confidence`, which exists **because R92's source lint structurally cannot catch this form**: `cc.confidence` is a bare float variable, not a `round(...)`/`float(...)` call, so no syntactic pattern matches it. Corrected while closing: `holon.py:375` was reported as a third bad-form site — it was one at `c674ec5`, and `59d46d1` had already fixed it |
| ~~R91~~ | **CLOSED 2026-08-12 — the driver's duplicate `dec:decidedBy` is deleted; `emit_data_grid` is now the single emitter.** `document.py` no longer writes the triple (nor imports `_READER_AGENT`); the comment above the block now records where the attribution lives and why | Same subject verified before touching anything: `datagrid.py:620` and `document.py:1497` both mint `{grid_uri}-admission`. After the deletion, `scripts/measure_dec_membrane.py --scope both` returns **CONFORMS, 0 refusing focus nodes over every scope and closure** (SHIPPED and RDFS), `tests/etkl/test_adoption_document.py` is 10/10, and `tests/etkl/` plus `test_corpus_stem.py` are **776 passed, 2 skipped, 1 xfailed** | — | **The row deferred this as harmless by RDF set semantics. That was true and beside the point: the duplicate was MASKING the real emitter.** With both lines present, deleting `datagrid.py:706` broke no test at all — the attribution looked pinned and was not. FALSIFICATION, with the duplicate gone: removing `datagrid.py:706` now fails at the MEMBRANE, `dec:decidedBy` minCount 1 refusing at document scope (`document.py:1517`), not merely in a unit test. Restored, green. The general lesson worth keeping: a duplicate that is invisible in the data is not harmless if it is the reason a test cannot fail |
| ~~R88~~ | **CLOSED 2026-08-13 (loop-membrane-parity, `df8d6c4`) — skolemized at the payload seam; `compile._DEC_ENGINE` deleted.** rudof cannot evaluate an `sh:sparql` constraint whose focus node is a blank node — it RAISES instead of returning a verdict — so the decision shapes were pinned to pySHACL at both membranes. The pin was a workaround for an engine incapacity, not a design preference, and split the membrane's one-engine story | loop-decision-membrane, 2026-08-10. rudof binds `$this` through `VALUES $this { _:b… }`, which is illegal SPARQL, and raises `ValueError: SHACL error: … Parsing query error: error at 3:54: expected UNDEF`. Isolated on one graph against `dec-shapes.ttl`+`iladub-shapes.ttl`: **IRI focus** → pySHACL `True` / rudof `True`; **BNode focus with `sh:sparql` present** → pySHACL `True` / rudof **raises**; **BNode focus with the `sh:sparql` triples stripped in memory** → rudof returns `True`/`False` correctly. Core constraints are unaffected; BOTH `pyrudof.ShaclValidationMode.Native` and `.Sparql` raise. It bit because `dec-shapes.ttl:23` and `iladub-shapes.ttl:67` are `sh:sparql` and the emitters mint blank nodes (`ground.py:90,145`, `promote.py:67,114,158`, `splitkey.py:125`). Found by `tests/etkl/test_rowrole_integration.py::test_caption_wrap_report_asserts_with_a_resolving_proposer`, **not** by the corpus battery, which supplies no proposers and therefore mints no `CandidateConcept`. The pin's real cost was measured (2026-08-11) at a GROUNDED document: stem p0 grounding membrane = pySHACL 1.70 s vs skolemize (100 ms) + rudof 0.59 s ≈ 2.5x, same verdict — so the honest argument for unpinning was always the one-engine story (`ILADUB_MEMBRANE=rudof` raised at the pinned call), not raw per-document cost | — | **DONE.** `_payload`/`_payload_nt` skolemize the data graph before serialization (`Graph.skolemize(authority=_SKOLEM_AUTHORITY, basepath=_SKOLEM_BASEPATH)`, `membrane.py:348`) — rudof's engine incapacity is routed AROUND, not fixed: MEASURED on rdflib 7.6.0, `Graph.skolemize(authority=…)` urljoins the given authority against an ABSOLUTE default basepath, so a custom authority's path is discarded and a `urn:` authority yields an illegal relative IRI; the fix needed an explicit authority **plus** a relative basepath, yielding `https://w3id.org/iladub/.well-known/genid/membrane/N…`. Reports are de-skolemized back to `_:` labels at the public seam (`_deskolemize`) before reaching a caller, never inside the leg functions. `compile._DEC_ENGINE` is deleted; no call site pins an engine any more. **Headline:** `ILADUB_MEMBRANE=rudof pytest tests/etkl/test_membrane_equiv.py tests/etkl/test_decimal_typing.py -q` → **44 passed** — this exact invocation RAISED at the pinned call before `df8d6c4`. Skolemize is verdict-neutral in both engines (admits the conformant graph, still REFUSES an under-furnished decision) and is **not decision-graph-only**: a real page carries **586 blank nodes**. rudof itself did **not** gain the capability — the upstream incapacity is still pinned directly, on an un-skolemized serialization, by `tests/etkl/test_membrane_equiv.py::test_rudof_itself_still_cannot_evaluate_sparql_on_a_blank_node_focus`; that test's own failure is the signal the skolemize step could be reconsidered. Measured cost of the extra parse on a real 8.6k-triple page: **170 ms** — the spec's earlier 0.2 ms figure was a tiny-graph measurement and licensed no claim about a real page |
| ~~R94~~ (raised at 12/84 closed) | **CLOSED 2026-08-13 (loop-membrane-parity, `ba92ccc`/`444ddc3`) — one N-Triples artifact, both legs.** The two membrane legs were not fed the same artifact, so `membrane.validate`'s "the engine is the only variable" was FALSE — and the whole two-engine equivalence battery was blind to any divergence that depended on a literal's in-memory value | 2026-08-11, verified by reading both legs and by measurement. `_validate_pyshacl` hands pySHACL the **live rdflib graph** (`membrane.py:97`, pre-fix); `_validate_rudof` hands rudof **`expanded.serialize(format="nt")`** (`membrane.py:158`, pre-fix). An n-triples round trip re-parses `"307.47"` into a real `Decimal`, so the serialization boundary silently *repaired* exactly the defect in R92 — measured: the same stem p0 graph gave pySHACL `conforms=False`, and `True` after a round trip, with rudof `True` throughout | — | **DONE, but NOT as this row's own remedy prescribed — and that refutation is the record, not smoothed over.** `membrane._payload` now builds ONE N-Triples artifact and both engine legs consume it (`membrane.py`, spec 2026-08-13-membrane-parity-design.md §4.1). This row's own "what would close it" said: *"feed both legs the same artifact … then re-run the equivalence battery and confirm every leg still agrees."* **They do not agree, and the prescribed remedy is REFUTED, not merely incomplete: the parser is inside the engine boundary, so "the engine is the only variable" is unachievable, not just false as measured.** Given byte-identical N-Triples input, rdflib's own parser rewrites `"5e-05"^^xsd:decimal` to `0.00005` on the way back in (exponential notation is outside `xsd:decimal`'s lexical space per the XSD spec) — so pySHACL, which only ever sees rdflib's already-parsed `.value`, judges a repaired value `True`, while rudof, which reads the lexical bytes as written, correctly judges `False`. **rudof is spec-correct here; pySHACL's parser is what silently launders the defect.** "Same artifact" was never sufficient, because an artifact bottoms out at whatever parses it, and the parser IS the engine. What actually closes the practical gap is Task 2 (`5f73732`/`d5ecab1`): `audit_literals`, a raising guard for the two literal-defect classes byte-parity hides (TYPE — `.value` type vs datatype, R92's class; LEXICAL — non-canonical lexical form, this row's `5e-05` class) — it makes such input unreachable in production before either engine ever sees it, which is *why* the one-engine story holds in practice despite the two engines disagreeing on an input the guard now forbids. Guard licence, measured: fast suite **1148 passed / 7 skipped / 1 xfailed**; corpus battery **36 passed**, **zero** violations from any `src/` emitter. The `membrane.py:48-53` docstring this row named as wrong is corrected — `validate`'s docstring now states the parity contract accurately at `membrane.py:45-57`: *"Both engines receive the same N-Triples document; each parses it with its own parser; parser differences are engine differences."* Also closes with R88 (`df8d6c4`): both were changes to what the payload-building seam hands out, decided together as this row anticipated |
| ~~R102~~ (18/92 closed) | **CLOSED 2026-08-17** (loop `the-gate-and-the-label`, `b09dbd1`) — the `dec` leg is unconditional at document scope. `document._legs_for_document` names the legs the document membrane runs: `dec` is in the result for every input combination, `tab` keeps `recognized or section_facts` bit-for-bit, and the page gate (`compile.py:1097-1100`) is untouched. **Re-measured by the same method that raised the row** — spy both `compile._validate` and the name `document.py:110` binds at import, all 7 documents, `validate_shapes=True`, with the `seen` set **reset per document**: a GLOBAL set reports a false 0, because every document compiles under the default `_DOC` so page-scoped decision URIs collide across documents (union of all 7 = 428 vs per-document sum = 769, the row's own figure). Result: **769 minted, 769 validated, 0 NEVER**, against **769 / 453 / 316** with the gate restored by `git stash` in the same session — reproducing bfs 113 and ons 203 exactly. Document-scope `_validate` calls **4 -> 7**; page-scope calls **14 -> 14**, so the page gate is demonstrably unmoved. Cost on the identical workload: **+2.8 s** across the three documents that newly open the dec leg (graincorp-capacity 10.1->10.4, bfs 24.0->25.5, ons 8.4->9.4) against ~320 s for the 7-document compile — not material, and nothing tuned (E2's "~4 s" was the both-legs figure and is not the cost of this change). Both legs conformed on all three, so the adjudication branch never opened and no compile raised. `pytest -q`: **1228 passed, 7 skipped, 1 xpassed**. ✎ **AMENDMENT (2026-08-17, spec §2.6):** the "Why deferred" cell below calls this fix *"a membrane-composition question."* **It is not one**, and the principle is now stated: *a gate change is in scope; a wiring change is not.* Ungating alters only **when** an already-wired shape set runs over graphs it already targets — composition is untouched and coverage can only grow. R99 (does `iladub:NoLeakShape` belong on this membrane **at all**?) is the wiring question, and stays open. Original text follows, uncorrected: **41% of the decision holons the compile path mints are NEVER validated by `dec-shapes.ttl`** — the promotion epistemics' own audit trail crosses no membrane for most of a document | 2026-08-17, measured by spying on `_validate` across all 7 corpus documents with default `validate_shapes=True` and taking the union of decision holons any call actually saw: **769 minted, 453 ever validated, 316 NEVER** (ons 203/218, bfs 113/232; the other five documents 0 never). At page scope, of **27** distinct pages (31 `compile_tables` calls), **14** have the page gate FALSE — and only **13** of those carry decision holons no membrane ever sees, because apple p1's 37 holons *are* validated at the **document** leg. Those 13 pages' holons sum to 113 + 203 = **316**, cross-checking the headline exactly. ⚠️ **CORRECTED 2026-08-17** (empirical review E1, `docs/superpowers/2026-08-17-empirical-review-findings.md`): this row previously read *"16 of 27 corpus pages carry decision holons no membrane sees"* — the count did not reproduce (it is 14, not 16) and the sentence encoded a false inference, *page gate FALSE ⇒ unseen*, which apple p1 refutes. The 316 headline is unaffected. Cause: BOTH `_validate` call sites are gated on tab-facts existing — `compile.py:1097-1103` runs only `if validate_shapes and (any tab:RecordTable or any tab:HierarchicalTable)`, `document.py:1584-1587` only `if validate_shapes and (recognized or section_facts)`. The grounding leg (`feed.py:586`) adds no coverage: it validates `ground.py`/`promote.py` output, not `BandRecorder`'s. `dec:DecisionHolonShape` is LIVE and NOT registered idle (119 focus nodes on apple) — **so this is not vacuity; it is a live shape that most of the corpus never reaches.** | Raised out of the R89 measurement, which needed it to decide whether a producer-side guard was a duplicate; it is a larger finding than R89 and was recorded nowhere. Fixing it means deciding whether a document with no tab-facts should still validate its decision holons — a membrane-composition question, and a possible perf cost on the 41%. Not adjudicated here. | Either ungate the `dec` leg from the tab-fact condition so every minted decision is validated (and measure the cost), or a measured finding that the ungated 316 cannot violate `dec:DecisionHolonShape` by construction — which must be shown, not assumed, since `BandRecorder.record`'s own guards (R89) are today the only check on them. **Note the coupling: R89's answer depends on this row.** A guard that looks like a membrane duplicate is the sole enforcement for 41% of its output. ⚙️ **SCOPE SEAM ANSWERED 2026-08-17** (plan 2026-08-17 Task 2, seam 2 — recorded here and not only in the task report, because it bounds what the close covers): the fix is at DOCUMENT scope, so a caller running `compile_tables` alone with the page gate false still validates no decision holon. Measured, `grep -rn 'compile_tables' src/ scripts/ tests/`: the only **production** call sites are `document.py:1241`, `:1304` and `:1441`, and all three are inside `compile_document` (which starts at `:1142`, with no intervening `def` — `grep -n '^def '`). So **no production caller invokes `compile_tables` outside `compile_document`**, and the document-scope fix reaches every holon the library itself mints. The residual is external only: `compile_tables` is publicly exported (`etkl/__init__.py:13,32`), so a third-party caller can bypass the document leg. Not raised as a new row — it is the same shape as R34's already-registered "an external caller can bypass the in-repo call-graph invariant." `scripts/probe_emitter_typing.py:124` calls it with `validate_shapes=False` and so is unaffected either way. |
| ~~R104~~ (18/94 closed) | **CLOSED 2026-08-17** (loop `the-gate-and-the-label`, `742e862`) — `_validate` carries the leg identity and the raise sites stop hardcoding `tab`. Signature is now `_validate(graph, legs=("tab", "dec")) -> tuple[bool, str, tuple[str, ...]]`; the third element lists exactly the refusing legs in `legs`' order and is `()` when conforming. Both raise sites — `compile.py:1126` and `document.py:1626` — build their message with `_refusal_message(subject, legs, text)` and keep their own subject noun (`"asserted holon"` / `"document-level facts"`). Pinned by four tests in `tests/etkl/test_compile_membrane_shapes.py`: a dec-only refusal reports `("dec",)` and **not** `tab` (the absence is the half that matters); a graph violating both reports `("tab", "dec")`; `_refusal_message` names exactly the failing legs across all three combinations; and `inspect.getsource` of both raising functions no longer contains `"failed tab: SHACL:"`. Falsified: making `_refusal_message` ignore `legs` turns the first and third red. The `:463` wrinkle — a conforming path discards the dec leg's report — is deliberately **unchanged** (spec §3.2 rules it out of scope in writing; nothing reads a conforming report). Original text follows, uncorrected: **`_validate`'s refusal message names the wrong membrane leg** — a `dec`-leg failure is reported as *"failed tab: SHACL"* | 2026-08-17, out of the R89 measurement. `compile.py:462-465` concatenates both legs' reports under one caller string, and the raise sites interpolate it: `compile.py:1103` `raise AssertionError(f"asserted holon failed tab: SHACL:\n{text}")`, `document.py:1587` the same for document-level facts. So a `dec:DecisionHolonShape` violation surfaces to the reader as a `tab:` failure. The SHACL report body does identify the offending decision-holon URI, so the information is present — the label above it is wrong. | Cosmetic in mechanism, not in effect: it sends a diagnosing session to the wrong vocabulary, and this repo has already paid for handoffs that read as more current than they are. Left out of R89 because R89 is a keep/delete decision about a Python guard, not an error-surface change. | Carry the leg name through `_validate` and interpolate it at the raise sites, plus a test that a dec-leg refusal's message names `dec` and not `tab`. Cheapest row in the register; the reason it is a row rather than a fix is only that raising it cost nothing and fixing it inside another loop would have been unscoped work. |
| ~~R87~~ | **CLOSED 2026-08-16** (loop `escalation-is-a-decision`: `a5166fe` vocabulary, `d2c9b74` derivation, `262dd2e` wiring at document scope, `0074144` shape into the membrane, `d1e5ab1` vacuity registry). **THIS ROW'S OWN INSTRUCTION WAS FOLLOWED AND WAS NOT SUFFICIENT**, which is the finding worth keeping. It said *parse it into the pinned leg*; doing only that gives a shape with 769 focus nodes that binds ZERO rows, because nothing in the pipeline ever wrote the predicates its body joins on. What made it live was a DERIVATION (`vocab/queries/escalation-furnish.rq`) turning each recorded escalation verdict into the three predicates the shape reads. Measured after: `dec:EscalationShape` binds one candidate per live escalation (apple 10, bfs 10, who-wfa 3, four documents 0) and refuses none — and O1 shows it REFUSING when `dec:escalatedTo` is removed from the derivation's template, so it is evaluated rather than merely parsed. **The blank-node sweep this row demanded is re-measured and its denominator was wrong:** 0 of 769 `dec:DecisionHolon` subjects are blank on the compile graph corpus-wide (the row says 0/14, a single page) and 0 of 23 `dec:ExpansionRequest`. No engine pin is inherited. ORIGINAL ROW FOLLOWS — **`escalation-shapes.ttl` is in NO membrane** — neither `compile._validate` nor `ground_document`'s gate parses it, so nothing a compile escalates is shape-checked | loop-decision-membrane, 2026-08-10. `grep -rn escalation-shapes src/ tests/ scripts/` returns **only** `tests/test_escalation_shacl.py:31,37` and `tests/test_escalate.py:69` — it appears nowhere under `src/`. **The spec deferred this as "clean on apple p0, unmeasured on the other six"; it is now measured on all seven.** Every corpus document compiled via `compile_document` and validated against `escalation-shapes.ttl` (12 triples) under BOTH closures: apple, bfs, cbh-stem, graincorp-capacity, graincorp-stem, ons, who-wfa all `(conforms=True, 0 refusing focus nodes, 0 results)`. **TOTAL 0 / 0** | Nothing violates it, so wiring it in changes no verdict today — which is exactly why it was not urgent enough to widen this loop's diff past the two membranes it set out to close. Adding a third shape file to a membrane is a measurement, and this loop already spent one on the `_FULL_ONT` seam | Parse `escalation-shapes.ttl` into `compile._validate`'s **pinned** leg, not the tab one. **CORRECTED 2026-08-11:** this row first said it carries no `sh:sparql` and so needs no engine pin. That was wrong and unmeasured — `grep -c sh:sparql vocab/shapes/escalation-shapes.ttl` returns **1**, and its `EscalationShape` targets `dec:DecisionHolon` (`:17`), the very class R88 makes rudof raise on whenever the focus node is a blank node. So closing R87 inherits R88's pin, and the load-bearing measurement is: **does any escalation-carrying decision have a BLANK-NODE subject?** `escalate_region` takes a `cand_uri` URIRef, which suggests no — but that is exactly the kind of reading-not-measuring claim this correction exists to punish. Measure it, then wire the shape into the leg the measurement selects, re-run the corpus battery, and confirm the 0/0 above still holds live rather than offline. **SWEEP DONE 2026-08-11:** the generalized question ("any `sh:sparql` shape whose target class is ever blank-node-instantiated") was measured for EVERY `sh:sparql` shape in EVERY file under `vocab/shapes/`, by enumerating each shape's focus nodes over a real compiled page (graincorp-stem p0, 8668 triples, after `membrane.subclass_closure`) and a real grounded graph (8276 triples; 38 records / 167 grounded / 385 proposed). **The measurement this row demanded is answered: YES.** `EscalationShape` and `DecisionHolonShape` both target `dec:DecisionHolon` — **0/14 blank on the compile graph, 167/167 blank on the grounding graph**; `NoLeakShape` targets `iladub:CandidateConcept` — **552/552 blank**. So escalation-shapes inherits the pin, on the grounding membrane above all. The sweep also CLEARS the rest: every `sh:sparql` shape on the UNPINNED tab leg has **0 blank focus nodes** (`tab:Cell` 0/603, `tab:EntryCell` 0/586, `tab:LeafRow` 0/57, `tab:LeafColumn` 0/17, `tab:HeaderNode` 0/35, `tab:HeaderSourceCell` 0/27, subjects-of-`tab:hasHeaderNode` 0/1), and `etkl-shapes` (`etkl:DocumentProjection`), `risk-shapes` (subjects of `risk:severity`), `iladub-hga-shapes` (`holon:GroundingRecord`) and `governance-shapes` (objects of `odrl:assignee`) have **zero instances in either graph** — no blank-node hazard, and nothing for them to validate today either **WHAT NOW PREVENTS RECURRENCE — and it is NOT the wiring.** The wiring is what R87 asked for and it would have been closed the same way a second time. The guard is `tests/etkl/test_vacuity_registry.py` (`d1e5ab1`): every wired shape is measured on the corpus by TWO criteria — focus-node count, and TERM REACHABILITY (a body naming a term that appears in no graph cannot fire) — and a shape idle by either must carry a registry row with a measured reason, while a registered shape that has gone live fails the test too. R87 is exactly a term-reachability failure, and its oracle (O2) is shown failing: disable criterion 2 and the guard stops catching R87. |

---

## R13 attempt 1 — what was learned (2026-07-30)

Branch `iladub-rule-column-refinement`, **abandoned, not merged, kept locally**. `main` is unaffected.
Recorded so attempt 2 does not re-derive any of it.

**What was right and is worth reusing**

- **The target is real and the answer is known.** GrainCorp's interval `[715.2, 829.92]` carries two blank
  runs, `744.2–763.2` and `789.2–808.2`, 19 bins each over 54 inked rows. Splitting at their centres
  (753.7, 798.7) gives **17** columns — the real header's count — with all 17 labels correct.
- **The architecture was sound and reviewer-verified.** A derived `Band.column_xs` field, kept separate
  from `Band.rules` (what the author drew), with `_rule_boundaries` preferring it. No `Rule` is ever
  synthesised. Additivity held under every adversarial input tried (unsorted / duplicate / near-duplicate
  boundaries, intervals narrower than the bin minimum, centres exactly on a boundary, space-only cells,
  chars wider than the interval). Interactions with the two preceding loops were clean.
- **`recover_leaf_grid` must carry EVERY boundary-bearing field onto its row-suffix sub-bands.** Dropping
  one silently disables everything downstream. This has now bitten **twice** — loop D for `rules`, attempt 1
  for `column_xs` (17 columns compiled as 15). Whatever attempt 2 adds, add it there too.

**What was wrong — do not repeat**

- **The interior condition is NOT what protects the shipped fixtures.** Measured: removing it *alone* leaves
  `ruled_tight_table_pdf` and `ruled_merged_table_pdf` at **+0**. The naive `+5`/`+2` requires removing the
  interior condition **and** flushing a run still open at the interval's end. Two independent mechanisms, and
  attempt 1's spec and source docstring both credited the wrong one.
- **Cells and score do not discriminate this change.** `509` / `0.9496` held *identically* at the broken
  15-column state and the correct 17-column one. The discriminating criterion is the **header-label count**
  (15 vs 17) — pin that, not the score.
- **`gutter_pct = 0.98` behaves discontinuously when `N` is per-interval.** A bin counts as blank iff inked in
  `≤ floor(0.02N)` rows — **zero** rows for `N ≤ 49`, **one** for `N ≥ 50`. Attempt 1 recomputed `N` per
  *interval* (GrainCorp ran at N = 4, 32, 33, 47, 54 in one table), where `infer_leaf_grid` uses a single
  per-*band* `N`. That change in meaning was undocumented and is the mechanism behind the crash.
- **The single-row case over-splits, it does not merge.** One row + 3 author rules yields **6** columns at
  confidence 1.0 (`['Date','Loading','Completed','Net','Weight','Tonnes']`), masked in the pipeline only
  because a one-line band classifies `NON_TABLE`.
- **A guard test that replicates production code instead of calling it is worthless.** Attempt 1's
  no-synthesised-`Rule` test copied `compile.py`'s band-construction block into the test body; patching
  `compile.py` to synthesise fake `Rule`s left every test green.

**The red test attempt 2 must start from**

A synthetic ruled table — 3 columns, Courier 9pt, header `ID | Date | Tonnes`, six rows of values carrying a
column-aligned internal space (`AB CDEFGH`, `01 JAN 2026`, `12 500`), vertical rules at the column edges only.
On `main` this compiles `RECORD_TABLE`, 18 cells, score 1.0. Any refinement that breaks it is wrong.
| ~~R103~~ (raised at 18/93 closed) | **`vocab/ontology/tab-datagrid.ttl` is loaded into NO membrane and NO probe — its domain/range rules are outside every check that exists** | 2026-08-17, out of the R61 measurement. `compile.py:437-449` builds the ontology graph from `tab.ttl` + `dec.ttl` + `iladub.ttl` and nothing else; `scripts/probe_emitter_typing.py:111` parses `tab.ttl` only. So the grid module's own ~20 domain/range declarations are checked by neither. Concrete instance found while measuring R61: `tab-datagrid.ttl:261` declares `tab:universeSource a owl:ObjectProperty ; rdfs:domain tab:ColumnUniverse`, while `datagrid.py:625-626` hangs `tab:universeSource` on the grid node (`tab:DataGrid`/`tab:UniformGrid`). **This is a fifth instance of R61's exact disagreement that nobody has counted**, because the only instrument that counts them cannot see the file. | R61's probe was scoped to `tab.ttl` when it was built and the omission was not noticed; widening it is a change to the probe's contract, and R61's own modelling question should be settled first so the probe is not rewritten twice. | Load `tab-datagrid.ttl` into the probe (cheap, and immediately produces a count), and decide separately whether it belongs in the membrane ontology — those are two different questions and only the first is mechanical. **Do not close this by fixing `tab:universeSource` alone**: the instance is not the residue, the blind spot is. 📐 **MECHANICAL HALF DONE — CARRY, NOT A CLOSE (2026-08-17, plan `plans/2026-08-17-the-gate-and-the-label.md` Task 4).** `scripts/probe_emitter_typing.py` now parses `ONT_FILES = ("tab.ttl", "tab-datagrid.ttl")` and attributes every violated rule to its declaring file. **The count, 27 corpus pages, `validate_shapes=False`: `tab-datagrid.ttl` contributes 13 typing rules, 3 of them violated, 18 violating nodes, of which 0 are a LIVE hazard and 0 sh:sparql** — none of `tab:ColumnUniverse`, `tab:GridAxiom` or `tab:CellDatatypeFamily` is targeted by any shape in `tab-shapes.ttl`/`tab-physical-shapes.ttl` (which are exactly the membrane's `compile._TAB_SHAPE_FILES`, and there is no `tab-datagrid-shapes.ttl`). `tab.ttl`'s own figures are unchanged at 5 rules / 56 nodes / 14 live / 14 sh:sparql, so the widening moved nothing that was already measured. Falsified: reverting `ONT_FILES` to `("tab.ttl",)` and re-running the corpus reports **56 nodes, 5 rules, and no `tab-datagrid.ttl` line at all** — the widening is load-bearing, not decorative. ⚠️ **A SECOND FINDING, and the more interesting one: ~~16~~ **14** of those 18 nodes are PROBE ARTIFACTS, not emitter defects.** (*The 16 was wrong when first written on 2026-08-17 — it was inferred from the two rules' totals rather than measured per node. Corrected 2026-08-18 by the measurement recorded at the end of this row.*) `range conformsTo -> GridAxiom` (12) and `range columnFamily -> CellDatatypeFamily` (4) point at **vocabulary individuals** — e.g. `tab:AggregateWitness`, which IS declared `a tab:GridAxiom` at `tab-datagrid.ttl:318`, and `tab:Quantity`, declared `a tab:CellDatatypeFamily` at `tab.ttl:227`. The probe's `types_of` reads the **page graph only**, which does not carry the ontology, whereas the membrane validates page graph **+** `_FULL_ONT` and therefore does see those types. So the probe over-reports whenever a range points at an ontology constant rather than a minted node — a class of false positive that only surfaced when the probe widened, because `tab-datagrid.ttl` is where those ranges live. **Not fixed here** (Task 4 is a measurement, and R61's modelling question stays deferred), but any future use of this probe as a gate has to handle it, and the pre-existing `tab.ttl` rows should be re-read with it in mind. Genuinely attributable to an emitter: **4 nodes, not 2** (corrected 2026-08-18) — `domain universeSource -> ColumnUniverse`, 2 nodes — `datagrid.py:625-626` hangs `tab:universeSource` on `grid_uri` (a `tab:DataGrid`) while `tab-datagrid.ttl:261` declares its domain as `tab:ColumnUniverse`. Exactly the instance the spec predicted; **reported, not fixed**, per this row's own instruction. **THE ROW STAYS OPEN**: the membrane question — does `tab-datagrid.ttl` belong in the membrane ontology at all — is untouched, and that was always the half that mattered. 🔬 **ARTIFACT/REAL SPLIT MEASURED PER NODE, 2026-08-18.** The 2026-08-17 entry above estimated the artifact class from two rules' totals. Re-measured properly — for every violation the probe reports, ask whether the MERGED ONTOLOGY supplies the type the page graph lacks (if it does, the membrane, which validates page graph + `_FULL_ONT`, sees it and the report is a false positive). 27 corpus pages, `validate_shapes=False`:

| declaring file | total | artifact | REAL | real & live |
| --- | --- | --- | --- | --- |
| `tab.ttl` | 56 | **0** | **56** | 14 |
| `tab-datagrid.ttl` | 18 | **14** | **4** | 0 |

**`tab.ttl` IS CLEAN: 0 of its 56 are artifacts.** The 2026-08-17 warning that its rows *"should be re-read with this in mind"* is discharged — every one of its 56 violating nodes is a minted `doc#…` node genuinely untyped everywhere, so R61's live figure of 14 stands unaltered. The artifact class is confined to `tab-datagrid.ttl`, exactly because that file is where ranges point at vocabulary constants. **AND A THIRD FINDING, which the corrected split exposed: `range columnFamily -> CellDatatypeFamily` splits 2 artifact / 2 REAL.** The 2 real ones are `tab:Text` — and `tab:Text` is declared `a tab:CellDatatype` (`tab.ttl:211`), **not** `a tab:CellDatatypeFamily`; `tab:Quantity` (`tab.ttl:227`) is the only individual ever declared with that type. Meanwhile `tab-datagrid.ttl:177` states in prose that *"tab:Text IS a legal family"*, and `datagrid.py:638` emits `columnFamily -> TAB[col.family]` accordingly. So the vocabulary contradicts its own prose, and the emitter follows the prose. **Reported, not fixed** — it is a modelling question of exactly the kind this row defers, and it is the second instance (with `tab:universeSource`) of the blind spot being the residue rather than any one bug. Measured with a throwaway script, not committed; reproduce by adding an `ont`-consulting branch to `probe`'s violation loop. **THE PROBE IS NOT FIXED** — see `2026-08-18-probe-artifact-handoff.md` for the two candidate designs and why the choice was not made at 222k context. 🧭 **THE 14 'ARTIFACTS' ARE TWO CLASSES, AND ONLY 2 ARE FALSE POSITIVES (2026-08-18, `2026-08-18-probe-domain-range-agreement.md`).** `compile.py:441-454` builds `_FULL_ONT` from `tab.ttl` + `dec.ttl` + `iladub.ttl` and **does not load `tab-datagrid.ttl`** — so the artifact/real split above, computed against the probe's own wider merge, was measured against a graph the membrane does not have. Re-split by declaring file: **2** are `tab:Quantity` (`tab.ttl:227`), genuinely visible to the membrane; **12** are the six `tab:GridAxiom` individuals (`RowAddressability`, `AggregateWitness`, `ColumnAlignment`, `SeedFollowsUniverse`, `NonDegeneracy`, `ColumnHomogeneity`), declared ONLY in `tab-datagrid.ttl` and therefore invisible to the membrane. **Those 12 are not noise — they are this row's membrane question with a number attached:** the emitter conforms to a vocabulary the membrane never validates against. The probe now reports them as their own class, OUTSIDE-MEMBRANE, and the handoff's design 1 (one merged type lookup) is refuted precisely because it would have silenced them; design 2 (skip ontology subjects) is refuted because `tab:Text` is one (`tab.ttl:211`). **THE ROW STAYS OPEN** — the membrane question itself is still undecided, and it is now the only half left 🏁 **CLOSED 2026-08-20 (loop `r103-membrane-decision`). THE ANSWER IS NO — `tab-datagrid.ttl` does NOT belong in `compile._FULL_ONT` — and the reason retires the question rather than deciding it on balance.** The measurement the 2026-08-19 handoff asked for, run and widened to ask what the change BUYS: 7 corpus documents compiled once, 27 pages, each closed TWICE (against `_FULL_ONT`, then against `_FULL_ONT` + `tab-datagrid.ttl`) and validated on BOTH legs each time. **Closure delta: 0 triples on every one of the 27 pages; every verdict identical at (conforms=True, 0 results).** The handoff's own worry — that *"no verdict moving could equally mean the change buys nothing"* — resolves to the second reading, and precisely: nothing moves because nothing CAN. `membrane.subclass_closure` (`membrane.py:448`, docstring *"the ontology is READ for its `rdfs:subClassOf` axioms and never mixed in"*, pinned by `test_subclass_closure_injects_no_ontology_triples`) consults ONE predicate, so of the 160 triples the file adds the membrane sees 6 axioms — and all 6 are inert against the emitters: `UniformGrid`/`MixedGrid ⊑ DataGrid` would re-derive a triple `datagrid.py:622-623` already emits explicitly (it emits `tab:DataGrid` AND `TAB[grid.grid_type]` on the same node); `AggregatingGrid` and `PivotFieldRepeatLabels` are emitted nowhere in `src/` (0 hits); `Decoration`/`AlignmentUniverse ⊑ ColumnUniverse` fire on nothing, because `datagrid.py:626` emits those as the OBJECT of `tab:universeSource`, never as an `rdf:type`, and the closure materialises on `rdf:type` only. ⚠️ **AND THE FINDING THAT MATTERS MORE THAN THE DECISION: this row's own 2026-08-18 entry, and the probe's `ONT_VISIBLE` class, both rested on a FALSE premise about the membrane.** That entry called the 12 `tab:GridAxiom` nodes *"this row's membrane question with a number attached"*, and the probe's docstring said `ONT_VISIBLE` meant the membrane *"sees the type and there is no hazard"*. Neither is true. **No ontology subject ever reaches an engine at all** — MEASURED 2026-08-20: the 4,773-triple closure of `ons-index-of-services-2026-02.pdf` p7 contains **0** `tab:`-namespace subjects, and `(tab:Quantity a tab:CellDatatypeFamily)`, `(tab:NonDegeneracy a tab:GridAxiom)` and `(tab:Text a tab:CellDatatype)` are each absent from it. So the 2 ONT_VISIBLE and the 12 OUTSIDE_MEMBRANE nodes are **the same situation**, and loading `tab-datagrid.ttl` would have repaired neither. The membrane-ontology list was never where those disagreements get fixed. **What shipped:** (1) the decision recorded at its own absence in `compile._build_membrane`, with the six-axiom inertness table and the measurement; (2) `test_tab_datagrid_axioms_are_unreachable_by_every_membrane_shape` (`tests/etkl/test_compile_membrane_shapes.py`) pinning the CONDITION that would reverse the decision — it fails, naming R103, the moment any membrane shape targets or `sh:class`-constrains `tab:DataGrid`, `tab:ColumnUniverse` or `tab:SuppressedRepeat`. FALSIFIED: appending a shape with `sh:targetClass tab:DataGrid` to `tab-shapes.ttl` makes it fail with exactly that message; reverted, 9 passed, `git diff --stat vocab/` empty; (3) the probe's two false claims corrected in place. **The names `ONT_VISIBLE` / `OUTSIDE_MEMBRANE` are KEPT** — what they still report truthfully is WHICH FILE DECLARES the type, which is what attributes a disagreement to the module that must fix it; only the membrane-visibility inference was wrong. ✅ **THE SEPARABILITY QUESTION THE 2026-08-19 HANDOFF FLAGGED IS ANSWERED: the two questions ARE separable.** Admitting the file could NOT have turned the `tab:Text` contradiction into a refusal — MEASURED: the only `sh:class` in either tab shape file is `sh:class tab:BBox` (one occurrence, `tab-shapes.ttl:19`), and none of the 13 shape-targeted classes is `tab:CellDatatypeFamily`. 📌 **WHAT THIS ROW HANDS TO R61, which already carries it:** the two emitter/vocabulary disagreements are REAL and still unfixed — `tab:universeSource`'s domain (2 nodes) and `columnFamily -> CellDatatypeFamily` on `tab:Text` (2 nodes). What R103 adds to R61 is that they are **unenforceable by the membrane by construction**, so they need a different instrument than the membrane-ontology list this row kept reaching for |
| ~~R105~~ (21/95 closed) | **`prog:source` is a LINE POINTER into a file that keeps growing, and the arc membrane checks only that it is PRESENT — not that its path exists, not that its line is in range, and not that the line says what the criterion claims** | 2026-08-21, task 5 of the `arc-denominator` loop, and measured by walking into it. `tests/arc-shapes.ttl` requires `prog:source` on every criterion (M1) and constrains its value in no other way; `tests/test_arc_manifest.py`'s environment leg checks `prog:oracleArtifact` existence (M5) and `prog:oracleTest` collectability (M5b) but never `prog:source`. So the six adjudications task 5 added to `tests/corpus-manifest.ttl` shifted every subject line from 44 downward and silently invalidated **six of the seven** `etkl` pointers (44->46, 55->59, 69->75, 84->92, 95->105, 106->118) in the same commit — with nothing red. Confirmed live during that task's falsification F1: an unrelated one-line edit to the corpus manifest produced `etkl:06 points at tests/corpus-manifest.ttl:105, which is not the subject line of financial/apple-fy2026q3-statements.pdf`. **The `etkl` leg is now GUARDED — this is a PARTIAL guard, not an absent one**: `test_etkl_criterion_sources_point_at_the_document_they_name` reads each of the seven lines out of the file and asserts it is that document's subject line. It is `etkl`-only by construction, because it resolves a pointer against the *corpus register* specifically. Unguarded at time of writing: `dec` (17 criteria), `holon` (6), `substrate` (3) = **26 pointers into other files**, each with the same failure mode and no check at all. | Task 5's scope was the `etkl` adjudication pass; a general check is a new membrane refusal (M10) and belongs to whoever owns `arc-shapes.ttl` + the environment leg. Task 3's implementer had already named M10 and declined it for the same reason, so this is the second decline, which is what makes it a residue rather than an oversight. Raised **before** task 6 rather than after, deliberately: task 6 adds ten `tab:` criteria with the same fragility, and a deferral recorded after the next task compounds it is recorded too late. | **M10 — an environment-leg refusal in `tests/test_arc_manifest.py`**, beside M5/M5b, since it is the same class of question (a fact about a file on disk, which no closed-world SHACL constraint can see): for every criterion, `prog:source`'s path must exist in the working tree and its line number must be within that file's length. That is general, cheap, and rung-agnostic. The stronger `etkl` check — the line must be the *right* line — cannot generalize, because only that rung has a graph to resolve the pointer against; the honest general form is path-exists + line-in-range, and the per-rung semantic check stays per-rung. **Do not close this by extending the `etkl` test to other rungs**: that fixes one rung a second time and leaves the class untouched (the R101 lesson). **CLOSED 2026-08-21 (task 6 of the `arc-denominator` loop, Ruling 18, third time of asking).** M10 ships in `tests/test_arc_manifest.py` beside M5/M5b/M5c/M7 — `_source_refusal` + `source_rows`, wired into `environment_refusals`. For EVERY criterion, met or not (unlike M5, which bites only on `met true` — a declaration date is asserted on every row, so its pointer is load-bearing on every row): `prog:source`'s path must exist in the working tree, and its line must be within that file's length. Both pointer forms already in the manifest are handled — `CLAUDE.md:252` and `docs/holonic-interaction.md:147-148`. **Scope held exactly where the row named it:** path existence + line range, and NOT what the cited line says. **The `etkl` join is kept, not subsumed** — `test_etkl_criterion_sources_point_at_the_document_they_name` resolves a pointer against the corpus register and can say the line is the *wrong* line; only `etkl` has a second graph to join against, so the weak-universal guard and the strong-narrow one are two instruments and both stay (the row's own "do not close this by extending the etkl test" instruction, honoured). **Measured on landing:** all 43 `prog:source` pointers in `tests/arc-manifest.ttl` (33 existing + task 6's 10) and every pointer in the 14 negative fixtures RESOLVE — M10 lands green, which confirms Ruling 13's hand-payment was correct at HEAD and is now the last one anybody has to make. **Falsified twice** (task 6 report, arms B1/B2): removing `_source_refusal`'s body makes `test_m10_a_source_pointer_that_resolves_to_nothing_is_refused` fail on `tests/arc-m10-stale-source-pointer-leak.ttl` (two arms: a vanished path, and `CLAUDE.md:999999`); and staling ONE live pointer to `CLAUDE.md:99252` turns the tracked manifest's own conformance test red with `M10: …criterion:dec:01 … but CLAUDE.md has 458 lines`. The second is the one that matters — it proves the guard is on the live manifest, not only on a fixture. **Nothing further to do:** recurrence is prevented by the refusal itself: any commit that inserts a line above a cited one, or moves a cited file, turns `tests/test_arc_manifest.py` red at the pointer rather than silently. |
| ~~R121~~ (22/111 closed) | **CLOSED 2026-08-23 (Task 1 of `the-worktree-that-resolves`, commit `7e4f84c`, "M19: resolve the library into the worktree it ablates") — M19's ablation cannot reach `vocab/` at all: the editable-install `.pth` pins `src/iladub/` to the MAIN tree, so any oracle that resolves its evidence through library code is blind to the worktree the ablation edits** | **Measured 2026-08-23**, raised by an adversarial review of `specs/2026-08-23-the-faithful-worktree-design.md` and verified independently before recording. `src/iladub/etkl/compile.py:374-383` (`_repo_vocab()` walks up from `os.path.abspath(__file__)`) and `src/iladub/etkl/gridregion.py:29-31` plus seven siblings (`Path(__file__).resolve().parents[3] / "vocab" / …`). In a worktree at `HEAD` with **the entire `vocab/` tree deleted**: `_repo_vocab()` -> `/Volumes/WD Green/dev/git/iladub/vocab`, `GRID_REGION_RQ.is_file()` -> `True`, `startswith(cwd)` -> `False`. **Blast radius: 8 of the 29 distinct declared `prog:oracleArtifact` files live under `vocab/`**, and for any oracle reaching them through `src/` they cannot be ablated. Arm 1 grounds an edge only when deleting Y's artifact makes X's oracle FAIL (`tests/test_arc_ablation.py:348`), so every such edge is **refuted on no evidence**; arm 2's direction is worse — a free PASS (`:359`), i.e. a false **assertion** path. **The shipped graph is NOT affected**, verified: `tests/test_boundary.py:6` and its siblings root at the TEST file's `__file__`, which in a worktree IS the worktree — which is why the 6 asserted edges grounded, and an edge that grounded is one whose oracle was ablation-sensitive. In the met set `etkl:01` is the ONLY oracle reaching its evidence through `src/` | — | The guard limitation 4 already prescribed at `:109-111` — refuse a removed path lying under a directory named by a `.pth` on `sys.path` — which converts the silent false result into a loud refusal but grounds no new edge. To actually **unblock** the `etkl` rung, `vocab/` must resolve to the checkout under test: a per-worktree isolated install, or an injection point (`ILADUB_VOCAB` / rootdir-relative resolution) that `_repo_vocab()` and the eight `parents[3]` modules both honour. **Pairs with [[R113]]**: even once ablatable, `dec-shapes.ttl` is declared by four criteria and `iladub-shapes.ttl` by three, so file-granularity leaves only `etkl:01 -> dec:06` unambiguous. Both must land for the `etkl` rung to yield more than one edge. **SHIPPED, and NOT the remedy this row prescribed — say so.** This row's own prescription was *"a per-worktree isolated install, or an injection point (`ILADUB_VOCAB` / rootdir-relative resolution) that `_repo_vocab()` and the eight `parents[3]` modules both honour."* **None of that shipped, and no library file under `src/` was touched.** What closed it instead: `_run_module` (`tests/test_arc_ablation.py`) prepends `<worktree>/src` to the ablation subprocess's `PYTHONPATH` env var. A plain-path `.pth` entry is outranked by `PYTHONPATH`, so every `src/`-resolved import — `_repo_vocab()`, `gridregion.py` and the other seven `parents[3]` siblings alike — re-roots onto the worktree with zero changes to any of those modules. Closure evidence: `test_m19_resolves_the_library_into_the_worktree_it_ablates` (`tests/test_arc_ablation.py:714`), a two-leg test run through the shipped `_ablate` — leg 1 asserts the probe (`tests/test_arc_worktree_probe.py::test_library_resolves_to_this_checkout`) PASSES in an un-ablated worktree (resolution lands in the checkout, not the main tree); leg 2 asserts it FAILS once `vocab/queries/grid-region.rq` is deleted inside that same worktree (the deletion is *observable* through library code, not merely resolved-past). Both legs pass at HEAD. **Pairs with [[R113]] still, per this row's own note**: once `vocab/` is ablatable, file-granularity still leaves only `etkl:01 -> dec:06` unambiguous — see [[R114]]'s amended remedy column |
| ~~R118~~ (22/108 closed) | **CLOSED 2026-08-23, in TWO independent halves — the GENERAL rule (Task 4, commit `5f2cad9`) and the LIVE instance (Task 2, commit `2d08f06`). M19 scored a pytest COLLECTION ERROR as a test FAILURE, which made arm 1 PERMISSIVE: an import break unrelated to the removed file read as consumption** | Task 3's review, deferred finding (2026-08-22), re-read at close: `tests/test_arc_ablation.py:199` sets `collect_error` from pytest's `^ERROR <module>` summary line and `:206-208` scores **every** requested node id of that module `FAILED`. Arm 1 refuses an edge only when **all** of X's results are `PASSED` (`:323`), so a `FAILED` id **admits**. The intended reading is right — a module that cannot import once its artifact is gone is genuine consumption — but the instrument cannot separate that from an import break with no relation to the removed file (a missing dependency, a syntax error on the branch, a failing `conftest`). Direction of the error is the unsafe one for arm 1 (admits). **Arm 2 is unaffected in the unsafe direction**: there a `FAILED` REFUTES (`:334`), so a spurious error only ever refuses. **AMENDED 2026-08-22 (final review, I-2) — the generic list above omits the LIVE INSTANCE this repo already carries: `baml_client`.** It is **gitignored** (`.gitignore:30`), generated by a dedicated CI step (`.github/workflows/ci.yml:23-24`) and tracked by nothing (`git ls-files \| grep -c baml_client` -> `0`), so a `git worktree add --detach <wt> HEAD` checkout **NEVER** contains it. MEASURED by running `pytest --collect-only -q` on the nine referencing modules inside a real worktree at `HEAD`: `14 tests collected, 5 errors` — five modules were reported to raise a collection `ERROR`. **CORRECTED 2026-08-23 (Task 5, re-derived, not carried from memory) — it is SIX, not five.** Re-running the identical command in a fresh throwaway worktree at the current HEAD (`git worktree add --detach $WT HEAD && cd $WT && …/python -m pytest --collect-only -q -p no:cacheprovider`): `1299 tests collected, 6 errors` — `tests/test_extract_baml.py`, `tests/test_loop.py`, `tests/test_m4_databook.py`, `tests/test_m4_pipeline.py`, `tests/test_targeted.py`, **and `tests/test_to_rdf.py`**, which breaks TRANSITIVELY (`tests/test_to_rdf.py:4` imports `iladub.extract_baml`, which imports `baml_client` at `src/iladub/extract_baml.py:8` — the same transcript this plan's M3 used to re-derive [[R121]]'s shadow for free). The five-module count undercounted the hazard by one module from the start | No live instance **among the six shipped edges** (see the amendment in the previous column for the live *mechanism*): all six asserted edges' arm-1 evidence was re-derived per test id in task 4's review and each is a real oracle failure carrying a stated `file:line` (`2026-08-22-arc-edges-authored.md` §6). And the obvious alternative is worse — scoring a collection ERROR as *"did not execute"* would refuse every edge whose oracle module genuinely cannot import without its artifact, which is the commonest **true positive** the instrument has | — | Read the ERROR's exception rather than only its existence: an `OSError`/`FileNotFoundError` naming one of the removed paths is consumption; anything else is an instrument failure and must **RAISE** with the transcript, exactly as an unresolved node id already does. Cheaper alternative carrying most of the force: run the module ONCE in an **un-ablated** worktree first and require it to import there, so only a break the ablation caused can score. **BOTH SHIPPED, and they close different halves.** (1) **The general rule** (Task 4, `5f2cad9`, `_scores`): a collection ERROR now scores `FAILED` only when its exception TEXT names one of the removed paths — anything else (missing dependency, broken conftest, an import break with no relation to the ablation) RAISES as an instrument failure instead, exactly as an unresolved node id already did (`tests/test_arc_ablation.py:412-418`). Pinned by `test_m19_refuses_a_collection_error_that_names_no_removed_artifact` (`:866`), with both directions covered: a genuine artifact-caused ERROR scores `FAILED`, an unrelated one RAISES. (2) **The live instance** (Task 2, `2d08f06`): `_materialise` copies `baml_client` into every M19 worktree before ablation runs, so none of the six modules above errors at collection for the `baml_client` reason any more — pinned by `test_m19_materialises_the_generated_client_it_did_not_check_out` (`:748`). Neither half depends on the other: (1) is safe even if a future loop removes materialisation and a `baml_client`-dependent module reappears as a collection ERROR (it would RAISE, not silently admit); (2) removes today's only known live instance of the class (1) generalises against |
| ~~R126~~ (24/115 closed) | **CLOSED 2026-08-25** by loop `the-membrane-reports-its-health` (`holon:05`), Tasks 1 and 3, via this row's own option (a): the spec NAMES the subject the health signal attaches to, and that subject IS an `etkl:DocumentHolon`, so the `rdfs:domain` is honoured rather than violated. **RE-RUN ON THIS ROW'S OWN VEHICLE, 2026-08-25** (`compile_tables(simple_table_pdf, 0)` and `compile_document(same)`, `./.venv/bin/python`): **Population 1** — `compile_document` now emits **331** triples (326 + the 5 seal triples Task 3 enumerated), score 1.0, of which **2** have `_DOC` as subject, where the row measured **0**: `<doc> a etkl:CompiledDocumentHolon` and `<doc> etkl:membraneHealth etkl:Intact`. **Population 2** — **13** distinct `rdf:type` values, of which **2 ARE `etkl:`** (`etkl:CompiledDocumentHolon`, `etkl:MembraneValidation`), where the row measured 11 and **0**. **Population 3** — `etkl:membraneHealth` is now a live PREDICATE, not only a string inside three `prog:statement` literals. `etkl:CompiledDocumentHolon rdfs:subClassOf etkl:DocumentHolon` (`vocab/ontology/etkl-holons.ttl:96-97`), so the property's declared domain is instantiated. **WHAT DOES NOT CLOSE, AND IT IS DELIBERATE:** page scope is UNCHANGED — `compile_tables` still returns 326 triples with **0** `_DOC`-subject triples — because spec §9 rules page-scope health out of this loop; the consequent asymmetry is R131. **The compounding `sh:severity` half closes too:** `etkl:Weakened` was unreachable because no `sh:severity` exists; §4.4 re-read it as a HELD proposition, and it is now reached on real input (`compile_document(false_transposed_pdf)` → `etkl:Weakened`). **What prevents recurrence:** `etkl:MembraneHealthShape` (`vocab/shapes/etkl-shapes.ttl`) is the membrane saying so rather than prose, with a conformance pair hand-wired into `tests/test_vocab_shapes.py`, and `tests/etkl/test_membrane_health.py::test_compiled_document_reports_membrane_health` is the criterion's declared oracle. **Two mis-cited ranges corrected in the same act:** this row's `etkl-holons.ttl:74-88` is **`75-89`** (`sed -n '75p;89p'` → `etkl:MembraneHealth a owl:Class ;` and `etkl:membraneHealth`'s closing `rdfs:comment`); the second, `tests/arc-manifest.ttl`'s `75-86`, sat in the `holon:05 -> holon:01` proposed-edge rationale, which M17 forced this loop to delete outright — so it is gone rather than corrected (see R135). ORIGINAL ROW FOLLOWS. — **The whole `etkl` doc-holon fabric is DECLARED VOCABULARY WITH ZERO INSTANCE DATA — a compiled document is never typed `etkl:DocumentHolon`, and the document URI is never the subject of any triple. `etkl:membraneHealth` therefore has no node to attach to, and `holon:05`/`holon:06` are larger slices than the arc criteria's prose implies** | 2026-08-24, direction-setting session for the `holon:05` loop, measured on LIVE compiles (source grep is a proxy for a runtime claim; both scopes were run). **Population 1 — every triple in a compiled graph whose subject is the document URI:** `compile_tables(simple_table_pdf, 0)` → 326 triples, **0** with `_DOC` as subject; `document.compile_document(same)` → 326 triples, score 1.0, **0** with `_DOC` as subject and **0** page-scoped doc URIs as a subject either. The doc URI is a URI *stem* only: 56 child subjects start with it (`…/doc#p0-reading`, `…/doc#region0-d0`, …) and the stem itself is never asserted. **Population 2 — every distinct `rdf:type` in a compiled graph:** 11 values at both scopes — `prov:SoftwareAgent`, `dec:{DecisionHolon,Option,Process}`, `tab:{BBox,EntryCell,HeaderNode,LabelCell,LeafColumn,LeafRow,RecordTable}` — and **NONE in the `etkl:` namespace**. **Population 3 — every typed instance of the fabric anywhere in the repo:** `grep -rn "a etkl:CleanDocumentHolon|a etkl:RawDocumentHolon|a etkl:DocumentHolon" --include="*.ttl" .` outside `vocab/ontology/etkl-holons.ttl` → **0 hits**; `etkl:membraneHealth` appears repo-wide only inside three `prog:statement` string literals, never as a predicate | Not deferred by a loop — **measured before one started**, which is the point. `tests/arc-manifest.ttl:354` states `holon:05` as *"a membrane-health check that computes and reports a compiled document's cleanliness … from validation results"*, and `vocab/ontology/etkl-holons.ttl:74-88` already declares `etkl:MembraneHealth`, its three individuals and the property with `rdfs:domain etkl:DocumentHolon`. Read together those two make `holon:05` look like *derive one value and attach it*. It is not: the domain of the property is a class **nothing instantiates**, so the loop must first mint a document holon node — which is `holon:06`'s Raw→portal→Clean traversal territory and the CLAUDE.md open item *"express the holonic interaction model in `vocab/`"* — or deliberately scope the health signal onto a subject that does exist and say which. Recorded now so the spec confronts it instead of discovering it mid-plan | Either (a) `holon:05`'s spec names the subject the health signal attaches to and justifies it — if that subject is not an `etkl:DocumentHolon`, the property's declared `rdfs:domain` is violated and the membrane must be the thing that says so, not prose; or (b) the loop mints the doc-holon node as its first product, which merges `holon:05` and part of `holon:06` into one slice and should be costed as such. **Compounds with the `sh:severity` finding**: `grep -rn "sh:severity" vocab/ tests/*.ttl` → 0, so `etkl:Weakened` (*"conforms but warnings are present"*) is unreachable at the same time as its domain is uninstantiated — two independent vacuity hazards in one criterion, and [[R106]] is the class for both |
| ~~R140~~ (25/129 closed) | **CLOSED 2026-08-27** by plimslop `5064705` (PR #2), on this row's OWN second branch: `PLIMSLOP_MODE_ORIGINATING=warn` is **documented as a deliberate standing setting** in plimslop's README (§ *The originating gate is held at `warn`*), dated, with the cost stated and a named release trigger. **The decision was NOT "restore `block`", and the measurement is why.** Ordering every preflight record against its own session's stop warning, 2026-08-27: **150 of 153 written AFTER the warning, 3 before** — the Stop hook is what produces the override data. So flipping `warn`→`block` now would move the *instrument* and the *prediction* (R141 §5) together, and a falling rate could no longer be attributed to the unit — the exact misattribution R141's counterfactual/observed split exists to prevent. `block` being **safe** now (floor satisfiable at turn one; `raised_before` fires it at most once) is not the same as `block` being **free** while it is the thing being measured. **The trigger is an instrument, not a promise**: `reader override` prints the hold and the live observed count on every run and switches to `HOLD RELEASED`, naming the variable to delete, at `n>=20` (`reader.py`'s own `N_REPORT`) — because a documented-and-forgotten condition is precisely the stale-row failure this three-way split was built to stop. **Falsification**: 2 inversions; the second (count all preflights, not the observed arm) **passed at first** — the fixture had made both denominators identical, so the test did not pin its own docstring. The **fixture** was fixed, not the assertion, and inversion 2 then failed as it must. 134 → 136 tests. **What stays true and uncosted**: `block` has still never enforced once, an untested path in a shipped default. **ORIGINAL ROW FOLLOWS** — **The originating gate is configured never to enforce, and the downgrade is recorded in neither repo.** plimslop ships `originating: block`; every block-type record ever written ran as `warn` | `plimslop/stop.py:39-41` ships `DEFAULT_MODES = {"originating": "block", "executing": "warn", "mechanical": "off"}` with the comment *"Enforcement strength tracks evidence grade."* Re-measured against `~/.claude/plimslop/corpus.jsonl` 2026-08-26: `block records: 44 ; action values: {'warn': 44} ; floor: {50000: 44} ; 44 distinct sessions`. The downgrade is an env var set inline on the Stop hook in **`~/.claude/settings.json`** (user-scope, machine-local, gitignored from every repo): `PLIMSLOP_MODE_ORIGINATING=warn PYTHONPATH=… python3 -m plimslop.stop`, consumed at `stop.py:78`. The second downgrade path — `stop.py:68`, which returns `warn` whenever `baseline >= LOWEST_FLOOR` — did **not** produce these records: measured `block records with baseline >= 50000: 0 of 44`, max baseline among them 46,243. So all 44 warnings came from the settings override, not from the unsatisfiable-gate guard | Deferred **deliberately and in that order**: restoring `block` under a floor that was unsatisfiable at turn one (a 46,243 baseline against a 50,000 total-window floor) would have made every session unstartable. The 2026-08-26 ruling (`docs/superpowers/2026-08-26-context-regime-ruling.md` §3) re-denominates the floor on `working = tokens − baseline`, which removes that objection — but only once plimslop implements the subtraction (R141). **Closes when**: R141 has landed, the override rate has been re-read, and `PLIMSLOP_MODE_ORIGINATING=warn` is either removed from `~/.claude/settings.json` or documented as a deliberate standing setting in plimslop's README. One line either way; the decision is the work, not the edit |
| ~~R135~~ (25/124 closed) | **CLOSED 2026-08-28** by loop `the-query-names-a-declared-term`, via this row's own "what would close it", both halves of it. **The instrument:** `tests/query_terms.py` extracts the owned terms an authored `.rq` names (exhaustive walk of the parsed algebra, cross-checked against a regex over the source text), and `vocab/shapes/query-declaration-shapes.ttl` — targeting `etkl:QueryArtifact`, with the declaring set carried as DATA because SHACL forbids `VALUES` inside `sh:sparql` — refuses a query naming a term no file under `vocab/ontology/` declares. **O1 was RED on a REAL leak, not a fixture's:** `vocab/queries/escalation-furnish.rq` names `risk:order`, which no ontology declared; declaring it (`vocab/ontology/risk.ttl`, 8 insertions, additive) is what turned the corpus green — `c0f5152`, full suite `1344 passed, 7 skipped, 1 xfailed`. **The edge:** `prog:criterion:holon:05` now carries a SECOND `prog:oracleTest` (the corpus test), so ablating `etkl-holons.ttl` leaves the six `etkl:` terms `membrane-health.rq` names undeclared and its oracle set red; `holon:05 prog:dependsOn holon:01` is asserted and O2 prints NOTHING over the whole manifest — `6268437`. **The falsification is the closure**, and it is the only thing separating this authoring from the two M19 killed: remove that second oracle and O2 reproduces the 2026-08-25 refusal verbatim in substance; restore it and O2 is silent. **NOT closed by this:** `R117` (same class, different artifact — subclass-axiom subjects in a `.ttl`) stays open with no live instance, and the population is `.rq` only — `R143` is the row that would generalize it, `R142` the row for the two owned namespaces that have no ontology file at all. ORIGINAL ROW FOLLOWS. — **Nothing checks that an `etkl:` term a RUNTIME artifact NAMES is DECLARED anywhere — and this is the second manifest edge M19 has killed for it.** `vocab/queries/membrane-health.rq` BINDs `etkl:Intact` / `etkl:Weakened` / `etkl:Compromised` as bare IRIs, and rdflib resolves an IRI whether or not an ontology declares it, so deleting the declaring file cannot make the derivation fail | Found by this task, 2026-08-25, and by the instrument rather than by reading. M17 forced `holon:05 → holon:01` from proposition to assertion the moment `holon:05` flipped to met (both grounds of its own rationale — A1 "unmet", A2 "no `prog:oracleArtifact`" — were repaired in one act). M19's arm 1 then refuted the assertion: `ablation_refusals(g)` → *"with holon:01's artifacts ['vocab/ontology/etkl-holons.ttl'] removed, every one of holon:05's oracle tests still passes ({'tests/etkl/test_membrane_health.py::test_compiled_document_reports_membrane_health': 'passed'}), so holon:05 does not consume holon:01"*. Per plan §0/C4 the edge was DELETED, not demoted (M17 refuses the demotion), and the reading survives as a comment in `tests/arc-manifest.ttl` | Raised rather than fixed because the fix is a NEW ORACLE, not an edit: something must resolve every `etkl:`/`iladub:`/`dec:` IRI appearing in `vocab/queries/*.rq` against the ontologies that declare them, and that is a loop of its own. **Same CLASS as R117, different artifact** — R117 is about `iladub-hga-align.ttl`'s subclass axioms going dangling; this is about a `.rq` naming an undeclared class — so R117's row does not cover it and closing R117 would not close this | A membrane (or a test) refuses a `.rq` that names a term no loaded ontology declares, with a negative fixture that must fail. **Then, and only then, re-author `holon:05 → holon:01`** — it becomes groundable for a reason that is measured, exactly as the `holon:02 → holon:01` note in `tests/arc-manifest.ttl` already says for R117 |
| ~~R117~~ (22/107 closed) | **CLOSED 2026-08-29** by `D2` (`vocab/queries/alignment-subject.rq`, AXIOM / derivation / open world) plus the membrane's extension to every tracked `.ttl` (`36ab900`, `bcea2b9`). **The row's premise that no live instance existed was FALSE** — see the correction on [[R144]]: the six `tab:aggFn*` in `vocab/ontology/tab-fno-align.ttl` had been dangling since the file was written, and the instrument found them on its first run. Quoted RED: `urn:iladub:artifact:vocab/ontology/tab-fno-align.ttl names https://w3id.org/iladub/tab#aggFnCount, which no owned vocabulary declares` (×6). **The oracle shipped is MORE general than the one this row asked for**: not "every `rdfs:subClassOf`/`subPropertyOf` subject in `iladub-hga-align.ttl`" but *every owned SUBJECT of any `*-align.ttl`*, licensed by the purpose of the file family rather than by triple position — which is what makes it reach a `rdfs:seeAlso` dangler that a subclass-only check would have missed. Subjects typed `owl:Ontology` are excluded positionally, MEASURED 9 → 6. HGA IRIs are read, never written: `tests/test_source_ownership.py` stays green. Repaired by DECLARING the six in `vocab/ontology/tab.ttl` (seam 3, measured: nothing consumes them, but `tab.ttl:163` CITES the align module for exactly these names, so deleting the block would break a citation the core ontology makes about itself). Falsified: undeclare the six → RED again naming the file and the term. **NOT done here, and carried forward as [[R151]]: re-authoring `holon:02 → holon:01`.** That is arc work, not membrane work, and this row is struck on its oracle half only — the edge is a separate act and must not be lost by being bundled into this closure. <br><br>**The row as originally written:** **Nothing checks that the terms `vocab/ontology/iladub-hga-align.ttl` ALIGNS are declared anywhere — a dangling `etkl:Foo rdfs:subClassOf holon:DataHolon` ships green** | Found 2026-08-22 by task 4's **refutation** of `holon:02 -> holon:01`, and re-measured at close in a throwaway worktree: appending `etkl:NoSuchTermAnywhere rdfs:subClassOf holon:DataHolon` to `iladub-hga-align.ttl` and running `tests/test_hga_alignment.py tests/test_source_ownership.py` -> **`9 passed in 0.44s`**. Cause: `test_alignment_axioms_present` parses `iladub-hga-align.ttl` **alone** (`tests/test_hga_alignment.py:37-39`) and `test_alignment_modules_only_point_outward` is a source-ownership **text** check. This is exactly why arm 1 refuted the edge: `iladub-hga-align.ttl` really does subclass five terms declared only in `vocab/ontology/etkl-holons.ttl`, yet with that file deleted both of `holon:02`'s oracles still pass — **the refutation found a hole in the ORACLE, not an error in the reading** (`2026-08-22-arc-edges-authored.md` §5, §9.1) | An oracle gap on a permanently green path, surfaced by a loop whose deliverable was edges. Closing it means deciding what *declared* means at the alignment seam (in our four namespaces only? transitively through `owl:imports`? by `rdfs:isDefinedBy`?) and that is an ontology decision, not an edge-authoring one. Note the neighbouring open row [[R53]]: `iladub:GroundedNodeShape` validates `groundsTo`'s **presence**, never its **resolution** — the same defect shape, one namespace over, and both are *"a pointer is checked for existing, not for pointing at anything"* | An oracle that loads `iladub-hga-align.ttl` **together with** the owned ontologies and asserts every `rdfs:subClassOf`/`rdfs:subPropertyOf` **subject** in it is declared in the owned tree (HGA terms stay objects — CLAUDE.md § Source ownership is untouched by this, and must be: the check reads `holon:` IRIs, never writes them). **Then re-author `holon:02 -> holon:01`** — the reading was sound, and `2026-08-22-arc-edges-authored.md` §5/§9.1 exists so it is re-authored rather than re-derived |
| ~~R142~~ (27/131 closed) | **CLOSED 2026-08-29** by `vocab/internal/{prog,docgov,corpus}.ttl` (`b9bc83a`) plus the exemption deletion (`36ab900`). **TWO CORRECTIONS IN PLACE, both against this row's own text.** (1) There are **three** such namespaces, not two: `cor:` (`https://w3id.org/iladub/corpus#`, 18 vocabulary-role terms) is invisible to an `.rq`-shaped instrument because it appears in no `.rq` at all, and joins the problem the moment the population reads `.ttl`. (2) **This row's stated closure — "`vocab/ontology/prog.ttl` and `vocab/ontology/docgov.ttl` declare the terms" — is REFUSED, because it contradicts three artifacts written by three different loops**: `tests/arc-shapes.ttl:27` ("never in vocab/ontology/"), `vocab/shapes/doc-governance-shapes.ttl:8`, `tests/corpus-shapes.ttl:2`. Following it would have turned test scaffolding into published, CC-BY, w3id-registered vocabulary — a change of posture, not a bookkeeping repair. Ruled by the maintainer 2026-08-29: declare them **internally**. `declaring_files()` widens to `vocab/ontology/*.ttl` (non-align) **+** `vocab/internal/*.ttl`, and "declared" stops meaning "appears in the published ontology tree" and starts meaning "declared by an owned vocabulary this repo authors" — the meaning the three statements always implied. The exemption is gone: the shape's five-namespace `STRSTARTS` is now the single owned root, and `test_the_exemption_is_gone` is the standing pin (restore the old block and it goes red). The vocabularies are AUTHORED, not transcribed, and the evidence is a NUMBER rather than an assurance: authoring them moved the census by exactly two, `corpus:Verdict` and `corpus:Adjudication`, classes the pre-loop tree never demanded — a transcription of the census cannot move the census. **A finding this closure paid for, recorded because it cost three authors an hour each:** `.gitignore`'s Confidential rule was the unanchored pattern `internal/`, which git matches at ANY depth, so the whole of `vocab/internal/` was invisible to `git status`. Anchored to `/internal/`; CLAUDE.md's Confidential class is repo-root `internal/` and `tests/docgov_extract.py:43` already read it root-anchored, so the anchor restores agreement between the two rules rather than changing either. <br><br>**The row as originally written:** **Two owned namespaces have no ontology file at all, and one of them is the arc instrument's OWN vocabulary.** The membrane's in-scope filter is not a policy choice, it is a workaround: a namespace with zero declarations makes every one of its terms a violation, so including it would paint the instrument red on day one for a reason that has nothing to do with a leak | Re-measured at `63892ae` by the loop's §0 census (`extract_named_terms` over all 46 authored queries, per-namespace):<br>`https://w3id.org/iladub/docgov#  named= 12 declared=  0 UNDECLARED=12  in_scope=False`<br>`https://w3id.org/iladub/progress#  named=  9 declared=  0 UNDECLARED= 9  in_scope=False`<br>The `prog:` terms are named by the 7 `arc-*.rq` files and by `tests/arc-manifest.ttl`; the `docgov:` terms by the `docgov-*.rq` files. Every other owned namespace is fully declared except `risk:`, whose single gap (`risk:order`) this loop CLOSED<br>**CORRECTED 2026-08-29** (the selection session for the loop after `R135`, at `87bf8cf`): **the 9 and the 12 are what the INSTRUMENT sees, not the vocabularies.** Re-censused over `.rq` *and* every tracked `.ttl` — rdflib, `URIRef` nodes and predicates only (never literals), local names split on `:` — `prog:` carries **87** distinct local names, 9 of them visible to the instrument; of the 78 invisible, **60 are instance IRIs** (`prog:criterion:holon:05`, `prog:rung:tab`), 5 are SHACL shape/prefix nodes, 1 is the singleton `prog:manifest`, and **12 are vocabulary** — `Manifest`, and `criterion` `declaredOn` `dependencyRationale` `interpreter` `metOn` `oracleArtifact` `oracleTest` `pythonVersion` `rdflibVersion` `retrospective` `source` — named by `tests/arc-manifest.ttl` and `tests/arc-shapes.ttl`. `docgov:` carries **33**: 12 visible, 10 shape nodes in `vocab/shapes/doc-governance-shapes.ttl` (written as longhand IRIs, which is why a prefixed-name scan misses them), and **11 further vocabulary** — `Document` `NavEntry` `Source`, and `citesExternal` `confidence` `excludedFromSite` `exists` `inNav` `path` `resolves` `title`. **The vocabularies are 21 and 23, not 9 and 12.** | Authoring two ontology files is a loop of its own, and it is not the loop that builds the instrument — the instrument had to ship first to make the gap visible as a NUMBER rather than an impression. Deferring it is the §4.1 exemption's tracked cost, and this row is that tracking | `vocab/ontology/prog.ttl` and `vocab/ontology/docgov.ttl` declare the 9 and 12 terms; then **delete the `in_scope` filter** and the corpus test stays green with no exemption at all. The deletion is the oracle — while the filter exists, the instrument cannot see these two namespaces even if a term goes missing<br>**CORRECTED 2026-08-29: the oracle AS STATED does not falsify this row's own claim.** Declaring *"the 9 and 12"* and deleting the filter turns the corpus green while **23 further vocabulary terms stay undeclared and invisible**, because every one of them is named from a `.ttl` and the population is `.rq` only. This row's headline is that *the arc's own measuring apparatus is undeclared*; that closure would leave 12 of `prog:`'s 21 terms undeclared **and report success** — which is [[R143]]'s own warning (*"green on its first run has not been shown to read the new files"*) landing inside this row. A closure that bites must (a) declare the measured **21 `prog:` / 23 `docgov:`** terms, and (b) extend the population to the arc's own `.ttl` artifacts for these two namespaces — which forces [[R143]]'s vocabulary/instance/shape split to be settled first, for these two namespaces only. |
| ~~R143~~ (27/132 closed) | **CLOSED 2026-08-29** (`69b596c` → `bcea2b9`). The population is now **every tracked `.ttl`** — 139 files after this loop authored three vocabularies, one named graph each (`tests/artifact_terms.py`, PROCEDURAL), minus one carved-out fixture directory. The row's central warning was HONOURED rather than worked around: the census was run first, and the three-way split it named was settled **before** any membrane read a `.ttl`, and settled positionally rather than by a filter — **a term must be declared when the graph USES IT AS VOCABULARY** (predicate position, or object of one of sixteen standard RDFS/OWL/SHACL predicates, or inside a SHACL property-path expression). That rule yields **53** demands against 203 under naive occurrence; the 150 that fall away include all 63 SHACL shape nodes and all 61 arc instance IRIs, and they fall away UNFILTERED — no owned IRI is used as a SHACL metaclass and `sh:node` with an owned object has zero occurrences. **HEADLINE CORRECTED: 209 → 203.** The 6 missing are `sh:namespace` **literals** whose lexical form starts with the owned root — which is the contamination *this row's own text warns about*, carried by the row's own figure ([[R149]]). Re-measured here: **18 owned-root-prefixed literal occurrences over the 136-artifact tree, 7 distinct values** (`sh:namespace` 11, `vann:preferredNamespaceUri` 7 — the spec's ×12/×8 sub-breakdown does not reproduce, the total 18 does; 21 occurrences / 8 distinct over the 139-artifact tree). **The instrument was NOT green on its first run**, which is what this row demanded: it went red on two live defects in two different families ([[R144]]'s correction and `etkl:Contract`), and the red is quoted in `36ab900`. <br><br>**The row as originally written:** **The instrument reads authored queries and nothing else.** `query_files()` globs `vocab/queries/*.rq` (`tests/query_terms.py:46-48`), so a shape, an example or a test fixture naming an undeclared owned term is invisible to it — the same defect class the instrument was built to catch, in a file family it does not look at | Measured as a SCOPE, not as a leak count: the population is 46 `.rq` files, and the other families have **not been enumerated** — that is the point of the row. `R117`'s artifact (`vocab/ontology/iladub-hga-align.ttl`) sits in one of the unlooked-at families, which is why closing this row would subsume it and closing `R135` did not<br>**CENSUSED 2026-08-29** (selection session, `87bf8cf`) — the enumeration this row asks for, run for the first time. Owned terms named per family, minus the **354** the non-align ontologies declare (`declaring_graph()`): `vocab/shapes` 11 files → **86** undeclared; `tests` 62 files → **112**; `examples` 48 files → **1**; align modules 6 files → **10**. **209 total — and the majority is category error, exactly as [[R130]] predicted:** in `tests/` alone **60 of the 88 undeclared `progress#` names are instance IRIs**, not vocabulary (the arc mints its instances inside its own vocabulary namespace), and 5 more are SHACL shape/prefix nodes. A naive `.ttl` extension would demand an ontology declaration for every one of them. | **`R130`'s standing warning applies**: do not start an enumeration whose population has not been measured. Extending the membrane before censusing the other families would produce a number nobody could check, and the loop that ships it would be enumerating and measuring in the same act | Census the other artifact families FIRST (how many owned terms does each name, how many are declared), then extend the population and let the corpus test go red on whatever it finds. A generalized instrument that is green on its first run has not been shown to read the new files at all<br>**2026-08-29: the warning is CONFIRMED, and the census names the design question it was hiding.** The three-way split — vocabulary term / instance IRI minted in the vocabulary namespace / SHACL shape node — is not a filter to be tuned; it must be settled *before* any membrane reads a `.ttl`, or the extension ships a tolerance in disguise (CLAUDE.md §8: a tuned constant is prima facie evidence the decision belongs elsewhere). A second contamination found in the same census, worth stating because it would silently inflate any future count: `sh:namespace` values are **literals** whose lexical form starts with the owned root, so an extractor walking `all_nodes()` without an `isinstance(n, URIRef)` guard reports the bare namespace IRI as an undeclared term. |
| ~~R144~~ (27/133 closed) | **CORRECTED IN PLACE, THEN STRUCK, 2026-08-29 — and the correction is the value; the closure is bookkeeping.** ⚠️ **THIS ROW'S CENTRAL MEASUREMENT IS FALSE, AND HAD BEEN SINCE `vocab/ontology/tab-fno-align.ttl` WAS WRITTEN.** [[R117]]'s hypothetical *was* realized: six owned IRIs — `tab:aggFnSum`, `aggFnMean`, `aggFnMin`, `aggFnMax`, `aggFnCount`, `aggFnProduct` — are subjects of `rdfs:seeAlso` in that align module, declared by no ontology, named by no query and no Python. `git grep -ln aggFn` returns the align module and two evidence-class plans, nothing else. **Striking this row silently would have erased the evidence that the register's own anti-staleness guard did not hold**, which is precisely what the row was raised to prevent — so the falsity is recorded here, in the row, before the strike. The premise was not careless: `R135`'s M5 looked for the instance and did not find it, because it looked with an `.rq`-shaped instrument in the one file family that instrument cannot read. That is the general lesson and it outlives the row: **an absence measured by an instrument that cannot see the relevant family is not a measured absence.** <br><br>**The row as originally written:** **`R117` is open on its oracle, not on a defect.** Nothing in the tree checks that the terms `iladub-hga-align.ttl` aligns are declared anywhere, so a dangling `etkl:Foo rdfs:subClassOf holon:DataHolon` would keep every test green — but no such dangling axiom exists today | The loop's M5 (spec §2.7) looked for the live instance and did not find one. `R135`'s closure does NOT touch this: `R135`'s instrument reads `.rq` files, and `R117` is about the subjects of subclass axioms in a `.ttl` — the reason `R117` was NOT struck when `R135` was | Not deferred by a decision — recorded so the absence of a live instance is on the record as a MEASUREMENT rather than as someone's later inference. A row whose hypothetical is unrealized looks identical to a row that rotted, and only writing it down tells them apart | `R143`'s generalization reaching `.ttl` subjects closes `R117` and this row together. Until then, neither should be struck on the grounds that nothing is currently broken |
| ~~R128~~ (25/118 closed) | **CLOSED 2026-08-30** by loop `the-membrane-returns-a-verdict`. `dec:supersedes` is now targeted by TWO shapes in `vocab/shapes/dec-shapes.ttl` — `dec:SupersessionShape` (`sh:targetSubjectsOf`: node-level `sh:class dec:DecisionHolon` for the declared domain; `sh:class dec:DecisionHolon` on the object for the declared range; an `sh:sparql` refusing a self-loop) and `dec:SupersededOnceShape` (`sh:targetObjectsOf`, `sh:path [sh:inversePath dec:supersedes] ; sh:maxCount 1`). **THE CARDINALITY IS ON THE IN-DEGREE, AND A MEASUREMENT DECIDED THAT.** All 7 tracked documents compiled at document scope with the subclass closure applied (the `corpus_graphs` recipe of `tests/etkl/test_vacuity_registry.py`), 2026-08-30: **9** `dec:supersedes` triples (4 cbh-stem, 5 apple; 5 of 7 documents have none), **max OUT-degree 5**, **max IN-degree 1**, 0 untyped subjects, 0 untyped objects, 0 blank nodes either side, 0 self-loops. The row invited *"a cardinality on the arc"*; `sh:maxCount 1` there would have REFUSED a correct document — apple's `.../p1/adopt#p1-datagrid-admission` supersedes five verdicts, because `document.py`'s adoption site adds one edge per superseded band in a loop. The true cardinality is the IN-degree, and it is the precondition `why-escalated.rq:18-24` and `effective-chain.rq:19-26` both REST on and neither enforces ("Unreachable today" — an assumption about the emitters, now a checked fact). **`sh:nodeKind sh:IRI` WAS WRITTEN, MEASURED, AND REMOVED** — see [[R152]]: vacuous at the compile membrane (skolemization mints an IRI for every blank node) and it would have falsified a premise `membrane._payload_nt`'s own comment records. **Artifacts:** `examples/supersession.ttl` conforms and carries the corpus's real fan-out; FOUR negatives, one per constraint, in `tests/supersession-{self-loop,two-superseders,untyped-object,untyped-subject}.ttl`; `tests/test_supersession.py` (17 tests) validates every case at BOTH seams — `iladub.validate.validate` and `membrane.validate` — and pins both engines, because `sh:inversePath` is the FIRST in `vocab/` and rudof's support for it was measured, not assumed. **FALSIFICATION:** deleting both shapes → **11 failed, 6 passed**; restored → **17 passed**. ORIGINAL ROW FOLLOWS. — **`dec:supersedes` is constrained by NOTHING** — document-only, and load-bearing for withdrawal, section repair and adoption. A malformed supersession cannot refuse at any membrane | `git grep -n "supersedes" -- vocab/shapes/` → **exit 1, no output** (re-run 2026-08-25 at this task's HEAD). Contrast `dec:condition`, `dec:rationale`'s sibling in the same event vocabulary, which `dec:EventShape` does target | Out of scope for `holon:05`, which validates the health signal and not the `dec` event vocabulary it reads. Same subject as R127 and R129 (ruling 2 of `2026-08-25-holon-05-o2-finding6-rulings.md`): **the `dec` membrane does not constrain what it derives from** | A shape targets the subjects of `dec:supersedes` — at minimum a `sh:class`/`sh:nodeKind` on the object and a cardinality on the arc — with a conforming example and a negative test that must fail, per CLAUDE.md § Serialization |
| ~~R129~~ (25/119 closed) | **CLOSED 2026-08-30** by loop `the-membrane-returns-a-verdict`. `membrane.suggester_agent(suggester_iri)` returns the `URIRef` or raises an `AssertionError` **carrying the offending value**, and it is called from all FIVE mint sites — `ground.py:_emit_candidate`, `splitkey.py:_emit_candidate`, `etkl/promote.py:_suggester`, `etkl/promote.py:emit_span_promotion`, `etkl/holon.py:_suggester_uri`. The fifth is not a proposer's string but a slug DERIVED from an escalation reason; a reason carrying a space produces the same unserializable IRI, so it is guarded on the same footing rather than exempted for being internal. **EXACTLY AS STRICT AS THE SERIALIZER, BY CONSTRUCTION** — the check is rdflib's own `URIRef.n3()`, the call that would have raised downstream, not a hand-written IRI regex (the `audit_literals` reparse idiom, and for the same reason: a second opinion can drift, and the permissive direction puts the crash back). **`AssertionError`, NOT `MembraneRefusal`:** that class's `legs` field is documented as *"never empty on a real refusal and never invented here"*, and no SHACL leg refused, because no SHACL ran. **WHY A PRODUCER-SIDE GUARD IS THE ONLY ENFORCEMENT THERE CAN BE** (CLAUDE.md § Producer-side guards, directly on the point): a graph carrying a malformed IRI cannot be SERIALIZED, so it never reaches a shape to be refused by — this is not a duplicate of a membrane constraint. **THE ROW'S OWN GAP IS CLOSED TOO** — it recorded that *"the end-to-end route from a proposer through `membrane.py:348` is read from the call chain, not driven by a test"*; `tests/test_suggester_guard.py::test_a_proposer_returning_a_non_iri_suggester_refuses_at_the_public_seam` DRIVES it through `ground_concept`. 10 tests, including one pinning that the raw `Exception` the guard replaces is catchable as neither `AssertionError` nor `MembraneRefusal`. **FALSIFICATION:** guard body replaced by a bare `return URIRef(...)` → **8 failed, 2 passed** (the 2 are correctly independent of the body); restored → **10 passed**. **Corpus-safe:** all 7 tracked documents compiled clean with the guard wired in, 2026-08-30. ORIGINAL ROW FOLLOWS. — **A non-IRI `suggester_iri` CRASHES the membrane rather than refusing.** A proposer returning `suggester_iri="not an iri at all"` dies with a raw `Exception` about N3/Turtle serialization — not an `AssertionError`, so it is neither a membrane verdict nor catchable as one, and `MembraneRefusal` (a subclass of `AssertionError`) will not catch it either | Site: `src/iladub/etkl/membrane.py:347-348`, the `skolemize(...).serialize(format="nt")` that ends `_payload_nt` (`:318`). The failure itself reproduced directly 2026-08-25 with `./.venv/bin/python`: adding `(URIRef("not an iri at all"), RDF.type, …)` to a `Graph` and serialising it raises `Exception: "not an iri at all" does not look like a valid URI, I cannot serialize this as N3/Turtle. Perhaps you wanted to urlencode it?` — the exact class and message the membrane would surface. **Stated precisely: the rdflib behaviour is measured; the end-to-end route from a proposer through `membrane.py:348` is read from the call chain, not driven by a test** | Out of scope for `holon:05` — the mint site belongs to the grounding portal, not to the compile-scope membrane this loop built. Same subject as R127/R128: the membrane does not constrain what it derives from | The mint site refuses a non-IRI suggester **at the boundary, as a verdict** — an `AssertionError` (or `MembraneRefusal`) carrying the offending value, not a serialization crash. A negative test that must fail is the oracle |
| ~~R133~~ (25/122 closed) | **CLOSED 2026-08-30** by loop `the-membrane-returns-a-verdict`. `_validate(graph, legs=())` now **REFUSES** — returns `(False, "no membrane leg was named: …", ())` — instead of raising `IndexError: tuple index out of range`. **THE DECISION THE ROW LEFT OPEN, AND THE GROUND FOR IT:** conform-with-empty-report was rejected because a validation that checked nothing and returned `True` licenses a caller to treat an unvalidated graph as one the membrane passed — CLAUDE.md § Core design principles 7, and the row's own `Prefer refuse`. `refusing` is `()`, which is honest rather than a gap: no leg refused, because no leg ran; the reason travels in the TEXT, since inventing a leg name would dress this as a SHACL verdict it is not. **THE DEFERRAL RATIONALE IS RE-MEASURED, not carried forward** — the 2026-08-29 handoff recorded that `_legs_for_document`'s returns had never been re-enumerated. They are now, by `test_legs_for_document_never_returns_an_empty_tuple`: total over its two booleans, `("tab","dec")` or `("dec",)`, never `()`. The crash was indeed unreachable from inside the tree; the repair is of a seam external callers and tests reach directly (CLAUDE.md § Producer-side guards). **Artifacts:** the `if not legs:` branch in `src/iladub/etkl/compile.py`, classified PROCEDURAL with its irreducibility stated; `tests/etkl/test_document_membrane_gate.py` (+2 tests). **FALSIFICATION:** the test was written FIRST and failed RED with the exact `IndexError: tuple index out of range` at `compile.py:523`; with the branch added → **7 passed**. ORIGINAL ROW FOLLOWS. — **`_validate` raises `IndexError` on an empty legs tuple** — the membrane's own entry point CRASHES rather than refusing or conforming | `src/iladub/etkl/compile.py:504-525`. On `legs=()`: `verdicts` is `{}`, `refusing` is `()`, so the `if not refusing:` branch is taken and `text = verdicts["tab"][1] if "tab" in verdicts else verdicts[legs[0]][1]` indexes an empty tuple at `:523`. **Driven, not read, 2026-08-25:** `./.venv/bin/python -c "…; _validate(Graph(), legs=())"` → `IndexError: tuple index out of range` | **Unreachable today, and that is the whole reason it is deferred rather than fixed** (spec §5.4): every legs tuple in the tree comes from one total function, `_legs_for_document`, which never returns empty. Fixing an unreachable crash spends the loop's budget on a path no input reaches, and the fix would need a decision — does a zero-leg validation CONFORM (nothing refused) or REFUSE (nothing was checked)? | `_validate` answers explicitly on `legs=()` — conforms-with-empty-report or refuses — with that decision justified in the code, and a test that pins whichever was chosen. **Prefer refuse:** a validation that checked nothing and returned `True` is a failure upward, the exact class CLAUDE.md §7 forbids |
| ~~R137~~ (25/126 closed) | **CLOSED 2026-08-31** by loop `close-four-mechanical-rows`. `tests/test_residue_register_integrity.py` — six arms over the three files: every index status routes, every index row has exactly one detail row in the file its status names, every detail row has an index row, a row is struck **iff** it is filed closed, no id declared twice. PROCEDURAL, with the same irreducibility M7 states one file away, argued in the module docstring. **FALSIFICATION — the row's own three recipes, each run 2026-08-31 and restored:** (a) delete the `\| R135 \| open \|` index row, orphaning its detail row → `test_every_detail_row_has_an_index_row` **FAILS** (1 failed, 5 passed); (b) flip `\| R126 \| closed \|` back to `open` while its detail row sits in `residues-closed.md` → `test_every_index_row_has_exactly_one_detail_row_in_the_file_its_status_names` **FAILS** (1 failed, 5 passed); (c) unstrike `~~R126~~` in `residues-closed.md` → `test_a_row_is_struck_if_and_only_if_it_is_filed_closed[closed]` **FAILS** (1 failed, 5 passed). (a) and (b) were the two that stayed GREEN across 67 tests when this row was raised. **DELIBERATELY NOT CHECKED, said in the docstring rather than left to be discovered:** whether an index line *says the same thing* as its detail row — a semantic question about prose, where this is a structural one about rows. ORIGINAL ROW: **The residue register — which CLAUDE.md calls canonical and directs every contributor to read — has NO integrity check.** An index row and its detail row can disagree, or one can vanish, and the suite stays green | Three falsifications run by this task, 2026-08-25, each restored from a backup copy afterwards, each against `tests/test_cockpit.py tests/test_arc_queries.py tests/test_arc_manifest.py tests/test_doc_governance.py`: **(a)** delete the whole `\| R135 \| open \|` index row, leaving its detail row orphaned in `residues-open.md` → **67 passed** (only the pre-existing `test_the_live_newest_handoff_declares_a_topic` red); **(b)** flip `\| R126 \| closed \|` back to `open` while its full row sits in `residues-closed.md` → **67 passed**; **(c)** the control arm, delete `\| R43 \|` — a row a `prog:blockedBy` NAMES → **M7 fires**, `"criterion:tab:02 is prog:blockedBy 'R43', which is not a row of .../residues.md"`. So the guard exists and works, and its scope is exactly the rows the manifest happens to name | Out of scope for `holon:05`. It is also not a defect of any one loop: the register was split into an index plus two detail files on 2026-08-12 for a context-budget reason, and the integrity check the split needed was never written | A test asserts the three-file invariant directly: every index row has exactly one detail row, in the file its status column names; every detail row has an index row; and a struck (`~~Rn~~`) detail row appears only in `residues-closed.md`. It is a ~20-line procedural test over markdown — the same irreducibility M7 states — and it would have caught both (a) and (b) |
| ~~R138~~ (25/127 closed) | **CLOSED 2026-08-31** by loop `close-four-mechanical-rows`, by the row's **preferred** remedy: §4.5's five citations are now SYMBOL references — `_seal`, its membrane-health derivation (`graph += interpret.run(MEMBRANE_HEALTH_RQ, graph)`), its `if not conforms:` branch — which cannot drift when unrelated code above them grows (CLAUDE.md plan-rule 7). The section is headed by a `⚠️ POINTERS CORRECTED 2026-08-31` note carrying the measured mapping, so a dated Evidence artefact is repaired visibly rather than silently rewritten, and `_seal`'s own docstring already cites `spec §4.5` back by symbol. **PLAN-RULE 7 WAS COMMITTED AND CAUGHT INSIDE THIS VERY EDIT, which is the finding worth more than the repair:** the note first listed the file's *other* stale citations by line number — a downward same-file citation that the note's own insertion shifted (`:890`→`:912`, `:905`→`:927`). Measured before writing, false once written — this is [[R139]]'s SECOND measured instance, in a different medium, and it is recorded in that row too. Re-measured AFTER the edit and replaced with a `grep -n "document.py:16"` command, which has no line number to invalidate. **OUT OF SCOPE by the row's own wording, and left standing rather than quietly widened:** the same drift in other sections of the file; the note names the command that finds them. ORIGINAL ROW: **Spec §4.5's citations to `document.py:1624`/`:1626` no longer resolve to `_seal` — they land inside the unrelated page-scope-adoption comment that grew below it.** Closing bookkeeping for `holon:05`, raised after R127-R137 were already written | Re-measured 2026-08-25 at HEAD, not carried from the row's draft. Within §4.5 (`docs/superpowers/specs/2026-08-25-the-membrane-reports-its-health-design.md:461-508`): `:1624` appears **twice** (`:465`, `:476`) and `:1626` appears **three** times (`:466`, `:486`, and inside the range `:1609–1626` at `:495`) — five citations total, but split 2-and-3, **not** the `:1624`×4 this row was drafted against; the discrepancy is recorded because the instruction was to write what reproduces. All five resolve, in `src/iladub/etkl/document.py` at HEAD, into `:1609-1636` — the `# ---- ADOPTION (spec 2026-08-09, R73)` comment (`grep -n "ADOPTION (spec" src/iladub/etkl/document.py` → `:1609`), not into `_seal`. `_seal` itself spans `:1175-1332` (`def _seal` at `:1175`; its `raise MembraneRefusal(...)` at `:1331-1332`; `def compile_document` next, at `:1335`). The membrane-health derivation is `graph.remove(...)` / `graph += interpret.run(MEMBRANE_HEALTH_RQ, graph)` at `:1323-1324`; the refusing branch is `if not conforms:` through the raise, `:1326-1332` | The spec text was written prospectively, before `_seal` was extracted into its own function — this is drift inflicted on a dated Evidence artefact by the implementation that followed it, not a claim made from reading (CLAUDE.md plan-rule 2's "measure before writing" does not reach this case: the claim was true when the spec was written). **Correction to the row as drafted:** the closest already-registered shape is [[R124]] (spec §4.4's stated mechanism superseded by the shipped materialise-before-unlink ordering) — both are a spec's prospective description overtaken by the code that followed it — **not R132**, which this row was first drafted against; R132 is the single-document-IRI residue, an unrelated shape, and the citation is corrected here rather than carried forward uncorrected | Re-measure §4.5's five citations against `document.py` at whatever commit the closing loop runs and update the line numbers, **or** replace them with symbol references (`_seal`'s membrane-health derivation, its refusing branch) that cannot drift when unrelated code above them grows |
| ~~R152~~ (34/142 closed) | **CLOSED 2026-08-31** by loop `close-four-mechanical-rows`. `test_no_membrane_shape_can_see_the_skolemization` in `tests/etkl/test_compile_membrane_shapes.py`, parametrized over every shape file that reaches `membrane._payload_nt`, asserting no `sh:nodeKind` and no `sh:select`/`sh:ask`/`sh:construct` body naming `isBlank`/`isIRI`/`isURI`/`BNODE`. **SCOPE WIDENED FROM THE ROW, and measured rather than assumed:** the row prescribed `wired_shape_files()` — the compile leg — but `_payload_nt` skolemizes for **three** legs (`compile.py:546`, `feed.py:615`'s `_GROUND_SHAPE_FILES`, `tiling.py:70`), so the premise is about all of them. The guard reads the compile ∪ grounding union from the modules that wire it (5 files: `dec`, `escalation`, `iladub`, `tab`, `tab-physical`), and a second arm — `test_the_tiling_leg_adds_no_shape_file_of_its_own` — PROVES the tiling leg is covered by inclusion instead of asserting it. **FALSIFICATION, each run 2026-08-31 and restored:** (a) `sh:nodeKind sh:IRI` added to a property shape in `dec-shapes.ttl` → arm `[dec-shapes.ttl]` **FAILS**; (b) `FILTER(isBlank(?this))` injected into a `sh:select` body in `tab-shapes.ttl` → arm `[tab-shapes.ttl]` **FAILS**; (c) `tab-physical-shapes.ttl` dropped from `_TAB_SHAPE_FILES` → the tiling arm **FAILS**. **THIS DOES NOT MAKE `sh:nodeKind` WORK at the membrane** — that is [[R88]]'s territory and would need the rudof blank-node incapacity re-measured first. It fails the day the hazard comes into existence. ORIGINAL ROW: **`sh:nodeKind sh:IRI` IS VACUOUS AT THE COMPILE MEMBRANE BY CONSTRUCTION, AND WOULD LOOK ENFORCED.** `membrane._payload_nt` SKOLEMIZES the data graph before either engine sees it (R88's unpin), so every blank node has become an IRI by the time a shape runs. Any `sh:nodeKind sh:IRI` a future shape author writes is therefore satisfiable-by-transport at the seam that matters operationally, while refusing correctly at `iladub.validate.validate` — the seam vocabulary examples are tested at. The two seams disagree, silently, in the permissive direction. | 2026-08-30, loop `the-membrane-returns-a-verdict`, while closing [[R128]] — whose stated closure criterion invited *"at minimum a `sh:class`/`sh:nodeKind` on the object"*. The constraint was WRITTEN, then measured and REMOVED. `tests/supersession-blank-object.ttl` (a `dec:supersedes` whose object is a properly-typed blank-node decision) gave `pyshacl_rdfs=False membrane=True` — same shapes, same data, opposite verdicts — and the fixture was deleted rather than shipped as a negative that only half-refuses. **THE SECOND HALF, and it is why this is a row and not a note:** `_payload_nt`'s own comment records the premise that makes skolemization verdict-neutral — *"no shape file uses `sh:nodeKind` and no `sh:sparql` body tests `isBlank`/`isIRI`/`isURI`/`BNODE` (spec §2 P4)"*. That premise is TRUE at HEAD and was verified here: `grep -rn "nodeKind" vocab/shapes/` → no output, `grep -rn "isBlank\|isIRI\|isURI\|BNODE" vocab/shapes/` → no output. It is a claim about the SHAPES, held only by nobody having written one — nothing enforces it. | Not deferred out of cost: shipping the constraint was the tempting move and it is the WRONG one, so the loop removed it and recorded why instead. Fixing the general hazard is a decision about the membrane's transport contract, not about `dec:supersedes` — either skolemization stops being invisible to shapes (which reopens R88's rudof blocker) or the premise becomes a checked fact rather than an unguarded comment. Neither belongs in a loop closing four repair rows. | A guard makes `_payload_nt`'s premise machine-checked: a test over `wired_shape_files()` asserting no wired shape uses `sh:nodeKind` and no `sh:sparql` body names `isBlank`/`isIRI`/`isURI`/`BNODE` — the same shape as `tests/etkl/test_vacuity_registry.py`'s registry arms, and cheap. It fails the day someone writes the constraint, which is the moment the hazard exists. **Closing it does NOT mean making `sh:nodeKind` work** — that is R88's territory and would need the rudof blank-node incapacity re-measured first. Related: [[R130]] (a shape can be idle for a reason the registry cannot see), [[R128]]. |
| ~~R139~~ (25/128 closed) | **A same-file citation pointing DOWNWARD is invalidated by the very edit meant to correct it — measuring the claim before writing it is not a sufficient guard for this class.** **CENSUS RUN 2026-08-31 — THE INSTRUMENT AS PRESCRIBED IS REFUTED, AND A NARROWER ONE IS SHIPPABLE.** The remedy this row's disjunct (a) names — *flag a `file:line` citation whose target is the same file and whose line is below its own position* — was measured over all 578 tracked `.py`+`.md` files at `794f518`, and it fails on the OPPOSITE side from the one predicted. **Recall 0/4:** the explicit `basename:NNN` form the rule describes occurs **once** in the whole repo, upward, inside a test assertion string; every real instance of this class is a BARE `:NNN`, which that regex never sees. **Precision 0.7%:** resolving a bare `:NNN` to *this* file — what the rule assumes — is wrong 521 times in 525, because in this repo a bare `:NNN` overwhelmingly means *the source file named upstream in the passage*, and 793 of 822 such hits sit in `.md` evidence docs pointing at `compile.py`/`document.py`. The quotation problem this row's closing loop predicted **is real but small** (226 of 525 naive hits are register/handoff quotations, yet only ONE is genuinely same-file, and it carries an explicit past-tense marker on its own line). **The filter ladder, exact:** naive 521/525 FP → drop fenced code 489/493 → resolve the referent 37/41 → drop targets beyond the file's own EOF 10/14 → restrict to `.py` comments **1/5**, with recall 4/4 held at every tier. So the shippable form is *source comments only* (`.py`/`.ttl`/`.rq`), bare + explicit, EOF filter, paragraph-scoped referent, past-tense suppression: **5 flagged, 4 real, 20% FP**. Over `docs/**` it is 71–99% FP and must NOT be run there — markdown here is a citation *narrative* whose referents cross heading boundaries, which no bounded lexical window resolves. A third form, prose *"at lines 12, 133"*, scores **0/44** — categorically the wrong referent domain: it almost always means lines of a **compiled PDF document**, not of a source file. **A THIRD LIVE INSTANCE WAS FOUND BY THE CENSUS AND FIXED IN THE SAME ACT:** `compile.py`'s comment on the `escalation-shapes.ttl` wiring cited `:1124` for the page-scope `_validate` call, which is at `:1200` — **76 lines out, and rotted TWICE** (`:1083`, corrected 2026-08-17, → `:1124` → wrong), *inside the very sentence explaining that a line citation two hundred lines above its subject rots*, and beside the `grep` that disproves it. Repaired line-neutrally (6 lines in, 6 out, re-measured AFTER) by deleting the number and promoting that grep to the citation. **WHAT IS STILL OPEN, and it is now a DESIGN decision rather than an unknown:** build the narrow source-comments lint, or record disjunct (a) as refused. The measurement no longer blocks either. **NOT independently re-verified:** the 117 of 521 FPs whose cross-file status is asserted rather than proven by the EOF test — the other 371 are provable, and the four LIVE hits were each re-measured by hand.  **SECOND MEASURED INSTANCE, 2026-08-31, in a DIFFERENT medium — the class is not Python-specific and the convention (b) did not prevent it.** Closing [[R138]] added a `⚠️ POINTERS CORRECTED` note to §4.5 of `docs/superpowers/specs/2026-08-25-the-membrane-reports-its-health-design.md`; the note listed the file's *other* stale citations by line number, all of them BELOW the note, and inserting the note shifted them (`:890`→`:912`, `:905`→`:927`). Measured before writing, false once written — plan-rule 7's exact failure, committed while applying plan-rule 7's remedy elsewhere in the same edit. Caught by re-measuring AFTER, and repaired by replacing the line list with a `grep -n "document.py:16"` command, which has no line number to invalidate. **What this changes for the row:** the instrument half (disjunct (a)) is no longer prospective — it now has **two** measured instances, one in `.py` and one in `.md`, and in both the author knew the rule and violated it anyway while obeying it. A convention a rule-aware author breaks twice is evidence for an instrument, not for restating the convention. The general lesson from `holon:05`'s final fix wave, which was itself correcting stale `file:line` citations | Confirmed at HEAD: the comment inside `_seal` at `src/iladub/etkl/document.py:1204-1207` reads *"Both writers of those edges — `:1536` (section repair) and `:1740` (datagrid adoption), which `grep -n \"DEC.supersedes\"` on this file shows are the only two … — write into THIS graph."* `grep -n "DEC.supersedes" src/iladub/etkl/document.py` returns exactly two hits, `:1536` and `:1740`, matching the comment's own numbers — line-neutral and correct at HEAD. `git show 31b10ee -- src/iladub/etkl/document.py` (the parent commit, `docs(holon:05): re-measure every citation and census in the seam's comments`) shows the block replacing three comment lines with three (`:1486`/`:1690` corrected to `:1536`/`:1740`), and its own "I4" paragraph states the reflow was deliberate: *"Reflowed line-neutrally on purpose: the block cites lines BELOW itself, so a +1 would have made the correction stale on arrival (it did, on the first attempt, before re-measuring caught it)."* The failed first attempt itself never reached a commit — it was caught and reflowed inside the same session — so this row is the only record of it | Not a live defect: the current state (post-`31b10ee`) is already correct and line-neutral, so there is nothing to fix in `document.py`. Deferred because CLAUDE.md plan-rule 2 ("every load-bearing claim about existing code is MEASURED, and carries its measurement inline") is necessary but **not sufficient** for this class: a comment citing lines below its own position in the *same file* can be falsified by the act of editing the comment itself, even though the citation was true at the moment it was measured. No rule in this repo currently distinguishes "cites another file" (safe to measure-then-write) from "cites downward within the same file" (the write can invalidate the measurement) | Either (a) a check that flags same-file line citations pointing below their own position and forces a re-measure pass **after** any edit that could shift them, not only before, or (b) a convention that same-file downward references cite symbols/anchors rather than line numbers, removing the hazard instead of guarding it. **PARTIALLY ADDRESSED 2026-08-25 (maintainer request): disjunct (b) landed as CLAUDE.md § Plan authoring discipline rule 7, which states the symbol-over-line preference, mandates a re-measure AFTER any edit touching a downward same-file citation, and adds the class to the reviewers-enforce list (now "all seven"). The row is NOT struck: rule 7 is reviewer-enforced prose, and disjunct (a) — a check that flags such citations — remains unbuilt, so nothing in the suite would catch the next instance. Rule 7 says so in its own text.** **CLOSED 2026-08-31 — THE INSTRUMENT IS BUILT, and the maintainer ruled to build rather than refuse (the census had left a design fork with two defensible answers and no measurement able to decide it).** Disjunct (a) ships as `tests/test_source_citations.py` + `tests/source_citations.py` (PROCEDURAL extractor) + `vocab/shapes/source-citation-shapes.ttl` (AXIOM, closed world) + `vocab/internal/srccite.ttl` (the declaration), spec `docs/superpowers/specs/2026-08-31-the-comment-cannot-cite-below-itself-design.md`. **The rule: in a tracked `.py`/`.ttl`/`.rq` COMMENT line, a `:NNN` token whose referent is its own file and whose target is below the comment and within the file's length is REFUSED.** It reproduced the census exactly on arrival — 7 tokens on 4 sites — and all four were REPAIRED, line-neutrally and re-measured AFTER the write: `document.py:138` and `:1206-1207` now cite the greps (`MEMBRANE_HEALTH_RQ`, `DEC.supersedes`) instead of line numbers; `compile.py:410-411`'s two numbers were never citations but quoted historical VALUES and are now written `line 1083`/`line 1124`; `arc-m19-false-edge-leak.ttl:13` carried two REAL cross-file citations whose anchors were unspellable stems, now written in full as `risk-shapes.ttl:17`/`dec-shapes.ttl:48`. **NO ALLOWLIST AND NO EXEMPTION MECHANISM SHIPS** — a past-tense or nearby-word suppressor would be the tuned lexical constant CLAUDE.md §8 forbids, and each of the four re-wordings makes its comment MORE accurate, which is the test of whether a refusal without an escape hatch is honest. **TWO THINGS THE CENSUS GOT WRONG, found by the implementation's own falsification pass and recorded because this row is where they are cheapest:** (i) the census's *paragraph-scoped referent* was never exercised — its regex put the slice-exclusion lookbehind after the optional anchor group, so the anchored branch could never match and every cross-file citation was suppressed by the EOF clause alone. With a working two-branch lexicon the referent rule is LOAD-BEARING: deleting it turns the tree oracle RED with **10** hits. (ii) The referent unit is the PARAGRAPH, not the contiguous comment BLOCK — a block-scoped referent leaked `feed.py:586` from `compile.py:402` onto `:1083` eight lines later and HID a real instance; the longest `#` block in this tree is **122** lines (`tests/arc-manifest.ttl:644`). **Falsified per plan-rule 4:** F1 a throwaway downward citation in `feed.py` flags and clears; F2 deleting `?cites <= ?len` reddens the EOF oracle; F3 deleting the anchor inheritance reddens 3 tests incl. the tree; F4 deleting `?ref = ?self` reddens 3. **WHAT IS NOT CLOSED:** `docs/**` — the census measured 71-99% FP there, and R139's OWN SECOND INSTANCE was in `.md` and is invisible to this instrument. Raised as [[R153]] rather than left inside a struck row. |
| ~~R45~~ | **CLOSED 2026-08-31** by loop `a-header-level-is-a-band-line` (spec `docs/superpowers/specs/2026-08-31-a-header-level-is-a-band-line-design.md`). **THE DEFECT was NOT the cross-tab span assignment**, which two prior loops framed it as and which is correct and unchanged: it was `matrix._level_tops`, which discarded the band's own line grouping and re-derived header levels from `round(w.top, 1)`, then filtered each level's labels at a `< 0.5` point tolerance. WHO page 0's one visual header line carries two baselines 0.9pt apart (`Year: | Month | ... | L | M | S` at top 118.7, `-3 SD ...` at 119.6), so that sub-point drift was promoted to a third header level, tearing the leaf row over overlapping columns and orphaning two nodes — which `tab:UnambiguousAccessShape` then **correctly** refused. The shape was right; the tree fed to it was wrong. `_level_tops` is deleted and a header level is now a band line (`band.lines[:split]`), the same grouping `header_rows_of` indexes; no tolerance is applied in that function at all. Classified **AXIOM** on a fresh argument (spec §4) — it deletes a tuned constant rather than adding one, and consumes a line grouping the band already asserts. **NOT ratified by the maintainer.** | **Closure evidence, all re-measured at the closing commit.** (1) whole-corpus two-sided oracle, `compile_document` on all 7 documents: WHO `0.5597 -> 0.9095966620305981`, tokens `445/350 -> 654/65`, verdicts `escalated 3 -> 0` (10 asserted / 8 ignored); the other six byte-identical on score, every verdict counter, every escalation reason, `tokens_asserted`, `tokens_escalated`, `adopted` and `repaired_bands` — **apple's `MATRIX_AMBIGUOUS x2` survives**, which is `R62` and is untouched. (2) `tests/etkl/fixtures.py::crosstab_drifting_leafrow_pdf` + `test_matrix.py::test_sub_point_leaf_drift_is_one_header_level` and `test_closing_slice.py::test_crosstab_with_sub_point_leaf_drift_compiles`, shown RED at HEAD, GREEN after, RED again with the fix reverted. (3) `test_corpus.py::test_expected_verdict[health/who-wfa-boys-zscore-0-5.pdf]` passing against an ACCEPTING `cor:adjudication` and `cor:scoreFloor 0.90` | Deferred from 2026-08-04 to 2026-08-31 across seven loops; the two immediately prior loops (`refocus-on-etkl`, `who-tree-refutation`) each named a different subject and each had its propositions refuted by measurement before this one landed on `_level_tops` | **What this closure does NOT license, and the reader must not read into it.** (a) `prog:criterion:tab:05` stays `prog:met false` on **`R62`** — apple's two firings are unchanged; only the `R45` `prog:blockedBy` edge is deleted. (b) **The corpus oracle behind this is LOW POWER**: a reach probe (spec §3.4.1) counted calls to `infer_column_tree_by_proximity` per document — `graincorp-stem 0 · graincorp-capacity 0 · bfs 0 · ons 0 · cbh 0 · apple 2 · who 3` — so five of the six unchanged rows are **vacuous**, and `graincorp-stem`'s 0.95 floor is protected by non-reach, not by robustness. The whole negative evidence is `apple`, reached twice, tree identical both times. (c) **`R154` is untouched and is now shipping inside an assertion**: the table WHO now asserts carries top-level header nodes reading `'Z-s'`, `'res (weight'`, `'kg)'` — structurally correct, textually wrong. That is the strongest standing argument for prioritising `R154`. (d) The tiling oracle certifies CONSISTENCY, not fidelity: the recovered tree was read against `extract_words` output, never against the published WHO table by a human. (e) The `0.6 x median glyph height` tolerance is **consolidated, not eliminated** — `text_lines` and `rule_aware_lines` still own it; this loop took the number of places deciding what a line is from three to two, not to zero |
| ~~R98~~ (18/88 closed) | **CLOSED 2026-08-31** **`tab:LicenceRefusalShape` is idle BY DESIGN, and spec §3's stated reason for it is WRONG** | R87 Task 5, 2026-08-16. Spec §3's M7 table gives the reason as *corpus does not exercise it*. **Measured: who-wfa DOES refuse a continuation licence, on pair (1,2), and `DocumentReport.refused_licences` records it.** The graph FACT is withheld deliberately: `document.py:1246-1253` writes `cur tab:licenceRefused prev` only when BOTH refused regions asserted a table, because the fact names two table URIs, and the inline comment states that asymmetry is intended (the same one `recognized`/`chains` already has). So the shape has 0 focus nodes and `tab:licenceRefused` is absent from all 7 documents — idle by both criteria, for a reason that is not a corpus gap. | The behaviour is correct as written. What is unknown is whether the state that would make the shape speak — a refusal between two ASSERTED tables — can occur at all. | Either a specimen (or fixture) where two asserted tables have their continuation licence refused, which makes the shape live; or a finding that the state is unreachable, which makes the shape dead and closes this as a deletion. **Do not close it by writing the edge unconditionally** — that would assert a fact naming a table URI that does not exist. **CLOSURE, on the FIRST of the two outcomes this row named — a specimen, not a proof of unreachability.** The `R45` loop (`docs/superpowers/2026-08-31-r45-closed-handoff.md`) made who-wfa's three pages stop escalating `MATRIX_AMBIGUOUS`, so both halves of its already-refused licence pair `(1, 2)` now assert a table and the writer's own condition — *both refused regions asserted a table, because the fact names two table URIs* — is met. MEASURED at the closing commit, `compile_document` on who-wfa: `refused_licences == ((1, 2),)` **unchanged**, and **one** `tab:licenceRefused` edge, `p2#mtable1 -> p1#mtable2`. `tests/etkl/test_vacuity_registry.py::test_no_registered_shape_has_gone_live` went RED on `tab:LicenceRefusalShape` **before** anything was edited — the registry's own rot-guard found this, not a person reading it. The registry row is deleted with its measurement recorded in place, and `prog:criterion:tab:10`'s `prog:blockedBy "R98"` edge is deleted per the maintainer's 2026-08-22 `R108` ruling; `tab:10` stays `prog:met false` on `R97`, `R99`, `R100`. **NOT closed by the prescribed route**: nothing was written unconditionally and no fixture was authored — an unrelated reading fix changed which state the corpus reaches, which is the case this row was waiting for and could not predict. |
| ~~R154~~ | **CLOSED 2026-08-31** by loop `a-boundary-that-cuts-ink-is-not-a-boundary` (spec `docs/superpowers/specs/2026-08-31-a-boundary-that-cuts-ink-design.md`). **The row asked for a discriminator between two populations; the measurement says there is only ONE.** The row reads its census as *"cbh/ons/bfs/gstem crossings are 100% genuine spanners"* against *"apple and who ... sub-point overhang"*. Sampling what is actually in each set shows the same phenomenon in both — a text run drawn across the grid and chopped: cbh `'GERALDTO'\|'N'`, ons `'1TheIOSo'\|'utputisdesigna'`, apple `'ThreeMo'\|'nthsEndedNineM'`, who `'Z-s'\|'cores(weightin'`. A "genuine spanner" here is a **banner or footnote spanning the table**, never two cells pdfplumber wrongly merged, so no boundary in the crossing set is a division the author drew and none needs to be preserved. **THE FIX** is `geometry._row_dividers`: an interior boundary divides a row only where the ink on both sides CLEARS it — `ruledroles._within`'s predicate applied one stage earlier, at the moment the chop would be made rather than downstream where it is detected. No new constant; `COORD_EPS` alone, already justified there as *"NOT a clearance threshold ... only a non-zero one"*, which is why WHO's 0.03pt overhang reads as a chop. **The decision is ROW-LOCAL and that is what separates it from the word-atomicity variant this row records as measured to fail**: `xs` is never mutated, so a boundary declined by a banner row still divides the twelve data rows beneath it and still reaches `refine_rule_columns`, `confirmed_boundaries` and `Band.column_xs` — `header_body_split` does not collapse. **EVIDENCE.** Fidelity, `page_bands(who,0)[2]`: line 0 `['Z-s','c','o','res (weight','i','n','kg)']` → `['Z-scores (weight in','kg)']`, and line 1 — `['Year: Month','Month','L','M','S','-3 SD',...,'3 SD']` — **byte-identical**, so the thing this row warned the coarse word swap breaks (`'-3 SD'` → `'-3'`,`'SD'`) does not break. Corpus, both modes, `validate_shapes=False`: gstem 0.96546→0.96589, gcap 1.0→1.0, cbh 0.90466→0.90919, apple 0.35560→0.35870, bfs 0.34384→0.34644, ons unchanged, **who unchanged at 0.9095966620305981**; every escalation reason counter unchanged **at document scope** — **and NOT at page scope, where cbh regresses (`compile_tables(cbh,0)` score `0.0698→0.0571`, four regions `MERGE_AMBIGUOUS→REGION_TILING_FAILED`), see [[R156]]**. **WHAT THIS CLOSURE DOES NOT LICENSE.** (1) **The score movements are not the result and must not be cited as one** — four rises of 0.0004–0.0045, each partly a DENOMINATOR effect, since welding two chopped fragments removes a token (cbh `asserted+escalated` 902→881). A smaller denominator is not better reading. The oracle here is two-sided and NEGATIVE: nothing regresses. (2) **This is a fidelity repair, and WHO's score does not move at all.** (3) `graincorp-stem` is a near-vacuous PASS row — `flush=1` — exactly the near-inertness this row predicted; the load-bearing negative evidence is cbh (53), apple (25), ons (20). (4) **The row's wider claim stands**: one division survives on WHO's header line, at a WORD GAP (`b=594.35`, `n` clearing 2.591pt, `k` clearing 0.167pt), because at character level a word gap and a column gutter are the same picture — see [[R155]]. (5) The twin stroke edges that make WHO's boundaries dense are NOT deduplicated; that still needs the width constant this row forbids. | Raised 2026-08-31; closed the same day, by the loop the predecessor handoff graded PROPOSED — the prediction was run BEFORE any plan was written, which is the only reason the two-population framing was caught |
| ~~R158~~ (41/147 closed) | **CLOSED 2026-09-01** by loop `corpus-reach-measured`. **THE ROW'S PREDICTION WAS RUN AND IS REFUTED.** Its successor handoff set the falsifiable form — *"most changed functions are reached by 3 or fewer of the 7 documents"* — and required it be run before anything was built on it. Measured over the 16 functions changed by the last six `src/`-touching loops: **6 of 16 (37.5%) reached by <=3 documents; 10 of 16 reached by ALL SEVEN.** The reframing that [[R68]], [[R97]], [[R99]], [[R45]] and PR #109 are *"five framings of one fact — the corpus does not enter the code under test"* **does not survive as a corpus property**: those five are the tail, not the rule. [[R45]]'s `infer_column_tree_by_proximity` reproduces exactly (gstem 0 · gcap 0 · cbh 0 · ons 0 · bfs 0 · apple 2 · who 3), so the row's evidence was right about its instances and wrong about their generality. **The row's stated closure condition is also met**: `scripts/reach_probe.py` is committed, prints reach beside the score, and answers for any function without a bespoke probe — the thing `grep -rl 'reach probe' src/ scripts/ tests/` could not find. **The instrument's own first version was wrong, in the understating direction, and that is the reusable finding**: compiling all seven documents in ONE process attributed every `functools.lru_cache`d function to whichever document ran first, reporting `_build_membrane` at 1/7 instead of 7/7. One interpreter per document is the fix, and `tests/test_reach_probe.py::test_run_gives_every_document_a_fresh_interpreter` pins it (falsified: reverting to a shared process fails it RED). What this closure does NOT settle is carried forward as [[R159]] | **Corpus REACH is unmeasured as a corpus property — five register rows and two loop findings are five framings of one fact: the 7-document corpus does not enter the code under test.** | closed, not deferred | — |
| ~~R161~~ (42/150 closed) | **CLOSED 2026-09-03** by `r161-section-repair-measurement` — the measurement the row asked for was RUN with a committed instrument (`scripts/section_repair_census.py`; readings in `docs/superpowers/2026-09-03-r161-section-repair-measured.md`). **Recognition runs on every apple page and derives no group, for two stacked reasons:** (1) `_leading_box_y` selects a 0.96pt inter-cell gap as the header box on every escalated band (apple's "rules" are spreadsheet cell-rectangle edges), so `_header_box_text` abstains and the header-box half of `section-repeat.rq`'s identity is emitted for at most one band per page — while the signature half agrees across EVERY band on p0 and p2; (2) apple's sections print DIFFERENT headings, so even a perfect candidate could never match — **section repair is the wrong instrument for this document by design, not by defect**, and the row's "silently declines to engage" described a tool applied outside its population. The eight `REGION_TILING_FAILED` bands all refuse on `CoverageShape`: header-less body sections of one statement table. Successor: [[R165]]. The row read: **Loop Q's section repair never fires on apple's p0 or p2 — 8 of the document's 11 escalations are `REGION_TILING_FAILED`, and `DocumentReport.repaired_bands` is the empty tuple on both pages.** The repair pass that stitches a wrongly-tiled region back together (loop Q, closing [[R154]]/[[R98]]'s successor) is wired and green on every corpus document that reaches it, and silently declines to engage on apple's own bands | Measured pre-loop, Task 1's JSON census on `the-body-starts-at-the-stub-build` (HEAD `3055ae8`): apple p0 bands 3, 5, 6, 7 and p2 bands 3, 4, 5, 7 all escalate `REGION_TILING_FAILED`; `repaired_bands == ()` on both `DocumentReport`s. Not re-measured post-loop — this loop's change (matrix body start + the uncarried-ink guard) touches neither `sectiongraph.py` nor the repair driver, and spec §4 scopes the question out | **The cause is unmeasured, and is larger than this loop's subject** — this loop's fixtures and code touch `matrix.py` and the two candidacy gates, not section recognition. Nobody has yet dumped why `sectiongraph.section_candidates` sees nothing to repair on these eight bands, as the WHO/apple tree dumps in this spec's §1.1 did for the matrix reader | A measurement of why `sectiongraph.section_candidates` recognizes no candidate section on apple p0 bands 3/5/6/7 and p2 bands 3/4/5/7 — dumping its own evidence graph the way this loop's spec §1.1 dumped `classify_matrix`'s tree — followed by a loop sized to whatever that measurement finds |
| ~~R165~~ (43/154 closed) | **CLOSED 2026-09-04** (loop `the-run-is-one-band`) - the run is proposed as ONE band and the existing tiling membrane disposes of the proposal. `vocab/queries/band-run.rq` (AXIOM, open world) joins two ADJACENT ruled bands when one's set of distinct rounded rule x-positions subsumes the other's, over `sectiongraph.run_evidence`'s transient per-page graph; `sectiongraph.merge_run_candidates` walks the derived pairs into maximal contiguous chains; `compile.merged_run_admissible` offers each merged run to the UNCHANGED closed-world chain `is_matrix_candidate -> classify_matrix -> assert_matrix_region -> region_tiles` on a scratch graph, and `compile.page_bands` splices only what the membrane accepts. MEASURED ON THE SHIPPED TREE, not the prototype every earlier figure in this row came from: 14 candidate runs across the corpus, of which exactly TWO are accepted - apple p0 2..7 and apple p1 2..7, both page tails. apple document score **0.18950437317784258 -> 0.6288659793814433**, per page p0 1.0 (asserted=124, escalated=0), p1 1.0 (asserted=56, escalated=0), p2 unchanged at 0.027027 because its run 3..7 is REFUSED at `is_matrix_candidate`. The SPARQL derivation was cross-checked against the committed `scripts/band_run_census.py` plain-Python relation on all 27 corpus pages: 0 mismatches, the same 14 runs. THE MEMBRANE HAS NOW SEEN A MERGED BAND - nothing before this loop had ever measured that - and `compile_document(validate_shapes=True)` returns the IDENTICAL score, so the merged holon crosses the contract membrane unrefused. The row's own REFUTED `tab:ruleXsSignature` licence is honoured: nothing here changes the split unconditionally. Band-index renumbering: still zero on this corpus (every accepted run is a page tail), and the non-tail case is FORCED in test rather than left unexercised - see [[R171]]. Oracle set: `tests/etkl/test_band_runs.py` (the derivation, the emitter's honest abstain, disjointness, the 8-field merge contract) and `tests/etkl/test_run_merge_seam.py` (O2 the fallback, the refusal leaving the page graph-identical, M1, O3 corpus-wide no-regression, O4 fragment/position, O5 the forced non-tail merge at page AND document scope). Fourteen measured test re-baselines carried in the same change. NOT closed by this: [[R160]] (adopted is `()` before and after - the one-band reading makes adoption UNNECESSARY on p0/p1 rather than restoring it, so the reader-authority question is untouched), [[R166]]'s p2 half, [[R167]], [[R168]], [[R169]]. Successors raised: [[R170]], [[R171]], [[R172]]. Original text follows, uncorrected: **Apple's escalated statement bands are header-less intra-page CONTINUATIONS of one table, and nothing carries a header from band to band within a page.** Every `REGION_TILING_FAILED` band on all three pages (p0 bands 3/5/6/7, p1 bands 3/5/7, p2 bands 3/4/5/7) is refused by the tiling membrane's `CoverageShape` + `UnambiguousAccessShape` on every leaf column — *"Leaf column is not covered by any header node of its table"* — because the section band (`Operating expenses:`, `Earnings per share:`, `Operating activities:` …) carries data columns and no header row of its own: the statement's single column header sits in band 2 at the top of the page, asserted, and the section bands are body continuations of that one grid, split from it by whitespace and a heading line. All six ruled bands on p0, and all five on p2, share ONE `tab:ruleXsSignature` — the author's own evidence that they are one table | Measured 2026-09-03 on `main` at `4cfee38` with `scripts/section_repair_census.py` (readings pasted in `docs/superpowers/2026-09-03-r161-section-repair-measured.md` § 3): 4 + 3 + 4 refusals, all `CoverageShape`; signature census `distinct rule-x signatures=1` on p0 and p2 (3 on p1). The rules themselves are spreadsheet cell-rectangle edges (vertical rules 13.2–14.4pt tall, hrule pitch 13.2pt with 0.96pt inter-cell gaps). The carriage seam exists page-to-page only: `compile_tables(carried_header_roles={band: reading})` (`compile.py:570`), fed from `document.py:1435` by a `continuation-of.rq` match whose clause (a) requires BOTH leaf header rows to exist | **A design, not a fix, and it is one decision with [[R160]]** — a header-less band can be licensed as a continuation of the asserted band above it only by a NEW intra-page licence (AXIOM candidate: same rule-x signature, no header row, the leaf-column count of the header band), and carrying that band's header roles through loop M's seam has never been driven from the same page — the reading object may carry page-scoped assumptions. Asserting p1's section bands under a carried header changes which reader has authority on the page whose datagrid adoption [[R160]] already lost, so the two rows must be specified together. The handoff's part 5 proposes the falsifying spike (forced `carried_header_roles={3: <band 2's reading>}` on apple p0) and grades it PROPOSED | **The spike was RUN 2026-09-03 and REFUTED on the mechanism, on all three pages** (`scripts/forced_carriage_spike.py`; readings in `docs/superpowers/2026-09-03-r165-forced-carriage-spike.md`): band 3 stays `REGION_TILING_FAILED` under forced carriage, asserted tokens unchanged (48/14/3). Three independent reasons: (1) apple produces NO `CarriedHeaderReading` anywhere — band 2 asserts through the MATRIX branch on p0/p1 (`classify_hierarchical` is `None`, loop L never tried) and ESCALATES on p2; (2) `carried_roles_for` refuses the receiving band because its only header row is the section heading (`Operating expenses:`), and the seam matches a REDRAWN header, it never supplies a missing one; (3) the diagnosis is confirmed at the mechanism — `header_rows_of` on every escalated section band returns exactly the section heading in column 0. **So the remedy above is struck: no loop routed through `carried_header_roles` can close this row.** What would close it now — PROPOSED, unmeasured: the evidence doc § 4 names two designs, (a) do not split at section headings (one `ruleXsSignature` = one band; the matrix reader already reads `Net sales:` as a row header `rh0` inside band 2), or (b) a second carried-block producer from the matrix column-header tree. The prediction to run first: does apple p0 compile as ONE matrix when bands 2–7 are handed to `compile_tables` as one band — a `page_bands` question. Specified together with [[R160]], and with [[R166]] in view since a fix that reads the section bands changes what band 4's record table asserts **That prediction was RUN 2026-09-04 and is CONFIRMED on p0 and p1** (`scripts/one_band_matrix_spike.py`; readings in `docs/superpowers/2026-09-04-one-band-matrix-spike.md`): merging bands 2–7 into one `Band` yields a `MatrixRegion` that passes `region_tiles` — p0 **124 entries** under `Three Months Ended`/`Nine Months Ended` with every section heading (`Operating expenses:`, `Earnings per share:`, `(1) Net sales by …`) read as a leaf ROW, where 48 entry-cells are asserted today; p1 **56 entries** over the whole balance sheet where 14 are asserted today. `Operating income 35,695 …` becomes a leaf row, which disposes of [[R166]]'s p0 half. **p2 REFUSES**, for two reasons neither of which is the merge: a single `—` (U+2014) body cell types as `tab:Text` and moves the stub|data split ([[R167]], new), and p2's header band is UNRULED so `Nine Months Ended` is three `Line.words` and the uncarried-ink guard refuses — [[R162]] verbatim, at the site [[R162]] names, ruled NEURAL and out of scope. **What is still unmeasured, and is the loop's real subject: how `page_bands` decides not to split.** The spike merges a run named on the command line; the `tab:ruleXsSignature` licence was not derived, not queried, not run on any non-apple document, and the band-index renumbering it forces on `section_repair_bands` / the decision log / every `#mtableN` URI (`compile.py:270-297`) was not examined. **THE LICENCE ITSELF WAS THEN RUN ON THE CORPUS 2026-09-04 AND IS REFUTED** (evidence doc § 7, all 7 documents / 27 pages): a maximal contiguous run of bands sharing one `tab:ruleXsSignature` **undershoots on apple p1** (3 distinct signatures across 6 ruled bands — band 4 carries one extra x `560.44`, band 6 is missing two — so the run stops at 2..3 and reaches 26 entries, not the 56 the spike measured for 2..7); **misses the header on apple p2** (band 2 is unruled, signature `None`, so the run starts at band 3 and the merged band is not even a matrix candidate); and **would swallow bfs p6's run 3..10, six `asserted` bands totalling 216 cells, into a band that is not a matrix candidate** (same shape at ons p7). Corollary the spec must carry: the merge cannot be an unconditional `page_bands` change — it has to be a PROPOSAL disposed by `classify_matrix` + `region_tiles`, falling back to today's bands when they refuse (CLAUDE.md §3 applied to a band split). `who`, `cbh` and both `graincorp` documents produce no runs at all, so the licence is inert on 5 of the 7 **THE BAND-INDEX RENUMBERING — named as the unknown that could sink the design — WAS MEASURED 2026-09-04** (evidence doc § 8). On this corpus **not one band renumbers**: every run the tiling oracle accepts (apple p0 2..7, p1 2..7) is a PAGE TAIL, so indices are only removed, never shifted, and the merged band inherits index 2 and mints the `#mtable2` band 2 already mints; every non-tail run (bfs p6, ons p7/p8, apple p1 2..3) is refused by the oracle before it can renumber. Both index-keyed pass-2 inputs are EMPTY where the merge fires (`section_repair_bands` is non-empty on 1 of 27 corpus pages, cbh p0, not apple; no apple band mints a `CarriedHeaderReading`). The full compile survives it: p0 and p1 both reach page score **1.0000**, zero escalated tokens, and the apple document score goes **0.1895 → 0.6289** ([[R160]]). **What the inventory nevertheless found, and nobody had listed:** the index is persisted into the SHIPPED graph (every `#table{idx}`/`#region{idx}`, every decision holon, and onward into `urn:iladub:region:` nodes in the GROUNDED graph via `ground.py:100`), and **three** two-pass flows use pass-1 indices against pass-2 results — section repair (`document.py:1487-1541`), carriage replay (`:1435,:1443,:1648`) and, the one the merge touches directly, adoption (`document.py:1657-1740`, where `grid_idx = len(pages[p].regions)` goes 8 → 3). 20 test files pin a band index; `tests/etkl/test_typing_equiv.py:31-79` is a positional list of 27 per-band verdict tuples and is the largest regression surface. **The gap that remains: the corpus has no non-tail run the oracle accepts, so the dangerous case has no evidence** |
| ~~R174~~ (44/163 closed) | **`test_stem_document_is_byte_identical_under_adoption` pins a document score that a reading change moved UP, and the loop that moved it re-baselined every OTHER pinned score it touched.** `tests/test_corpus_stem.py:357` asserts `stem_document.score == 0.9654553611484971`; the tree has read `0.9658886894075404` since `20cc5b8`. Split out of [[R173]] by the 2026-09-04 bisect because it has a DIFFERENT origin commit and a different remedy from the other seven — it is **not** an adoption failure: the same test's `adopted == ()` assertion passes at every commit probed. | MEASURED 2026-09-04 (`docs/superpowers/2026-09-04-r173-bisect.md` § 4), by probing 7 commits in parallel `git worktree`s with the gitignored corpus symlinked and `PYTHONPATH` overriding the venv's `.pth` (which otherwise resolves `iladub` to the MAIN checkout's `src` and silently measures `main` at every probe). `5743af3` → **30 passed**; `20cc5b8` → 1 failed, 29 passed, and the value is byte-identical at `20cc5b8`, `f4fd540`, `d757279` and HEAD. `git log -1 --format=%p 20cc5b8` → `5743af3`, so the origin is that single commit: PR #146, [[R154]] *a boundary that cuts ink is not a boundary*, which changed `src/iladub/etkl/geometry.py` (+53) and `src/iladub/etkl/gridregion.py`. | Deferred because **why** the score rose is not measured, only **when**. The bisect offers a reading — the same DENOMINATOR effect `20cc5b8` itself ruled on for apple p0 (`0.1170 → 0.1198`, re-baselined in `tests/etkl/test_decisionlog.py` with the ruling *"a smaller denominator is not better reading"*) — and types it **PROPOSED**: it is an analogy between two documents, and 4.3e-4 is small enough to fit several mechanisms. Note the shape that makes this a governance finding and not just a stale number: `20cc5b8` re-baselined THREE pinned tests, each with a written ruling, and all three are NON-CORPUS. The one test it left is the one CI cannot run. | Count stem's ink tokens and cells either side of `20cc5b8`. If the denominator shrank, re-baseline `tests/test_corpus_stem.py:357` carrying [[R154]]'s own ruling and close this. If it did not, the score moved for a reading reason and **the number must not be edited** — the test's docstring says *"If this number moves, STOP and report it — never lower it"*, and the finding is then a reading regression or gain to diagnose, not a pin to update. Either way the CI-visibility half stays with [[R173]]. &nbsp; ✎ **RUN 2026-09-05 — CONFIRMED** (`docs/superpowers/2026-09-05-r174-stem-denominator.md`). A pure denominator effect: numerator byte-identical at **2152**, denominator **2229 → 2228**, `2152/2229 = 0.9654553611484971` and `2152/2228 = 0.9658886894075404` — the pinned value and the value the tree reads. Not one additional cell was read: 2047 `tab:EntryCell` nodes and 2104 `tab:cellText` triples at BOTH, and a diff of every `(cell, cellText)` pair is 0 lines. The whole document-wide diff is ONE line — stem p0 band 2 L0, `['Friday, 31', 'July 2026']` → `['Friday, 31 July 2026']`, a date banner that is NOT a cell at either commit, so the weld took one token off the ESCALATED leg only. The denominator is a WORD count, not a cell count (`src/iladub/etkl/document.py:1757-1760`, `src/iladub/etkl/compile.py:1044-1046`), which is why one word moves a document score. **Closure is now mechanical**: re-baseline `tests/test_corpus_stem.py:357` to `0.9658886894075404` carrying [[R154]]'s own ruling verbatim, and record that the numerator did not move. &nbsp; ✎ **AND THE ROW'S OWN FRAMING IS CORRECTED**: this row said the R154 loop *"re-baselined every OTHER pinned score it touched"*, which reads as though it had not seen this one. **It saw it.** `git log -1 --format=%B 20cc5b8` line 34 records `gstem .96546->.96589`, and line 37 warns the rises are *"partly a DENOMINATOR effect"*. The gap is not attention — a test nothing runs cannot be re-baselined by a careful author. That makes R174 the worked instance of [[R173]]'s CI-visibility half, not a separate oversight. &nbsp; ✎ **CLOSED 2026-09-05** (loop `r173-r174-rebaseline`, `docs/superpowers/2026-09-05-r173-5a-rebaseline.md`) — the mechanical re-baseline the row prescribed, carried out exactly as prescribed: `tests/test_corpus_stem.py::test_stem_document_is_byte_identical_under_adoption` now pins `0.9658886894075404` (cited by SYMBOL, not line: the row's own `:357` names the assert BEFORE this closure added 20 docstring lines above it — it reads `:377` now, CLAUDE.md § Plan authoring rule 7), and its docstring carries [[R154]]'s own ruling verbatim (*a smaller denominator is not better reading*), the arithmetic (numerator 2152 unchanged, denominator 2229 → 2228), the one-line document-wide diff, and the reason the pin went stale — `20cc5b8` published the move and could not re-baseline a test nothing runs. VERIFIED with the corpus present: `pytest tests/test_corpus_stem.py -q` → **13 passed in 310.87s**, against 1 failed / 12 passed before. The number was RAISED, never lowered, and no assertion was weakened: the test's `adopted == ()` and `len(chains)` assertions are untouched. **The CI-visibility half this row is the worked instance of stays with [[R173]] and is NOT closed here.** |
| ~~R175~~ (45/164 closed) | **The document-scope adoption pins now rest on a ONE-BAND synthetic fixture, so the control that makes three of them non-vacuous cannot move off the corpus with them.** `currency_marker_escalating_pdf` produces exactly three regions — `superseded`, the asserted grid, and the `DATAGRID_RESIDUE` — and no `ignored` region. `test_an_unsuperseded_band_on_apples_page_one_is_untouched` exists precisely so that a change which superseded EVERY region would not pass the withdrawal and supersession tests for the wrong reason, and it therefore stayed on apple, corpus-gated, while the five pins it controls moved into CI. | MEASURED 2026-09-05 (`docs/superpowers/2026-09-05-r173-5a-rebaseline.md` § 2 and § 6) by compiling the fixture at document scope and listing its regions: `region0 superseded (REGION_TILING_FAILED) / region1 asserted (…#p0-datagrid, 16 tokens) / region2 escalated (DATAGRID_RESIDUE, 2 tokens)`, `adopted=(0,)`, `notes=()`. Verified in a corpus-free worktree: `13 passed, 6 skipped` — the control is one of the 6. | Deferred because [[R173]] 5a was a re-baselining action, not a fixture-building one, and drawing a second band that reliably classifies and is left UNTOUCHED by the grid is a reading question about the fixture, not a re-baseline. Raised rather than left in the evidence document because the asymmetry is invisible from the test file: five pins look CI-covered and their control does not run there. | A second band in the synthetic adopting document that the page's grid does NOT supersede — then move the control test onto it and assert both halves in CI: that the superseded band's effective chain is not the escalated one AND that the untouched band's two query readings still agree. If no such band can be drawn, that is itself the finding: say so, and record that the control is corpus-bound by construction. **CLOSED 2026-09-06** — `docs/superpowers/2026-09-06-r175-the-second-band.md`. The band CAN be drawn, and the mechanism is `compile.py:1276`: a report is superseded only when `i in _led.touched AND r.tokens_escalated > 0`, so a `NON_TABLE` band — which books 0 tokens — cannot be superseded whether or not the grid read its ink. `currency_marker_escalating_with_note_pdf` is `currency_marker_escalating_pdf` plus ONE line of note (a SIBLING, not an edit: two other modules name that fixture's single band in prose), and it adopts at IDENTICAL page arithmetic — `adopted=(0,)`, `score=0.8888888888888888`, `16/2`, regions `[superseded, ignored(NON_TABLE), asserted(grid), escalated(residue)]` — so **no pin was re-baselined to buy the control**. Two- and three-line notes were compiled too and REJECTED: they classify UNSUPPORTED_TABLE and book 23/6 escalated tokens, moving the page to `16/25` and breaking `test_the_ledger_and_the_graph_agree_on_the_adopted_page`'s `booked == p.escalated`. `test_an_unsuperseded_band_on_the_adopting_page_is_untouched` now asserts BOTH halves in one CI run (that `_superseded(p)` is non-empty, and that the two shipped queries agree with no `supersededBy`); the module goes **7 → 8 tests in CI** (`-m "not corpus"`: `8 passed, 5 deselected`), `13 passed`/1 red-by-design overall. FALSIFIED by widening `document.py:1738` to `range(grid_idx)`: `effective-chain.rq` then answers the grid's admission as the effective reading of an untouched band (`eff=[(0,'verdict')]` vs `why=[(0,'multi_table'),(1,'kind'),(2,'verdict')]`, 3 of 3 rows carrying `supersededBy`). The apple test was RENAMED, not deleted, to `test_the_two_queries_agree_on_an_untouched_band_of_a_real_document`: apple adopts nothing since `4cfee38`, so it had no superseded region to be the control OF. |
| ~~R172~~ (43/161 closed) | **"124 entries vs 48 cells" compares two different counters, and no content diff has ever been run.** Every statement of this loop's payoff - in the spec, in both spike documents, in the corpus manifest's new adjudication node and in this register - compares `assert_matrix_region`'s RETURN VALUE (entries minted for the merged band) against `RegionReport.cells` (the asserted entry-cell count of the previous reading). Nothing has ever checked that the 124 entries CONTAIN the 48, that no cell present at baseline is absent from the merged reading, or that the extra 76 are real ink rather than a different way of counting the same ink. | Raised 2026-09-04 from spec `2026-09-04-the-run-is-one-band-design.md` § 7, and RULED A SEPARATE ROW rather than folded into O3 after running O3: **O3 bounds the ink and does not identify the cells.** It asserts `rep.asserted` (a token count) per page against a pre-merge baseline, which catches a page that LOSES ink; it cannot catch a merged reading that asserts the same number of tokens against different cells, and it says nothing about cell identity at all. | Deferred because the instrument does not exist: there is no committed cell-level differ, and writing one is a measurement task of its own rather than a step of the one-band change. Not a blocker - the ink ledger in `docs/superpowers/2026-09-04-r165-three-claims-measured.md` § B already accounts for apple p1's tokens as a SET (42 asserted + 0 escalated + 28 counted-nowhere, the 28 being the stub column the matrix branch has always excluded), so the loop is not shipping blind; it is shipping without a per-cell proof. | A diff over the two readings' `tab:EntryCell` content on apple p0 and p1 - baseline vs merged - showing that every cell the previous reading asserted is present in the merged reading with the same text and the same column, and naming what the additional entries are. &nbsp; ✎ **DETECTOR 2026-09-04** (`docs/superpowers/2026-09-04-r173-bisect.md` § 5): this row now has a live detector in the suite. `test_an_adopted_page_never_scores_one_by_construction` PASSED at `d757279` and FAILS at HEAD, because apple p1 reads `CompilationReport(score=1.0, …, asserted=56, escalated=0)` after the merge. It is the zeroing-tautology guard firing on a page that reached 1.0 WITHOUT adopting (`adopted == ()`) — so it is asking exactly this row's question. **Do not re-baseline it as part of [[R173]] 5a.** &nbsp; ✎ **THE DETECTOR IS NOW EXPLICIT, 2026-09-05** ([[R173]] 5a): `tests/etkl/test_adoption_document.py::test_an_adopted_page_never_scores_one_by_construction` is **left failing on purpose** and its docstring says so — it asserts `p1.escalated > 0` and apple p1 reads `score=1.0, asserted=56, escalated=0`. Every other test in that module was re-baselined or re-pointed; this one was not, because it is this row's finding rather than a stale pin. The invariant it used to carry (*ink the grid did not read keeps escalating*) is now pinned separately on the synthetic adopting fixture as `test_the_adopted_page_keeps_the_ink_the_grid_did_not_read`, so nothing is lost by leaving it red. **Closing this row is what turns it green**: rule on apple p1's reading, then either delete the test with that ruling recorded or repair the reading it caught. &nbsp; ✎ **CLOSED 2026-09-07 — THE DIFF IS RUN, AND THIS ROW'S OWN CRITERION IS REFUTED ON p0** (`docs/superpowers/2026-09-07-r172-the-cell-level-diff.md`; instrument `scripts/entry_cell_diff.py`, both readings produced in ONE process at HEAD by forcing `compile.merged_run_admissible` to refuse, keyed on the ink anchor `p{page}-{int(x0)}-{int(top)}` rather than on URIs, which cannot cross two partitions). **apple p1: LOST 0, CHANGED 0, GAINED 42** — every gain landing on a real baseline WORD with identical text, every one out of a band that escalated, NONE out of the three `ignored` prose bands. The criterion is met exactly and `escalated=0` is honest. **apple p0: LOST 4, CHANGED 16, GAINED 80 — and the criterion is REFUTED, in the merged reading's favour.** All 4 lost and all 16 changed are band 4, which is 20 of 20, its ENTIRE baseline entry set: that baseline `RECORD_TABLE` read the `Operating income` DATA ROW as its column header (columns literally labelled `('35,695',)`, `('28,202',)`, `('122,432',)`, `('100,623',)` — [[R166]]'s shape at a second document position) and read four row stubs as entry cells. Every lost cell is present in the merged graph as a `tab:LabelCell`; none vanishes. **The criterion asked the merge to preserve a false assertion.** The correct general invariant — no baseline entry cell VANISHES (entry or label), and no gained entry INVENTS ink — holds on both pages and is now pinned by `tests/etkl/test_band_runs.py::test_the_merge_loses_no_cell_and_invents_no_ink`, falsified on both clauses. **The detector is DELETED, not re-baselined**: `test_an_adopted_page_never_scores_one_by_construction`'s `p1.escalated > 0` is REFUTED, not stale — the ruling stands in a comment at its old site, and the module now reads 12 passed with no red. **A finding this row was NOT asking about is split out as [[R176]]**: `score=1.0` here is not *everything was read* but *everything the denominator counts was read*, and the denominator MOVED — 24 words on p0 and 28 on p1 leave `asserted+escalated` entirely. |
| ~~R176~~ (46/166 closed) | **The score's token denominator is not stable across a verdict change: when a band stops escalating and starts being read, its row-stub ink leaves `asserted+escalated` altogether rather than moving across.** So `score = asserted/(asserted+escalated)` is not comparable between two readings of the same page, and `score=1.0` means *everything the denominator counts was read*, not *everything was read*. | MEASURED 2026-09-07 (loop `r172-the-cell-level-diff`, `scripts/entry_cell_diff.py`, evidence `docs/superpowers/2026-09-07-r172-the-cell-level-diff.md` § 4). apple p0: denominator `148 (=48+100)` → `124 (=124+0)`, **delta −24**; words in banded ink 194, counted NOWHERE 46 → 70. apple p1: `84 (=14+70)` → `56 (=56+0)`, **delta −28**; words 119, counted nowhere 35 → 63. The mechanism is read off the two accumulators, not inferred: an ESCALATED band books every word of every line it holds (`src/iladub/etkl/compile.py:744`, `escalated_total += sum(len(ln.words) for ln in band.lines)`), an ASSERTED matrix band books only its DATA cells' words (`compile.py:923`, `asserted_total += sum(len(c.words) for c in data_cells if cell_round_trips(c, b))`). | Deferred because it is **not [[R172]]'s question and not this loop's diff**: R172 asked whether the merged CELLS are right (they are, and on p0 they are a repair), and folding an accounting change into that answer would make one diff answer two questions. It is also **structural rather than introduced by [[R165]]** — the merge is only the first change large enough to make it visible on a whole page; the [[R165]] three-claims ledger already recorded p1's 28 as *counted nowhere* (`2026-09-04-r165-three-claims-measured.md` § B) without recognising it as a denominator effect on the score. Raised rather than left implicit because it is the site of the real concern behind the detector this loop deleted: a page CAN reach 1.0 for a reason other than reading everything. | **A fork must be taken FIRST, and it is a design decision, not a measurement**: either (a) COUNT label ink in the denominator — a row stub is structure the reading recovered, so booking it as unread would be its own kind of wrong — or (b) EXCLUDE it from both sides, so an escalated band stops counting it too. Then: the change made, the corpus-wide score movement measured (it is inferred from two pages today), and every pinned score re-baselined with a written ruling — a [[R174]]-shaped exercise priced at many tests rather than one. **The prediction to run first is that apple p0 and p1 stop reading `1.0`.** &nbsp; ✎ A second, smaller thing measured in the same diff and parked here rather than given a row: apple p1's `100,702` / `93,568` entries carry the row label `('respectively',)`, a continuation fragment of the multi-line `Common stock and additional paid-in capital …` stub. It is a row-header QUALITY defect, not an ink-identity one — nothing in that loop measured how general it is. | &nbsp; **CLOSED 2026-09-07** (loop `r176-the-denominator-that-moves`, spec `docs/superpowers/specs/2026-09-07-the-denominator-that-moves-design.md`). **The fork is taken for (a) — COUNT it — and not as a preference:** (b) asks an ESCALATED band to partition ink it has just refused to partition, and at `compile.py:783` (`MULTI_TABLE_AMBIGUOUS`, before `classify` runs) and `:1212` (`KIND_NOT_SUPPORTED`, no region built) there is nothing to consult. **THE ROW'S OWN PREDICTION IS REFUTED.** It predicted apple p0/p1 stop reading 1.0; they do not, and `scripts/unbooked_ink_fate.py` says why — 100% of their unbooked ink is covered by an emitted `tab:LabelCell` (p0: 172 = 124 entry + 48 label + 0 orphan; p1: 98 = 56 + 42 + 0). That `1.0` was CORRECT all along; it was merely not provable from its own operands. **What shipped:** `_book_recovered_ink` (`compile.py`, four call sites) books every word of an asserted band once — asserted when a carried structure holds it, escalated when nothing does. Corpus: asserted-band unbooked ink **262 words → 0**, **17 of 24 dirty bands → 0 of 24**, band population unchanged (24/21/146), so accounting moved and no reading did. Pinned per assert-site by `tests/etkl/test_read_band_books_every_word.py`, including the row-hierarchical site NO corpus document reaches. One operand pin re-baselined with a written ruling (`test_apple_adopts_nothing_because_the_page_asserts_outright`, 56 → 98, score unchanged at 1.0). Successors [[R177]] and [[R178]]. &nbsp; ✎ The two `compile.py:NNN` citations in this row's Measured column were true when written and were moved by the repair itself; the sites are now `_book_recovered_ink`'s four call sites, found by symbol.
| ~~R177~~ (48/168 closed) | **`assert_hier_region` emits every header `tab:LabelCell` WITHOUT `tab:hasBBox`**, so a hierarchical table's header labels carry text but no provenance-to-the-page (core design principle 6). All five sibling emitters write one. | MEASURED 2026-09-07 (loop `r176-the-denominator-that-moves`). `src/iladub/etkl/holon.py:529-532` adds `RDF.type`, `TAB.hasCell`, `TAB.cellText` and `TAB.hasLabel` and no bbox; compare `:134`, `:184`, `:246`, `:278`, `:330`, which all do. Detected by `scripts/unbooked_ink_fate.py`, which reports the count it had to skip: `who-wfa p0 → LabelCells=33 (no bbox: 0/24)` — 24 of 57 label cells on one page, 24 on p1, 12 on p2. | Deferred because it sits on the `#htable` branch, which R176's repair deliberately does NOT touch (that branch already books every word of its band, and adding the repair there would double-book). Folding a provenance fix into an accounting loop would make one diff answer two questions. | Give `assert_hier_region` the same bbox writer its siblings use — `nd.x0/top/x1/bottom` are already in hand at that site, so nothing has to be recomputed. The falsifying oracle is `unbooked_ink_fate.py`'s own `no bbox:` counter reaching `0/0` on who-wfa p0. Worth checking in the same pass whether any SHACL shape targets `tab:LabelCell` with a `tab:hasBBox` cardinality — if one does, these 24 are a membrane violation that nothing currently validates, which would make this larger than a provenance gap. &nbsp; **CLOSED 2026-09-07** (loop `the-label-cell-that-cannot-be-found`, spec `docs/superpowers/specs/2026-09-07-the-label-cell-that-cannot-be-found-design.md`). **The row's own prescribed remedy was REFUTED first**: it said `nd.x0/top/x1/bottom` were "already in hand at that site", and they are not — `HeaderNode` (`headers.py`) carries `center_x` and nothing else geometric; the row had conflated it with `RowHeaderNode` (`rowheaders.py`), which does carry all five. The repair therefore CARRIES the geometry through the node: four optional bounds + `page` appended to `HeaderNode`, populated at its construction site from the `SourceCell` already in hand, and written at the emitter under a presence test so `span.build_reading`'s ink-less flank node stays honestly boxless. The one field-by-field node rebuild became a `dataclasses.replace` — falsification F4 shows reverting that alone fails all three pins, so it is load-bearing, not tidying. **O1: corpus bbox-less LabelCells 79 → 0.** **Every one of the 27 page readings is byte-identical** (`score`/`asserted`/`escalated` diffed, spec § 4.1) — which had to be measured rather than assumed, because `region_tiles` runs the tiling membrane over the very graph this emitter writes and `tab:WrappedCellShape` fires only when `tab:hasBBox` is present. **Its real significance was found mid-loop**: it blinded `scripts/unbooked_ink_fate.py`, the instrument [[R178]]'s row prescribes as its own disposal — the corpus reported 111 orphan words where 12 are real, so running R178's disposal before this row would have sent a loop chasing a footnote rule and an [[R166]] re-frame for 99 words that were never dropped. Successor [[R179]] — the membrane never required a box on a label at all. |
| ~~R179~~ (49/169 closed) | **No SHACL shape requires `tab:hasBBox` on a `tab:LabelCell`.** `tab:EntryCellPhysicalShape` (`vocab/shapes/tab-physical-shapes.ttl:13-19`) requires it — `minCount 1`, `sh:class tab:BBox` — for every `tab:EntryCell`, and nothing does the same for labels. So [[R177]]'s 79 boxless label cells violated CLAUDE.md § Core design principles 6 (provenance-to-the-page) for months with the membrane silent. | MEASURED 2026-09-07 (loop `the-label-cell-that-cannot-be-found`, spec § 3.5): `grep -rn "LabelCell" vocab/shapes/*.ttl` returns exactly one hit, a `FILTER NOT EXISTS` inside an unrelated SPARQL constraint (`tab-shapes.ttl:294`). R177's own row asked whether such a shape existed; the answer is no, which is why R177 was a provenance gap and not a membrane violation. | Deferred because R177 was a producer-side repair and this is a membrane decision with a different blast radius — it would target EVERY `tab:LabelCell` in every shipped graph, including `span.build_reading`'s legitimately boxless flank filler (a column with no header ink), which a naive `minCount 1` would reject. Folding it in would have made one diff answer two questions. | Decide first whether the shape is `minCount 1` on all labels (and the flank filler must therefore stop being a `tab:LabelCell`) or a conditional shape in `WrappedCellShape`'s style (fires only when the node it labels carries geometry). **Note this is NOT free**: `region_tiles` runs the tiling shapes over every candidate region at `compile.py:836,910,967,1023,1166`, so a new label shape becomes a gate on what may assert at all — measure the corpus verdict diff before merging, exactly as this loop did (spec § 4.1). See also § Producer-side guards vs the membrane: the emitter's presence test is not made redundant by a shape unless the shape provably covers every product of it. **CLOSED 2026-09-07** (loop `the-membrane-that-never-required-a-box`, spec `docs/superpowers/specs/2026-09-07-the-membrane-that-never-required-a-box-design.md`) — `tab:LabelCellPhysicalShape` ships, in `vocab/shapes/tab-physical-shapes.ttl` **and** in `tiling._PHYSICAL_SHAPE_IRIS`, so it gates region admission rather than only crashing final validation (§ 3.2; falsification F3 shows the file-level negative test cannot see that defect). Corpus verdict diff **WHOLE-FILE IDENTICAL** across 7 documents / 27 pages, so the prediction it was raised with holds. **One half of the row's own fork is REFUTED**: option (b), *"fires only when the node it labels carries geometry"*, is **not expressible** — no `tab:HeaderNode` carries geometry in any emitted graph (10 `TAB.hasBBox` write sites, every one onto a cell). The shipped condition is the label's OWN non-empty `tab:cellText`, which over this tree selects the same single exempt node (`span.build_reading`'s flank filler) and is the exact converse of `tab:WrappedCellShape`. Option (a) is **not taken and not closed**: it needs the filler's `tab:hasLabel` deleted, which four consumers read — `reshape.py:29` among them, the round-trip oracle — making it an emission change, raised as [[R180]]. **AND the disposal both this row and the predecessor handoff prescribed was INSUFFICIENT**: the corpus diff came back WHOLE-FILE IDENTICAL on a shape that was still defective. `tests/etkl/` caught it — `test_closure_equiv.py::test_both_closures_agree_on_a_mutated_real_page_graph` — because `tab:hasLabel` has `rdfs:range tab:LabelCell` and `rowgroups.py:93` points it at the SOURCE `tab:EntryCell` of a derived group, so under FULL closure that entry is a label and the new constraint fired on it. The R19 accident at a new site. Both constraints now carry `FILTER NOT EXISTS { $this a tab:EntryCell }`, provably lossless because `tab:EntryCellPhysicalShape` asks strictly more of an entry, pinned synthetically and by the closure oracle (falsification F6). Spec § 4.3. |
| ~~R178~~ (48/168 closed) | **who-wfa's `Year: Month` axis caption is ink the matrix reader carries into no structure at all** — not a data cell, not a column-tree node, not a row-header node. After [[R176]] it books `escalated`, so the score now SEES it; the reading still does not carry it. | **POPULATION MEASURED 2026-09-07** (loop `the-label-cell-that-cannot-be-found`), and only after [[R177]] was closed — which is the fact this row now turns on. Run on the BLINDED prober the corpus reported **111 orphan words in 3 documents across 4 shapes**; run on the repaired one it reports **12 words in 2 documents across 2 shapes** (`scripts/unbooked_ink_fate_corpus.py`; spec `2026-09-07-the-label-cell-that-cannot-be-found-design.md` § 1.3). The survivors: (a) the **3-word `Year: Month` caption**, once each on who-wfa p0/p1/p2 — the row's original sample, intact; (b) **9 items on graincorp-stem p0** — a date banner `Friday, 31 July 2026` plus eight uncarried column-header fragments (`Date of Grain`, `Unique Slot`, `Loading Date`, `Nomination Time` ×2, `Nomination Date` ×2, `Loading`), on a band whose other 17 header cells WERE carried. | Deferred because [[R176]] was an accounting loop and [[R177]] was a provenance loop; making the reader CARRY this ink is a reading change and has belonged in neither. | **The row's own prescribed disposal is now RUN, and two of its three readings are WITHDRAWN as instrument artefacts**: the ons p4 "footnote sentence" (17 words) and the who-wfa "data rows" (65 words, proposed as [[R166]] at a new position) were carried label ink all along — `label=0 orphan=17` → `label=17 orphan=0`, and `label=0 orphan=13` → `label=13 orphan=0` twice per page. What is left is genuinely two questions, and they should not be answered by one rule: the caption is a *placement* question at the matrix site (ink belonging to neither axis), the graincorp fragments are a *coverage* question at the hierarchical site (ink the header tree declined to node). Measure which of the two is worth a loop before designing either; 12 words in 2 documents may not be worth a reading change at all, and saying so is a legitimate closure. **CLOSED 2026-09-07** (loop `the-box-that-covers-one-line`, spec `docs/superpowers/specs/2026-09-07-the-box-that-covers-one-line-design.md`) — ruled **measured, and not worth a reading change**, on a population corrected a THIRD time. The row's own split into "two questions" is **REFUTED**: the graincorp half is not a coverage question, because the header tree carried every one of those words. 8 of the 9 graincorp items are ink a `tab:LabelCell`'s `tab:cellText` DOES carry, whose `tab:hasBBox` covers only the last of the three wrapped source lines it was joined from — `'Date of Grain Loading Commencement'`'s box is byte-identical to the word `'Commencement'`'s (spec § 2.1), and `unbooked_ink_fate`'s `_covered` decides an orphan by box containment, so carried-but-under-covered reads exactly like dropped. Raised as [[R181]]. What is genuinely unread is **4 word-groups in 2 of 7 documents**, all out-of-table captions: `Year: Month` on who-wfa p0/p1/p2 (absent from the graph — measured, and NOT the carried `Month` header, which is a separate word-group at x136.37) and the `Friday, 31 July 2026` print banner on graincorp-stem p0. That is the shape the row predicted at raise time — *small and caption-shaped* — now the whole population rather than 25% of it. Carrying a caption means minting a structure the table does not have (it spans no columns, names no row); [[R176]] keeps all four booked `escalated`, so the score still counts them unread. **What would REOPEN it:** a document where out-of-table caption ink is load-bearing (an axis caption carrying the unit, a banner carrying the as-of date the rows need); none of the 7 does. Spec § 3. |
| ~~R181~~ (51/171 closed) | **A `tab:LabelCell` whose `tab:cellText` is a wrap-join across N source lines carries a `tab:hasBBox` covering only ONE of them.** On graincorp-stem p0 the column header is three physical lines; the 17 emitted labels carry the full joined string (`'Date of Grain Loading Commencement'`, `'Unique Slot Reference Number'`, `'Date Nomination Received'`, …) and a box identical to the LAST line's word alone (`376.32, 81.03, 417.58, 86.31` == the word `'Commencement'`). Provenance-to-the-page (CLAUDE.md § Core design principles 6) is therefore PARTIAL where [[R177]] made it present: the box exists, and it does not cover the text it is the provenance of. | MEASURED 2026-09-07 (loop `the-box-that-covers-one-line`, spec § 2.1). **CORRECTED ON CLOSURE: 7 of 17 labels, not 8** — 8 is the count of continuation FRAGMENTS (1 in row1 + 7 in row2), landing on 7 labels because col7 takes two (`'Date of Grain'` + `'Loading'` → `'Commencement'`); the prober's `orphan=9` is those 8 plus the furniture banner, which is why the arithmetic looked consistent. The other 10 are single-line and correct. It is the ONLY page of the corpus's 27 that exhibits it — every orphan word-group on the other 3 orphan-bearing pages is accounted for as a genuine caption (spec § 2.2) — but that claim is scoped to ASSERTED bands, since `unbooked_ink_fate.fate` skips every other verdict (`scripts/unbooked_ink_fate.py:88`). The wrap-join itself is the RIGHT reading: `Date of Grain / Loading / Commencement` is one header wrapped over three lines, not a three-level hierarchy, so the defect is localised entirely in the box. | Deferred because it changes emitted geometry — it needs the whole-file corpus diff and the ~14-minute suite [[R179]] paid for, which is an executing loop, not the ruling [[R178]]'s closure required. Folding it into that ruling would have made one loop answer two questions (CLAUDE.md § Plan authoring discipline rule 6). | **The site is NOT yet established, and must be measured before anything is designed.** `headers.py:458` builds a `HeaderNode` from ONE `SourceCell` and takes its four bounds; `cells._cell_from` (`cells.py:96-103`) already unions its words' bounds correctly. So the joined text and the single-line box do not come from the same place, and which site performs the three-line join is unmeasured — naming it from reading would be the rule-2 failure exactly. Then decide the remedy: an emitter fix (union the bounds of every line the join consumed), a containment shape (no shape enforces box-covers-text in EITHER direction — `tab:WrappedCellShape` is bbox ⇒ non-empty text, and [[R179]]'s `tab:LabelCellPhysicalShape` requires only the box's PRESENCE), or both. **`rowrole.py:182` is a SIBLING site, not the same one** — the R179 handoff names it as the row-header analogue (`replace(nodes[tgt], text=merged)` keeps the target's box); this measurement is on column headers, and whether the two are one defect is open. Disposal: the orphan count on graincorp-stem p0 must fall 9 → 1, leaving only the print banner, with no other page's count moving. **CLOSED 2026-09-07** (loop `the-box-that-covers-the-join`, spec `docs/superpowers/specs/2026-09-07-the-box-that-covers-the-join-design.md`). **The site is `rowrole.py:174,182`, and BOTH of this row's suspects are refuted** — `group_wrapped` does not merge the three lines at all (it returns them as rows 1/2/3, exactly as `headers.header_rows_of`'s KNOWN LIMIT docstring predicts), and `headers.py:62`/`:80` are not on the path. The join is loop-C's `build_row_reading`: `extra` stored `cell.text` and DISCARDED the `SourceCell` whose four bounds it had just read for `_column_containing`, so `replace(nodes[tgt], text=merged)` kept the leaf row's box. **This row's own caution is also refuted**: `rowrole.py:182` is not a row-header ANALOGUE, it is the site — `rowrole` decides the role of each header ROW in a COLUMN-header block, and `grep -n 'replace(' src/iladub/etkl/rowrole.py` returns exactly two hits, the other being a region rebuild carrying no cell geometry. So there is no sibling site and that open question closes with it. Remedy: union the target node's bounds with every consumed fragment's, PROCEDURAL (exact arithmetic over a membership the NEURAL role vector already fixed; precedent `cells._cell_from`), no tuned constant. Disposal MET exactly as prescribed: graincorp-stem p0 orphan **9 → 1**, the survivor being the furniture banner `Friday, 31 July 2026`, which is carried as a `tab:RegionCaption`; label-covered word-groups 17 → 25, score/asserted/escalated unchanged. **The fork's second arm is REFUTED as unexpressible** — a containment shape has nothing to compare against, because `emit_reading_evidence` writes `tab:sourceText` + `tab:sourceRow` on a `tab:HeaderSourceCell` and no geometry at all; raised as [[R182]]. **And the predecessor handoff's prediction is refuted on its stated basis**: `grep -rn '_bbox_xy' src/ tests/ scripts/ vocab/` finds `feed.py:285` reading a LABEL's bbox, so “no site reads a LabelCell's bbox” is false; it survives on the narrower measured fact that that call takes `x0` alone and `x0` moves for none of the 7 (the union widens `top` in all 7 and `x1` in 5). Unlike its three predecessors this loop ships a CI-RUNNABLE pin — the synthetic `caption_and_wrap_band` fixture reproduces the shape with no corpus, so [[R173]]'s “un-pinnable artefact” cost does not recur here. |
| ~~R182~~ (52/172 closed) | **A `tab:HeaderSourceCell` carries no geometry, so no SHACL shape can check that a label's box covers the text it is the provenance of.** `emit_reading_evidence` (`src/iladub/etkl/rowrole.py`) writes `tab:sourceText` and `tab:sourceRow` on each source cell and nothing else — no `tab:hasBBox`, no `tab:onPage`. SHACL can therefore see THAT a label's text was joined from three source cells and cannot see WHERE any of them was. **CLOSED 2026-09-08** (loop `the-box-must-cover-its-provenance`, spec `docs/superpowers/specs/2026-09-08-the-box-must-cover-its-provenance-design.md`). BOTH must-measure-first conditions were answered, and each was a surprise in the OPPOSITE direction. (1) **There is no label→source edge at all** — measured on graincorp-stem p0's real emission, a `tab:HeaderSourceCell` has exactly ONE in-edge (`tab:hasHeaderSourceCell`, from the table), a `tab:LabelCell` only `tab:hasLabel` + `tab:hasCell`, and ZERO one-hop edges join them; the row's caution that `prov:wasDerivedFrom` is a different edge is CONFIRMED. *(This row's columns below cite `tab.ttl:443` for it; this loop inserted two properties above that point, so it now reads `:449` — cite the symbol `tab:RepeatedHeaderRow`, not the line. Same for `tab.ttl:545` → `:551`, `tab:markerRegion`.)* (2) **`tab:hasBBox` trips nothing** — adding it plus `tab:onPage` to all 26 source cells left the captured tiling-gate graph AND the doc-scope membrane conforming, because `membrane.subclass_closure` materialises no domain/range typing on either engine (`membrane.py:458-465`, `:111-125`). **BUT THAT CONCLUSION WAS OVER-REACHED, AND THE LOOP'S OWN SUITE REFUTED IT.** The two measurements were about the GATE and the MEMBRANE; what the loop wrote down was *"the R19 mechanism is closed at its root"*. On that it edited both R19 comments to say so, raised [[R183]] to collapse `tab:markerRegion`, and emitted `tab:hasBBox` on all 26 source cells. `tests/etkl/test_closure_equiv.py` then failed: `membrane.rdfs_closure`'s owlrl leg materialises `tab:hasBBox rdfs:domain tab:Cell`, so `tab:WrappedCellShape` refused 26 nodes the membrane admitted (`full=False sub=True on the real stem page`). The whole-corpus guard had reported *whole-file identical* — it runs through the membrane, the one leg blind to this by construction. **Remedy:** the geometry rides dedicated `tab:sourceRegion` + `tab:sourcePage` (`rdfs:domain tab:HeaderSourceCell`) — the exact `tab:markerRegion` pattern the loop was about to retire; both R19 comments (`_emit_unit_markers`'s docstring and `tab:markerRegion`'s in `tab.ttl`) are restored with this evidence added; [[R183]] is withdrawn before anything was built on it. The label side keeps `tab:hasBBox`, because a `tab:LabelCell` genuinely IS a `tab:Cell`. The principled form of the ruling outranks the test: `vocab/ontology/tab.ttl` PUBLISHES that domain axiom under CC-BY, so any consumer running a stock RDFS reasoner over our graph derives it — `scripts/measure_dec_membrane.py:18` names `rdfs_closure` as exactly that consumer's view. A third question the row did not ask was also measured first: the constraint traverses BLANK-NODE bboxes, no prior shape reads bbox coordinates in SPARQL, and **both engines agree** (rudof and pySHACL each pass the union box and refuse the leaf-only one). SHIPPED: `source_cells` widened to `(row, cell, derived_into)`, `tab:sourceRegion`/`tab:sourcePage` + `prov:wasDerivedFrom` emitted, `tab:LabelCoversProvenanceShape` in the file AND in `tiling._PHYSICAL_SHAPE_IRIS`. Corpus verdict diff **whole-file identical** on 27 pages (a guard: one page emits the edge). Falsified by reverting [[R181]]'s union — the gate then REFUSES the reading and `resolve_header_row_roles` returns None — and by removing each of the three emission pieces in turn. All five pins are CI-runnable on a synthetic fixture ([[R173]]). No successor row: the question this loop expected to leave open ([[R183]]) was answered against itself. | MEASURED 2026-09-07 (loop `the-box-that-covers-the-join`, spec § 3.2), by reading the three `g.add` lines of the `source_cells` loop in `emit_reading_evidence`. This is the reason [[R181]]'s fork option ‘a containment shape’ was disposed as UNEXPRESSIBLE rather than deferred on cost. | Deferred because making the shape expressible is a GRAPH change — emitting geometry on every `tab:HeaderSourceCell`, which is the target of `tab:HeaderContentConservedShape` and therefore inside the tiling gate — and so is strictly larger than the two-line union fix it would have guarded. Shipping both in one loop would have made one loop answer two questions (CLAUDE.md § Plan authoring discipline rule 6). | Emit `tab:hasBBox` + `tab:onPage` on each `tab:HeaderSourceCell` (the `SourceCell` is in scope at the call site — `source_cells` currently projects `(row, cell.text)` and discards it, the same discard [[R181]] was), then a shape requiring a `tab:LabelCell`'s box to contain every source cell's box it was derived from. **Two things must be measured first, not assumed:** (1) whether the label→source link exists in the graph at all — `prov:wasDerivedFrom` points a REPEATED header row at its originating `HeaderSourceCell`s (`vocab/ontology/tab.ttl:443`), which is NOT the same edge, and without an edge the shape has no path; (2) whether adding `tab:hasBBox` RDFS-types a `HeaderSourceCell` as a `tab:Cell` and trips `tab:WrappedCellShape` at the gate — the R19 mechanism, named explicitly at `compile.py:227` and `tab.ttl:545` as the reason `tab:markerRegion` exists as a separate property. Disposal: a shape that FAILS on today's graincorp-stem p0 emission with the [[R181]] union reverted, and passes with it. |
| ~~R183~~ (53/173 closed) — **WITHDRAWN, not closed** | **The claim: `tab:markerRegion` exists only to dodge a hazard that no longer exists.** Raised 2026-09-08 inside [[R182]]'s loop on the strength of two correct measurements (the tiling gate and the doc-scope membrane both admit `tab:hasBBox` on a `tab:HeaderSourceCell`, because `membrane.subclass_closure` materialises no domain/range typing). | **REFUTED THE SAME DAY, by the loop's own full suite.** `tests/etkl/test_closure_equiv.py` compares `membrane.rdfs_closure` (owlrl, FULL RDFS) against `subclass_closure` on the real stem page: the full leg materialises `tab:hasBBox rdfs:domain tab:Cell`, types all 26 `tab:HeaderSourceCell`s as `tab:Cell`, and `tab:WrappedCellShape` refuses them (they carry `tab:sourceText`, not `tab:cellText`). `full=False sub=True on the real stem page`. | **Why the row was wrong, and it is the transferable part:** the measurements asked *does the MEMBRANE refuse this?* and the row claimed *the mechanism is closed at its root* — different claims, one unexamined quantifier. `grep -rn "rdfs_closure" src tests scripts` returns three consumers besides its definition, one of which (`scripts/measure_dec_membrane.py:18`) documents itself as *"what a CONSUMER applying our published axioms sees"*. The corpus guard could not catch it either: it runs through the membrane, the one leg blind to this by construction. | **Nothing to do — the row is the record, not a task.** `tab:markerRegion` stays; `tab:sourceRegion` + `tab:sourcePage` were minted as its siblings for [[R182]]; both R19 comments are restored with this evidence added. Do not re-raise this without first enumerating every consumer of `rdfs_closure` and reckoning with the published axioms, which bind consumers this repo does not run (CLAUDE.md § Authorship / FAIR posture). |
| ~~R61~~ | **Emitter-typing is now an unenforced invariant** — 45 `rdfs:domain`/`rdfs:range` rules in `vocab/ontology/tab.ttl` (28 domain, 17 range) land on classes shapes target (e.g. `hasCell rdfs:range tab:Cell`, `atColumn rdfs:range tab:LeafColumn`, `markerRegion rdfs:domain tab:UnitMarker`, `hasBBox rdfs:domain tab:Cell`); each was a path by which a node could enter a shape's target set without an explicit `rdf:type`, and all 45 are now inert for typing purposes (subclass-only closure, loop-subclass), so every shape's reach depends entirely on emitters typing every node explicitly — an invariant nothing in this codebase enforces | Fix-wave final review, 2026-08-06: the evidence that nothing verdict-relevant was lost is spec 2026-08-06-subclass-only-closure-design.md §1's table, measured on stem page 0 only | **The sharp part:** a lost typing that SHOULD have fired appears as a region flipping escalated→asserted — i.e. as a score IMPROVEMENT — exactly what apple looked like before diagnosis (spec §7: p1#mtable4 `MATRIX_AMBIGUOUS` → asserted). The corpus score gate alone cannot distinguish "a real fix" from "a shape silently stopped seeing a node it should still see"; only the closure differential (`tests/etkl/test_closure_equiv.py`) can, and per R59 it sees neither apple nor CBH — only stem page 0. Building the probe is out of scope for a fix wave | A corpus-wide invariant probe asserting that every node carrying a domain/range-implying property also carries the corresponding explicit `rdf:type`, with adjudicated exceptions (the two `iladub:CandidateConcept` escalation nodes this loop's own close-out found relying on domain typing: `mtable4-cc0_2`, `mtable4-cc1_2`) **PROBE BUILT AND RUN 2026-08-12 — R61 stays OPEN, but is no longer unmeasured.** `scripts/probe_emitter_typing.py` asserts the invariant over all 27 corpus pages, closing each node's explicit types under `rdfs:subClassOf` exactly as the membrane does. **Result: 5 violated rules, 56 violating nodes — of which 14 land on a class an `sh:sparql` shape targets, and those 14 are the live hazard.** They are 12x `range atColumn -> tab:LeafColumn` and 2x `domain onPage -> tab:Cell`, every one of them on the DATA-GRID path (`p7-datagrid`, `p7-datagrid-c2`, `p7-datagrid-c4`; ons p7/p8). The other 42 are inert for shape reach: 18x `range hasLabel -> tab:LabelCell` and 24x `domain x0/x1 -> tab:BBox`, whose classes no shape targets. **WHY IT STILL DOES NOT CLOSE, and why widening the exception list would be the wrong repair:** the 14 are not a forgotten `rdf:type`. `datagrid.emit_data_grid` mints `tab:GridColumn` nodes, points `tab:atColumn` at them (the ontology declares that property `rdfs:range tab:LeafColumn`) and hangs `tab:onPage` on the grid itself (declared `rdfs:domain tab:Cell`). Either the ontology is wrong about those two properties or the emitter is — a MODELLING decision, and the same `datagrid.py:635-636` emission that R92's close-out independently flagged for putting `tab:x0` on a non-`tab:BBox` subject. One emitter, two residues; decide it once. **Row correction:** this row says 45 rules (28 domain, 17 range). `tab.ttl` today carries 104 domain and 48 non-literal range (property, class) pairs, and carried comparable numbers before the closure shipped — so 45 was a filtered subset (rules landing on shape-target classes), never the total, and must not be quoted as one 🔎 **THE PROBE MEASURES 0 INSTANCES OF THIS ROW'S OWN INVARIANT (2026-08-18, `2026-08-18-probe-domain-range-agreement.md`).** Every violation was re-classified by asking WHICH graph supplies the missing type. 27 corpus pages, 74 violations, **UNTYPED: 0** — not one flagged node is missing an `rdf:type`. All 56 `tab.ttl` violations are DISAGREE: the node IS typed, just not as the rule says (`hasLabel -> LabelCell` on `Cell, EntryCell` ×18; `x0`/`x1 -> BBox` on `GridColumn, MeasureColumn` ×24; `atColumn -> LeafColumn` on `GridColumn` ×12; `onPage -> Cell` on `DataGrid, UniformGrid` ×2). So this row's headline — *emitter-typing is an unenforced invariant* — describes a failure mode the corpus has never exhibited, while the 14 live nodes are the emitter/ontology MODELLING disagreement this row's 2026-08-12 entry already identified. The instrument is renamed to match what it measures: `scripts/probe_emitter_typing.py` → **`scripts/probe_domain_range_agreement.py`**, now with a four-class report (UNTYPED / DISAGREE / ONT-VISIBLE / OUTSIDE-MEMBRANE) of which only the first two gate. **THE ROW STAYS OPEN, unchanged in substance:** live is 14 before and 14 after — the modelling decision (is the ontology wrong about `atColumn`/`onPage`, or is the emitter?) is still unmade, and until it is, the probe cannot be wired as a CI gate because it exits 1. Nothing runs it today: no test, no workflow. **Deferred here rather than as a new row:** the wiring is blocked on this row's own decision, not on anything separate 🔎 **THE LIVE COUNT IS NO LONGER 14 — IT IS 32, MEASURED 2026-09-08** (loop `the-box-must-cover-its-provenance`, running this row's own probe). The 18 this row calls inert *"for shape reach … whose classes no shape targets"* — `range hasLabel -> tab:LabelCell` — went LIVE when [[R179]] shipped `tab:LabelCellPhysicalShape` (`sh:targetClass tab:LabelCell`) on 2026-09-07. **14 + 18 = 32.** Nothing recorded the change, because nothing runs the probe, which is this row's own finding turned on itself. **This is NOT 18 new defects:** R179 knew the case and neutralised it inside the shape (`FILTER NOT EXISTS { $this a tab:EntryCell }`). The sharper statement is that 18 of the 32 are held off by a single carve-out in a single shape's SPARQL, and **nothing pins that carve-out as load-bearing** — delete it and the shape refuses 18 nodes. **And the row now has a decisive argument it lacked:** [[R182]] committed a fresh instance of this exact class (emitting `tab:hasBBox` on a non-`tab:Cell`), and the ruling that came out of it is that `vocab/ontology/tab.ttl` PUBLISHES these axioms under CC-BY, so any consumer running a stock RDFS reasoner derives them — `scripts/measure_dec_membrane.py:18` documents `rdfs_closure` as exactly that consumer's view. The membrane's tolerance is not a defence, and neither is a shape's carve-out. That settles the direction of the modelling decision this row has been blocked on: **the emitter is wrong where the emitted node is genuinely not of the declared class, and the axiom is wrong only where the class was mis-declared** — either way it must be RULED, not left to a validator that happens not to look.  **CLOSED 2026-09-08** (loop `the-axiom-binds-the-emitter`, spec `docs/superpowers/specs/2026-09-08-the-axiom-binds-the-emitter-design.md`). All 60 DISAGREE nodes RULED, one by one: **DISAGREE 60 → 0, live hazard 32 → 0, `tab.ttl` 0 violations of any kind** (`scripts/probe_domain_range_agreement.py`, 27 pages). Seven rulings — **2 EMITTER**: the `datagrid.py:635-636` site this row and R92's close-out both flagged, ruled per [[R182]]'s remedy (a column has an x-INTERVAL and no y extent, so it could never satisfy a box shape; `tab:colX0`/`tab:colX1` domained on `tab:GridColumn` replace `tab:x0`/`x1` domained on `tab:BBox`). **5 AXIOM**: `hasLabel` range → `tab:Cell` (`rowgroups.py:93` deliberately points at the SOURCE EntryCell, and its own docstring says so); `atColumn` range → new abstract `tab:Column`; `onPage` domain → new abstract `tab:PageLocated`; `universeSource`'s domain and range were TRANSPOSED (declared `rdfs:domain tab:ColumnUniverse` with no range, while the emitter writes grid → universe); `tab:Text` was missing the `tab:CellDatatypeFamily` declaration `tab-datagrid.ttl:177`'s own prose already asserts. 🔎 **THE OBVIOUS RULING FOR `atColumn` WOULD HAVE BEEN A DEFECT, and only measuring caught it.** `tab:GridColumn`'s comment calls it *“a transient leaf column”*, inviting `rdfs:subClassOf tab:LeafColumn`. But `tab-shapes.ttl:15,128` target `tab:LeafColumn` with shapes requiring `?tbl tab:hasLeafColumn $this`, and a grid column is `tab:hasGridColumn` of a GRID — the subclass edge would have dragged 12 nodes into two shapes they cannot satisfy. The class comment is loose prose; the shapes are the operative definition. 🔎 **THIS ROW'S “nothing pins that carve-out as load-bearing” HAS AN ANSWER: it cannot be pinned, because it is not load-bearing.** Two measurements. (a) With BOTH `FILTER NOT EXISTS { $this a tab:EntryCell }` filters deleted, `tests/etkl/test_closure_equiv.py` is **18 passed in 81.74s** — ruling 1 removed the range that put an entry into `tab:LabelCellPhysicalShape`'s target set, so [[R179]]'s 2026-09-07 measurement no longer reproduces. This row's *“delete it and the shape refuses 18 nodes”* is now false, and the ruling is what made it false. (b) The attempted falsification on a both-typed node returned the sharper fact: the ONLY shape refusing it is `tab:EntryCellPhysicalShape`, which requires cellText + onPage + hasBBox unconditionally — strictly more than the label shape asks. The exclusion is **LOSSLESS BY DOMINATION**, exactly what the shape's comment claimed and what nobody had measured. A test asserting *“delete the filter and the shape refuses”* would have pinned the ENTRY shape while claiming to pin this one. The filter is KEPT (CLAUDE.md § Producer-side guards vs the membrane: the condition is PROVABLE total coverage) and the domination is pinned instead. **THE GATE SHIPS** — this row's stated closure condition, and the half it said was blocked *“on this row's own decision, not on anything separate”*: `tests/etkl/test_domain_range_agreement.py::test_the_gate_no_domain_range_disagreement_on_an_emitted_graph` runs the probe's OWN classifier over a graph built by the real emitters and fails on any DISAGREE or UNTYPED. Built from synthetic `Line`/`Word` objects, never a corpus PDF, because the corpus is gitignored and a gate that skips in CI is [[R173]]'s trap. FALSIFICATION: all 7 rulings invert to RED against a green control, and the gate independently catches 5 of the 7. **Its coverage is narrower than the corpus probe's** — `hasLabel` is emitted by `rowgroups.py`, not by `emit_data_grid`, so reverting ruling 1 trips only the ontology assertion — raised as [[R184]]. |
| ~~R185~~ (55/174 closed) | **The release gate is HARD-BLOCKED and the governance model has no way to express a DRAINED contradiction.** `scripts/release_gate.py` reports `RELEASE BLOCKED — undrained contradiction(s) since 2026-08-02` naming `docs/superpowers/{plans,specs}/2026-08-10-the-decision-membrane*.md`. Both were in fact drained on 2026-08-11 — that spec carries its own note, *“Doc impact — RESOLVED 2026-08-11, in this loop … No contradiction remains, so the release gate is unblocked by this spec”* — and that sentence is **false**, measurably. `tests/docgov_extract.py:95`'s regex is `\*{0,2}Doc impact:\*{0,2}\s*(none\|increment\|contradiction)\b`, which the RESOLVED line (em-dash, no colon, no listed keyword) cannot match, so the doc still extracts as `dg:docImpact "contradiction"`. `vocab/queries/docgov-release-gate.rq` then blocks on impact + date alone: **there is no triple in the model meaning “fixed”.** | MEASURED 2026-09-08 in the [[R61]] loop, which ran the gate to de-risk the release loop rather than to work on it. Three measurements: (a) `PYTHONPATH=src python3 scripts/release_gate.py` → BLOCKED on those two paths; (b) `extract(Path('.'))` reports `docImpact contradiction` for both, so the RESOLVED note is invisible to the extractor; (c) `_since_date` (`scripts/release_gate.py:30-41`) EXCLUDES tags on HEAD — *“during a release run HEAD carries the tag being built”* — so tagging does NOT move `?since` past 2026-08-10 and the block does **not** self-clear at tag time. | Deferred because [[R61]] is a ruling loop about emitter/axiom agreement and this is a documentation-governance model gap; folding it in would make one loop answer two questions (CLAUDE.md § Plan authoring discipline rule 6). It is raised rather than fixed because the remedy is a DESIGN FORK, not a typo. | **The fork, and it must be RULED before code.** `docs/superpowers/**` is Evidence — *immutable after loop close* (CLAUDE.md § Documentation governance) — so the contradiction cannot be drained by editing the original `Doc impact:` line, and the 2026-08-10 spec's attempt to drain it by APPENDING a second line is exactly what the extractor cannot see. Arms: **(1)** teach the extractor a drain marker (a `Doc impact — RESOLVED <date>` form) and let a later line supersede an earlier one — cheap, but it makes Evidence mutable in the one field the gate reads, which is the property the class exists to forbid; **(2)** record the drain OUTSIDE the evidence file, in a mutable register the query joins against — consistent with the `residues.md` exception, and the arm that preserves immutability; **(3)** rule that a contradiction is drained by the RELEASE that follows it, and make `?since` inclusive of the tag being built — one line in `_since_date`, but it silently drops any contradiction raised the same day, which is [[R26]]'s known day-granularity hole made load-bearing. **Do not pick arm 3 for being one line.** Whichever arm wins, the check is that `scripts/release_gate.py` exits 0 with the two 2026-08-10 docs still declaring what they declare. **CLOSED 2026-09-08** (loop `the-drain-is-a-release-act`) — **arm 2**, ruled in `docs/superpowers/specs/2026-09-08-the-drain-is-a-release-act-design.md`: the drain is a RELEASE-time act, recorded as a `docgov:ContradictionDrain` in the new `tests/docgov-drains.ttl` and joined by one `FILTER NOT EXISTS` in the existing AXIOM — no new procedural parsing. `scripts/release_gate.py` now exits **0** with both 2026-08-10 docs still declaring `contradiction` verbatim and no Evidence file edited, which is this row's own stated closure condition. **TWO of this row's framings are corrected by the loop that closed it.** (1) Arm 3 is refused on a stronger ground than the [[R26]] day-granularity hole it names: MEASURED, `?since` = the tag being built returns `[]` for EVERY document, so the gate is *vacuous* during exactly the run it gates — not merely coarse. (2) Arm 1's stated defect — that it *"makes Evidence mutable in the one field the gate reads"* — **does not hold, and is withdrawn**: nothing enforces Evidence immutability (`tests/test_doc_governance.py` is membrane + staleness only), the 2026-08-10 spec was in fact edited post-close with nothing complaining, and appending is not mutation. Arm 1 is refused instead because it puts a release-time fact into a document written before the fix existed — its appended *"the release gate is unblocked by this spec"* was false when written and the gate blocked four further weeks — because it lets the blocked document certify its own release, and because it buys procedural regex where arm 2 buys none. Also measured: `contradiction` has been declared **once ever** across 50+ specs, and the drain register does NOT prove the page was fixed (spec §3.3) — it makes the judgement dated, attributed and enumerable. Falsified three ways: query clause removed → 3 red; shape retargeted → 6 red; one drain record deleted → gate re-blocks on exactly that document. Successor [[R186]] |
| ~~R186~~ (56/175 closed) | **"Evidence is immutable after loop close" is a NORM, not a membrane — nothing checks it.** CLAUDE.md § Documentation governance calls `docs/superpowers/**` immutable after loop close and names exactly two exceptions (`residues.md`, a gated generated cache). No instrument enforces it: `tests/test_doc_governance.py` runs a SHACL membrane over classification/nav/frontmatter/doc-impact plus two staleness derivations, and has no immutability check of any kind. The property the class exists to guarantee is unguarded. | MEASURED 2026-09-08 in the [[R185]] loop, which needed the fact to weigh arm 1 and found it false. Two measurements: (a) `grep -n "immutab" tests/test_doc_governance.py` → nothing, and the module's four tests are `test_membrane` + three derivations; (b) the case in the wild — `docs/superpowers/specs/2026-08-10-the-decision-membrane-design.md` gained a whole appended `Doc impact — RESOLVED 2026-08-11` block after its loop closed, and no instrument complained then or since. | Deferred because [[R185]] is a release-gate ruling and this is a documentation-governance enforcement gap; folding it in would make one loop answer two questions (CLAUDE.md § Plan authoring discipline rule 6). It is also **not obviously a defect to repair**: R185's closure argues that appending is not mutation, so a naive `git log`-based lint would fire on the one edit this repo has actually made and judged benign. | **A DESIGN FORK, and the first question is whether immutability is the right rule at all.** Arms: **(1)** enforce it — a lint over `git log` per Evidence path, refusing commits that touch a spec/plan dated before the current loop; cheap to state, and it would have refused the 2026-08-11 append that this repo now relies on as the record of what went wrong. **(2)** narrow the rule to what actually matters — the `Doc impact:` DECLARATION line is immutable, the rest of the file is append-only; that is the one field a release reads, and R185 built the drain register precisely so nobody needs to edit it. **(3)** rule that the norm is unenforceable-by-design and say so in CLAUDE.md, deleting the word "immutable" in favour of "append-only, and never rewritten" — the honest option if arms 1 and 2 both cost more than the hazard. **Measure before choosing:** how many tracked Evidence files have commits after their own loop's close, and how many of those rewrote rather than appended. Nobody has counted; [[R185]] found exactly one by accident. **COUNTED 2026-09-08** (`the-two-counts`, evidence `docs/superpowers/2026-09-08-the-two-counts.md` §1): 327 files after exclusions, **20 with a post-close edit (6.1%) at a ≥1-day threshold — but 5 at ≥2 days and ZERO at ≥7**, because the dominant case is a `Status: → SHIPPED` stamp the next day, which is arguably the loop closing. Of 26 events, **20 REWRITE / 6 APPEND** — rewriting is what has actually happened. TWO corrections to this row: (a) R185's single known case (`3251d5f`) is **+10/−1, a REWRITE** — the block was appended *and* the `Status:` header rewritten, so the *appending is not mutation* argument rests on a case that is not purely an append; (b) the strongest case, `265275d`, **admits the offence in its own commit subject** ("three guards, and the rule broken while applying it") and negotiates in prose how far it may go — the norm is breached knowingly, not ignorantly. The count cannot see within-loop edits (squash merges hide them), so 6.1% is a floor. **ARM 1 IS STRUCK 2026-09-08** (`two-loops-sequenced`, evidence `docs/superpowers/2026-09-08-two-loops-sequenced.md` §1): the previous handoff's 5b — *arm 1 is now cheap, because at ≥7 days it fires on zero* — is **REFUTED on the mechanism**. Its own named check (ii) turned out answerable locally: PRs up to **#145 were merge-committed, not squashed** (#146+ are), so within-loop edits ARE in history for the pre-2026-09-01 era, as the same-day zone the count reported unclassified. Classified over the same 329-file population: **113 files (34.3%), 203 events, 148 REWRITE / 55 APPEND**. Two consequences. (a) The ambiguous zone is **5.6× the ≥1-day signal and 34× the ≥2-day signal** — ≥7 days fires on zero because it discards **229 events to keep none**; *a window wide enough to be safe is wide enough to be empty*, which inverts 5b's inference. (b) **The rewrite rate is the same on both sides of the clock — 73% same-day, 77% later** — so the separator is not separating two behaviours; it is one behaviour cut by a calendar. Any time-windowed git-history rule inherits this. **This row now has TWO live arms, both of them sentences in CLAUDE.md rather than code, and both are the MAINTAINER's to rule** — which is why the same document rules R186 and [[R187]] **two loops, sequenced, R186 first**: R186 needs François, R187 does not, and folding them would block the buildable half on the unbuildable half. See §3 of that evidence for the invariant this row shares with [[R187]] and [[R188]] — derive it there, do not re-derive it here. **A PROPOSAL BETWEEN THE TWO SURVIVORS EXISTS 2026-09-08** (`docs/superpowers/2026-09-08-r186-proposal.md`), and it reports that **each arm is wrong about the thing it was most confident about.** Arm 3's *premise* is FALSE: the arm-1 refutation kills only **time-windowed** rules, and the branch-protection ruling made the PR boundary exact — since `5743af3` (2026-08-31) **one commit per PR without exception across 32 commits**, so "a later PR" and "deleted a line" are both decidable with no proxy and no tuned constant. Squash-era: **59 files, 2 edited by a later PR, BOTH pure append (+N/-0), 0 rewrites** — and one of the two is the append this repo consciously labelled correct, so a rule refusing *any* later edit fires only on the good behaviour. Arm 2's *granularity* is wrong: **14 post-introduction commits touch a `Doc impact:` line and ZERO change the declared value** (all are additions, backtick/bold normalisations, or prose after the keyword) — freeze the VALUE, not the line, and the rule costs nothing. Proposed: arm 3's sentence + arm 2's field at value granularity + an explicit prospective boundary. **STILL THE MAINTAINER'S TO RULE — nothing in CLAUDE.md is touched.** **CLOSED 2026-09-08 — the maintainer RULED, choosing the proposal over both surviving arms.** Put to François in the loop that follows `two-loops-sequenced`; the answer selected the proposal document's §4 wording verbatim. `CLAUDE.md` § Documentation governance now reads: Evidence is **append-only after loop close — a later PR may add, and may never delete or modify an existing line** — and a file's `Doc impact:` **declared VALUE** is frozen outright, with the two named exceptions unchanged. **Binding prospectively from `5743af3` (2026-08-31)**, with the reason stated in the contract itself: before the squash boundary no instrument can separate loop iteration from post-close editing (229 events / 133 files, 73% vs 77% either side of any clock); after it, one commit per PR makes *a later PR* exact and `--numstat` makes *deleted a line* exact. So the word *immutable* is gone, arm 2's field survives at VALUE granularity (14 real edits permitted, 0 defects lost), and arm 3's sentence survives WITHOUT its concession — the premise that the norm is unenforceable-by-design was itself falsified. **NO LINT SHIPS WITH THE RULING** — a gate that would fire on zero on day one is [[R188]]'s subject, and the squash-era evidence for the enforced half is n=2, which is a well-defined instrument and not a demonstration that rewriting stopped. If squash-era rewrites appear, the rule starts refusing real work and that outcome — not this closure — should decide it. |
| ~~R187~~ (56/176 closed) | **A wiki page states a corpus measurement in the PRESENT TENSE with no date on the claim, so a reader cannot tell a dated reading from a current one — and two such figures are already superseded.** `docs/wiki/concepts/data-grid.md` says *"stem unchanged at `0.9654553611484971`"* (`:174`, `:188`, `:206`) and *"apple `0.06068601583113457` → `0.35560344827586204`"* (`:206`). Both were true when written. Neither is the tree's reading today. The page carries `updated: 2026-08-09` in frontmatter, which dates the *file*, not the *claim* — and the file has since been edited without those figures being re-measured. | MEASURED 2026-09-08 in the `release-v0.0.4` loop, while weighing whether to promote the page. (a) `grep -nE '0\.9654\|0\.6289' docs/wiki/concepts/data-grid.md` → three hits at `:174,188,206`. (b) [[R174]], **closed 2026-09-05**, records the stem figure moving to `0.9658886894075404` at `20cc5b8`. (c) apple has read `0.6289` since the one-band matrix ([[R165]], PR #159). So 2 of the 4 figures the last five handoffs have quoted are demonstrably stale in the page that quotes them. | Deferred because the release it was found in ruled **promote zero**, so no stale figure reaches a published page and nothing is urgent. Repairing it in-loop was refused for a stronger reason: **rewriting a loop's measured figures in place destroys the record rather than dating it.** `docs/wiki/**` is a proposition and freely rewritten — but a *measurement* inside one is evidence of what was read on a day, and silently updating it makes the page unfalsifiable. | **A design question first, a lint second — and the arms differ on what a wiki measurement IS.** Arms: **(1)** date the claim, not the file — require a measured figure in `docs/wiki/**` to carry its date and commit inline (`0.9654… @ 20cc5b8`, superseded figures struck rather than replaced), which is what `docs/superpowers/**` already does by convention; a lint could then refuse an undated decimal in a wiki page. **(2)** promote-time only — leave wiki prose alone and make the *promotion* step re-measure every figure the page states, which puts the cost on the one act that turns a proposition into an Assertion (`RELEASE.md` step 1); nothing enforces it today. **(3)** rule that wiki measurements are historical by definition and say so once at the top of each page, deleting the present tense — cheapest, and it gives up the ability to tell a reader which figures are still live. **Measure before choosing:** how many decimals across `docs/wiki/**` are corpus measurements at all (as opposed to thresholds, versions, confidences), and how many of those the tree still reads. This loop counted them in ONE page. **COUNTED 2026-09-08** (`the-two-counts`, evidence §2): 44 decimals across 12 wiki pages → **26 corpus measurements, 6 STALE / 11 LIVE / 9 UNKNOWN**. The six reduce to **exactly two quantities — the two this row already named; the whole-wiki sweep found ZERO new stale quantities**. Present-tense vs dated: **21/5**, and all six stale are present-tense (three assert currency in words: "unchanged at", "the **live** stem"). Concentration: 2 of 12 pages carry all of it, 8 carry no corpus measurement at all — **an outlier in concentration, typical in kind: the predictor is the QUANTITY CLASS (a document-level score), not the page**. Four findings that move the fork: (a) **this row's scope is wrong** — `tests/etkl/test_datagrid.py:1085` carries the stale stem figure in prose, outside `docs/wiki/**`; (b) **[[R174]]'s re-baseline was half-applied** — `tests/test_corpus_stem.py:421` still uses the superseded figure as a live comparator bound; (c) **precision defeats naive matching** — `0.9655` and `0.9654553611484971` are one quantity, so a lint on this row's literal finds 3 of 6; (d) **`updated:` — this row's own fallback — is wrong on `table-holon-compilation.md`**, which declares 2026-08-03 while citing a 2026-09-07 spec. **RESCOPED 2026-09-08** (`two-loops-sequenced`, evidence §2): this row says *"a design question first, a lint second"*, and **the lint already exists and already flags both pages this row names.** `tests/test_doc_governance.py` runs extraction → SHACL membrane → three SPARQL derivations; run with `-W always::UserWarning` it emits `data-grid.md staleAgainstCode src/iladub/etkl/compile.py` and `table-holon-compilation.md staleAgainstCode src/iladub/etkl/document.py`, both also `inPromotionQueue true` — **4 passed, 2 warnings**. Three measured defects explain why a correct signal passes: (i) the HARD gate `docgov-staleness-evidence.rq` is **citation-gated** (`?page dg:cites ?src`, `?src docClass "evidence"`) and `data-grid.md`'s `sources:` names three 2026-08-08/09 specs and **none of [[R174]], [[R165]] or any loop that superseded its figures** — a page goes stale only against sources it names, and it never names the loop that refuted it; (ii) the live edge is `docgov-staleness-code.rq`, **warning-only by design** (its comment: *a hard gate here would make every loop a doc loop*) — that prediction now measures **9 of 12 wiki pages** flagged; (iii) **inline citations are not extracted**, so finding (d) above is invisible to the instrument. **So this is a RECALIBRATE-AND-RESCOPE of running code with a green-suite oracle, not a lint to design** — and it is the SECOND of two sequenced loops, after [[R186]]'s maintainer ruling. The shared invariant is derived once in §3 of that evidence ([[R188]]); cite it, do not re-derive it. | **CLOSED 2026-09-08.** The instrument gained a FIGURE scope and the wiki disposition ships HARD. What it is: `tests/corpus-manifest.ttl` now carries 18 `cor:reading` nodes — dated readings, append-only, one node per distinct value, every row naming where it was transcribed from; `tests/docgov_extract.py` emits a `dg:FigureOccurrence` for every decimal literal that denotes one; `vocab/queries/docgov-undated-figure.rq` derives `dg:statesUndatedReading` where the literal's own markdown BLOCK carries no date or sha; `tests/test_doc_governance.py::test_no_wiki_page_states_an_undated_reading` fails on it. **The disposition was PROPOSED and is now confirmed by measurement, on a design one step from the one proposed.** The spec's O3 census, run before any code: with the register alone, the gate carried THREE false positives, not the one predicted — the `1.0000` triple AND a `0.1` pair ("quarantines exactly like a 0.1-scored one"), which under O3's own rule would have dropped it to a warning. Both are killed by a DERIVED quantity, not a chosen one: the register's separating precision ([[R193]]), plus the one exemption ([[R194]]). Result: **11 denoting occurrences in `docs/wiki/**`, all 11 real, zero false positives.** **The row's own scope claim was corrected twice by the instrument.** Its named surface was `data-grid.md`; the gate found 8 undated readings across `data-grid.md` AND `neurosymbolic-exemplars.md` (which the 2026-09-08 sweep had classified as carrying nothing stale). And it does NOT fire on `table-holon-compilation.md`, whose `0.9655` a hand census had listed as a finding: the block that states it opens `**Loop M (2026-08-03)**`, so it was dated all along and the hand census had read a window of the block instead of the block. **The repair dated the claim and rewrote no figure**, per this row's own deferral ruling. Not closed by this: `staleAgainstCode`, which §0 of the spec refutes as THIS detector and not as itself. |
| ~~R189~~ (57/179 closed) | **The figure-staleness gate specified for [[R187]] stops at tracked markdown, and three statements of a superseded corpus score sit outside it in `src/` and `tests/`.** `src/iladub/etkl/compile.py:1288` (*"leaves the stem document byte-identical at 0.9654553611484971"*), `:1302` (same figure, naming the pin that no longer holds it) and `tests/etkl/test_datagrid.py:1085` (*"the whole stem is one chain of 3, 2152 cells, at 0.9654553611484971"*) all state, as present fact, a document score [[R174]] superseded on 2026-09-05. | MEASURED 2026-09-08 at `9eb5cd0`, `git grep -n 0.9654553611484971`, recorded in `docs/superpowers/specs/2026-09-08-a-figure-carries-its-date-design.md` §1.3. The same grep shows `tests/test_corpus_stem.py:358,416` carrying the figure and NOT being defects — both name the supersession in the same sentence — which is the distinction the spec's `dg:blockDated` makes mechanical. | Deferred because `tests/docgov_extract.py` walks `git ls-files '*.md'` and nothing else; reaching `.py`/`.ttl` comments is a scope expansion whose false-positive surface the spec's O3 census cannot yet bound (a source file is full of decimals that are not readings). Scoping it in would have made one loop answer two questions. | Extend the figure walk past markdown, and re-run the O3 census over the new population BEFORE the disposition is decided. **The prior is worse here, not better:** a `.py` file's decimals are mostly parameters, so the uniqueness rule of the spec's §3.3 is doing more work on a harder population. Do not inherit markdown's disposition. **MEASURED 2026-09-09 at `4991dd1`, and this closure criterion is now known to be insufficient on its own** (`docs/superpowers/2026-09-09-a-figure-at-the-end-of-a-sentence.md`). Extending the walk and re-running the census was done, and it found **none of the three sites above** — each writes the reading at the END OF A SENTENCE, which the shipped `_DECIMAL` refused (`(?![\w.])` rejects a following dot unconditionally, against its own comment's stated intent of refusing only a longer dotted token). **The scope was never the blocker.** That defect is fixed in this loop; the walk is NOT extended. Corrected facts for whoever closes this: (1) **the population is 4, not 3** — `tests/etkl/test_membrane_health.py:429` states `0.9655` and *"77 escalated tokens"*, both superseded, and is named nowhere above; (2) **this row's own prior is REFUTED** — of 13 false positives in 17 undated code occurrences, **zero is a parameter**; the register's uniqueness rule does exactly what it was expected to do; (3) the false positives come from somewhere neither this row nor the handoff's 5a predicted — the figure gate's OWN unit fixtures (10 of 13), a vocabulary `rdfs:comment` (1), and [[R196]]'s block mis-scoping (2) — for a **76% false-positive rate against the 0-of-11 that licensed the hard wiki disposition**; (4) the four findings are deliberately LEFT UNREPAIRED as the only live positive population a code-scope gate could be tested against. Close this by gating and repairing in one change, and settle [[R196]] first.  **CLOSED 2026-09-09** (`docs/superpowers/2026-09-09-a-claim-lives-in-prose.md`, spec `specs/2026-09-09-a-claim-lives-in-prose-design.md`). Both halves of the criterion done in one change: the walk now covers tracked `.py` PROSE — comment runs and docstrings, recovered exactly by `tokenize`/`ast`, with `blocks` still the closure boundary inside them — and `test_no_source_file_states_an_undated_reading` gates class `code` HARD. **All four sites repaired by DATING the claim, never by rewriting a figure**, and two more with them: `tests/etkl/test_adoption_document.py:369` states two superseded apple scores under the unresolvable phrase *"before this loop"*, which the predecessor census had classified as a false positive. Population was 6, not 4. **The disposition's licence is the PRE-repair census** (20 prose occurrences, 10 undated) and the gate is FALSIFIED: reverting the date at `compile.py:1288` turns it RED with the site and the reading's dates. [[R196]](a) was settled first, as this row required — and refuted. |
| ~~R198~~ (59/187 closed) | **`cor:readAt` records when the corpus register was REFRESHED, not when a figure was read — so it cannot say where any figure came from.** The register holds 7 distinct `readAt` days for a repo whose 95 figure-bearing evidence documents span six weeks. | MEASURED 2026-09-09 at `9716755` (`docs/superpowers/2026-09-09-the-register-is-a-refresh-log.md` §3): **109 occurrences in 43 documents state a reading whose registered `readAt` is LATER than the document's own date** — `2026-08-20-escalation-reason-census.md:67` reports `ons 0.9720`, registered `2026-09-05`. The census did not quote the future; the register recorded that value sixteen days after it was measured. | Deferred because repairing it is a register REDESIGN — per-reading provenance (the commit or day a value was first observed), not a field edit — and nothing is lying today: the register never claimed `readAt` was a first-read date, it was simply read as one. | Close it by recording, per reading, the commit at which the value was first observed, and re-deriving `readAt` from that. **What would NOT close it:** adding more `readAt` dates on each refresh — that makes the log denser without making it provenance. Until it closes, **no date-based judgement about a figure's origin is sound**, and any supersession test built on `readAt` UNDER-counts.  **CLOSED 2026-09-09** (`docs/superpowers/specs/2026-09-09-first-observation-is-recoverable-design.md`), at `4b886a9`. The predecessor handoff's 5a named the blocking question and it is ANSWERED: first observation is **recoverable for 18 of 18** readings, in ONE 9.6-second `git log -G<alternation> -U0 -p HEAD` pass, agreeing with 18 per-value `-S` pickaxes on every commit and every date — so the feared going-forward-only compromise is unnecessary and **no register redesign ships**. Measured lag: `readAt` post-dates first observation for **8 of the 17 quotable readings, by 1 to 16 days**. Closed by (a) `scripts/first_seen.py`, the dating primitive R192's handoff complained did not exist — DERIVED from git, never stored, because a derived value committed as data is a stored label; (b) `tests/test_first_seen.py`, 7 tests, every one falsified (one falsification found a real gap — the shallow-clone guard was tested but its CALL SITE was not, so deleting the call left the suite green); (c) `corpus:readAt`'s `rdfs:comment`, whose sentence *"The earliest is when the reading was first taken"* was the false claim and now says refresh-log. **NOT edited: `tests/corpus-manifest.ttl`** — every row in it is true, and the defect was in reading a refresh date as provenance. `1.0` is the one unrecoverable value (its first added line on main is a 2026-05-31 MkDocs deploy, 81 days of noise) and it needs no new exemption: it is already `cor:notQuotable` for the SAME root cause — an undistinctive literal cannot be matched against prose or against history. **The harm was real and is now measured**: see [[R192]]'s amendment. |
| ~~R167~~ (43/156 closed) | **The em-dash `—` (U+2014), US-GAAP's nil glyph, types as `tab:Text` — one such cell disqualifies a whole data column and moves the stub|data split.** `celltype.is_blank` recognises `''`, `(blank)` and the ASCII `-` only (`src/iladub/etkl/celltype.py:67-72`), so `_cell_datatype('—')` returns `tab:Text`; `vocab/queries/stub-data-split.rq`'s `SUM(IF(?ct = tab:Text, 1, 0)) = 0` then rules that column out of the data suffix **CLOSED 2026-09-09** (loop `the-nil-glyph`) — fixed as VOCABULARY, not a Python literal: `tab:Blank`'s published CC-BY comment already enumerated the nil markers in prose, so extending only `is_blank` would have made the contract false. `tab:nilSpelling` now carries the four markers and `celltype._NIL_SPELLINGS` reads them from the `tab.ttl` parse the module already did at import; deleting a triple turns three tests RED. **The row's own blast-radius list is corrected**: 8 call sites in 4 modules, not 3 — `unitmarker.py:43` and `datagrid.py:443,517,525,563` are missing from it, and `datagrid.py:274` is a comment. **Population MEASURED 2026-09-09 at `3fe0ce2`: ONE em-dash and ZERO en-dashes in the whole corpus**, and that cell sits on an escalated band, so the defect was real but INERT — O4 ran both readings in two processes (the importers bind `is_blank` at import, so patching reaches only `_cell_datatype`) and all **7 documents / 27 pages are byte-identical by canonical graph hash**. The pin that matters is the CONSEQUENCE, not the typing: a synthetic three-column grid whose data column holds one nil cell returns k=1, and **against the stashed baseline tree returns k=2** — reproducing this row's apple measurement without the corpus, in a test CI can run ([[R173]]). Successor: none. [[R166]]'s p2 half was measured in passing and is worse than its row records — see the handoff's 5b. | Measured 2026-09-04 on `main` at `c9e6941` with `scripts/one_band_matrix_spike.py` (readings in `docs/superpowers/2026-09-04-one-band-matrix-spike.md` § 4.1). Apple p2 bands 2–7 merged: column 1's 30 body cells are `ParenthesizedNumber`x15, `Numeric`x12, `Currency`x2 and **`Text`x1 — row 29, `'—'`**. `stub_data_split` returns k=2 instead of 1 and `matrix_body_start` returns 2 instead of 3; with `is_blank` extended to `—`/`–` and nothing else changed, both are correct (`split=2 k=1 body_start=3`). The band-2-alone reading is unaffected (k=1) because the em-dash is in the body, so this surfaces only once the body is in the same band as the header | **Raised, not fixed, because it is not this spike's subject and fixing it alone closes nothing**: p2 still refuses at the next stage on [[R162]], which is ruled NEURAL. It is also a one-line change to a function four modules read (`feed.py:222`, `datagrid.py:274,331`, `celltype._cell_datatype`), so it needs a corpus regression run, not a patch inside a measurement loop | A test that pins `_cell_datatype('—') == tab:Blank` (and `–`, U+2013) beside its falsification, plus a corpus run showing no document's score moves for a reason other than this. Distinct from [[R78]], which is about genuinely AMBIGUOUS suppression markers (`..`, `n/a`) whose value is unknown — the em-dash is decidable from format alone and is the typographically-correct spelling of the `-` `is_blank` already accepts |
| ~~R207~~ (61/197 closed) | **A section marker whose text is a `skos:prefLabel` of a contract field's admissible scheme is quarantined, although the oracle that admits it (`scheme_member`, `src/iladub/ground.py:81`) is already in the file.** `feed._inject_section_captions` (`feed.py:368`) mints `SurfaceConcept(text, text, region, is_section_marker=True)`; `ground_concept` (`ground.py:191`) reaches it through `exact_field` (`:73`), which normalises the marker's TEXT against contract field NAMES — `KWINANA` matches no field called `kwinana` — so the marker is `proposed` and the abstaining proposer leaves it there | Measured 2026-09-10 at `6842c5f` by `scripts/placement_population.py`: all 49 of cbh's ceiling cells are section markers on `cbh:port`, 4 distinct strings; graincorp mints no marker that any oracle admits (ceiling 0) | Deferred because the loop that found it was a count, and because the gain is unmeasured: if cbh's rows already ground `cbh:port` from a per-row column, binding the marker duplicates it. **Measure first**: records on cbh with no `cbh:port` after `ground_document`; ~58 means the marker IS the row's port and the binding is a 49-cell gain, ~0 means it is redundant | A marker-aware branch in `ground_concept` — for `is_section_marker`, try scheme membership over the scheme-bound fields, ground on a unique hit — pinned by a test on a synthetic section table with a scheme-bound caption, falsified by removing the branch; and the port-count measurement above recorded beside it. AXIOM, no model, no constant  **CLOSED 2026-09-10** (loop `r207-marker-grounds-by-scheme`, `docs/superpowers/2026-09-10-r207-marker-grounds-by-scheme-handoff.md`). The measurement the row demanded was RUN first and came back at the gain end: **58 of 58** cbh records carried NO `cbh:port` after `ground_document` — no per-row port column exists, the marker IS the row's port. Closed by `ground.marker_field` (AXIOM: the ONE scheme-bound field whose scheme carries the marker's value as a prefLabel; zero or two abstain; never called on a non-marker) and one `elif` in `ground_concept`, minting `splitkey`'s own `unique-admitting-field-rule` suggester so the per-name and per-value grains read as one rule. After: all **49** section-prefixed records carry exactly one `cbh:port` equal to their identity prefix, the 9 unsectioned carry none, grounded 134 → **183**, proposed 775 → **726**; the instrument's cbh ceiling **49 → 0** (`marker-grounded 49`), graincorp unchanged. Pinned by 6 synthetic tests + 1 corpus pin; falsified — `elif False` fails 3 of 7, restored 7 pass |
| ~~R208~~ (61/198 closed) | **Two data rows arrive as ONE record on graincorp-stem, every cell joined, and the joined value then fails its oracle**: `Mackay Mackay` ×4, `Mackay Mackay Mackay` ×1, `Portland Portland` ×1, `Wheat Wheat` ×4, `Wheat Chickpeas` ×1, `Wheat Chickpeas Chickpeas` ×1, `Accepted Accepted` ×5, `Accepted Accepted Accepted` ×1, `15,000 15,000` ×2, `10,000 15,000` ×1, `10,000 15,000 15,000` ×1 — 22 of the document's 48 exact-refusals, and the mixed ones (`Wheat Chickpeas`) show the joined rows are DIFFERENT rows, not a repeated cell. `pdftotext -layout` lines 21-22 show a pair: two consecutive `Mackay` rows, the second with a blank month | Measured 2026-09-10 at `6842c5f` by `scripts/placement_population.py` (the `exact-refused` breakdown, identical on two runs); the page read with `pdftotext -layout corpus/ag-trade/graincorp-stem-2026-07-31.pdf`. The other 26 refusals: 18 scheme gaps (`Woodchip` 6, `SHUTDOWN` 6, `Cement` 4, `COMMENCED` 1, `Peas` 1; cbh's single refusal `Silica Sand` is the same class), 6 subtotal rows reaching the port field (`Fisherman Islands Total`, `Port Kembla Total`, `Carrington Total`, `Geelong Total`), 2 beyond the instrument's top-20 print | Deferred because the cause is a PROPOSITION read off the rendered page, not off the record's provenance — the prediction is that row segmentation keys on a column the continuation row leaves blank. Raised because it is the ONLY population the GLiNER2 spec's placement B (cell splitting) was ever offered on this corpus, and on inspection it is a row boundary the page draws, not a fused string — CLAUDE.md § 0's neolegacy pattern if a text model were asked to split it | One run printing `rec.row_id` and each joined concept's `region` (the instrument has both in hand) confirming or refuting the blank-key cause; then a row-segmentation fix in `table_records` pinned on a synthetic two-row stem with a blank key cell, or a ruling that the second row is a continuation the author meant  **LOCATED UPSTREAM 2026-09-10** (loop `r207-marker-grounds-by-scheme`, handoff § 2 and § 5a): the blank-key cause in `table_records` is **REFUTED**. The joined values are ONE `tab:EntryCell` per column in the compiled graph (every cell of the fused record at region `y=71`: `p1-96-71`, `p1-162-71`, …) while `page_bands(pdf, 1)` band 1 holds the three Mackay lines as three separate `Line`s (top 71.31 / 77.79 / 84.27) — so the weld is between lines and EntryCells, inside the hierarchical table's row structure, and the fused record's identity is a row PATH (`2026/27 > Oct 26 > Mackay Mackay Mackay`, no discriminator) — the port was read as a row-header level. Six fused records (38, 54, 70, 91, 93, 110); Nov 26 welds only 2 of 4 consecutive Mackay lines, so "repeated value" alone is not the rule. Next measurement: the handoff's § 5a  **CAUSE LOCATED 2026-09-10** (loop `r208-the-weld-is-noise`, handoff `2026-09-10-the-weld-is-noise-handoff.md` § 2): the § 5a prediction (a repeated row-header value read as one spanning cell) is **REFUTED** — the weld is in `cells.group_wrapped`, BEFORE any row-header derivation, and it is decided by coordinate noise. Graincorp-stem's row pitch is uniform (every body gap 6.48 pt, so `lead`, the median, is 6.48 too), and the seven at-pitch partial lines (a data row whose month column the author left blank: 15 of 17 columns, no hrule between data rows inside a port group) have gaps within 2.3e-5 pt of the median — 3 fall under it by that noise and weld, 4 fall over it and stay rows (`scripts/at_pitch_weld_probe.py`). The B3 spec (`specs/2026-07-22-b3-wrap-continuation-procedural-design.md` § 3) predicted this class, deferred a distribution-aware split *until a real jittery document demonstrates the need*, and claimed the round-trip backstop would escalate a mis-merge: the first is now demonstrated and the second is **refuted** — the fused rows pass tiling and reach the grounded graph. Spike (disclosed constant, `round(·, 2)` on both sides, NOT shippable): 6 of 7 corpus graphs hash-identical, graincorp-stem p1 73→77 leaf rows, p2 65→68, repeated-token cells 56→0, exact-refused 48→24 (the 24 left are the 18 scheme gaps + 6 subtotal rows), score unchanged 0.9659. Detector: `tests/etkl/test_wrap_continuation.py::test_at_pitch_partial_line_under_coordinate_noise_is_still_not_merged` (strict xfail). Closes when `gap < lead` at equality is decided by a derivation, not noise — a spec in a fresh session; candidates in the handoff § 5a  **SPEC WRITTEN 2026-09-10** (loop `r208-the-tightest-certain-boundary`, `specs/2026-09-10-the-tightest-certain-boundary-design.md`): the handoff's `pitch_min` prediction was RUN over every corpus band and **held** — a candidate continues iff its gap is tighter than the minimum gap over the band's *certain pairs* (consecutive lines that fail the structural test or are hrule-vetoed: row boundaries by construction); 13 of 13 graincorp at-pitch candidates refused (p0 2, p1 7, p2 6), the four B3 pins unchanged, the strict-xfail detector passes; corpus-wide 30 candidates in 4 documents, the 2 genuine wraps still merge, 0 lie in the interval the new rule refuses and the old accepted; 3 bfs bands with no certain pair fall back to `lead`, byte-identical. The "round to the source's stated precision" alternative is **REFUTED**: graincorp's `Tm` ty operands are stated to 6 decimals (`90.879997`, `82.080002`) and the at-pitch disagreement is ~2e-5 pt, above that resolution. § 8 class PROCEDURAL (a derived minimum, no constant), inherited from B3. Closes when spec § 5's O1-O4 are green on a merged branch: handoff `2026-09-10-the-tightest-certain-boundary-handoff.md` § 5a  **CLOSED 2026-09-10** (loop `r208-the-boundary-implemented`, handoff `docs/superpowers/2026-09-10-r208-closed-tightest-boundary-handoff.md`). Spec § 5 executed with no separate plan: `group_wrapped` enumerates the band's certain pairs once (raw consecutive pairs that fail the structural test or are hrule-vetoed) and gates the wrap on `gap < tightest_row_gap`, their minimum, falling back to `lead` when the band certifies nothing; PROCEDURAL, a derived minimum, no constant. RED before GREEN: with the strict xfail removed and T1-T3 added, the detector + T2 + T3 fail on the old gate and 8/8 pass on the new; O4 run — gate reverted in place, exactly those three fail, T1 and the four B3 pins pass. O1: graincorp p1 band 1, 72 certain pairs, `tightest_row_gap` 6.479965, 7 candidates, welded 3 → **0**. O2: `corpus_verdict_snapshot.py` at `f905445` vs this branch — six hashes unchanged, graincorp-stem `737cf651cd9c`/30909 → **`19ef4cf500b0`/32359, the spike's own hash**, so the derived rule reproduces the disclosed-constant spike's graph byte for byte; `tab:LeafRow` p0 57, p1 **77**, p2 **68**; score 0.9659 unchanged. O3: `placement_population.py` exact-refused 48 → **24** (the 18 scheme gaps + 6 subtotal rows, no joined value left); `cellText` values whose first and last token repeat 56 → **0** of 2209. 52 related unit tests pass. Not closed with it: the § 4(b) noise-floor residual (one order lower, no corpus document reaches it) and the refuse-when-nothing-certified arm (spec § 6) |
| ~~R201~~ (61/190 closed) | **bfs page 6 is ONE table split into six bands, and the REAL header is on the page** — band 2 carries `Grandes régions | Total | 0-19 ans | 20-39 ans | 40-64 ans | 65-79 ans | 80 ans ou plus | Rapport de | Rapport de`, and bands 4, 5, 6, 8 and 9 each assert their own first row (a regional subtotal: `Région lémanique 1 736 124 365 836 …`) as their column header. That is **5 of the 11 corpus false headers and 40 of the 73 numeric label cells, on one page** — and unlike apple p2 band 6 ([[R166]]) the correct header is present a few bands above, which makes this the instance a cross-band remedy should be designed against | Measured 2026-09-10 at `4905355` with `scripts/header_row_census.py` (the label/row1 pairs) and `pdfplumber.extract_text` on p6, which shows the six bands as one continuous table under one header. Spec `docs/superpowers/specs/2026-09-10-the-header-is-assumed-design.md` § 2 rows 5-10 | Deferred as a DESIGN FORK with no arm chosen, after the obvious arm was RUN and REFUTED in the same loop: `scripts/forced_carriage_spike.py corpus/gov-stats/bfs-population-bilan-2023.pdf 6 2 4` at `4905355` gives band 2 `header_reading=None` and `carried_roles_for(...) -> None`, so the forced compile moves nothing (`baseline=276 forced=276`). **The reason generalises past what the apple spike recorded**: a `CarriedHeaderReading` is minted at exactly ONE site, `src/iladub/etkl/ruledroles.py:549`, inside the hierarchical assert path — the whole record-table branch mints none, matrix or flat, so the carriage seam was never available to either half of [[R166]]. [[R165]]'s band-run machinery does not reach these bands either (`is_matrix_candidate` → `classify_matrix`, `scripts/band_run_census.py:85-95`). Also measured on the same run and not yet accounted for: **band 3, the table's grand-total row `Total 8 962 258 …`, is `ignored` outright (0 cells), as is band 7 (`Zurich …`)** | Bands 4/5/6/8/9 read under band 2's header — or escalated — by one of three arms, none costed: (1) escalate the record-table branch where the label row carries no evidence; (2) merge the page's bands and re-enter classification from the top; (3) extend the carriage seam so a record-table assertion can mint a reading, which is what the refutation above opens ✎ **CLOSED 2026-09-12** (`b2bdbbe`): the spec's § 5 closure condition is met — bands 4/5/6/8/9 read under band 2's header. 222 → 267 entries (+9 cells on each of the five bands); the document score is **unchanged** at 0.40331491712707185 because the token ledger did not move (`(asserted, escalated) == (276, 25)`, per-band tokens 36/54/36/72/63) — what changed is which labels the tokens hang under, which the score cannot see. Six of seven corpus documents are byte-identical before/after. Band 2's own body is NOT repaired (its 6 cells are unchanged) and [[R166]]'s six donor-less bands are untouched. Pinned by `test_bfs_p6_reads_267_entries_under_band_2s_labels`. Exposure → [[R216]], [[R217]] |
| ~~R203~~ (61/193 closed) | **The donor's own header row is assumed, exactly as [[R166]] says every record-table header row is.** Grid donation (spec `docs/superpowers/specs/2026-09-10-the-grid-the-author-drew-design.md` § 4) reads a continuation band under the *donor's* line-0 labels. Nothing in the relation's three clauses — tiling, same column count, wholly-drawn boundaries — is evidence that the donor's line 0 is a header rather than its first data row; that is still `src/iladub/etkl/classifygraph.py:53` naming `band.lines[0]` `tab:HeaderWord` before any query runs. A wrong donor propagates a wrong header to every band beneath it, which is strictly worse than today's per-band error | Measured 2026-09-10 at `93fa7ce`: `scripts/grid_agreement_census.py` finds a unique donor for 5 bands, all bfs p6 band 2, and [[R166]]'s spec § 2 row 5 reads band 2's line 0 as the TRUE header — a READING, made from the printed rows and `pdfplumber.extract_text`, not a derivation. Related and measured in the same run: band 2's own lines 1-3 are the header's WRAPPED continuation (`Cantons` / `dépendance` / `dépendance des`) asserted as a data row, so the donor's body is false even where its labels are true | Deferred because the relation is sound on this corpus and the loop that measured it ships no behaviour: donation is specified, not built, so there is nothing yet for a wrong donor to corrupt. Raised because it is the ONE clause a donation plan cannot leave unexamined — it is the difference between fixing 5 bands and mis-labelling 5 bands | A derivation of the donor's header row (the open question [[R166]] § 3 left, now asked of ONE band per page instead of every band), or a demonstration that a donor whose line 0 is data cannot pass the tiling membrane **MEASURED 2026-09-10 (`scripts/grid_donation_spike.py`, handoff `2026-09-10-the-donated-reading-tiles-handoff.md` § 2): the membrane arm is REFUTED by construction** — band 2's line 1 (`Cantons / dépendance / dépendance des`, 3 words) donated as the header of the 9-column reading `region_tiles` on all 5 bands, identically to the true header, because `tab:hasLabel` is bound by exactly one shape (`DerivedRowGroupShape`, `tab-shapes.ttl:337`) and none on `tab:HeaderNode`. Only the derivation arm remains; a candidate is the page datagrid's `HeterogeneousColumn/every-measure` refusal, which names band 2's line 0 on bfs p6 (handoff § 5c, PROPOSED) ✎ **MEASURED CORPUS-WIDE 2026-09-11** (`scripts/donor_header_criterion.py`, `docs/superpowers/2026-09-11-the-donor-header-is-derived.md` § 1): the page-datagrid criterion agrees with the R166 census on 12 of 12 readings — the one true header (bfs p6 band 2, page line 3) is refused `HeterogeneousColumn/every-measure`, all 11 false label rows are body rows. The CONVERSE is refuted (titles and rows of a second table are refused too), so the derivation is one-directional: donate iff the donor's line 0 is refused `every-measure`. One donor in the corpus exercises it. The derivation arm now exists; the row closes when the donation plan ships it ✎ **CLOSED 2026-09-12** (`b2bdbbe`): shipped as the plan's DECISION C. The licence is a fact on the donor node whose value is the datagrid's own published refusal string, in the lexical form `emit_data_grid` writes into `dec:rejectedBecause` (`datagrid.py:610`) — not a constant of this loop's choosing — and it is emitted only where the band's line 0 re-identifies UNIQUELY BY INK ([[R202]]). Falsified three ways: deleting the triple pattern admits an unlicensed donor; deleting the term's declaration fails the declaration membrane by IRI; deleting the drawn clause admits a donor whose vector carries a compiler-inferred gutter. Its membrane arm was struck 2026-09-10 and stays struck. **One-sided evidence, recorded rather than glossed:** the licence fires on exactly ONE donor corpus-wide → [[R217]] |
| ~~R212~~ (63/202 closed) | **An ignored `NON_TABLE` band's text reaches no triple.** graincorp-capacity's title (`ELEVATION CAPACITY TABLE`), print line (`As At Tuesday, 4 August 2026`), issuer (`GrainCorp Operations Ltd …`) and footer (`GrainCorp advise that the tonnages shown are indicative only and are subject to change.`) are all absent from the compiled graph. The `NON_TABLE` branch of `compile_tables` (`src/iladub/etkl/compile.py:805-815`) writes unit markers when a band carries them, then a `RegionReport`, and nothing that carries the band's text. That is a §5 loss (context discarded) and a §6 gap. The maintainer's 2026-09-11 ruling licenses the capacity measure's name from exactly these two lines, and the licence has no carrier the portal could cite | Measured 2026-09-11 on `0ca29b0`: `compile_document("corpus/ag-trade/graincorp-capacity-2026-08-04.pdf").graph` → 5710 triples, and a literal-substring search over every object for `ELEVATION`, `CAPACITY TABLE`, `tonnages`, `indicative`, `GrainCorp Operations` and `As At` → 0 hits each (spec `specs/2026-09-11-the-span-the-author-drew-design.md` § 2.1). How many ignored bands corpus-wide carry text is NOT measured | Deferred because the spec read the ruling as *the contract author names the field*, which needs no carrier (spec § 3.3) — ✎ **REFUTED AT REVIEW 2026-09-12: the maintainer ruled the MACHINE derives the measure's name, so that ground for deferring is gone and this row is a prerequisite of etkl:02's measures** (spec § 0, § 3.3, § 5 step 2). Loop C's `tab:RegionCaption` furniture carries lines inside a table's header region only, and whether ignored bands should reuse it is a separate design question | A census of ignored bands with text over the corpus, then a provenanced carrier for their lines, with a test that graincorp-capacity's title and footer are in the graph and a falsification arm that removes the emitter. A prerequisite of the spec's alternative reading (§ 6, first bullet) if the maintainer rules the machine must consume the licence — ✎ **they so ruled on 2026-09-12, so it is a prerequisite outright**. Raised by [[R211]]'s spec ✎ **MEASURED 2026-09-12 on `a3ff34b`** (evidence `2026-09-12-r212-invariant-arm-evidence.md`): the carrier's load-bearing premise is no longer a prediction — a throwaway emitter over the whole corpus left all 7 documents byte-identical on score, on each page's `(asserted, escalated)` ledger and on every per-region verdict, while all 7 canonical graph hashes moved (§ 1). Triple deltas are +3 per ignored band and reproduce the § 2.1 census independently. Three corrections a plan must take from the evidence rather than the spec: the NON_TABLE branch is at `compile.py:814-824` (this row's `:805-815` is stale); `document.licence_evidence` is refused as the producer on four independent legs — wrong population, pair-scoped, `recognized` is `[]` on graincorp-capacity itself, and its graph is discarded by `is_licensed` (§ 2); and spec § 5 arm 1 cannot be built as worded, since `compile_tables` derives its own bands from a path and no test monkeypatches `page_bands` — the `reportlab` route works and is the RED state, 0 hits in 420 triples (§ 3) ✎ **PLAN 2026-09-12** (`plans/2026-09-12-the-ignored-band-carries-its-text.md`, handoff `2026-09-12-the-plan-is-written-handoff.md`): the plan takes the five decisions the spec and the evidence left open. The term is `etkl:IgnoredBand` + `etkl:bandText` + `etkl:bandIndex`, with the classifier's reason carried **iff** an enumeration of `regions.py`'s `NON_TABLE` return sites shows it total — measured by the implementer, not assumed here. The subject is `{doc}#ignored{idx}`, deliberately DISTINCT from the `{doc}#region{idx}` the same branch already mints as a unit-marker hanger (`compile.py:819-820`), so a ruled ignored band does not fuse two vocabularies onto one node. `_band_text` moves to `bands.py` and becomes public, and **four** references follow it, not the two the evidence named: `document.py:476` and `:480` are prose mentions inside `licence_evidence`'s docstring family, measured this session. **The shape is NOT wired into the compile membrane**, on a measurement the evidence had not taken: `_validate` runs only when the page graph already holds a `tab:RecordTable`/`tab:HierarchicalTable` (`compile.py:1441-1445`), so a furniture-only page — the case with the most ignored bands — never reaches a shape at all, and wiring would buy enforcement that looks total while being absent exactly where it matters. Enforcement is instead the vocabulary convention plus a test validating a REAL compiled page graph against `etkl-shapes.ttl`. Consequence: the previous handoff's § 5b prediction (a wired carrier shape costs zero vacuity-registry rows) is left **untested by design**, not confirmed. This row closes on the corpus arm's output (plan Task 4 Step 3), never on a green CI ✎ **CLOSED 2026-09-12** (branch `r212-the-ignored-band-carries`, evidence `docs/superpowers/2026-09-12-the-ignored-band-carries-its-text-evidence.md`). The carrier ships: `etkl:IgnoredBand` + `etkl:bandText` + `etkl:bandIndex` + `etkl:ignoredBecause`, minted by `holon.emit_ignored_band` at one call in the NON_TABLE branch of `compile_tables`, on the subject `{doc}#ignored{idx}` kept disjoint from the unit-marker hanger. **The corpus arm is what closes this row, not a green CI** (spec § 5 arm 5): `compile_document("corpus/ag-trade/graincorp-capacity-2026-08-04.pdf")` now carries all four bands verbatim — `GrainCorp Operations Ltd ABN 52003875401`, `ELEVATION CAPACITY TABLE\nAs At Tuesday, 4 August 2026`, the page's real header, and `GrainCorp advise that the tonnages shown are indicative only and are subject to change. 1` — each with its band index, the classifier's own reason and a page through `iladub:fromRegion / iladub:onPage`. Every probe this row measured at **0 hits in 5710 triples** now hits: 5710 → 5746, which is 4 bands × 9 triples. **The invariant is re-confirmed on the SHIPPED emitter**, not only the throwaway: two-tree corpus snapshot, 7/7 scores SAME, 7/7 per-page `(asserted, escalated)` ledgers SAME, 7/7 per-region verdict sets SAME, 7/7 document-level decision sets SAME, 7/7 canonical graph hashes MOVED, +1314 triples over 146 ignored bands at a **uniform 9.00 per band**. The 146 reproduces the spec § 2.1 census independently — same total and all seven per-document figures — which is the proof spec § 6 owed for its band-ordering replication. The shape is deliberately NOT wired into the compile membrane (a page of pure furniture never reaches `_validate` at all); enforcement is the vocabulary convention plus a test validating a REAL compiled page graph, with a focus-node census pinned beside it. Raised [[R220]] |
