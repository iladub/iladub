# Handoff — the R97–R101 loop (unstarted)

**Date:** 2026-08-17
**Branch:** `main`, HEAD `09449e4`, in sync with `origin/main`, tree clean
**Written at:** ~78K tokens, 1.6× the 50K originating floor — which is why this is a handoff
and not a spec. Nothing below was designed; it is the reading that a fresh session should not
have to repeat.

**AMENDED 2026-08-17 at `4d5f569` (HEAD has moved on: `f48be87`, `09b96a8`, `4d5f569`).**
Two changes, both after the original was written — see § Amendments at the end for what they
change about the reading. **R101 was added to this loop on the maintainer's instruction**, so
the title and Goal below now say R97–R101.

## Goal

Adjudicate the four residues R87 raised about its own membrane — three shapes that are wired
but say nothing (**R97**, **R98**, **R99**), and one that says something on one leg and
nothing on the other while the guard cannot express the difference (**R100**) — together with
**R101**, the same failure arriving through CI rather than through SHACL.

They are one loop because they share a single question — *is a check that says nothing a gap
in the corpus, or a check that should not exist?* — and, if the loop takes R101's closing
condition seriously, a single answer shape: a **registry of expected silence**, with a row per
silent check and a guard that fails when one starts speaking.

**R100 and R101 are the odd ones**: both change a guard rather than adjudicate a shape.
Decide in the spec whether they belong here, and say why in either direction.

## Where the primaries are

| primary | what to establish there |
| --- | --- |
| `docs/superpowers/residues-open.md:77-81` | **The five rows in full. Read these before anything else** — the index line is a pointer and this repo has already paid for treating one as the residue. Each row carries its own measurement, its own "why deferred", and its own closing condition. `:81` is R101, added after this handoff was first written. |
| `docs/superpowers/residues-open.md:81` (R101) | The CI half. Its closing column proposes **the `VACUITY_REGISTRY` pattern applied to skips instead of shapes**, and names the symmetry with R97–R100 as the reason. It also carries one prohibition: **do not close it by adding `demo` to the CI install line.** |
| `tests/etkl/test_vacuity_registry.py` | The guard. `VACUITY_REGISTRY` at `:87`; the six rows this loop is about at `:95` (`NoLeakShape`), `:105`, `:108`, `:111` (`LicenceRefusalShape`), `:121`, `:124`. The module docstring records the deviation from the plan's criterion 2 and the measurement that forced it. |
| same file, `:325`–`:360` | The four guard tests. **`test_no_registered_shape_has_gone_live` (`:337`) is the coupling that makes this loop non-trivial** — see § Unverified. |
| same file, `:435` (`test_t5_4_...`) | Criterion 2 pinned on a real shape. It asserts 20 focus nodes in the fixture, not the corpus's 11 — do not conflate the two figures. |
| `docs/superpowers/specs/2026-08-13-escalation-is-a-decision-design.md` | §3's **M7 table** (`:110`) is the survey these residues correct, and **§4.5** (`:216`) is the registry's design and its seed. §4.5 says explicitly that R87 does **not** adjudicate corpus-gap vs dead-shape. |
| the same spec, §3 M7 | **R98 CORRECTS it.** M7 gives "corpus does not exercise it" as `tab:LicenceRefusalShape`'s reason; the measurement in R98's row shows who-wfa *does* refuse a licence. A spec on `main` is wrong on this point today. |
| `src/iladub/etkl/document.py:1246-1253` | Why `tab:licenceRefused` is withheld, and the inline comment stating the asymmetry is intended. R98 turns on this. |
| `src/iladub/etkl/compile.py:1083` | The page-scope `_validate` call. R100's two legs are this one and `document.py`'s. |
| `src/iladub/etkl/membrane.py:19` | `engine_name()`. The R87 loop's figures are all the **rudof** leg — see § Unverified. |
| `540e608` (PR #105), commits `1d3ed83`..`c7c08ec` | The loop that raised all four. Each commit message is its task report. |

## What was decided, and where each is recorded

Almost nothing — that is the point of the loop. What is settled:

1. **R87 deliberately did not adjudicate these.** Recorded in the spec's §4.5 and in each
   residue row's "why deferred" column. This loop is not revisiting a judgment; it is making
   one for the first time.
2. **The registry is keyed by SHAPE, and measures the 7 document graphs.** Recorded in
   `test_vacuity_registry.py` and in `d1e5ab1`. R100 is the proposal to change that key.
3. **R98's behaviour is correct as written** — the withheld edge is intended. Recorded in
   `document.py:1246-1253` and in R98's row. Only the *spec's stated reason* is wrong.
4. **R98 must not be closed by writing the edge unconditionally.** Recorded in R98's row's
   closing column, with the reason: it would assert a fact naming a table URI that does not
   exist. This is the one prohibition the loop inherits.
5. **`iladub:asserted` is written on the grounding path, not the compile path.** Recorded in
   R99's row. R99 is therefore a membrane-*composition* question, not a shape defect.
6. **R101 is in this loop.** Decided 2026-08-17 by the maintainer, in conversation.
   **Recorded nowhere but this file** — which makes it reversible, not settled. What is *not*
   decided is whether it shares the answer with R97–R100 or merely the diagnosis; that is the
   spec's call, and the loop is allowed to conclude they are two loops after all.

## Unverified or assumed

* **Whether R97–R99 are closable at all in one loop is unknown, and it is the first thing the
  spec must confront.** All three close on *specimens the corpus does not contain* or on a
  decision to delete. If the answer for all four `tab:` shapes is "we need documents we do not
  have", the loop's real deliverable is a deletion decision plus a corpus-acquisition residue —
  which is a legitimate outcome, but a different loop from the one the title implies. **Do not
  discover this halfway through.**
* **The guard bites back on any close that makes a shape live.** `test_no_registered_shape_has_gone_live`
  (`:337`) FAILS while a now-live shape still has its registry row. Every closing change is
  therefore two edits — the shape and its row — and the guard will catch you if you forget the
  second. Named in R97's row. Verify the exact failure mode before writing the plan; the
  assertion is at `:345`.
* **The pySHACL leg has never run against `540e608`, let alone `09449e4`.** It was green once at
  `d1e5ab1`'s tree, which differs only in docs. Every figure in R87 and in these four rows is
  the **rudof** leg (`membrane.py:19`; corrected in `e3073e5`, and earlier commits on that
  branch say the reverse). Running the other leg before designing is cheap and would either
  confirm the four counts or produce the loop's first finding.
* **R100's cost is asserted, not measured.** Its row says keying by (shape, leg) "doubles every
  row and changes the seed the plan specifies". Nobody has counted what that actually costs, and
  its own row names the cheaper alternative — measure whether *any* other wired shape's liveness
  differs by leg. If none does, the (shape, leg) key is unnecessary and R100 closes without a
  guard change. **Measure that before designing the key.**
* **The four `tab:` shapes of R97 are treated as one row in the index and four in the registry.**
  Whether they adjudicate together or separately is undecided. `tab:BaseFactShape` in particular
  now has a consumer it did not have when R97 was raised — `09449e4` landed the 1NF export leg
  and `tests/test_zero_etl_export.py` exercises base facts end to end. **Nobody has checked
  whether that test's graph gives `tab:BaseFactShape` a focus node**; if it does, R97 is already
  one-quarter closed and the registry row is stale. This is the single cheapest measurement in
  this handoff and it was not run.
* **Scope creep risk, named:** R99 opens "which membrane should this shape be on", which is a
  composition question with no natural boundary. R87's own spec declined to open it. If the spec
  takes R99, it must state where the composition question stops.

## Constraints that bind this loop

Not assumptions — repo rules, listed because they change the shape of the answer:

* **The neurosymbolic gate (CLAUDE.md §8).** A shape is an AXIOM/constraint artefact; any
  adjudication that reaches for Python to decide liveness is a defect. The guard is already
  SPARQL-over-graphs and should stay that way.
* **Plan-authoring discipline (CLAUDE.md, rules 1–5).** Rule 5 especially: a plan-supplied test
  must be reconciled against the spec's "what is NOT done" section. This loop is *made* of
  things a previous spec scoped out, so that reconciliation is not a formality here.
* **A loop is a session.** The spec goes in a fresh one.

## The next concrete action

**Measure whether `tests/test_zero_etl_export.py`'s compiled graph gives `tab:BaseFactShape` a
focus node.** One run, in a fresh session, before any spec exists. It is the only thing in this
handoff that can change the loop's shape — if the export leg already exercises base facts, R97
is partly a stale row rather than a corpus gap, and the loop starts from a different question.

Then write the spec, with the first section answering: *are R97–R99 adjudicable without new
corpus documents, and do R100 and R101 belong in this loop?*

## Amendments (2026-08-17, after the original)

Both landed between the original writing and `4d5f569`. Neither invalidates anything above;
both change what the first measurement means.

1. **R101 was raised and added to the loop** — `4d5f569`, row at `residues-open.md:81`. It is
   the CI-side instance of this loop's own failure: a green tick at 1103 passed / 120 skipped
   while an entire test module never ran, because a module-level `importorskip` reports **one**
   skip for the whole module however many tests it holds. A check wired and silent, reported as
   health — R97–R100 in a different notation.
2. **The export leg now actually runs in CI** — `09b96a8` moved `pandas`/`pyarrow` from the
   `demo` extra to `etkl`; run `31998847175` went 1103 → **1106 passed**, 120 → **119 skipped**.
   **This sharpens § Unverified's `tab:BaseFactShape` question**, which was written when those
   tests were skipping: the graph that might give the shape a focus node is now built on every
   CI run, so the measurement is both easier to take and more consequential if it comes back
   positive.

One caution for whoever runs measurement #2's arithmetic again: `09b96a8`'s commit message
predicted `120 → 117` on the reasoning that three skipped tests would become three passes. That
prediction was **wrong and is preserved uncorrected in the public history** — the module
collapses to a single skip entry, so the true signature is `+3 passed / −1 skipped`. Verified
with a three-test scratch module behind a bogus `importorskip`, which reports `1 skipped`.
