# Open residues — detail

Full text of every OPEN row: what it is, where it was measured, why it was deferred, and what would close it.

**This file is DETAIL. The index — and the conventions — live in [`residues.md`](residues.md). Find a row by its `R<n>` id.**

| # | Residue | Measured | Why deferred | What would close it |
| --- | --- | --- | --- | --- |
| R3 | **Nested-subset vote** in `recover_leaf_grid` | Loop D spec §2 Finding 3: the correct 15-column grid IS found by the longest suffix, then outvoted 35-to-16 by shorter suffixes that are nested subsets of it, not independent witnesses | Changes grid derivation for every table in the suite | A statistic that respects nesting, plus a fixture battery. **Ruled documents now route around this; the first ruleless document with a narrow gutter will hit it** |
| R5 | **Proposal inputs not recorded** in `emit_row_role_promotion` | Loop C.1 final review | Not a regression; best done alongside a live run | Record the context the proposer was shown, so an auditor can reproduce the proposal |
| R6 | **Centre-only merge candidate** for wide parents — a parent spanning cols 1–3 gets one candidate | Loop C.1 final review | Pre-existing in shape; needs a live run to judge | Report every covered column's candidate |
| R7 | **Live BAML path unreachable** — `BamlRowRoleProposer` is never constructed in `src/` | Loop C.1 final review | No live run attempted yet | Wire it behind `baml_proposer_available()` |
| R8 | **`ProposeHeaderSpan` missing** from `baml_src/` — B1.3's live path cannot run | Loop C | Pre-existing, unrelated to the loops since | Author the function |
| R9 | **Conservation shape unreachable** through the row-role driver | Loop C final review: no reachable role vector loses text | Sound as a regression backstop; covered directly by `test_conservation_shape.py` | Either a reading that genuinely loses text, or accept it as a backstop and say so |
| R10 | **`detect_bands` cuts one line too high** — the report date lands inside the table band | Loop C.1 §2 Finding 4: date x 398.3 ≈ page title x 399.0, matching no column | Segmentation is shared by every path; large blast radius | Title-block exclusion at segmentation **Interaction with loop G attempt 2 (latent):** a title/date line left inside the band above the header/body split contributes chars to the header-glyph evidence and could spuriously confirm a candidate; witness clauses are interval-bounded so out-of-band ink cannot, and no measured document reaches it today. |
| R11 | **Mixed header rows** cannot be expressed by a per-row role | Loop C §3.2 | No measured document exhibits one | Per-cell roles, when evidence demands it |
| R12 | **Split-table recurrence** — a solitary parent over a repeated layout | Loop C.1 §6.2; `stem.pdf` is a single page with no repeated block | No target document | Cross-block layout matching |
| R14 | **The collapse can delete an author-drawn boundary** when only a skipped line carried that column's ink | Loop D final review, pinned by `test_skipped_line_ink_loses_an_author_drawn_boundary_KNOWN_LOSS`: rule at x=90.0 discarded, two real columns merged, reported at confidence 1.0 with no escalation | Depends on R10 (a caption left in the band is what forces the suffix skip) | Close R10, or make the occupancy test span the whole band rather than the accepted suffix |
| R15 | **NEURAL residual: a wide label over blank/unclaimed columns** where the covering partition genuinely cannot decide | Loop B §7.4 / Loop C — still escalates honestly `MERGE_AMBIGUOUS` | No measured document needs it | The `span_proposer` seam, if a real document ever requires it |
| R16 | **The UNRULED path keeps the split-number defect** — pdfplumber's `extract_words` splits `2` from `0,000` on its own | Loop F: only `rule_aware_lines` (the ruled path) was fixed | Closing it means owning word segmentation for every document, not just ruled ones | Apply the same two-presence-test rule to a char-derived word segmenter for unruled bands |
| R18 | **Groups without a confirmed total stay ungrouped — and an unconfirmed total POLLUTES the next group** | Loop I (2026-07-30), measured on the real document: `Geelong Total` (Aug) and `Portland Total` (Sep) refused because the unconfirmed totals above them (Port Kembla's blank total; a non-confirming Fisherman Islands total) stay ordinary rows, flow into the confirmed total's member walk, and break key uniqueness (conflicting port values → honest refusal). Also: a boxed source cell yields the faithful-but-doubled key `Mackay Mackay`. **Final review (M-3), a SECOND, distinct doubled-key mechanism**: nested groups whose key comes from the SAME source cell (an inner `Mackay Total` and an outer total over the same port, both `hasLabel`-pointing at that one `EntryCell`) double the key in the record PATH instead — `Mackay > Mackay > h0-r0` — even when the cell's own text is not itself doubled. Not covered by the boxed-cell case above | Refusal is the correct §7 behavior — the alternative (guessing group boundaries) is exactly what the loop forbids; the cascade is inherent to H-confirmed-only evidence. **Also (re-review m-3):** co-resident level-0 groups over the same rows (a month with its single port, admitted by the C-1 overlap exemption) reach the feed's deepest-cover tie-break, which keeps ONE key and silently drops the other (a §5 context loss, better than the pre-fix region loss but undocumented); the nested-same-cell case passes the membrane (source-faithful) and is merely noisy, not incorrect; **loop K's key INJECTION has the same first-wins tie-break** — two same-column groups with different key cells over one row inject only the first (mirror of the path-level drop; unreachable through derive_row_groups today) | Ditto-fill evidence (rowheaders' blank-below convention) cross-checked against H membership where both exist would recover the refused groups; a per-boxed-line key read would clean the doubled key. A path-dedup rule (collapse an immediate repeated segment, e.g. `X > X` → `X`) would clean the nested-same-cell case. All deferred until a document *needs* them |
| R20 | **Unconstrained contract fields stay propositions** — vessels, exporters, dates, times, slot references carry no verifiable constraint in the stem contract, so all their values quarantine as `CandidateConcept`s (323 of 460 on the real document; incl. the season keys, whose real header `GC Fin Year` matches no declared field) | Loop K capstone (2026-07-30), measured: 137 grounded / 323 proposed; every grounded node behind exactly one PromotionDecision; the grain scheme correctly refused non-grain cargo (Woodchip ×6, Cement ×3) and boxed doubled values ('Accepted Accepted', 'Sorghum Sorghum') | BY DESIGN (§7: assert only what the contract can verify — credibility over completeness); a richer contract is the contract author's decision, not the compiler's | Declare more verifiable fields (date patterns, a vessel registry scheme, a fin-year pattern field) in the contract when a consumer needs them grounded; the propositions are never dropped and remain promotable. Also (loop K final review M7): nothing marks an INJECTED value as inherited-from-group rather than read-at-the-row — honest and detectable via the source-cell provenance, but a consumer comparing region row-coordinates should know |
| R23 | **`dg:AssertionInNavShape` unreachable from live extractor output** — `classify()` assigns "assertion" only when `path in nav`, so its violation can never fire on real facts (a stray `docs/*.md` surfaces as classless instead) | Phase-1 final review: shape passes only via a synthetic fixture; spec §3 defines Assertion as nav + `docs/narrative/`, and the narrative half is not a location rule | Behavior is safe (stray files still caught, by a less precise message); fixing changes classification semantics | Implement `docs/narrative/` as a location rule; then the shape becomes live for narrative files missing from the nav |
| R24 | **A wiki page citing an untracked/gitignored path is permanently exempt from staleness with a green membrane** — `SourceExistsShape` checks on-disk existence only; untracked → no `dg:lastCommitDate` → no staleness edge. Same integrity risk: `residues.md` is Evidence-classed but deliberately mutable — the first wiki page listing it in `sources:` hard-fails the lint on every loop that appends a row | Phase-1 final review, measured on the shipped shapes + extractor | No wiki page currently cites an untracked path or residues.md; zero live impact today | Add a `dg:tracked` fact + shape (cited sources must be tracked), and either carve residues.md out of evidence-staleness or forbid it in `sources:` |
| R25 | **The ≥2-references wiki admission rule is unenforced** — index membership is now linted (dg:WikiIndexMembershipShape, Phase 3), but whether a concept EARNS a page (≥2 evidence docs, or evidence + code) remains judgment | Phase-1 final review; Phase 3 Task 1 shipped index-membership lint | Phase 3 Task 1 implemented linting; ≥2-references is a judgment call | Decide whether admission is lintable (citation-count query over the fact graph) or stays judgment; document the decision in the governance spec's successor |
| R26 | **The release gate fails OPEN on a same-day or backdated contradiction** — `?d > ?since` at day granularity: a contradiction spec dated the same calendar day as the previous release tag, or filename-dated before it but landing after, never blocks any release | Phase-2 final review (2026-08-01), proven against the live query: same-day fixture → `[]`, backdated-filename fixture → `[]`; silent and permanent because draining is inferred from date ordering, not a fact | First implementation; correct in the common case; first release has no prior tag (fallback 2026-07-31 covers all dated docs); RELEASE.md step 2 now tells the releaser to eyeball same-day contradictions | Phase 3: an explicit drain fact (e.g. `dg:drainedBy <release>` set at promotion) or full-timestamp comparison instead of filename dates |
| R27 | **`_IMPACT` scans the whole file unanchored** — a spec omitting its `Doc impact:` header but quoting the phrase in prose/fences gets a bogus declaration (near-miss measured: the Phase-2 plan carries `"Doc impact: contradiction\n"` in a code fence, saved only by its valid line-11 header winning first); the module comment "invalid declaration emits no fact" also misdescribes the invalid-then-valid case | Phase-2 final review (2026-08-01), measured on `tests/docgov_extract.py:95` | No live doc trips it; anchoring changes membrane semantics and deserves its own tests | Anchor the regex to line start (`^` + MULTILINE) or the doc header region, fix the module comment, and add the invalid-precedes-valid fixture (folds in the Task-1 deferred minor) |
| R28 | **The promotion queue is one-shot** — `FILTER NOT EXISTS { ?page dg:promotedTo ?target }` removes a page from the queue forever at first promotion; later increments to an already-promoted wiki page (or the un-promoted remainder of a partially-promoted catalog) can never re-enter | Phase-3 final review (2026-08-01): neurosymbolic-exemplars promotes 4 exemplars of a larger catalog; its future updates will never queue again | First release drains one page; the visible-lag model still works via staleness warnings; a re-queue design (compare `dg:updated` against a promotion date) belongs with R26's drain-fact work | Add a `dg:promotedAt` date at promotion; queue = unpromoted OR updated-after-promotedAt; design together with R26's `dg:drainedBy` |
| R30 | **The parent-vs-wrap-fragment residual fires INSIDE the engaged header block, not outside it** — a short merged parent below the block rule, left-aligned on the leaf label's own origin, welds onto the leaf (`'Arr Tonnes'` asserted 0.9195 on a page where BASE escalated); irreducible by ruled geometry because every cell in a left-aligned column shares that origin BY CONSTRUCTION, and reading label TEXT is forbidden (R4). Also registered here: CENTRE-aligned wrapped headers inside an engaged block are an honest miss — their lines share neither edge, `_shares_origin` refuses, the row derives `level`, and the derivation silently abstains rather than asserting wrong | Loop L Task 2, round-2 re-review, measured on the live stem; the module docstring originally (and wrongly) stated this residual sits *outside* clause 0's engagement context — corrected at loop close (round-2 re-review) to say it fires inside an engaged block | It is a reading judgment ("does `'Arr Tonnes'` read as one column name?"), not a geometry gap — the ruled-evidence twin of loop C's borderless merged-parent/wrap-fragment split | Named disposition (§8): NEURAL propose → tiling-oracle dispose, reusing loop C's rowrole slice pattern (`emit_row_role_promotion`) |
| R31 | **A double-drawn header-block border disengages the law** — two hairlines ~0.6pt apart, drawn by the author as one visual border, read as two separate rules, so clause 0's exactly-one-rule precondition fails and the row abstains to BASE even though a human reads a single border | Not yet measured on a shipped document (loop L Task 2 adversarial review; reasoned from the vertical side's known identical artifact) | Conservative by construction — abstaining to BASE is the safe default (§7: never assert wrong), so this is a coverage gap, not a wrong assertion | A presence test — no header ink between the twin rules, i.e. they bound the same empty band — never a distance/tolerance threshold; `grid._rule_boundaries` already documents the identical double-rule artifact on the vertical side, so the fix should reuse that precedent |
| R32 | **Loop L amplifies a pre-existing Loop G defect: header-confirmed boundary refinement can fabricate a column boundary the author never drew** — a boundary opened at a space inside banner/spanner ink gets confirmed as if it were an author-drawn rule; clause 1 of the header-stack law (1:1 leaf-to-column alignment) then evaluates against that fabricated grid and may pass on it | Pre-existing since Loop G attempt 2 (R13's mechanism); loop L's module docstring (`ruledroles.py` KNOWN RESIDUES) names it directly as a consequence the new law inherits, not one it introduces | Contained today only by clause-0's narrowed engagement (fewer rows reach clause 1 at all) plus leaf-refusal (P3) — a scope reduction, not a fix | Loop G's gutter-confirmation needs to distinguish author-drawn rules from ink-confirmed gutters — make banner/spanner ink ineligible to confirm a boundary |
| R34 | **The carried-header MATCHING law is Python control flow, not an AXIOM — and its `carried_header_roles` entry point is public and unguarded** — `ruledroles.carried_roles_for` answers "which carried row does this page-N row repeat?", which IS a decision, with a greedy in-order scan; the law it implements is fully declarative (an in-order counterpart, exact per-column text identity, leaf-is-the-leaf, and a skip admissible only when the skipped row's confirmed role is `furniture`) and its AXIOM twin already exists in this very loop as `continuation-of.rq`'s clause shape over the same presence/equality evidence. Second face of the same residue: `compile.compile_tables(carried_header_roles=…)` is a PUBLIC parameter, and nothing in the signature enforces that the reading was licensed by continuation recognition — an external caller can assert one table's header reading over another table's rows | Loop M Task 3 review, 2026-08-02. (a) The law: the reviewer read the scan as the declarative clause set it is. (b) The API surface: the reviewer traced the in-repo call graph and confirmed the invariant HOLDS today — `document.compile_document` is the only producer and keys the map by the band the AXIOM recognized — so this is an unguarded surface, not a live defect. (c) The one place the procedural form actually bit was found by reproduction, not inspection: the skip rule was unconditional on role, so a page redrawing the leaf but omitting a WRAP row was accepted and the chained tables carried different labels for the same column ('Total Grain Tonnes' vs 'Grain Tonnes') with no oracle catching it — FIXED in code this round (role-conditioned skip) and pinned by `tests/etkl/test_document.py::test_carriage_refuses_a_skipped_continuation_row` | The fix closes the measured defect, not the class: the law stays procedural, so the NEXT clause added to it is written in Python again, with no evidence graph to audit and no refusal the reviewer can read as a query. Registering rather than converting now, because the conversion is a loop-sized change (an evidence emitter, a query, and its negative battery), and task 3's slice is the carriage itself | **A `repeats-row.rq` derivation** (the way R30 names its NEURAL disposition): emit the two blocks' rows as presence/equality facts — the SAME shape `continuation_evidence_from_facts` already emits for leaf cells, extended to every header row and carrying each carried row's confirmed role — and derive the counterpart relation declaratively, with the in-order, identity, leaf, and inert-skip conditions as clauses. Then make the API surface follow it: the licence becomes a fact IN the evidence (the recognition the AXIOM licensed), so a caller cannot hand in an unlicensed reading without the derivation refusing it — closing (b) structurally instead of by docstring. THIRD face, registered by Task 3's re-review and consumed by Task 4's chain-walk: recognition's evidence stops at the LEAF row, so a licensed chain can span tables whose header blocks differ ABOVE it — any consumer of the chain must therefore never assume label identity across a link (Task 4 reads each table's fields from its OWN header path for exactly this reason), and the refused-carriage signal (`carried_roles_for` returning None on a recognized pair) exists and is DISCARDED today; recording that refusal on the `tab:continuesTable` link is the in-kind closure, since it is precisely the evidence that a linked pair's readings did not correspond. **FOURTH FACE — the in-kind closure is now REAL, but for a DIFFERENT refusal (Loop O, 2026-08-03):** the LICENCE's refusal is recorded rather than discarded — `DocumentReport.refused_licences`, the graph fact `cur tab:licenceRefused prev`, and `tab:LicenceRefusalShape` (the two verdicts exclusive over one pair) — which is exactly the shape this row prescribes, applied to the licence instead of the carriage. So the pattern is demonstrated in-repo and the third face's remedy is no longer hypothetical; the CARRIAGE's own refusal is still discarded, so that face is open. Face (b) is narrowed but not structurally closed: in-repo, carriage now happens only inside the LICENSED arm of `compile_document` (licence *and* recognition, where before it was recognition alone), yet `compile_tables(carried_header_roles=…)` remains a public parameter whose signature still enforces nothing |
| R36 | **The feed's pass-3 identity guarantee is enforced on the `row_id` STRING, not the minted record subject** — `table_records`' Pass 3 (`feed.py`) settles any id two rows still share by comparing `rid` strings and appending the colliding row's own IRI; `_record_uri` then slugs that (possibly-disambiguated) string into a URI (`[^A-Za-z0-9._-]+` -> `_`) AFTER settlement has already decided the ids were distinct, so two `rid` strings differing only in a character the slug regex erases can still mint the SAME subject | Loop M Task 4 re-review, 2026-08-03: a forged graph with two document scopes whose ids differ only by a slug-erased character measured **2 distinct ids -> 1 subject**. UNREACHABLE from `compile_document` today — its `page_doc_uri` qualifies every id with `/p{n}`, and digits survive slugging, so no document compiled through the shipped driver can reach it | Not a live defect on any measured document (the only reachable ids are digit-qualified); fixing pass 3 to compare post-slug strings changes the minted id shape for every already-passing fixture and deserves its own regression pass, not a loop-M-close edit | Key the residual-collision check on `str(_record_uri(rid))` instead of the raw `rid`, or settle collisions with an ordinal component instead of a slug-erasable one (digits survive slugging) |
| R37 | **The document-level arithmetic pass's retraction semantics are a MODELLING CHOICE, not a derived fact** — reading a chain's members as ONE logical group spanning the page break (rather than independent per-page running totals that genuinely restart) is what the pass always prefers; nothing in the graph or an oracle disposes between the two readings, the arithmetic simply prefers the wider window. **NARROWED (Loop O, 2026-08-03): the choice is now only ever exercised inside a LICENSED continuation.** The licence (R33) gates the document-level window, so a recognized-but-unlicensed pair never forms one at all — measured on the marked case-3 fixture, `arithmetic=()` after the gate where the same fixture produced `groups_superseded=1` before it. What is NOT narrowed: within a licensed chain the two readings are still undisposed — licensing establishes that the furniture around the CUT is page-invariant, which says nothing about whether the author restarts a total per page | Loop N Task 2 review, 2026-08-03: demonstrated concretely by the constructed retraction case (`page_local_group_two_page_pdf`, `test_retracted_witness_takes_its_derived_group`) — a page-local SUB=250 confirmation with its own derived group is refused once the document-level walk-back reaches 500. Counted per chain in `DocumentReport` (`ChainArithmetic.retracted`/`.groups_retracted`); measured INERT on the real stem (0 retractions across p0/p1/p2, ledger 41/62/0/21) | Defensible and counted, but no evidence in the graph or an oracle currently decides which authoring convention (one logical total cut by pagination vs. a genuine per-page restart) applies; not needed to close R35 on the shipped stem, where the wider-window reading is measurably correct | A discriminator (semantic, never a tuned constant) distinguishing "page cut mid-group" from "author genuinely restarts the total per page" — most likely riding the same continuation-recognition evidence R33's body-side discriminator needs for case-2/case-3, or an explicit modelling declaration on the contract. **That evidence now EXISTS** (loop O's licence, `vocab/queries/continuation-licence.rq`) **and is measurably not sufficient on its own**: the stem licenses both pairs and its wider-window reading is still a preference no oracle disposed — so the discriminator this row needs is a further one, not a re-use |
| R38 | **`feed._header_path` DOES read a `tab:DerivedRowGroup`'s `tab:headerLevel`** (the max-selector at `feed.py:290`, picking the deepest header covering a row) — demonstrated on a constructed graph (level 1 → `"INNER"`, level 0 → `"OUTER"`). The harmful state this exposes — a SURVIVING group whose PARENT was retracted, left with a now-too-deep level — was registered by Task 2 as "not proven unreachable," not as closed | Loop N Task 2 round-2 re-review, 2026-08-03, disproved Task 2's own first claim ("nothing reads the level") by direct demonstration; the corrected docstring states no consumer before Task 3 depended on the value being exact (`RowNoOverlapShape` exempts every derived-group pair). Loop N Task 3 subsequently found the state MOOT for every path reachable today: `_retract_orphaned_groups` fires only inside `reconcile_chain_arithmetic` (chains only), and Task 3's supersession replaces EVERY page-derived group in a chain wholesale rather than leaving any survivor (`test_the_re_derivation_leaves_no_stale_level_or_member`) — so no group in a chain ever "survives" a retraction anymore; there is nowhere left for the harmful state to occur through the shipped call path | Closed for the only reachable path today (chains, post-Task-3 full rebuild); not proven unreachable in general — a future change letting page-derived groups partially survive a chain's arithmetic pass without a full rebuild would reopen it, and non-chain group mutation paths were not separately audited | Either a structural invariant/test asserting no code path can retract a group's witness without also invalidating or recomputing every group's `tab:headerLevel` in the same holon, or re-affirm at each future touch of `_retract_orphaned_groups`/`_supersede_page_groups` that full-rebuild-on-touch still holds |
| R39 | **`vocab/queries/row-group-nesting.rq` (loop I's AXIOM, unchanged) is now the dominant cost in a chained document's compile** — Task 3 made the closure holon the LOGICAL TABLE, tripling the group count per holon (18–20 per page → 57 on the head), and the query is a triple-nested self-join (child × parent × the `?mid` no-intermediate guard) whose cost grows superlinearly in groups per holon | Loop N Task 3 (2026-08-03): 97.0 s over the stem's 57 head-attached groups (55 parent links; coincidentally equal to, and unrelated to, R35's 57 cross-page aggregates edges — independent measurements); independently reconfirmed in the fix round at 92.9 s (`progress.md`). Whole-document compile ~180–202 s; whole-graph SHACL validation (Task 4) an additional ~41–47 s on top | The query itself is loop I's shipped AXIOM, correct and untouched; rewriting it needs its own differential-equivalence proof (per the derivation-scaling playbook), which is a loop-sized effort, not a task-3/4 edit | Apply `docs/superpowers/specs/2026-07-20-derivation-scaling-design.md`'s playbook — replace the `NOT EXISTS` pair-scan containment/no-intermediate guards with a `GROUP BY` over member-set cardinalities, proved equivalent by the same committed-differential method `row-group-key-logical.rq` used this loop (62 stem calls + fixture calls + a seeded randomized battery) |
| R40 | **The document's outermost total (`Grand Total`) reads as an unconfirmed, ungrouped row** — not a new bug, R4-family's documented zero-member walk-back refusal: the row immediately above it (`2026/27 Total`) is an already-confirmed SAME-LEVEL aggregation, so the walk-back collects ZERO members and the candidate is never confirmed, even though its printed value is EXACTLY the sum of the chain's non-aggregation rows | Loop N Task 3 review, 2026-08-03, corrected after a measurement error (an earlier naive `Decimal()` parse wrongly claimed disagreement): re-measured with `rows._numeric_token_sum`, the 132 non-aggregation rows other than the Grand Total itself sum to exactly 2,402,500 (927,500 + 804,000 + 671,000 across p0/p1/p2) — the printed Grand Total, to the cent. This is the ONE record among the stem's 132 (excluding the Grand Total row) whose id is a bare page-scoped discriminator with no group label at all (`p2 htable1-r64`) — the single opaque id in the otherwise one-kind identity space R35's closure measured | Loop-H's honest refusal (R4), unchanged and correct — the alternative (confirming a same-level-adjacent zero-member total) is exactly the false-positive direction R4 already refuses to guess | Nothing to close — named here so the one remaining opaque id in an otherwise-uniform identity space is not rediscovered as a defect; would only become open if a document's grand total ever needed grouping context beyond bare identity |
| R43 | **ONS gov-stats document: 1× `REGION_TILING_FAILED` plus an unnamed oddity — an `UNSUPPORTED_TABLE` kind carrying verdict `asserted`** — score 0.4419 over 9 pages; one printed region tuple is `('UNSUPPORTED_TABLE', 'asserted', None)`, which is worth naming because an UNSUPPORTED kind is not expected to carry an `asserted` verdict at all | Corpus battery, 2026-08-04, `test_expected_verdict[gov-stats/ons-index-of-services-2026-02.pdf]` (measured outcome PASS as `Unadjudicated`; `battery-run-final.log` line 27 carries the full printed region-verdict list) | Not fixed in this loop; both the tiling failure and the `UNSUPPORTED_TABLE`/`asserted` combination are generalization-gap observations for a future reading loop to characterize, not fix here | Its own reading loop: first characterize whether `UNSUPPORTED_TABLE` + `asserted` is a benign classification quirk or a genuine verdict-semantics gap (worth its own investigation before touching tiling), then address the `REGION_TILING_FAILED` region |
| R44 | **BFS gov-stats document: 2× `KIND_NOT_SUPPORTED`, 2× `REGION_TILING_FAILED`, 5× `ROUND_TRIP_FAIL`** — score 0.3438 over 7 pages, chains `[1,1,1,1,1,1,1]` (no continuation recognized across any page pair); 8 `RECORD_TABLE` regions do assert | Corpus battery, 2026-08-04, `test_expected_verdict[gov-stats/bfs-population-bilan-2023.pdf]` (measured outcome PASS as `Unadjudicated`; `battery-run-final.log` line 23 carries the full printed region-verdict list) | Not fixed in this loop; three distinct escalation reasons on one document is a broad generalization-gap surface better characterized by a dedicated reading loop than patched piecemeal here | Its own reading loop: triage the three escalation reasons separately (kind support, tiling, round-trip) against the document's actual layout before deciding which, if any, share a root cause |
| R46 | **Fetcher hardening — `_pdf_facts` crashes uncaught on non-PDF bytes at BOTH call sites** (first-fetch of a rotted URL serving HTML, and the present-but-unpinned re-derivation path added by the final-review fix wave), and a missing PDF Producer prints `cor:producer "None"` in the pin block | Task-1 task review + final-review fix-wave re-review, 2026-08-04 (code inspection of `scripts/fetch_corpus.py` — no battery crash observed; all seven seed fetches were real PDFs) | First-fetch-only, human-in-the-loop paths; the crash is visible and honest, the exit-nonzero-until-pinned contract holds either way; all current corpus content is PDF | Wrap `_pdf_facts` in a guard returning a clean `"failed"` (file kept for inspection) + `producer or ""` in the pin print + a non-PDF-bytes unit test; fold into the next fetcher-touching loop |
| R47 | **Both the grid-region peel and the hrule-box weld are scoped LEADING-only — a trailing full-width strip is neither peeled nor welded.** The peel is scoped, after fix round 2, to a LEADING run only (`gridregion.peel_leading_captions`: `captions = lines[:min(gset)]`, everything from `min(gset)` on — interior AND trailing non-grid lines alike — stays in the band, unpeeled). Final review F1 (2026-08-04) found the weld side was NOT leading-scoped: `weld_hrule_boxes` welded EVERY full-width hrule box, so a trailing total line below the grid's closing rule — which extends the band's y-range past that rule, pulling it into the ruled sub-band's hrules — formed a (header-bottom, grid-bottom) box spanning every data row and welded them all into one fabricated row (reproduced pre-fix at RECORD_TABLE score 1.0 by the reviewer's probe). Fixed by scoping the weld licence to the leading (topmost) full-width box only, matching the peel. A below-grid total line inside the same band as the grid (CBH's per-section totals) now reads exactly as main reads it today for BOTH mechanisms: neither peeled into a caption, nor welded into an adjacent data row — just an ordinary trailing band line | Loop P fix round 2, 2026-08-04: the original (Task-2) peel classified EVERY non-grid line as a caption, including trailing lines — this swallowed page-local subtotal/total rows in loop H/N's fixtures (`test_continuation_licence.py::test_marked_case3_does_not_stitch`, `test_logical_arithmetic.py::test_retracted_witness_takes_its_derived_group`), a measured regression against main (68ae0c2), fixed by narrowing the peel to a leading prefix. Final review F1, 2026-08-04: the weld side of the same leading-only licence was measured NOT to hold — pinned as a regression by `tests/etkl/test_grid_region.py::test_weld_scope_leading_box_only_trailing_total_not_welded` (`sectioned_ruled_table_pdf(trailing_total=True)`, the reviewer's probe shape), RED before the fix (three data rows welded into one), GREEN after (weld restricted to box index 0). CBH's per-section totals (measured: total `374,904` at y≈210 vs. the section's rule bottom 199.6) remain the real specimen, currently in a *separate* `detect_bands` band from the grid — this row's own PEEL/WELD gap was, at the time this row was written, still unexercised by any real document, only by the new synthetic fixture pin. **Loop Q Task 7 update (2026-08-04, `tests/test_cbh_e2e.py` probe):** the adjacent-but-distinct total-CONFIRMATION mechanism loop Q Task 4 built (`document._confirm_section_total`, spec §4.1 — reads a table's own LAST row below its closing rule as a total candidate, a different mechanism than this row's peel/weld) WAS run against the real CBH document and confirmed **zero** `tab:SectionTotal` facts (`rep.notes == ()` too — no candidate was even found, not merely a non-matching one) — loop Q shipped an adjacent oracle without closing this row's gap; the real specimen's below-grid content is still read as an ordinary, unattributed trailing band line | Reading/peeling trailing strips (CBH's below-box totals) was never loop P's scope (spec §3 is strips ABOVE the grid, R42 gap (a), now CLOSED); leaving trailing lines alone is the conservative, main-preserving default until a loop actually needs to read them | A trailing-strip reading/carry mechanism (peel or otherwise) for below-grid full-width lines, plus a trailing-strip fixture pinning the desired behavior. **Stale pointer struck (Task 7):** "natural home is loop Q" no longer holds forward — loop Q ran (2026-08-04), closed R42, and shipped Task 4's adjacent total-confirmation oracle (see R50) without building this row's peel/weld; the natural home is now a dedicated follow-up loop |
| R48 | **`sectiongraph._leading_box_y`'s header-box-candidate selection can, on a document shape the interior-crossing test does not discriminate, locate a false header box and emit a coincidentally-matching signature.** The counterexample this row originally flagged as hypothetical ARRIVED: the real CBH specimen draws additional full-width strip separators ABOVE the true header box (heading, then a separate notices strip, then the header), so box-0-only selection (the original fallback: "first two distinct drawn hrule y's") picked the NOTICES box — wrong, its text differs per section — and `section_candidates` silently returned no group on the real page (measured post-Task-4, controller probe, 2026-08-04). THIS instance is CLOSED by fix round 2: selection is now "the FIRST candidate box (strict full-width first, then the X-extent-free y-only pairs, both now enumerated in full rather than box-0-only) whose y-range an ink-interior rule's [top,bottom] genuinely (strictly) overlaps" — verified end-to-end on the real specimen (`section_candidates` now returns `((1, 3, 5, 7),)`, band 9 correctly abstains). The residual class stays open, narrower than before: a document whose NOTICES (or any other non-header full-width strip) is itself crossed by an ink-interior vertical rule — a shape where interior-crossing no longer discriminates the header box from an impostor — is still unmeasured and would still mis-select | Loop Q Task 2 fix round 2 (task review + controller probe), 2026-08-04: the original hypothetical materialized on `corpus/ag-trade/cbh-stem-2026-08-03.pdf` page 0 (band 9's distinct rule-x signature, and its `headerBoxText` abstain, show the fix's disposal working as designed — a differently-shaped section correctly fails to join the group, not because of a bug but because it IS a different section, still under investigation separately); the residual class (a non-header strip crossed by an interior rule) has not been measured on any corpus document | The specific mechanism this row named (box-0-only, ignoring geometry) is fixed; the residual (interior-crossing ambiguous between two strips) needs a document that actually draws interior rules crossing more than one full-width box, which no measured document does | A counterexample document where more than one candidate box is interior-crossed (so the FIRST-such-box tie-break can be wrong), OR promoting box-candidate disposal fully into `section-repeat.rq` (emit every interior-crossed candidate as a fact, let the compound-match derivation pick among them instead of this function's first-wins order) |
| R49 | **Spec §4.2's GRAPH-level key form (`tab:DerivedRowGroup` covering a section's rows, `tab:hasLabel` → the source heading cell, `prov:wasDerivedFrom` → the confirming total row) was never built — loop Q Task 5 implemented only the FEED-level positional half** — records carry the section key positionally in `row_id` (`"GERALDTON > p0 table0-r1"`, `feed.table_records`) and as injected candidate `SurfaceConcept`s (`feed._inject_section_captions`, `is_section_marker=True`), but no `tab:DerivedRowGroup`/`tab:hasLabel`/`prov:wasDerivedFrom` triples are ever emitted for a section's key — §4.2's "attribution never waits for naming" half is implemented; the graph-level derived-group half is not | Loop Q Task 5 review, 2026-08-04: the brief ("Key attribution — captions as candidate markers, injected into records") was read and implemented as the feed-level positional form only; post-implementation grep of `src/iladub/etkl/` and `src/iladub/feed.py` for `DerivedRowGroup` emission tied to `tab:RegionCaption`/section repair found none | Nothing downstream consumes the graph form yet: Task 6's naming cascade reads the FEED's injected candidate `SurfaceConcept`s directly (`concept.value`/`is_section_marker`), and loop N's existing document-level row-group derivation already covers the chain's ARITHMETIC side (subtotal-confirmed groups) — a graph-level key group would duplicate identity information the positional form already carries, for no consumer | Emit the `tab:DerivedRowGroup` + `tab:hasLabel` (→ the heading/caption cell) + `prov:wasDerivedFrom` (→ the confirming `tab:SectionTotal` row) structure WHEN a future consumer needs the key as a graph-addressable node rather than a `row_id` string — or an adjudicated decision (François) that the positional form suffices permanently and this row closes without code |
| R50 | **`document._confirm_section_total`'s totals oracle reads only the table's LAST row as the total candidate — several STACKED trailing lines below a section's closing rule (a subtotal followed by a running total, a note line, etc.) are unmeasured and would defeat the oracle by construction**, since only the single last row is ever tested against loop H's `detect_aggregation_rows` confirmation | Loop Q Task 4 review, 2026-08-04 (`.superpowers/sdd/2026-08-04-loop-q-section-repair/task-4-report.md` §6, "Residue candidates for the loop close": *"the totals oracle judges only the table's LAST row — several stacked trailing lines below the closing rule are unmeasured"*); reconfirmed by Task 7's real-document probe (2026-08-04): CBH's own per-section totals produced **zero** `tab:SectionTotal` facts end to end, consistent with (though not solely explained by) this single-row scope | Not fixed in this loop; the fixture battery (`with_totals=True`) only ever draws ONE trailing total line, so the stacked case was never exercised even synthetically — narrower scope was the honest ship, not an oversight | A fixture with two or more stacked trailing lines below a section's closing rule, plus generalizing `_confirm_section_total` to walk back from the FIRST non-grid line after the closing rule (loop H's own aggregation-row walk-back idiom) rather than assuming the printed total is always the table's literal last row |
| R51 | **`document._band_subgraph`'s subject-collection licence leans on the `_index_suffix` URI-prefix minting convention holding everywhere a new subject is minted, in either direction** — it collects every subject reachable under the adopted table's own URI or its `{table}-` prefix; this is the SAME convention `_index_suffix` documents for minting, read backward as an extraction rule, so any future minting code that names a subject WITHOUT that prefix (or reuses a foreign prefix that happens to collide) would silently drop facts from the adopted subgraph or pull in ones that do not belong to the table, with no membrane check to catch either | Loop Q Task 4 review, 2026-08-04 (`.superpowers/sdd/2026-08-04-loop-q-section-repair/task-4-report.md` §6: *"`_band_subgraph` leans on the URI-prefix minting convention (same licence `_index_suffix` documents)"*) — a documented coupling between two functions in `src/iladub/etkl/document.py`, not a measured failure on any corpus document (all current minting sites follow the convention) | Not fixed in this loop; every minting site measured today follows `_index_suffix`'s convention, so nothing is currently wrong — the residue is the COUPLING itself, unenforced by any test or shape | A unit test asserting `_band_subgraph`'s prefix scan and `_index_suffix`'s minting agree on a synthetic subject naming scheme, OR promoting the prefix convention into a single shared helper both functions call so the licence can never drift apart silently |
| R52 | **Class-level documentation debt: `tab:DetectedAggregationRow` (loop H), `tab:continuesTable` (loop M), and `tab:SectionTotal` (loop Q) ship with NO dedicated conformant/negative example `.ttl` pair under `examples/`** — CLAUDE.md's house rule ("Every vocabulary/shape ships with a worked example that conforms and a negative test that must fail") is met for these three terms only by their pytest fixtures (synthetic PDFs compiled in-test), never by a standalone `.ttl` pair a reader could open and validate independently of the compiler | Task 7 grep, 2026-08-04: `grep -rl "DetectedAggregationRow\|continuesTable\|SectionTotal" examples/ vocab/` returns ONLY `vocab/` files (`vocab/queries/row-group-key.rq`, `vocab/queries/continuation-of.rq`, `vocab/queries/row-group-key-logical.rq`, `vocab/shapes/tab-shapes.ttl`, `vocab/ontology/tab.ttl`) — zero hits under `examples/`; `SectionTotal`'s own task report independently named the gap for itself (task-4-report.md §6: *"`tab:SectionTotal` ships without a dedicated conformant/negative example .ttl pair (the pytest positive/negative pair plays that role)"*), and the same grep shows the two OLDER terms (loops H, M) carry the identical debt, unregistered until now | Spans three loops (H, M, Q) each of which shipped its term with pytest coverage judged sufficient at the time; consolidating into one row rather than three, since the fix (one example pair per term, or one combined worked example touching all three) is the same shape of work regardless of which loop is credited | One `.ttl` example (conforming) + one negative fixture per term — or a single combined worked example under `examples/` exercising a logical table with a confirmed aggregation row, a continuation link, and a section total together, since all three co-occur on the real CBH/GrainCorp documents anyway |
| R53 | **`iladub:GroundedNodeShape` validates `groundsTo`'s PRESENCE only (`sh:minCount 1`), never its RESOLUTION — a `GroundedNode` whose `groundsTo` IRI resolves to no real ontology/contract term still conforms to the membrane** | Loop Q Task 6 review reproduction, 2026-08-04 (F1, CRITICAL — reproduced exactly as stated: `["Berth: 12A", "Berth: 7B"]` against `cbh-contract.ttl`, which has no `berth` field, asserted `groundsTo = urn:iladub:dimension-name/berth` and `vocab/shapes/iladub-shapes.ttl`'s `GroundedNodeShape` passed it). **The leak ITSELF is FIXED**: `splitkey.py`'s arm 1 (`resolve_split_key_name`, `src/iladub/splitkey.py`) now quarantines an explicit-but-unverified key name instead of asserting it (arm `explicit-unverified`), pinned by `tests/test_split_key_naming.py::test_explicit_unverified_key_quarantines`. What stays open is the MEMBRANE GAP itself: nothing in the shipped SHACL stops a *different* future caller (a hand-rolled assertion path that skips `splitkey.py`'s own check) from reintroducing the identical fabricated-target class of bug, because `GroundedNodeShape` still has no way to refuse it | Task 6 fix-round-1 report (`.superpowers/sdd/2026-08-04-loop-q-section-repair/task-6-report.md`, "Residue for Task 7's register": *"`iladub:GroundedNodeShape` validates only `groundsTo`'s presence... nothing in the shipped SHACL membrane refuses a `GroundedNode` whose `groundsTo` IRI resolves to no real ontology/contract term"*) | The one reproducing call site is fixed at the CALLER (arm 1's own check); closing the shape-level gap needs a resolution constraint that can be evaluated against an arbitrary supplied contract/terms graph at validation time, which `sh:minCount` alone cannot express — deferred rather than rushed into a SHACL-SPARQL constraint untested against every existing `groundsTo` target across the repo (contract fields, SKOS schemes, HGA alignment IRIs) | A SHACL-SPARQL (`sh:sparql`) constraint on `GroundedNodeShape` requiring `groundsTo` to resolve to a term declared as `rdf:type`-something (or `rdfs:domain`/`rdfs:range`-participating) in the contract or terms graph supplied to the validating run — plus a regression fixture pinning the F1 reproduction case failing conformance at the SHAPE level, not just at the caller |
| R54 | **`feed.table_records`'s identity-prefix and the loop-Q E2E's cascade marker-set both assume the section HEADING is drawn ABOVE its notices — a positionally-FIRST-caption rule, unstated as an invariant.** `table_records` prefixes every row of a captioned table with that table's `tab:RegionCaption`s taken positionally-FIRST (`caps[0][0]`, `src/iladub/feed.py`'s `_logical_row_paths`/`table_records` body, computed from `_table_captions`'s row-then-node-ordered read) — true on CBH (the heading precedes its berth-notice lines, measured), but never checked. A document that draws a notice/disclaimer line ABOVE its section heading would (a) misattribute every row's record identity to the notice text instead of the true key, and (b) feed a corrupted (notice-first) value into the naming cascade's marker set (`tests/test_cbh_e2e.py`'s identity-prefix construction). **CLOSED for the BROADER class that actually materialized (fix round 2, 2026-08-04):** the counterexample was not a notice-before-heading document — it was the real GrainCorp STEM, where `_table_captions` was reading loop C's row-role reading-furniture captions (`rowrole.emit_reading_evidence`'s plain `tab:RegionCaption`, e.g. a print-timestamp line, NEVER a section key) as if they were section-repair evidence at all, on a table loop Q never touched. Measured: `still-quarantined` drifted 1265→1341, record identities read `'Friday, 31 > 2025/26 > Jul 26 > Mackay'` (truth `'2025/26 > Jul 26 > Mackay > r9'`). Fixed by scoping `_table_captions` (and the `compile._emit_band_captions` emitter) to a new owned subclass `tab:SectionCaption` — a plain `tab:RegionCaption` is never read as key evidence, closing this instance structurally (any table with ONLY loop-C furniture captions now contributes zero candidates and zero prefix, pinned `tests/test_feed_section_keys.py::test_plain_region_caption_never_injects_or_prefixes` + a stem regression pin in `tests/test_corpus_stem.py::test_stem_document_grounds_full`) | Loop Q Task 7 review round 1, 2026-08-04 (code inspection — no corpus document then violated the ordering assumption); **Task-5 fix round 2, 2026-08-04 (controller close-battery + probe): the STEM measured the drift above** — a different mechanism (wrong-caption-SOURCE, not wrong-caption-ORDER) than this row originally hypothesized, closed by the `tab:SectionCaption` scoping | The scoping fix closes every case where the polluting caption comes from a DIFFERENT emitter (loop C) than section repair's own peel. It does NOT close the row's ORIGINAL, narrower hypothetical: within one REPAIRED section table, if the author drew a notice line above the true key heading, both would be genuine `tab:SectionCaption`s and `caps[0][0]` would still pick the notice — no measured document does this (every real specimen's key precedes its notices), so fixing blind remains the overfitting risk CLAUDE.md's "No overfitting" rule forbids, and the safety property still holds unchanged: the naming cascade's scheme-membership disposal degrades a polluted marker set HONESTLY (quarantine, never a false assertion) | The residual, narrower class: a counterexample document (a REPAIRED section whose own first `tab:SectionCaption` is a notice, not the key) to drive the fix with a real specimen, OR promoting marker selection from "positionally-first caption" to "the caption(s) that pass scheme-membership filtering" once such a document exists in the corpus |
| R56 | **The hierarchical `n == 0` ROUND_TRIP_FAIL branch does not carry absorbed unit markers** — every OTHER verdict path in `compile_tables` now emits `tab:UnitMarker` facts for a band whose currency-marker column was absorbed (the seven asserting branches by column, the nine `escalate_region` branches + `NON_TABLE` on the region node — loop-unitmarker's final-review C1 fix), but the hierarchical branch's `n == 0` case escalates from INSIDE `assert_hier_region` (it writes ROUND_TRIP_FAIL into its own scratch graph rather than calling `escalate_region`), and `_emit_unit_markers` there sits behind `if n:` — so a band that absorbed markers and then hits `n == 0` still drops the marker ink from both the graph and the token accounting | Loop-unitmarker final-review fix-wave re-review, 2026-08-06 (code inspection of `src/iladub/etkl/compile.py`'s hierarchical branch, ~lines 668-687): a gap of the SAME class as the fix wave's C1 finding, but PRE-EXISTING and outside C1's named branch list because this path never calls `escalate_region()`. No fixture or corpus document reaches it: it needs a band that both derives a marker column (borderless, same-symbol, same-row numeric neighbor) AND compiles hierarchically to a zero-cell round-trip failure | The C1 fix wave was scoped to the `escalate_region` call sites the finding enumerated; widening it into `assert_hier_region`'s internal escalation means either threading the band's markers into that function or hoisting its ROUND_TRIP_FAIL emission back into `compile_tables` — a restructure with its own regression surface, not a fix-wave edit, and unreachable today | Emit the marker facts on the same candidate URI `assert_hier_region` uses for its internal ROUND_TRIP_FAIL escalation (and add `_marker_word_count(band)` to `escalated_total` there), plus a fixture that genuinely reaches the branch with markers absorbed — or hoist that escalation to the caller so the one C1 pattern covers every path |
| R57 | **Membrane redundancy — 8.2 s of the final pass's 12.6 s re-checks shapes the region gate already checked** (gate subset 8.2 s, remaining eleven shapes 5.1 s, under pySHACL) | Loop-membrane spec §6, measured 2026-08-06 under pySHACL | At rudof speed the redundancy now recovers a negligible amount (§5's ~2.1 s per validation makes the ~0.06 s recoverable share not worth pursuing for performance); closing it needs a per-shape region-locality proof — a claim this repo has been bitten by before (loop M's duplicate `doc#table0`, R36's slug collisions) | A per-shape proof that every gate shape is region-local, then scope the final pass to non-gate shapes — or an adjudication that the two membranes' scopes (intra-region gate vs whole-graph final pass) stay as they are |
| R59 | **Real-document agreement is necessary but NOT sufficient evidence for an engine swap** — the differential battery over the corpus was green, yet the full suite then found a genuine engine disagreement on a SYNTHETIC fixture (`tests/etkl/test_row_groups.py`) that the battery's positive leg never exercised. **Widened at final review (2026-08-06):** the positive leg's gap is TWO-dimensional, not one — (i) corpus-only scope (no synthetic fixtures, the dimension that surfaced this exact disagreement) AND (ii) single-page scope (only `graincorp-stem-2026-07-31.pdf` page 0 is exercised, not "every corpus document's compiled page graphs" as spec §3.3 item 1 originally claimed — that claim was corrected in the spec at the same time this row was widened, since it described intent, not what shipped). **Further widened at the loop-subclass fix-wave final review (2026-08-06):** the new closure differential (`tests/etkl/test_closure_equiv.py`) inherits the IDENTICAL single-page gap on the IDENTICAL page — its own positive leg (`test_both_closures_agree_on_a_real_page_graph`) exercises only `graincorp-stem-2026-07-31.pdf` page 0, same as the engine battery. Named explicitly: **apple** (`corpus/financial/apple-fy2026q3-statements.pdf`) is the one corpus document whose verdicts demonstrably MOVED this same loop (spec 2026-08-06-subclass-only-closure-design.md §7: region p1#mtable4 flipped `MATRIX_AMBIGUOUS` → asserted, score 0.0105540897 → 0.0326975477), and neither battery's positive leg sees it | Loop-membrane close, measured 2026-08-06: root cause was ours, not rudof's — `rdfs_closure` used plain `owlrl.RDFS_Semantics`, whose `one_time_rules()` fabricates hidden literals by value-space unification (a cell's `tab:onPage "0"^^xsd:integer` gained a spurious `"0.0"^^xsd:decimal` twin from an unrelated `tab:y0`, tripping `EntryCellPhysicalShape`'s `sh:datatype`); pySHACL avoids this via its own `CustomRDFSSemantics` subclass that disables those rules (its docstring names the exact reason). Fixed in `f9ea992`, pinned by `test_rdfs_closure_does_not_fabricate_hidden_literals` | The fix closes the measured instance; the battery's positive leg still draws only from a single real corpus page, so the same class of value-coincidence could recur through a different synthetic fixture OR a different corpus page/document the battery does not see. Deliberately NOT widened in this fix wave — it would add several minutes to a battery that already runs ~4 minutes, and the fix wave's brief was to correct the spec's claim rather than build out coverage | Extend the battery's positive leg two ways: (1) the repo's synthetic fixture graphs (not just corpus documents), so a future engine or inference change is checked against the exact shape of graph that surfaced this disagreement; (2) every corpus document's compiled page graphs, not just the stem's page 0, matching what spec §3.3 item 1 originally (and incorrectly) claimed already shipped |
| R60 | **rudof's n-triples parse is now the membrane's dominant cost** — ~0.58 s on an 8k-triple page, now that RDFS closure has dropped to 0.047 s (R58) | Loop-subclass close, measured 2026-08-06: per-validation split with closure at 0.047 s and rudof's own validate at ~0.08 s leaves the n-triples parse as the largest single component | It is a Python↔Rust boundary cost, not a semantic one — closing it means either a smaller serialized payload or an in-process binding, neither of which this loop scoped | Measure whether rudof parses Turtle faster than N-Triples on our payloads (Turtle is ~3× smaller), or whether a future pyrudof exposes a graph-passing API that avoids serialization entirely |
| R61 | **Emitter-typing is now an unenforced invariant** — 45 `rdfs:domain`/`rdfs:range` rules in `vocab/ontology/tab.ttl` (28 domain, 17 range) land on classes shapes target (e.g. `hasCell rdfs:range tab:Cell`, `atColumn rdfs:range tab:LeafColumn`, `markerRegion rdfs:domain tab:UnitMarker`, `hasBBox rdfs:domain tab:Cell`); each was a path by which a node could enter a shape's target set without an explicit `rdf:type`, and all 45 are now inert for typing purposes (subclass-only closure, loop-subclass), so every shape's reach depends entirely on emitters typing every node explicitly — an invariant nothing in this codebase enforces | Fix-wave final review, 2026-08-06: the evidence that nothing verdict-relevant was lost is spec 2026-08-06-subclass-only-closure-design.md §1's table, measured on stem page 0 only | **The sharp part:** a lost typing that SHOULD have fired appears as a region flipping escalated→asserted — i.e. as a score IMPROVEMENT — exactly what apple looked like before diagnosis (spec §7: p1#mtable4 `MATRIX_AMBIGUOUS` → asserted). The corpus score gate alone cannot distinguish "a real fix" from "a shape silently stopped seeing a node it should still see"; only the closure differential (`tests/etkl/test_closure_equiv.py`) can, and per R59 it sees neither apple nor CBH — only stem page 0. Building the probe is out of scope for a fix wave | A corpus-wide invariant probe asserting that every node carrying a domain/range-implying property also carries the corresponding explicit `rdf:type`, with adjudicated exceptions (the two `iladub:CandidateConcept` escalation nodes this loop's own close-out found relying on domain typing: `mtable4-cc0_2`, `mtable4-cc1_2`) **PROBE BUILT AND RUN 2026-08-12 — R61 stays OPEN, but is no longer unmeasured.** `scripts/probe_emitter_typing.py` asserts the invariant over all 27 corpus pages, closing each node's explicit types under `rdfs:subClassOf` exactly as the membrane does. **Result: 5 violated rules, 56 violating nodes — of which 14 land on a class an `sh:sparql` shape targets, and those 14 are the live hazard.** They are 12x `range atColumn -> tab:LeafColumn` and 2x `domain onPage -> tab:Cell`, every one of them on the DATA-GRID path (`p7-datagrid`, `p7-datagrid-c2`, `p7-datagrid-c4`; ons p7/p8). The other 42 are inert for shape reach: 18x `range hasLabel -> tab:LabelCell` and 24x `domain x0/x1 -> tab:BBox`, whose classes no shape targets. **WHY IT STILL DOES NOT CLOSE, and why widening the exception list would be the wrong repair:** the 14 are not a forgotten `rdf:type`. `datagrid.emit_data_grid` mints `tab:GridColumn` nodes, points `tab:atColumn` at them (the ontology declares that property `rdfs:range tab:LeafColumn`) and hangs `tab:onPage` on the grid itself (declared `rdfs:domain tab:Cell`). Either the ontology is wrong about those two properties or the emitter is — a MODELLING decision, and the same `datagrid.py:635-636` emission that R92's close-out independently flagged for putting `tab:x0` on a non-`tab:BBox` subject. One emitter, two residues; decide it once. **Row correction:** this row says 45 rules (28 domain, 17 range). `tab.ttl` today carries 104 domain and 48 non-literal range (property, class) pairs, and carried comparable numbers before the closure shipped — so 45 was a filtered subset (rules landing on shape-target classes), never the total, and must not be quoted as one 🔎 **THE PROBE MEASURES 0 INSTANCES OF THIS ROW'S OWN INVARIANT (2026-08-18, `2026-08-18-probe-domain-range-agreement.md`).** Every violation was re-classified by asking WHICH graph supplies the missing type. 27 corpus pages, 74 violations, **UNTYPED: 0** — not one flagged node is missing an `rdf:type`. All 56 `tab.ttl` violations are DISAGREE: the node IS typed, just not as the rule says (`hasLabel -> LabelCell` on `Cell, EntryCell` ×18; `x0`/`x1 -> BBox` on `GridColumn, MeasureColumn` ×24; `atColumn -> LeafColumn` on `GridColumn` ×12; `onPage -> Cell` on `DataGrid, UniformGrid` ×2). So this row's headline — *emitter-typing is an unenforced invariant* — describes a failure mode the corpus has never exhibited, while the 14 live nodes are the emitter/ontology MODELLING disagreement this row's 2026-08-12 entry already identified. The instrument is renamed to match what it measures: `scripts/probe_emitter_typing.py` → **`scripts/probe_domain_range_agreement.py`**, now with a four-class report (UNTYPED / DISAGREE / ONT-VISIBLE / OUTSIDE-MEMBRANE) of which only the first two gate. **THE ROW STAYS OPEN, unchanged in substance:** live is 14 before and 14 after — the modelling decision (is the ontology wrong about `atColumn`/`onPage`, or is the emitter?) is still unmade, and until it is, the probe cannot be wired as a CI gate because it exits 1. Nothing runs it today: no test, no workflow. **Deferred here rather than as a new row:** the wiring is blocked on this row's own decision, not on anything separate |
| R62 | **Apple page 0 is unlocked by one band, not compiled** — after loop-quantity fixed band 4's transpose/homogeneity defect, one `MATRIX_AMBIGUOUS` and four `REGION_TILING_FAILED` regions remain on page 0; page score **0.0000 → 0.1170**, whole-document score **0.0326975477 → 0.0606860158** | Loop-quantity Task 1, 2026-08-06, measured live via `compile_tables`/`compile_document` on `corpus/financial/apple-fy2026q3-statements.pdf` | Deliberately out of scope, named before the loop started (design spec §7: "this loop unlocks one band, not the document"); the remaining regions are separate reading-judgment gaps (a tiling-failure class plus one matrix-ambiguity class), each its own generalization-gap surface per the corpus-battery convention (cf. R43–R45) | Its own reading loop(s): characterize the `MATRIX_AMBIGUOUS` region and the four `REGION_TILING_FAILED` regions against apple page 0's actual layout before deciding whether they share a root cause or need separate treatment |
| R63 | **`unit-marker-column.rq`'s OWN-COLUMN purity check treats only `tab:Blank` as a wildcard, not `tab:ParenthesizedNumber`** — deliberately asymmetric with the datatype-homogeneity queries (which now treat both as non-mismatching, via family normalisation and abstention respectively); registered so the asymmetry reads as a ruling, not an oversight | Ruled by François during loop-quantity Task 2, 2026-08-06 | The OWN-COLUMN purity check asks a different question than the five homogeneity queries: "is anything else present in this column?", not "what type is this column?" `tab:Blank` means absent — a genuine wildcard; a parenthesized number is a present, real value, so its presence genuinely makes a marker column impure. Treating it as a wildcard there would be wrong, not merely inconsistent | Nothing — the asymmetry is correct as designed. Would only become live if a future document showed a marker column whose only non-blank cells were parenthesized numbers and a human reader would still call the column pure; no measured document does |
| R64 | **Abstention (`tab:ParenthesizedNumber`/`tab:datatypeAbstains`) weakens the header-boundary scan** — an abstaining cell can no longer TERMINATE a multi-row header. A row-1 cell shaped `(2026)` or `(1)` previously mismatched as `tab:Text` against a Numeric body mode and forced `s_col = 2`; post-loop-quantity it abstains instead, `s_col = 1`, and a header row folds into the body | loop-quantity-typing final review, 2026-08-06 (reasoned from `header-body-split.rq`'s mismatch scan + `_cell_datatype`'s abstention rule; no measured document exhibits it) | This is the accepted `tab:Blank` semantics (an abstaining datatype never counts as a mismatch) extended to a strictly larger set (`tab:ParenthesizedNumber` joins `tab:Blank`), and it is the honest reading per §7 — not a bug to reflexively patch. It is, however, the ONE place the abstain rule can make `header-body-split.rq` MISS a boundary it used to catch, so it is registered rather than left implicit | Nothing yet — house-format registration. Would close by finding (or synthesizing) a document whose header row 1 carries a parenthesized-number-shaped cell (a year in parens, a footnote marker) immediately above a Numeric/Currency body, and confirming whether the folded-in header row breaks a downstream shape or is merely absorbed harmlessly |
| R65 | **`celltype._CURRENCY`'s `[\d,]+` admits a comma-only numeric body** (e.g. `"$,,,"`) — the identical looseness `_PAREN_NUMBER` had before this loop's fix (`\d[\d,]*`, requiring >=1 digit); `_CURRENCY` was deliberately left unfixed | loop-quantity-typing final review, 2026-08-06 (reviewer's regex read; no measured document exhibits a comma-only currency cell) | `_CURRENCY` is pre-existing (not owned by this loop) and has a wider blast radius — it feeds `tab:Currency`, which now also participates in the `tab:Quantity` family across all five homogeneity queries, so a change there is not purely cosmetic: a garbage currency-shaped cell that currently reads `tab:Text` (breaking a column's homogeneity) would instead read `tab:Currency`/`tab:Quantity` and STOP breaking a column it previously broke, changing verdicts, not just tightening a grammar. Out of scope for this fix wave | Fix `_CURRENCY` to `\d[\d,]*` (mirroring `_PAREN_NUMBER`'s fix) in its own loop, with the differential battery run against the four no-regression corpus documents first, since the change can move `tab:Currency` verdicts, unlike `_PAREN_NUMBER`'s fix (which only affects an abstaining datatype's own recognition, not what it normalises to) |
| R66 | **The reader's `optionSpace` is thin — `kind`'s three-value enum is the only judgement in the recorded chain with more than two options; every other judgement is a boolean dressed as a choice.** Apple region3's chain: `multi_table` chosen=single (single vs. — nothing else recorded), `kind` chosen=UNSUPPORTED_TABLE (the one three-way choice: RECORD_TABLE/UNSUPPORTED_TABLE/NON_TABLE), `matrix_candidate` chosen=not_matrix (matrix vs. not), `hierarchical` chosen=hierarchical (the terminal branch, no sibling recorded), `region_tiles` chosen=does_not_tile (tiles vs. does not), `verdict` chosen=escalated. Region4's chain shows the same shape: `transposed` chosen=upright (upright vs. transposed), `row_grouped` chosen=flat (flat vs. grouped). The record now makes this shape visible on real documents rather than argued in a spec | Spec `2026-08-07-reading-decision-record-design.md` §5 (the honest limit, predicted before measurement) and §6.1 (the close, the apple region3/region4 chains quoted verbatim above), 2026-08-07 | This is exactly the evidence spec §7 slice B (candidates as ontology classes) needs and was deliberately left out of scope for slice A — slice A's job was to make the option space queryable, not to widen it | Spec §7 slice B: replace the three-value `RegionKind` enum with a carried set of topology candidates named in `tab.ttl`, narrowed by observation rather than by an early Python branch — at which point judgements like `matrix_candidate`/`hierarchical`/`row_grouped` stop being separate boolean gates and become options within one wider space |
| R67 | **The recorded chain is complete at compile-path granularity, not at query granularity** — judgements made *inside* a called function (e.g. `classify`'s internal `classify-kind.rq` run, the header/body split's own AXIOM) are recorded only by that function's overall result, not by the SPARQL-level steps it took to reach it | Spec `2026-08-07-reading-decision-record-design.md` §3 design table (the recorder is glue that observes judgements already made, threaded through the compile path — not a probe inside every query) and §6.1's close, 2026-08-07 | Recording every internal query step would mean threading the recorder through every AXIOM query, not just every named compile-path judgement — a materially larger surface than this slice scoped, and not needed to answer the three §4 questions, which are all compile-path-level questions | Would close by deciding whether a finer-grained record is ever needed (e.g. to debug why a *specific* SPARQL query inside `classify` returned what it did) and, if so, threading the recorder one level deeper into that query's own evaluation |
| R68 | **No real corpus document exercises the transposed path — the R55 ordering (`looks_transposed` before `transpose_is_coherent`) is proven only on synthetic fixtures** | Narrowed this loop, 2026-08-07 — `tests/etkl/test_transposed_chain.py` runs the committed `judgement-order.rq`/`why-escalated.rq` against `tests/etkl/fixtures.py::false_transposed_pdf` and `::transposed_table_pdf` and asserts `order(transposed) < order(transpose_coherent)` on both branches of the oracle: `false_transposed_pdf` → `transpose_coherent` chosen=`incoherent` → verdict `escalated TRANSPOSED`; `transposed_table_pdf` → `transpose_coherent` chosen=`coherent` → verdict `asserted`. The original corpus scan still stands unrepealed: apple (`region4-d2`=upright, `region6-d2`=upright), capacity (`region3-d2`=upright), WHO (`region4-d2`=upright), stem (zero `transposed` judgements at all — path never reached), and CBH was never scanned for it. **Correction to the original row:** the residue was overstated — its own "what would close it" clause named a fixture reaching `transpose_is_coherent`, and two such fixtures (`false_transposed_pdf` at `tests/etkl/fixtures.py:268`, `transposed_table_pdf` at `:288`) already existed when the row was written; nobody had run the committed queries against them. A register that overstates a gap is the same class of defect as one that hides it (spec `2026-08-07-ordering-proof-and-supersession-design.md` §2) | Sourcing a real document that genuinely transposes a table is corpus acquisition, not a code change; the two `pytest.skip("R68: …")` calls (`tests/etkl/test_decision_queries.py::test_judgement_order_answers_the_r55_question`, `tests/etkl/test_decisionlog.py::test_band_4_records_transposed_before_coherence`) keep the corpus gap visible in test output meanwhile | A real document that genuinely transposes a table entering the corpus, at which point both skips become live assertions |
| R71 | **The kind gate is load-bearing** — suppressing `looks_transposed` on an `UNSUPPORTED_TABLE` band is currently the only thing protecting two stem regions from a false-positive transposition reading. Slice B (carrying topology candidates instead of branching early) cannot start until that is fixed. **The closing path first proposed — harden both oracles with a derived header/body boundary — is now measured circular for exactly the case it exists to fix** (spec `2026-08-07-harden-transposition-oracles-design.md`, closed 2026-08-07 blocked, see its §10): the only boundary derivation available, `header_body_split`, finds the header by searching for a type-homogeneous **column**, but a transposed table has no type-homogeneous column **by definition** — so feeding its output into a transposition oracle answers a different question than the one being asked | Spec `2026-08-07-kind-gate-load-bearing-design.md` §3/§4, 2026-08-07. Corpus-wide scan: only `stem` p0 region2 and p2 region1 are suppressed-positive (apple, capacity, WHO, CBH: none). Both have `looks_transposed=True`, `transpose_is_coherent=False`, and compile today at 586 and 741 asserted cells. Un-gating routes them to the incoherent branch, which escalates at 0 cells — 1,327 of stem's 2,152 asserted cells. `looks_transposed` misfires because both bands carry a multi-row **wrapped column header**: `classify_evidence` (`src/iladub/etkl/classifygraph.py`) reads header words from `band.lines[0]` only, and `assign_cells` (`src/iladub/etkl/regions.py`) has no header/body split, so the wrapped header's own trailing lines — not just the leading line `['Friday, 31', 'July 2026']` / `['Date of Grain']` — enter the cell grid as body rows, seeding Text cells into every column and destroying the column type-homogeneity whose absence `looks_transposed` tests for. Verified by ablation: dropping only the leading line leaves `looks_transposed=True` on both bands; only stripping the entire wrapped-header block (3 lines on p0, 2 on p2) flips both to `RECORD_TABLE` with `looks_transposed=False` (spec §4). Pinned by `tests/etkl/test_kind_gate_is_load_bearing.py` (10 tests). **The guard's coverage is asymmetric between the two bands, and this matters:** page 0 region2 is guarded at both the oracle-fact level (kind/`looks_transposed`/`transpose_is_coherent`) and the routing level (`test_page0_region2_still_compiles_through_the_unsupported_path` calls `compile_tables(STEM, page_number=0)` directly and asserts `verdict == "asserted"`, `cells == 586`, ~10s). Page 2 region1 is guarded only at the oracle-fact level — compiling stem page 2 standalone yields `verdict=escalated, cells=0`; its 741 asserted cells exist only under `compile_document`'s cross-page header carry (~150s), which this guard does not invoke. A future change to the routing (e.g. deleting the `RECORD_TABLE`/`UNSUPPORTED_TABLE` branch in `compile.py`) would therefore be caught immediately for page 0 but would silently drop page 2's 741 cells with no test failure until the full corpus/document suite is run. **This loop (2026-08-07, spec `2026-08-07-harden-transposition-oracles-design.md`) measured the proposed fix directly against the two transposition fixtures, not just the corpus, and that is where the circularity showed up:** `header_body_split` on `false_transposed_pdf` (4 lines, the R55 detect-then-refuse specimen) returns **3**, collapsing the body to its single last row; wiring that boundary into `looks_transposed`/`transpose_is_coherent` (Task 3, since reverted) made the region compile as an ordinary 9-cell `RECORD_TABLE` instead of escalating `TRANSPOSED` — a wrong assertion replacing a correct escalation. On `transposed_table_pdf` (3 lines, the other R55 specimen) `header_body_split` returns **None** (no column is ever homogeneous — that absence is the transposition signature itself), so the caller's None→1 fallback left it unaffected. On the two stem bands themselves the wiring worked exactly as intended — `looks_transposed` True→False and `transpose_is_coherent` stayed False on both, unchanged — so **the blocker is the fixtures, not the stem specimens**. Tasks 1–2 of that spec (the `tab:bodyStartsAt` term, `celltype.grid_evidence`'s `body_starts_at` parameter defaulting to 1, and both `.rq` files reading it) shipped at commit `9abcdc5`; Task 3 (wiring the call sites) was reverted, so every stem-band number above (586/741 cells, 1,327-cell stake, guard pinning `looks_transposed is True`) is unchanged. **Correction (final review 2026-08-07): the branch is NOT inert — an earlier version of this row said it was.** It ships **one acknowledged behavioural change**: `transpose-coherent.rq` previously had **no row filter at all** (only the column bounds `?ac >= 1`/`?bc >= 1`), so it read every physical row including the header; the shipped `?r >= ?bodyStart` **excludes row 0 even at the default `?bodyStart = 1`**. Measured over 400 random grids at the default: **21 divergences, every one incoherent → coherent** (escalate → assert); over `body_start ∈ {1,2,3}` (1,200 cases), 127, all the same direction, all of one shape — a header row whose types mismatch over a body whose types do not. The change was **kept on merits** (excluding the header from a *body*-coherence check is the intended semantics, and header pollution is what this residue exists to fix) and is pinned by `test_transpose_coherent_diverges_from_old_only_on_the_header_row` (`tests/etkl/test_derivation_equiv.py`), which characterises the divergence and fails on any *other* one. `looks-transposed.rq` and the `tab:bodyStartsAt` plumbing **are** inert (that query's old filters were the literal constants `?r >= 1`/`?cr >= 1`, so the substitution is a genuine identity at 1, asserted by `test_looks_transposed_new_matches_old`; and no caller passes a non-default `body_starts_at`). **Corpus scores are unchanged by unreachability, not by inertness** — no corpus band reaches the transposed path at all (R68) — a strictly weaker guarantee that stops holding the moment slice B routes a band down that path. Also fixed at final review: `transpose-coherent.rq` bound `tab:bodyStartsAt` *inside* its `FILTER NOT EXISTS`, so a graph missing the triple made the group unsatisfiable and the query asserted **coherent from absent evidence** (CLAUDE.md §8 derive-by-absence). The binding is hoisted above the filter — behaviour-identical when the triple is present (400 grids × `body_start ∈ {1,2}`, 0 mismatches) and now failing **closed** (False) when it is absent; pinned for both oracles by `test_both_transposition_oracles_fail_closed_without_a_body_boundary`. That sub-finding is fully closed and carries no residue row of its own. Controller-verified at 9abcdc5: `pytest tests/test_corpus_stem.py tests/test_cbh_e2e.py tests/etkl/test_kind_gate_is_load_bearing.py tests/etkl/test_closing_slice.py tests/etkl/test_transposed_chain.py -q` → 46 passed in 301s | The only header/body boundary derivation available, `header_body_split` (`src/iladub/etkl/headers.py`), locates the header by searching for the first row from which at least one leaf column is family-homogeneous non-Text. A transposed table is *defined* by having **no** type-homogeneous column — that absence is precisely what `looks_transposed`/`transpose_is_coherent` test for. Using an instrument whose premise is the negation of the oracle's subject matter is circular, not a wiring gap: a boundary signal independent of column typing is new derivation work to design and validate, not a call-site change. R10 remains refuted as the cause of the original wrapped-header misfire (unchanged, see Measured) but is orthogonal to this circularity | Either (a) a boundary signal that does not depend on column type-homogeneity — geometry such as wrapped header lines sharing rules or vertical line-spacing, or the header/body machinery the matrix and hierarchical paths already use — designed and validated against the two transposition fixtures as well as the corpus; or (b) attacking the root instead of routing around it: `classify-kind.rq` reads header words from `band.lines[0]` alone, which is *why* a wrapped-header band is classified `UNSUPPORTED_TABLE` in the first place. Teaching `kind` to recognise a multi-row header block would dissolve the oracle problem rather than instrument around it — bigger, since it touches the classifier every document passes through, but it removes the need for either oracle to guess a body boundary at all. Whichever path is chosen must be measured against `false_transposed_pdf`/`transposed_table_pdf` in addition to the corpus — this loop's finding is precisely that corpus-only measurement (prior spec §4) missed the fixture failure (prior spec §7) |
| R74 | **The cbh data grid leaks one row from a SECOND table on the same page** — line 75 (`Stock at Port (Main Storage Area) as at 29/07/2026 … 1,951,264`) belongs to the stock-at-port table, which has its own header and its own columns, and is admitted into the ship-roster grid | loop-author-split, 2026-08-09, measured against a full cbh page-0 transcription: 45 of 45 vessel rows admitted, zero metadata leaked, and exactly this one row from table B. Recorded in prose at `2026-08-08-data-grid-types-elements-axioms.md` §8.4 (*'cbh's rectangle spans a stacked panel'*) but never given a register row. AMENDED 2026-08-09 (loop-aggregate-witness): line 75's measure is table A's EXACT grand total — 374,904 + 737,289 + 660,363 + 178,708 = 1,951,264 — so the line carries TWO tables' ink (table B's title and table A's grand total) rather than being a stray table-B row. Whoever derives `tab:StackedGrids` must split the LINE, not merely exclude it | `tab:StackedGrids` is DEFINED in `tab-datagrid.ttl` and never derived; separating two co-located tables is its own slice | Derive `tab:StackedGrids` — two grids on one page whose column extents differ are different grids. Pinned meanwhile by `tests/etkl/test_datagrid.py::test_cbh_p0_table_b_leak_is_pinned_not_hidden` so the leak cannot drift silently. **CORRECTED 2026-08-20** (loop `the-arc-has-a-denominator`, Task 2): this row previously named `test_cbh_p0_known_defects_are_pinned_not_hidden`, which does not exist — verified `./.venv/bin/python -m pytest --collect-only -q \| grep known_defects_are_pinned_not_hidden` → 0 matches. The real pin, verified to collect (`./.venv/bin/python -m pytest --collect-only -q \| grep table_b_leak`), is `tests/etkl/test_datagrid.py::test_cbh_p0_table_b_leak_is_pinned_not_hidden` |
| R76 | **Spec-writing discipline: proposal and disposal must come from DIFFERENT sources** — five specs blocked in one session, all with the same shape: a round trip that replays its own recipe, a field that selects then confirms itself, a union that admits itself. Nothing could be wrong in a detectable way | 2026-08-09, measured across `2026-08-09-two-regimes-…`, `2026-08-09-projection-names-…` and `2026-08-09-naming-an-implicit-dimension-…` §9.3. The loop that SHIPPED the same day (the data grid) had a hand transcription as its oracle BEFORE it had a design | It is a process defect, not a code defect, so it has no code fix; it is recorded here because the next loop on this ground will re-derive it otherwise | Before writing a spec: name what proposes, what disposes, and why they are independent; name the artifact that would falsify the spec and build it first if it does not exist; type every premise (measured-on-evidence vs measured-on-fixture vs proven-algebraically vs read-not-run). And start every loop in a fresh session — the fourth spec of that day misquoted this register, a fatigue signature, not a knowledge gap |
| R77 | **cbh's four panel totals are missed on the SCORE path too, for the identical no-label reason** — `rows.detect_aggregation_rows` (loop H) requires exactly two occupied columns, one numeric and one not, so a label-less total is not even a candidate | loop-aggregate-witness, 2026-08-09. The data grid now admits all four (R75 closed), but the grid is NOT on cbh page 0's score path: `compile_tables` reports `asserted=66, escalated=879, score=0.0698`, and `datagrid_fallback` fires only when `asserted == 0 and escalated == 0`. So the four rows are read by the grid and still unread by the compile | Scope: R75's residue was measured on the transcribed grid oracle, and loop H's nesting rule ("back to the previous CONFIRMED aggregation row whose label column <= L") is keyed on the label column a label-less candidate does not have — relaxing it is its own slice with its own oracle | Give `detect_aggregation_rows` a label-less candidate shape whose nesting level comes from something other than the label column, and re-measure cbh p0. Expected movement is small and real: 4 cells of 945, 0.0698 -> ~0.074 |
| R78 | **An unparseable member cell is summed as zero by `confirms_aggregate`** — the candidate side of G8's row-admission disposer is strict (one unparseable occupied cell refuses the row) but the member side drops a present-but-unparseable cell from the sum, so for that column the member reads as zero | Final review of loop-aggregate-witness, 2026-08-09: `confirms_aggregate({1:'300'}, [{1:'100'},{1:'200'},{1:'..'}])` returns True. `is_blank` (celltype.py) covers only `''`, `(blank)` and `-`, so suppression markers such as `..` and `n/a` are not blanks. Pinned by `test_confirms_aggregate_drops_an_unparseable_member_cell` | The failure direction is toward REFUSAL, not false admission — a suppressed member makes the printed total exceed the computed sum — and tightening it would need an oracle for suppression markers that no transcribed page supplies. Tightening unmeasured is what this project forbids | Transcribe a page that prints a suppression marker inside an aggregated column, then decide whether such a member refuses the row or is genuinely zero |
| R79 | **An adopted page's unread structure is escalated as ONE page-level residue candidate**, not per line and not per band — a consumer wanting to know *which* structural lines the grid dropped must read the candidate's `iladub:surfaceText` as a newline-joined line list, because no typed refusal exists per line | loop-adoption-scope, 2026-08-09, measured on apple p1 (`compile_tables(APPLE, 1, validate_shapes=False, datagrid_adopt=True)`): a single `DATAGRID_RESIDUE` escalation at `<doc>#p1-datagrid-residue` carrying one `iladub:surfaceText` literal, **40 escalated tokens over 11 lines**, reported as one `RegionReport` at region index 9 of 10 | One candidate keeps the line-granular ledger and the graph in agreement at the cost of graph addressability, and per-line refusal has no consumer yet. Nothing is lost in the *accounting* — `adoption.build_ledger` is already per line (`LineLedger.residue` is a tuple of line indices); the loss is only that the graph exposes the run as one literal | A consumer that needs to address a dropped line individually; then the residue becomes one candidate per contiguous line-run, keyed by the ledger's own line indices |
| R80 | **apple p1's indent hierarchy is read by nobody** — the shipped reader escalates every band of the page and the data grid then reads its 28 entry rows FLAT, so the balance sheet's group labels are escalated as residue rather than carried as structure | loop-adoption-scope, 2026-08-09. Measured with adoption OFF (`compile_tables(APPLE, 1, validate_shapes=False, datagrid_adopt=False)`): 0 asserted / 97 escalated, score 0.0, over 5 escalated bands whose reasons are `KIND_NOT_SUPPORTED`, `REGION_TILING_FAILED` ×3 and `ROUND_TRIP_FAIL` — **no `tab:HierarchicalTable` and no `tab:RecordTable` node is emitted at all**, so the hierarchy is not merely unread, it is never typed. Measured with adoption ON, from the residue `surfaceText` (the R79 candidate): **11 unread lines / 40 tokens**, of which **7 are group labels** — `ASSETS:`, `Current assets:`, `Non-current assets:`, `LIABILITIES AND SHAREHOLDERS’ EQUITY:`, `Current liabilities:`, `Non-current liabilities:`, `Shareholders’ equity:` — and the other 4 are the two-line split date header (`June 27, September 27,` / `2026 2025`) plus a two-line wrap of the common-stock row. The adopted page scores **0.7802** (142 asserted **tokens** / 40 escalated **tokens**), never 1.0000, precisely because of them | The grid reads a RECTANGLE by definition; a balance sheet's indent hierarchy is a *different* reading, and no oracle for it exists on this page — the transcription covers the 28 leaf rows only. Escalating the labels is the honest §7 answer (a visible refusal), not a silent loss | Transcribe apple p1's group structure, then decide whether the grid derives `tab:RowGroup` from indentation as an AXIOM over the evidence graph, or keeps refusing honestly. Not a tolerance on indent pixels — that would be the §8 defect this loop's exemplars exist to prevent |
| R83 | **At PAGE scope an adopted page's `rep.graph` escalates nothing for a band the grid never TOUCHED, while the report still books its tokens** — `compile.py` rebuilds the page graph (`graph = Graph()`), discarding every pass-1 escalation candidate, but `adoption.build_ledger`'s untouched term (`adoption.py:90-93`) keeps counting the untouched escalated bands' ink. So `rep.graph` is authoritative for the TOUCHED bands, the grid and the residue; the REPORT is the authority for the rest. **The DOCUMENT path has no gap** — `compile_document` keeps the driver's page graph and withdraws only the superseded bands (`document.py:1445-1447`) | Final review of loop-adoption-scope, 2026-08-09. **Dormant on the whole corpus: the untouched term is EMPTY on both adopting pages.** stem p1 (`compile_tables(STEM, 1, validate_shapes=False, datagrid_adopt=True)`) has exactly one escalated band — region 1, `REGION_TILING_FAILED` — and the grid touches it (the other two bands are `ignored` at 0 escalated tokens): 1025 tokens asserted / 44 tokens escalated at 0.9588, all 44 the grid's OWN `DATAGRID_RESIDUE` candidate, which the rebuild does emit. apple p1 likewise. Pinned by `tests/test_corpus_stem.py::test_page_scope_adoption_escalates_in_the_graph_everything_it_books` | Re-escalating the untouched bands into the rebuilt graph fixes page scope and **breaks document scope**: the driver's own untouched-band candidate is still standing under the page doc URI, so a re-escalated copy would arrive beside it under the adoption doc URI — two escalating nodes over one band's ink against a ledger that books it once, the double count R73 exists to prevent, moved to the other side. `sum(r.tokens_escalated) == escalated_total` is unaffected either way; the disagreement is graph-vs-ledger, never ledger-internal | A page that actually brings an untouched escalated band (the pinning test is what will say so). Then either the driver stops merging `rep_a.graph` wholesale and adopts the adoption doc's escalations as authoritative for the whole page, or page scope grows a withdrawal that is selective the way the driver's already is |
| R84 | **`adoption.build_ledger` counts every admitted line's tokens as asserted, whether or not any band was scoring that ink** — a grid row falling inside an `ignored` band (0 escalated tokens, never in the page's denominator) would add to the numerator *and* the denominator, raising the adopted page's score for ink no band was ever accountable for | Final review of loop-adoption-scope, 2026-08-09. **Not live: measured 0 on both adopting pages.** Admitted grid lines lying outside every escalated band — apple p1: **0 of 28** admitted lines (escalated bands 2,3,4,5,7 of 8), stem p1: **0 of 77** (escalated band 1 of 3); tokens outside in both cases **0**. The arithmetic is identical to the pre-existing `datagrid_fallback` accounting, which has shipped since the data-grid loop | **The arithmetic is deliberately UNCHANGED**: the alternative — dropping such a line from the numerator while the graph asserts its cells — would make the score disagree with the graph in the other direction, and no page in the corpus exercises either branch, so the choice would be made on no evidence. Changing an accounting rule with a 0-of-105 measurement behind it is the overfitting this project forbids | A page whose data grid reads ink inside an `ignored` band; measure the score with the ink counted and with it excluded, and decide which one the graph supports. The fix also has to reach `datagrid_fallback`, which shares the rule |
| R85 | **`transpose-coherent.rq` is quadratic in row count** — it is the only one of the four row-count-sensitive derivations never converted to the aggregation form the other three carry. Its `FILTER NOT EXISTS` group is a pair self-join over body cells (`?a tab:atGridRow ?r ... ?b tab:atGridRow ?r`), while `header-body-split.rq`, `stub-data-split.rq` and `looks-transposed.rq` each declare *"AGGREGATION FORM (linear in rows) ... replacing the O(cells^2) pair self-joins"*. The absolute cost is invisible at corpus row counts and severe beyond them | loop-perf-guard, 2026-08-10, measured on this machine with 4 body columns, each point the minimum of 3 warmed runs (`tests/etkl/test_derivation_perf.py` helpers). Wall clock at n = 25/50/100/200/400 rows: **0.163 / 0.390 / 1.137 / 3.705 / 13.736 s**. Log-log **local exponent climbs 1.26 → 1.55 → 1.70 → 1.89** and the doubling ratio climbs **2.39 → 2.92 → 3.26 → 3.71**, both converging on the quadratic values 2.0 and 4.0. The three sibling queries are flat by comparison — measured T(2N)/T(N) of **1.82 / 1.87 / 1.82**. The wall-clock guard this loop removed (`_BOUND_S = 2.0` at n=50) never saw it, because 50 rows costs 0.39 s: the cliff was always there and the bound was looking in the wrong place. Pinned by `test_transpose_coherent_does_not_scale_quadratically`, a non-strict `xfail` at N=100 (measured ratio 3.31 against the 3.0 linear/quadratic discriminator) | Closing it means rewriting the query into the sibling aggregation form (*NOT EXISTS a body row whose col>=1 non-abstaining cells have `COUNT(DISTINCT ?type) > 1`*), and that query carries three paragraphs of deliberately chosen, separately pinned semantics: the `tab:bodyStartsAt` binding is **hoisted above** the `FILTER NOT EXISTS` so the ASK fails *closed* on absent evidence (CLAUDE.md §8, no derive-by-absence), and its own comment records that the hoist is an existential/universal divergence when a band carries more than one body bound. A `SELECT` subquery cannot see an outer binding, so the aggregation form must re-bind `tab:bodyStartsAt` internally the way `looks-transposed.rq` does — which reverses that quantifier choice. That is a semantic change to a load-bearing derivation, not a perf tweak, and it does not belong in a commit whose subject is the CI guard | Rewrite `transpose-coherent.rq` to aggregation form under the existing differential oracles — `test_transpose_coherent_diverges_from_old_only_on_the_header_row` and `test_both_transposition_oracles_fail_closed_without_a_body_boundary` (`tests/etkl/test_derivation_equiv.py`) — deciding explicitly what the re-bound `tab:bodyStartsAt` means when a band carries two values, then flip the `xfail` in `tests/etkl/test_derivation_perf.py` to a live assertion and drop its N back to a cheaper size (that test is 15.5 s of the file's 28.3 s today) |
| R86 | **A quarantined concept mints NO decision holon, so the proposition half of the epistemics is unattributed** — `iladub:CandidateConcept` records who *suggested* it (`iladub:suggestedBy`) but nothing records who decided NOT to promote it, or against what. The assertion half is now fully accountable at both membranes; the refusal half is a bare status | loop-decision-membrane, 2026-08-10. `ground.ground_concept` returns `"proposed"` at `ground.py:199` (no contract field) and `:203` (no grounding oracle) **before any `pd` is constructed** — `_emit_grounded` is the only site that mints one. Measured on the two contracted corpus documents by `scripts/measure_dec_membrane.py --scope grounding`: cbh-stem **909 candidates = 134 grounded + 775 quarantined**, `iladub:PromotionDecision=134`; graincorp-stem **1850 = 585 + 1265**, `PromotionDecision=585`. So **2040 refusals across the two documents carry no decision at all**, and the four membrane cells are 0/0 precisely because a node that mints nothing cannot violate a shape | Spec 2026-08-10 §7 scoped this out deliberately: minting a `dec:DecisionHolon` per refusal would multiply the graph by ~3.4x (2040 new holons + their option spaces against 719 today) for a question nobody has yet asked of it, and the *right* modelling — one decision per refused concept, or one per document naming the refused set — is not decidable from the current evidence. This loop's mandate was the assertion path | Decide whether a refusal is its own decision holon or a member of a document-scope one, then emit it and extend the oracle's grounding scope to count refusal decisions beside promotions. The shape work is already done: `dec:DecisionHolonShape` would validate them unchanged |
| R89 | **`BandRecorder.record`'s Python guard now duplicates two constraints the membrane enforces** — `if len(options) < 2: raise` and `if chosen not in options: raise` (`decisionlog.py:43-45`) restate `dec:DecisionHolonShape`'s `dec:optionSpace sh:minCount 2` and its `sh:sparql` chosen-in-option-space constraint (`dec-shapes.ttl:17,23`) | loop-decision-membrane, 2026-08-10, reading `src/iladub/etkl/decisionlog.py:43-45` against `vocab/shapes/dec-shapes.ttl:17-32`. Both constraints became membrane-enforced in this loop (Task 6 for compile, Task 7 for grounding). Task 7 deleted the *test-side* duplicate of `iladub:GroundedNodeShape` at `tests/test_corpus.py` on exactly this reasoning | **It is NOT the same case as the deleted test duplicate, which is why this loop did not touch it.** The test duplicate was an assertion about a finished graph, where the membrane is strictly better. This is a producer-side **fail-fast**: it raises at the call site that built the bad decision, with that call site on the stack, rather than at a membrane thousands of triples later. Deleting it trades a precise error for a late one — a judgement call about defensive programming, not a §8 defect, and not this loop's to make | ✅ **HALF MOVED 2026-08-17 — the rule is decided and recorded; the application is not. THIS ROW STAYS OPEN.** (a) **Done:** the project's rule is adopted and now lives in CLAUDE.md § Core design principles → *Producer-side guards vs the membrane (adopted 2026-08-17, R89)*, no longer only in a handoff (`docs/superpowers/2026-08-17-r97-r104-handoff.md:28-31`, which is exactly the failure mode this repo has paid for twice). The rule: *delete a producer-side guard only when the membrane provably validates **every** product of that producer* — the condition is **provable total coverage**, and a fail-fast guard is not automatically a duplicate of a membrane constraint. (b) **Still open:** whether `BandRecorder.record`'s two guards (`decisionlog.py:43-45`) are then deleted. ~~R102~~'s close makes the rule's condition *satisfiable* for this producer for the first time — at **document scope** (`769/769` validated, measured; see that row) — but *satisfiable* is not *applied*: the coverage is document-scope only, and `compile_tables` remains publicly exported, so a caller outside `compile_document` still validates nothing. Deciding the deletion is a separate act and was explicitly scoped out (spec §7). Note the coupling ran the way the row predicted: R89's answer depended on ~~R102~~, and ~~R102~~ was the case that supplied the rule's own worked example. |
| R90 | **The bbox is dropped from every `ROUND_TRIP_FAIL` proposition** — a cell whose ink crosses a gutter is escalated as an `iladub:SourceRegion` carrying page and text but no geometry, so provenance-to-the-page (CLAUDE.md §6) is coarser for refused cells than for admitted ones | loop-decision-membrane, 2026-08-10, `src/iladub/etkl/holon.py:55-77` (`_emit_roundtrip_fail_cell`) and its docstring, which records the loss inline. Cause: `tab:hasBBox` declares `rdfs:domain tab:Cell` (`tab.ttl:103-107`), so hanging it off a proposition retypes the proposition as a `tab:Cell` and hands it to every cell shape — the R19 mechanism. There is **no owned property for geometry on an `iladub:SourceRegion`** | Carrying the bbox anyway was the R19 defect this loop's Task 2 closed at the source; inventing an `iladub:` geometry term to replace it is new vocabulary, and this loop was under a byte-read-only rule for `vocab/`. `prov:wasDerivedFrom` still pins the cell's origin and its IRI encodes x0/top, so the geometry is recoverable, just not stated | Either declare an owned geometry property whose domain is `iladub:SourceRegion` (and align it, not clone, if HGA gains one), or narrow `tab:hasBBox`'s domain so carrying geometry no longer entails being a `tab:Cell` — the same shape of fix R69 needed for `dec:confidence` |
| R95 (17/85 closed) | **The `audit_literals` guard's own bypass, `_payload`/`_payload_nt`'s `audit: bool = True` keyword, is a standing hazard** — a guard with a test-only disable flag can always be flipped off by a future call site, silently re-opening the exact byte-parity blind spot R94 closed | loop-membrane-parity, 2026-08-13, `src/iladub/etkl/membrane.py:318` (`_payload_nt`) and `:351` (`_payload`) both carry `audit: bool = True`; `grep -rn "audit=False" src/` returns nothing under `src/` today — no production call site disables it. Fenced by `tests/etkl/test_decimal_typing.py::test_the_audit_escape_hatch_is_not_used_in_production`, a deliberately blunt substring scan over `src/**/*.py` for `audit=False`, not a semantic check | The parameter exists on purpose — `tests/etkl/test_membrane_equiv.py` and `tests/etkl/test_decimal_typing.py` need to construct a KNOWN-bad payload to prove the guard raises on it, and that construction has to go around the guard once to do so. Removing the parameter removes the only way to test the guard; keeping it keeps the hazard the substring scan exists to fence | Nothing better than the fence is obviously available without losing test coverage of the guard itself; if a future refactor finds a way to construct a bad payload without a bypass parameter (e.g. a test-only builder function outside `src/`), the parameter could be dropped and the scan retired with it |
| R96 (17/86 closed) | **`audit_literals`'s LEXICAL half is silently a no-op if `rdflib.NORMALIZE_LITERALS` is ever `False`** — the guard reparses a literal's own lexical form (`Literal(str(o), datatype=o.datatype)`) and compares it to the original string, but that reparse only rewrites a non-canonical lexical form (the `5e-05` class) when rdflib's module-global `NORMALIZE_LITERALS` is `True`, its documented default. Nothing in this repo sets it, and nothing pins that it stays unset | Found during Task 2's review of `audit_literals` (`src/iladub/etkl/membrane.py:216-259`), loop-membrane-parity, 2026-08-13. This is a DIFFERENT hazard from R95: R95 is our own parameter, tested and fenced; this is an external library's process-wide global that nothing in `src/` or `tests/` touches, sets, or asserts the value of — an undocumented environmental premise the guard's LEXICAL half depends on without stating it. Given its own row rather than folded into R95 because the failure mechanism, the owner (rdflib, not us), and the fix (an assertion or an explicit set, not a code-review fence) are all different | Not yet load-bearing: nothing in the repo or its dependencies sets `rdflib.NORMALIZE_LITERALS`, so the default holds throughout every process this code runs in today | Either assert `rdflib.NORMALIZE_LITERALS is True` at import time in `membrane.py` (fail loud if some future dependency flips it) or set it explicitly rather than relying on the imported default, and add a test that flips it off, calls `audit_literals` on a known-`5e-05`-class graph, and confirms the guard's behaviour under that condition is understood (raise, or documented no-op) rather than merely unexamined |
| R97 (18/87 closed) | **Four wired `tab:` shapes the corpus does not exercise** — `tab:AggregationCellShape`, `tab:BaseFactShape`, `tab:PivotedDimensionShape`, `tab:SectionTotalShape` | R87 Task 5, 2026-08-16, `tests/etkl/test_vacuity_registry.py`. Each measured **0 focus nodes** on all 7 corpus documents (targets resolved against the subclass-closed graph, the graph the membrane actually validates). They are registered in `VACUITY_REGISTRY` with that reason, so the guard is green and the question is recorded rather than lost. | A shape validating a feature no corpus document exercises is either a CORPUS GAP or a DEAD SHAPE, and this loop does not adjudicate which — spec §4.5 says so explicitly. Adjudicating needs specimens the corpus does not contain. | For each: either a corpus document that exercises the feature (closing it as a corpus gap), or a decision to delete the shape (closing it as dead). Until then the registry row is the honest record. **Note the registry's other arm bites here:** if any of these goes live, `test_no_registered_shape_has_gone_live` FAILS until the row is removed. |
| R99 (18/89 closed) | **`iladub:NoLeakShape` can never fire on the compile membrane** — 11 focus nodes and an unreachable body term | R87 Task 5, 2026-08-16. Measured **11 focus nodes** on the corpus (it targets `iladub:CandidateConcept`, which the compile path does mint), so criterion 1 pronounces it healthy — **but its body names `iladub:asserted`, which appears in NO compiled graph**; the status value is written on the grounding path. **This is a second shape of R87's own class and it was invisible to spec §3's M7 table, which counted focus nodes only.** Found by the registry's criterion 2 the day that criterion shipped. | Adjudicating it means deciding whether `NoLeakShape` belongs on the compile membrane at all, which is a membrane-composition question this loop did not open. | Either a compile path that writes `iladub:asserted` (making the shape live where it is wired), or moving the shape to the grounding membrane where its term exists, or a measurement showing it is already enforced there and the compile-leg wiring is redundant. It is registered meanwhile. |
| R100 (18/90 closed) | **`dec:EscalationShape` is live at document scope and IDLE at page scope, and the vacuity registry cannot express that** | R87 Tasks 3-5, 2026-08-16. `compile._validate` is called at page scope (`compile.py:1124`) and at document scope (`document.py`), with ONE shape set both times, while the escalation furnishing runs at document scope ONLY — measured, because no page graph ever carries a `dec:supersedes` edge (0 in 13 page graphs) and furnishing at page scope raises 4 spurious expansion requests on cbh-stem and 5 on apple. So the shape binds rows on one leg and none on the other, by design. The registry (`d1e5ab1`) is keyed by SHAPE and measures the 7 document graphs, which is what spec §3's M7 measured; keyed that way, this shape is simply live. | Keying the registry by (shape, leg) doubles every row and changes the seed the plan specifies. The deliberate idleness is recorded in `compile.py`'s comment and in `262dd2e`, so it is auditable today — just not by the guard. | Either a registry keyed by (shape, leg) with a row recording this one as idle-at-page-scope-by-design, or a measured finding that no wired shape's liveness differs by leg except this one, which would make the (shape, leg) key unnecessary. **The risk being carried:** a shape that goes idle on ONE leg only is invisible to the guard as it stands. ⚠️ **CITATION CORRECTED 2026-08-17** (plan Task 3, item 3): this row and the comment it paraphrases both cited `compile.py:1083` for the page-scope `_validate` call. Measured at `b09dbd1`, `grep -n 'conforms, text, legs = _validate' src/iladub/etkl/compile.py` → **`:1124`** (the looser `_validate(graph` also matches the `def` at `:453`); `:1083` is `denom = asserted_total + escalated_total`. Corrected in both places (`compile.py:408-411` carries the same note). The *finding* — live at document scope, idle at page scope — is unaffected, and this loop's R102 fix does not change it: the page gate is untouched, so `dec:EscalationShape` stays idle on the page leg exactly as this row says. |
| R101 (18/91 closed) | **A green CI tick can cover less than it appears to: an optional-dependency guard removes a whole test module from the run, and the skip count does not show how much was lost** | 2026-08-17, on `main`. CI run `31998036351` was green at **1103 passed / 120 skipped** with `tests/test_zero_etl_export.py` — the three tests `09449e4` had just shipped for the L of ET(K)L — **not run at all**. Cause: `.github/workflows/ci.yml` installs `.[baml,dev,docs,etkl]`, `09449e4` put `pandas`/`pyarrow` in the **`demo`** extra only, and the module guards on them at `:11-14`. **The generalizable half, and the reason this is a residue rather than a fixed bug:** a module-level `importorskip` reports **ONE skip for the entire module** however many tests it holds — verified directly, a 3-test scratch module reports `1 skipped`, not 3. So the skip count is not proportional to the coverage lost, and no number in the CI summary distinguishes "one optional test skipped" from "an entire subsystem never ran". This instance is fixed (`09b96a8`; run `31998847175` → 1106 passed / **119** skipped, +3/−1, exactly the module collapsing). | The instance is closed; the class is not. **48 test modules reference `importorskip` and 8 carry a module-level `pytestmark`** — which of those guards are module-level (and so collapse a module into one line) versus function-level is NOT enumerated. The dominant names are `reportlab` (in `dev`) and `pdfplumber` (in `etkl`), both installed by CI and therefore inert; `rapidocr` sits in the `ocr` extra, which CI does **not** install. Sorting intended skips from accidental ones is a survey, and deciding the policy — should CI install every extra, or should some subsystems be deliberately unrun? — is a design question this loop did not open. | An enumeration of every module-level skip guard in `tests/`, each classified **intended** or **accidental** against the CI install line, and then one of: a CI job installing all extras, or — the better shape, because it survives a new extra being added — **a registered expectation for skips, exactly as `VACUITY_REGISTRY` does for idle shapes** (`tests/etkl/test_vacuity_registry.py:87`): every module-level skip must have a row giving its reason, and a skip that disappears fails the guard until its row is deleted. Note the symmetry with R97–R100: the same failure — *a check that is wired but says nothing, reported as health*. **Do not close it by simply adding `demo` to the CI install line**; that fixes today's instance a second time and leaves the class untouched. |
| R106 (21/96 closed) | **An arc criterion can shipping-pass as `prog:met true` while citing evidence that contains NO focus nodes for the shape it claims — a VACUOUS POSITIVE — and the membrane admits it silently.** The rule that catches it exists, in prose, enforced by nothing | 2026-08-21, task 4 of the `arc-denominator` loop, and measured by shipping the bug. `prog:criterion:dec:10` was authored at `5367dcf` citing a graph with **zero** focus nodes for the shape it asserted was exercised. The implementer ran its own non-vacuity check and the check **passed** — because it counted focus nodes over a WIDER file set than the row actually cited, so the count it returned was not a count of the cited evidence. The fix (`674d16f`) generalised the rule to *counts are taken over exactly the files each criterion cites*, and the re-reviewer then applied that rule to **all ten** other met-true `dec` rows: every one measured >= 1, so the instance is confined to that single row. **The class is not:** reverting `dec:10` to its vacuous citation still yields `shacl_ok=True` — the arc membrane (`tests/arc-shapes.ttl`) has no constraint expressing non-vacuity at all, and the rule's only home is prose at `tests/arc-manifest.ttl:553-555`. Task 5 (`1a93451`) was held to the rule **by hand** — its reviewer opened `docs/superpowers/2026-08-20-escalation-reason-census.md` and checked all ten attributed figures against their cited sections; every one landed, so the class did not recur. **By hand is not an oracle**, and task 5's ten citations were checked by a reviewer who was told to look; nothing checks task 6's | Task 4's scope was the `dec` rung's 16 criteria and the loop was already two fix rounds deep on Ruling 12 when the vacuous positive surfaced; authoring a new membrane refusal at that point was declined as new scope, twice, and the whole-branch review was named as the owner. Raised as a row now — rather than left in the loop's handoff prose, where it had sat since 2026-08-21 — because a defect class recorded only in an unmerged branch's handoff is debt that disappears with the branch. Note this is the **sixth** instance of the family R101 names outright: *a check that is wired but says nothing, reported as health* (R97, R98, R99, R100, R101) | **M11 — a non-vacuity refusal that COMPUTES the count rather than trusting the author's.** The honest form re-derives, for every criterion asserting `prog:met true`, the focus-node count over **exactly the files that criterion's own `prog:source` / `prog:oracleArtifact` cite** — never a wider set, which is the precise error that made the original check pass — and refuses a zero. Note it is the *scoping* that is load-bearing, not the counting: a non-vacuity check over the whole repo would have passed `dec:10` too. Whether it belongs in `tests/arc-shapes.ttl` (closed-world, over the manifest graph) or in `tests/test_arc_manifest.py`'s environment leg (it must open cited files, which no SHACL engine can do) is the open design question, and is likely the latter for the same reason M10/R105 is. **Do not close it by hand-checking one more rung's citations** — that is the R101 lesson, and it is exactly what task 5 already did once |
| R107 (22/97 closed) | **The arc membrane admits two `prog:Rung` nodes sharing one `prog:rungKey`** — nothing counts rung nodes, so a duplicate-key manifest passes SHACL silently. Both readers are immune to it today, and Task 8 measured that they are immune **for different reasons** — *"that is luck, not design"* | First measured 2026-08-21 by Task 7's reviewer (`arc-denominator` loop, ledger § Task 7 fix round 1) and **re-measured independently 2026-08-22** by this seat: appending `prog:rung:etkl-DUP a prog:Rung ; prog:rungKey "etkl"` to `tests/arc-manifest.ttl` and running `validate_manifest` (`tests/test_arc_manifest.py:68-78`, pySHACL `inference="rdfs", advanced=True`) returns **`Conforms: True`**. `prog:RungShape`'s M6 constrains the **value** of `prog:rungKey` per node; no constraint counts nodes per key. **Reader immunity, also re-measured on that graph:** `vocab/queries/arc-position.rq` returns the identical five rows (`dec 11/17`, `etkl 1/7`, `holon 4/6`, `substrate 0/3`, `tab 1/10`) with and without the duplicate — it counts *criteria*, `COUNT(DISTINCT ?c)` / `COUNT(DISTINCT ?metC)`, since the Task 7 fix at `111645f`. Before that fix the same graph read `('etkl','2','4')` for a true 1/2 — `COUNT`/`SUM` counted **solutions**, so every count doubled. `cockpit.arc()` is immune by a **different** route: its regex reader never reads a `prog:Rung` node at all (Task 8's self-review, ledger § Task 8) | Task 7's fix round took both halves of the finding it was given (duplicate-safe counts, and the header claim replaced by the measurement) and **explicitly declined the eleventh membrane refusal as a third thing outside its mandate**; the re-review judged that **sound scoping**, on four measured claims — chiefly that `_refused_by_shacl` (`tests/test_arc_manifest.py:333-342`) asserts an **exact set** of refusal numbers, so an eleventh refusal moves a contract owned by Tasks 2 and 6 and needs a new fixture plus a one-refusal proof. Live impact today is **zero**: 5 rung nodes, 5 distinct keys | A **uniqueness refusal in `tests/arc-shapes.ttl`** — a `sh:sparql` constraint over the manifest graph refusing two `prog:Rung` nodes with the same `prog:rungKey`. It belongs in SHACL, not in a `.rq` and not in the Python legs: this is a **closed-world cardinality question about what crosses the membrane**, which is exactly what CLAUDE.md §8 reserves SHACL for (unlike M5/M7/M10, which must open files and therefore cannot be SHACL). Closing it means a fixture refused by that constraint **alone**, and updating the exact-set assertion in `_refused_by_shacl` from ten refusals to eleven. **Do not close it by hardening the readers** — they are already immune, which is the trap: the gap is invisible from the fractions and will stay invisible until a third reader is written that trusts the key |
| R108 (22/98 closed) | **A `prog:blockedBy` naming a CLOSED register row is admitted — M7 checks presence, not state**, so a rung criterion can go on reporting itself blocked by a residue that was repaired | Measured 2026-08-21 (Task 7's review, ledger § Task 7 concern 3) and **re-read 2026-08-22** at `6c66bfe`: `tests/test_arc_manifest.py:322-325` emits M7 only when `residue not in known`, and `known` comes from `register_rows()` (`:227-234`), which harvests `^\| *(R\d+) *\|` from the register **index** — a regex that matches `\| R92 \| closed \|` exactly as it matches an open row. `tests/arc-shapes.ttl` carries no `prog:blockedBy` constraint beyond M8 (well-formedness). **R105 is now exactly such a row** (closed 2026-08-21 by this loop's Task 6), so the case is live, not hypothetical. Live impact today is **zero and the error direction is the safe one**: the 15 residues actually named as blockers (`tests/arc-manifest.ttl:1055-1225` — R43 R44 R45 R62 R71 R74 R77 R79 R80 R83 R84 R97 R98 R99 R100) were all re-measured **open** in Task 7, and a stale edge on a closed residue makes a rung look **less** ready than it is, never more | Raised inside Task 7 as a candidate and correctly **not fixed there** — Task 7's mandate was the four derivation queries, and the reviewer recommended registering it explicitly rather than widening the fix round. It also cannot be answered in the graph: the residue's **state** is a fact of a markdown file that is not in the manifest graph at all, which is why M7 already reads the register in procedural Python beside the SHACL leg | **Extend M7 to refuse a `prog:blockedBy` whose row is closed**, parsing the state from the same index line the id comes from (`| R105 | closed | …`) — a two-value read, in the leg that already opens that file, with a fixture that fails until the edge is removed. **Decide the policy first, because it is the whole cost:** either a closed blocker is an **error in the manifest** (and every closing change must then delete the edges naming it, making residue-closure a two-file change), or it is a **warning the arc reports** (cheaper, but a warning nothing fails on is R101's family — *a check that is wired but says nothing, reported as health*). The first is the honest one, and this row exists to make that choice visible rather than to prejudge it. ✅ **DECIDED 2026-08-22 by the maintainer: the FIRST — a closed blocker is a manifest ERROR and M7 must refuse it.** So closing a residue becomes a two-file change by design: strike the row, then delete every `prog:blockedBy` edge naming it, or the arc refuses. Whoever implements it owns that consequence — the refusal message should say which edge to delete, not merely that one is stale |
| R109 (22/99 closed) | **The `<path>:<line>` pointer grammar has two divergent parsers, and the same defect wears four faces** — M5 (on `prog:oracleArtifact`) *strips* the line and checks existence only, while M10 (on `prog:source`) *checks* the range. This is the R105 family, one file away from where R105 was closed | Named by the whole-branch review of the `arc-denominator` branch (2026-08-22) and **re-measured element for element by the seat that raised this row**, against `tests/test_arc_manifest.py` at `6efc032` (`_LINE_SUFFIX = re.compile(r":\d+$")`, `:63`; the M5 strip at `:294`): (1) **out-of-range is admitted silently** — `_LINE_SUFFIX.sub("", "CLAUDE.md:999999")` → `CLAUDE.md`, `exists=True`, so a `met true` criterion citing line 999999 of a 458-line file earns **no** refusal, where the *same* pointer on `prog:source` earns a correct `M10: … but CLAUDE.md has 458 lines`; (2) **a RANGE defeats the strip and produces a FALSE refusal** — `"CLAUDE.md:1-2"` survives `:\d+$` unchanged, so M5 reports a file that plainly exists as *"does not exist in the working tree"*; (3) **a bare path escapes the line half entirely** — `_source_refusal("X", "CLAUDE.md")` → `None`; (4) **a directory pointer CRASHES the leg** — `_source_refusal("X", "scripts:1")` raises `IsADirectoryError`, not a refusal. Latent on every count today: the review checked all 13 line-suffixed artefacts by hand and every one resolves and points at its exact shape/def line; 0 of 43 live `prog:source` values are bare (23 line, 20 range) | The four faces surfaced in three different tasks' reviews as separate minors, each individually cosmetic-looking, and none of them was a defect in the code the task was building. Fixing the grammar mid-loop would have moved M5 and M10 — contracts owned by two earlier tasks — for a class that no live pointer exercises | **One shared `split_pointer(value) -> (path, start|None, end|None)`, used by BOTH M5 and M10**, plus a `sh:pattern ":\\d+(-\\d+)?$"` on `prog:source` in `tests/arc-shapes.ttl` so the grammar is stated once in the membrane rather than twice in two regexes, plus a `try/except` around the `read_text()`. **Do not close it face by face** — that is what produced four minors instead of one row, and fixing any one of them leaves the two parsers divergent, which is the actual defect. Note the asymmetry is the tell: M10 already does the honest thing, so the fix is *M5 adopting M10's reader*, not new logic |
| R110 (22/100 closed) | **The arc membrane degrades its own error messages: two `sh:nodeKind sh:IRI` constraints carry NO numbered `sh:message`, and three predicates carry no property shape at all** | Whole-branch review, 2026-08-22, re-checked at `6efc032` in `tests/arc-shapes.ttl`: `RungShape` (`:37`) and `ManifestShape` (`:156`) assert `sh:nodeKind sh:IRI` with no `sh:message`, where `CriterionShape` (`:65-66`) carries M9 for the identical constraint — so a blank-node rung is refused with `refusal numbers found: set()`, a refusal a reader cannot name. And `sh:path prog:blockedBy`, `prog:oracleArtifact`, `prog:oracleTest` appear **nowhere** in the file (the eleven property shapes present are `rungKey ofRung statement declaredOn source met retrospective metOn interpreter pythonVersion rdflibVersion`) — an IRI-valued `prog:blockedBy` conforms, then degrades cleanly to `M7: … is not a row in …` rather than crashing | Message quality, not admission: every case measured degrades to a *correct* outcome by a *worse* route. Raised as a row because the refusal numbers are the membrane's contract with its reader — `_refused_by_shacl` asserts an exact set of them — and an unnumbered refusal is invisible to that assertion | Numbered messages on both `sh:nodeKind` constraints, and property shapes for the three unconstrained predicates (`prog:blockedBy` → `xsd:string`, matching how M7 reads it). Worth doing **in the same change as [[R107]]**, which adds a uniqueness refusal to the same file and moves the same ten-refusal exact-set contract |
| R111 (22/101 closed) | **Three legibility defects on the shipped strip: `frontier`/`ready` carry no unit, the module docstring's illustration is stale, and a dead `else 0.0` branch would render the one thing §9 forbids** | Whole-branch review, 2026-08-22, on `scripts/cockpit.py` at `6efc032`: (1) the strip prints `frontier 15  ready 17` where 15 counts **residues** and the frontier query returns **20 edges** — a reader cannot tell which unit either number is in; (2) the docstring's WHAT-IT-SHOWS illustration predates the branch — the live strip reads `residues ▰▰▱▱▱▱▱▱ 22/98`, carries no `▸` row, and the script now reads five files, not three; (3) `frac = met/declared if declared else 0.0` — `arc()` returns `declared` either `None` or `>= 1`, so the `else` is unreachable, and were it ever reached it would render `0/0`, which spec §9 forbids outright | Cosmetic on the branch that shipped the strip, and (3) is dead code rather than a live bug. Kept off the fix commit because the strip's shape is a **taste call for the person who reads it daily** — the same reason [[R107]]'s sibling finding I-1 (the three-row composed status line in gitignored `.claude/settings.local.json`) was parked for the maintainer | Units on `frontier`/`ready` (or the count changed to match the label), the illustration re-rendered from the live strip rather than edited, and the `else` branch replaced by an assertion — a dead branch that renders a forbidden output is worth a raise, not a default. **Note the illustration will go stale again**: the honest form regenerates it, or the test that renders the strip asserts the docstring matches |
| R112 (22/102 closed) | **60 `ResourceWarning`s the suite cannot see — pytest's warning plugin never un-ignores them, so the count that is reported as health (`10 warnings`) is not the count of warnings the run raises** | 2026-08-22, closing the `arc-denominator` loop, while attributing the standing 10. Measured directly: `.venv/bin/python -m pytest tests/etkl/test_transform_gate.py tests/test_source_ownership.py -q -W "error::ResourceWarning"` → **`13 passed, 60 warnings`**, none of which appears in a default run. Cause: `_pytest/warnings.py:38-46` upgrades only `DeprecationWarning`/`PendingDeprecationWarning` to `always`; `ResourceWarning` keeps CPython's `ignore` default, so the summary line cannot show it. The emitters are ours — unguarded `open(...)` in `tests/` (20 sites by `grep -rn '= *open('` excluding `with open`; the attributing seat counted 30 including `open(...).read()` forms) — and they are 51 in `tests/etkl/test_transform_gate.py` + 9 in `tests/test_source_ownership.py`. ⚠️ **The earlier figure was wrong in both parts**: a Task 7 minor recorded *"42 unclosed files"* across **three** modules including `tests/test_doc_governance.py`, which measures **0** | The 10 warnings the branch kept reporting were the debt of record, and they are now attributed (8 rdflib `ConjunctiveGraph` deprecations from the JSON-LD parser, not ours and with the obvious `Dataset` remedy refuted; 2 deliberate `UserWarning`s from the doc-governance staleness reports). These 60 are a *different* class, no product consequence, and closing them touches 20-30 test call sites — a mechanical change with a wide diff, deliberately not folded into a loop-close fix commit | Either `with` at every site, or a `filterwarnings` entry admitting `ResourceWarning` to the default run so the number is visible before it is fixed — **the second is the honest order**, because the defect is not the leaked handles but that the reported count is not the raised count. Note the sibling fact from the same attribution: the standing **"exactly 10" is data-dependent** — it becomes 8 the moment a release drains the wiki promotion queue, so a future leg reporting 8 is not a regression and must not be read as one |
| R113 (22/103 closed) | **A5's ablation grounds consumption at FILE granularity, so an edge means "X's oracle needs this FILE", never "X needs the term at line n"** | 2026-08-22, closing `the-arc-has-edges`. The instrument deletes whole files: `_ablate` calls `target.unlink()` (`tests/test_arc_ablation.py:248`) and `_oracles` strips the `:<line>` suffix off every `prog:oracleArtifact` before ablating (`:166`), so `dec:16 prog:dependsOn dec:01` is grounded as *"consumes `dec-shapes.ttl`"* and not *"consumes the shape at line 15"*. Two live consequences, both on the record: `dec:09 -> dec:07/08/10` was **not authored at all**, because three criteria declare `vocab/shapes/iladub-shapes.ttl` (`tests/arc-manifest.ttl:699,718,752`) and file granularity offers no non-arbitrary target (`2026-08-22-arc-edges-authored.md` §1, rejected reading 6); and the asserted `holon:04 -> dec:07`/`dec:10` rests on evidence that is **three-way ambiguous** at file scope, separated by the reading and by A4, not by the ablation (same file, §6, and task 4's review Minor 2) | Not a defect — it is the spec's own **stated bound**: §4 concedes it and §8 records *"ablation stays at file granularity; Q6/A6 bound the imprecision rather than remove it"*. A6 (artifact-file disjointness between the two ends) is what keeps the bound sound rather than merely admitted, and it is enforced in the membrane (M16's A6 arm) | Ablation at LINE granularity — excise the declared range rather than the file, and re-run both arms. Two things must land first: [[R109]] (this repo has divergent `<path>:<line>` parsers, and a line-granularity ablation would need the one that does not exist yet), and an answer to what a partially-excised Turtle file should do, since a file that no longer parses fails every oracle for the wrong reason. Until then the honest form is the one shipped: assert at file scope and say so |
| R114 (22/104 closed) | **`etkl:01`'s oracle cannot EXECUTE inside a worktree, so no edge can ever be asserted at either of its ends — and it is the `etkl` rung's only met criterion** | 2026-08-22 (task 1 step 2; re-confirmed by task 3's `test_m19_reads_a_skip_that_carries_its_reason_at_any_terminal_width` and by `2026-08-22-arc-edges-authored.md` §1, rejected reading 9). `corpus/` is gitignored (`.gitignore:43-44`), so a `git worktree add --detach … HEAD` checkout never contains it; `tests/test_corpus.py:67` then calls `pytest.skip("corpus not populated: …")`. `etkl:01`'s ONLY `prog:oracleTest` is `tests/test_corpus.py::test_expected_verdict[ag-trade/graincorp-stem-2026-07-31.pdf]` (`tests/arc-manifest.ttl:158`). M19 scores a SKIP as *"did not execute — pytest reported ['SKIPPED'], which is neither a pass nor a failure and grounds nothing"* (`tests/test_arc_ablation.py:212-216`) and therefore **refuses** the edge on whichever arm ran it. Task 1's sweep found **no** oracle that FAILS in a worktree; this is the one that does not run at all | Spec §9.3 names this class of finding *"worth a residue row of its own"* rather than a blocker, and the fact is about the corpus fixture, not about the arc. Task 4 routed around it at authoring time rather than discovering it as a "cannot judge" mid-ablation, which is why the loop shipped — but the routing is a hand discipline recorded in prose, and the next author reads the manifest, not this row, unless something points them here | Either (a) M19 materialises the gitignored corpus into the worktree (copy or symlink from the main tree) so the oracle executes, or (b) a committed synthetic stand-in document the same oracle can run on. Failing both, an **A7 precondition in the membrane** — refuse an asserted edge whose end has an oracle known not to execute in a worktree — so the refusal is stated up front by the closed world instead of discovered by the procedural leg. Note this makes the entire `etkl` rung un-assertable today, which is why (a) or (b) is worth more than (c). **CORRECTED 2026-08-23 — this column argued from the gitignored corpus alone, and that was only half the blocker.** Materialising the corpus (option a) makes `etkl:01`'s oracle **run**; before 2026-08-23 it would ALSO have stayed **un-ablatable**, because `etkl:01` was the one criterion in the met set whose oracle resolves its evidence through library code the same way [[R121]] describes — ~~so the corpus half bought nothing on its own~~. **[[R121]] is now CLOSED** (Task 1, `7e4f84c`): the `PYTHONPATH` fix re-roots that resolution onto the worktree, so the ablatability half of the blocker is gone. **Two blockers remain, not three — corrected from Task 1's own read that dismissed limitation 4 on the wrong test:** (i) the corpus is still deliberately NOT materialised in any M19 worktree (Global Constraint 6 / M5 — materialising it turns the parser self-test red), so `etkl:01`'s oracle still SKIPs in every worktree this instrument builds and options (a)/(b)/(c) above remain live and undone; (ii) [[R113]] (file-granularity) now governs which edges TO or FROM `etkl:01` can be authored unambiguously once it does run — per [[R121]]'s own closure note, only `etkl:01 -> dec:06` is unambiguous at file scope; any other candidate shares its artifact with other criteria the way `dec:09 -> dec:07/08/10` already does |
| R115 (22/105 closed) | **The 80% orphan question is NOT subsumed by this loop and stays open: 65 of 80 open register rows block no criterion of any rung** | **Re-measured 2026-08-22 at loop close** (the origin handoff's "59 of 74" was quoted, never re-measured — spec §7 says so explicitly). `awk` over `docs/superpowers/residues.md` -> 102 rows, 80 open, 22 closed; rdflib over `tests/arc-manifest.ttl` -> **15** distinct residues named in `prog:blockedBy` (`R43 R44 R45 R62 R71 R74 R77 R79 R80 R83 R84 R97 R98 R99 R100`), all 15 of them open rows; **80 - 15 = 65 open rows block nothing**. **The 80 is a snapshot taken BEFORE this loop's own seven rows (R113-R119) were appended, and the register header on the same commit reads `109 rows, 22 closed, 87 open` — corrected by the final review, M-3.** Re-measured at the tree's final state: 109 index rows / 87 open (counted off `docs/superpowers/residues.md`), rdflib over `tests/arc-manifest.ttl` returns the **same 15** `prog:blockedBy` residues, and all seven new rows block no criterion — so the figure that is true of the shipped tree is **87 - 15 = 72 of 87**, not 65 of 80. Both numbers are true of different moments; the shipped-tree one is 72/87. Spec §7 is the argument that this loop does not touch it: a **criterion -> criterion** edge gives a *residue* nowhere new to attach, `prog:blockedBy` is unchanged, and `vocab/queries/arc-orphan.rq` returns the same set the day this shipped as it did before | Out of scope by spec §8's first bullet — *"the register is not mirrored into the graph"* — and the work is a triage judgement about 65 rows, not a code change. What the graph did buy is different and is worth having on its own: `arc-reach.rq` gives a blocker a **ranked consequence** instead of a count (R44 and R62 gate five unmet criteria each; R97-R100 one each). Recorded here rather than left for the next handoff to inherit as settled, which is what spec §7 asks for | A triage pass over the 65: each row either gains a criterion that names it in `prog:blockedBy` (attaching it to a rung) or is re-filed / closed as work no stated goal requires. [[R101]] was the first measured instance, 2026-08-21, and is still open. Two cautions for whoever runs it: [[R108]] means a `prog:blockedBy` naming a **closed** row is admitted, so 65 is a **lower bound** on mis-filing; and "blocks no criterion" is a finding about the arc's coverage as much as about the row — spec 2026-08-20 §7.4 already called the first instance *"a finding, not a gap"* |
| R116 (22/106 closed) | **M20 candidate: an ORPHAN `rdf:Statement` rationale node, and a `prog:dependencyRationale` hung directly on a criterion, are refused by nothing — the membrane permits dead prose** | Task 2's review, Minor 4 (2026-08-22), **re-measured at close on `19ba03f`**: appending to a copy of `tests/arc-manifest.ttl` BOTH an `rdf:Statement` node whose `rdf:subject`/`rdf:predicate`/`rdf:object` name a triple that is **not in the graph**, AND a bare `prog:criterion:tab:07 prog:dependencyRationale "hung directly on a criterion"`, then running `validate_manifest(copy)` -> **`Conforms: True`, refusal numbers `set()`**. M18 states *exactly one rationale per proposition* from the **proposition's** side (a proposition with none, or with two, is refused); nothing states the converse from the **rationale node's** side, and nothing constrains where `prog:dependencyRationale` may appear | Controller ruling at Task 2's review: it permits **dead prose only**. No edge can go falsely green through it — a rationale naming no edge grades no edge, and M17 still refuses a proposition whose ends are groundable — so it fails the bar for reopening a task already reviewed green. Deferred, not dismissed | **M20** in `tests/arc-shapes.ttl`, two arms and a fixture each (the file's own exactly-one-refusal discipline): every node carrying `prog:dependencyRationale` that is an `rdf:Statement` must have its (subject, predicate, object) present as a **direct triple** and its `rdf:predicate` be `prog:proposedDependsOn`; and `prog:dependencyRationale` must not appear on a `prog:Criterion`. Worth doing **in the same change as [[R110]]** — numbered messages and missing property shapes in the same file, and the same ten-refusal exact-set contract in `_refused_by_shacl` |
| R119 (22/109 closed) | **M19's progress-line parser silently assumes this repo's pytest config adds no options — a future `addopts` (xdist, colour) makes every ablation raise, with a stack trace that says nothing about `addopts`** | Task 3's review, deferred finding, **re-measured at close**: `pyproject.toml`'s `[tool.pytest.ini_options]` (`:96-101`) carries `testpaths`, `pythonpath` and `markers` and **no `addopts`**, and `_PROGRESS` (`tests/test_arc_ablation.py:142-143`) matches exactly pytest's plain `-v` rendering, `^<nodeid> <OUTCOME>[ (reason)]\s+[ nn%]$`. Under `pytest-xdist` the progress line gains a `[gw0]` prefix; under forced colour it gains ANSI escapes; either makes the pattern miss every line. The subprocess runs with `cwd` **inside the worktree** (`:255`), so the assumption is on the **committed** config at HEAD, not on a developer's shell — which is the good half of the news | It **fails loudly and never falsely**: an unmatched line leaves the requested id with no outcome, and `_scores` RAISES with the full transcript (`:219-226`) rather than scoring it — the same refusal that catches task 1's combined-invocation instrument defect. So the cost is a confusing red, not a wrong edge. Undeclared, though, and the change most likely to trip it is the one this branch makes attractive: an `addopts = "-n auto"` to cut the ~36-minute suite | Take the rendering out of the repo's hands: pass `-o addopts=` (or `-p no:xdist --color=no`) to the ablation subprocess so the module chooses its own output shape, and state the assumption in the module docstring beside the `-v`-versus-`-rA` argument already there. A test that runs one ablation with `addopts` forced non-empty and asserts it still resolves would pin it |
| R120 (22/110 closed) | **A load-bearing measurement CITED IN SHIPPED SOURCE is not reproducible from repo state: Task 1's `44` blast-radius pairs and its `18` A6-disjoint subset live only in an uncommitted scratchpad** | Raised by the final whole-branch review (2026-08-22, triage T-1) after the close handoff's *Unverified or assumed* §1 deferred it here. The two figures are quoted as fact in **shipped source** — `tests/test_arc_ablation.py`'s module docstring, stated limitation 2: *"Task 1 measured **44** ordered cross-criterion blast-radius pairs in the met set, of which **18** share no declared `prog:oracleArtifact` file at all"* — and limitation 2 is what bounds the reading an arm-1 failure supports, which Task 4 used when grading every edge asserted-vs-proposed. The raw 17x25 outcome matrix and the scratch scripts that produced both numbers were never committed (`docs/superpowers/2026-08-22-worktree-oracle-seam.md` reports the conclusions, not the data). **What IS reproducible from the tree, re-measured at this fix wave with rdflib over `tests/arc-manifest.ttl`:** the denominator — 17 met criteria, so `17 x 16 = ` **272** ordered pairs — and the shape of the disjointness relation (246 of the 272 ordered met-pairs share no declared `prog:oracleArtifact` file, of which the 18 are the subset that also fell in the 44). **The 44 and the 18 themselves are not**: they need the per-pair ablation outcomes, which only a run produces. **A SECOND, INDEPENDENT INSTANCE OF THIS ROW'S OWN CLASS, found and corrected 2026-08-23 (Task 5, `the-worktree-that-resolves`, M7):** the SAME module docstring also stated, at its live-manifest test, *"MEASURED 2026-08-22 over those 6 edges: 9 endpoint criteria, 6.69 s"* — a second load-bearing number cited in shipped source that did not reproduce. Re-derived with `asserted_edges`/`_oracles` over the live manifest: **8** endpoint criteria (the `9` was the *seven*-edge figure, counting `holon:02` after the same docstring says its edge was deleted), a union of **10** oracle ids across **4** modules, corrected in `tests/test_arc_ablation.py`'s `test_m19_the_live_manifest_carries_no_refuted_edge`. **This is evidence R120's class is not a one-off**: two unreproducible load-bearing numbers, in the same docstring, found on two different days by two different reviews | Deferred, not disputed: Task 1's reviewer spot-checked the citations underlying the 18-pair claim (`tests/test_escalation_shacl.py::test_escalation_conformant_passes` loading `dec-shapes.ttl` beside `escalation-shapes.ttl`; `tests/test_hga_alignment.py` building a graph wider than any one criterion's artifact) and they held, so nothing suggests the figures are **wrong** — the defect is that they cannot be **re-derived**, which is exactly what CLAUDE.md § Plan authoring rule 2 requires of a load-bearing claim (*"every load-bearing claim about existing code is MEASURED, and carries its measurement inline"*). Recording it in a handoff's *Unverified* list was not enough: CLAUDE.md § Deferred residues says the register is where deferrals live, and a handoff is read once while the docstring citing the number ships forever | Commit the census as a script (a `--census` mode on the ablation module, or `scripts/`), so `44` and `18` regenerate on demand and the docstring can cite a command instead of a memory; then re-run it and either confirm both figures or correct the docstring against the new output. **Pairs with [[R113]]** (the file-granularity bound that limitation 2 qualifies) and with [[R118]]/[[R119]] (the other two instrument limits measured in the same task). Cheaper interim that closes most of the risk: state **in the docstring** that the two numbers were measured once, on 2026-08-22, from data that did not ship — so a reader knows they are a citation and not a re-derivation |
| R122 (24/111 closed) | **After §4.1 re-roots `src/` onto the worktree, is ANY oracle still resolving its evidence to the main tree? This loop declines to ask, and ships no hermeticity machinery — permanent or one-shot — that could answer it** | **Nothing is measured here, and that absence IS the row.** What *is* measured: spec §2.2/§2.3 inventoried exactly **two** resolution styles and closed both — (i) `import iladub…`, pinned to the main tree by the plain-path editable install `.venv/lib/python3.12/site-packages/_editable_impl_iladub.pth` and now outranked by `_run_module`'s `PYTHONPATH=<worktree>/src` prefix (Task 1, `7e4f84c`); (ii) the `__file__`/`parents[N]` helpers — `src/iladub/etkl/compile.py:374-383` (`_repo_vocab()`) and eight `parents[3]` siblings such as `src/iladub/etkl/gridregion.py:29-31` — re-rooted by the same change. Both are proved ablation-**sensitive**, not merely resolvable, by `test_m19_resolves_the_library_into_the_worktree_it_ablates`: leg 1 the probe PASSES un-ablated, leg 2 it FAILS once `vocab/queries/grid-region.rq` is deleted inside that same worktree. A **third** style nobody thought to name is not ruled out, and no instrument in this repo would notice one. Task 6's live run does **not** cover it: `ablation_refusals(MANIFEST)` → `[]` with 13 `_scores` invocations across 4 modules and **0** collection ERRORs proves the six asserted edges survive, not that the worktree is hermetic | Spec §9's final bullet declines it explicitly: an audit-hook probe was scoped out as **not worth the loop, its expected finding being empty**. Deferring is legitimate; leaving it unrecorded is not (CLAUDE.md § Deferred residues), and it is declared in shipped source as well — `tests/test_arc_ablation.py` limitation 4's closing paragraph states that only the two styles above were measured and that F3 therefore stays **declared, not measured**, forward-referencing this row. Note the direction of the residual risk is the dangerous one: an oracle still reading the main tree passes arm 2 on evidence that was never removed, which is [[R121]]'s own FALSE-GREEN direction, not the safe spurious-refutation one | A **one-shot audit** that, inside an ablated worktree, records every path the endpoint oracles actually open — an `audit` hook on `open`/`io.open` in the subprocess, or `fs_usage`/`strace` around it — and refuses any path resolving outside the worktree root. One run over the union of endpoint oracle ids turns F3 from declared into measured; if it finds nothing, limitation 4 is **retired** rather than corrected a third time. Cheaper adjacent probe that closes most of the risk: have each control subprocess assert `iladub.__file__` and `sys.path[0]` lie under its own `cwd`. **Do not close this by reasoning** — the two styles above were also "obviously all of them" until spec §2.4 measured the census that dismissed them and found it wrong twice over |
| R123 (24/111 closed) | **A declared `prog:oracleArtifact` that is a PYTHON MODULE is genuine consumption the §4.5 rule refuses to score — it RAISES. And the census that dismissed this as hypothetical is wrong: 2 of the 29 declared artifacts are `.py` TODAY** | Raised by Task 4 (`5f2cad9`) as finding 2, on the reasoning that an import break names the **dotted module** and never the path, so §4.5's name-present test cannot match. Task 4 dismissed it as not live — *"all 29 declared artifacts are `.ttl`/`.rq`/`.md`-class data files, zero are `.py`"*. **That census is REFUTED, measured 2026-08-23 (Task 6) over the live manifest with `tests/test_arc_manifest.py`'s `MANIFEST`/`_LINE_SUFFIX`:** 29 distinct files across 48 triples, by suffix **`.ttl` 27, `.py` 2** — `tests/etkl/fixtures.py` (declared by `prog:criterion:tab:06` as `tests/etkl/fixtures.py:726`) and `tests/etkl/test_vacuity_registry.py` (declared by `prog:criterion:tab:10` as `:87`). `tests/etkl/fixtures.py` is imported by **36** test modules (counted with `grep -rl "etkl.fixtures" --include="*.py" tests/`), and at **module scope** by `tests/etkl/test_closing_slice.py:5`, which is the oracle module of both `tab:01` and `tab:03`. Measured end-to-end through the shipped instrument, no fixture and no manifest edit — `_ablate(["tests/etkl/fixtures.py"], ["tests/etkl/test_closing_slice.py::test_multi_table_ambiguous_escalates"])` → **`RuntimeError: M19 instrument failure: tests/etkl/test_closing_slice.py failed to COLLECT, and its exception names none of the removed artifacts ['tests/etkl/fixtures.py']`**, the exception being `ModuleNotFoundError: No module named 'tests.etkl.fixtures'` — the dotted name, never the path. The contrast is measured too: `tests/etkl/test_tiling_gate.py:55` imports the same file *inside a function*, so removing it scores an ordinary `failed`; the limitation bites **only** on a module-scope import | **Latent, not live, and the direction is safe.** Neither `tab:06` nor `tab:10` is an endpoint of any of the 6 asserted edges (measured — the 8 endpoints are `dec:01`, `dec:06`, `dec:07`, `dec:10`, `dec:16`, `holon:01`, `holon:03`, `holon:04`), so no shipped edge is scored through this path today. When it does bite it raises loudly rather than admitting silently (Global Constraint 5 / spec §5: instrument failure, never a refusal, never a false green) — the same disposition `_scores` gives an unresolved node id. Fixing it now would also mean widening a rule in the same loop that shipped it, and this loop asserts no new edge (spec §8 item 2), so nothing depends on the widening | Teach §4.5 a **second evidence-positive form**, not a looser one: for each removed path ending `.py`, derive its dotted module name from the repo-relative path (`tests/etkl/fixtures.py` → `tests.etkl.fixtures`) and accept an exception naming **that**. The discipline is unchanged — a name PRESENT, never consumption inferred from absence (CLAUDE.md §8, the open-world half) — and the falsifying oracle already exists as a live pair: `tab:06`'s declared artifact removed against `tests/etkl/test_closing_slice.py`'s oracle must score `FAILED`, while an unrelated removal against the same oracle must still raise. Do it **before** any loop authors an edge with `tab:06`, `tab:10` or any future `.py`-declaring criterion as an endpoint; until then, an author who hits the raise should read it as this row and not as a broken checkout. **Compounds with limitation 4 and [[R113]]**: at file granularity, deleting `tests/etkl/fixtures.py` blasts 36 modules at once, so even once the name matches, the arm-1 signal it produces is coarse |
| R124 (24/113 closed) | **Spec §4.4's stated mechanism for why `_refuse_materialisation_collision` must exist is superseded by the code's shipped ordering: no pre-empted deletion is possible, so no false green arises from that path** | Measured 2026-08-23 (final whole-branch review fix wave, `the-worktree-that-resolves`) in `tests/test_arc_ablation.py`'s `_ablate`: `_materialise(wt, repo)` runs at `:476`, and the `removed_files` unlink loop runs immediately after at `:477-485` — materialisation happens strictly BEFORE deletion, so a deletion of a materialised path is not pre-empted; the reviewer's own empirical check (materialise → `exists: True`, unlink → `exists: False`, main-tree copy still present) confirms it directly. Spec §4.4 (`docs/superpowers/specs/2026-08-23-the-worktree-that-resolves-design.md`) instead argues the guard exists because `_materialise` would restore a deleted file "straight after `git worktree add`, BEFORE `removed_files` is ever deleted" — the opposite ordering from what ships. The guard is still correct to exist, for a different reason: `baml_client/` is gitignored, so *without* materialisation `_ablate`'s committed-tree check (`target.exists()` after `git worktree add --detach HEAD`) would correctly refuse any artifact under it as absent from a worktree checked out at HEAD; materialisation instead smuggles that uncommitted, gitignored file past the very check that exists to enforce M19's committed-tree invariant | `docs/superpowers/**` is Evidence class and immutable after loop close (CLAUDE.md § Documentation governance), so the spec itself cannot be corrected — the four prose sites that repeated its claim (`_refuse_materialisation_collision`'s docstring, its raise message, `test_m19_refuses_an_artifact_that_materialisation_would_restore`'s docstring, and `tests/arc-m19-materialised-artifact.ttl`'s fixture comment) were rewritten to the measured ordering and the real (gitignored-past-committed-tree) hazard in this same fix wave, but the spec text itself stands uncorrected and this row is where the correction is recorded instead | The next spec that restates §4.4's argument does so from the measured ordering (materialise-before-unlink) and the gitignored/committed-tree hazard, not the pre-emption claim; no code change is implied — the guard's *behaviour* was always correct, only its stated *reason* was wrong |
| R125 (24/114 closed) | **Nothing constrains the shape of a `prog:oracleArtifact` path — a Global Constraint 2 exposure, pre-existing and not introduced by this branch** | Measured 2026-08-23 (final whole-branch review fix wave): `tests/arc-shapes.ttl` carries no `sh:pattern` (nor any other path-shape constraint) on `prog:oracleArtifact`. `_ablate` (`tests/test_arc_ablation.py`, around `:478`) builds `target = wt / f` from each declared artifact string and then calls `target.unlink()` with no containment check — a criterion declaring `../../x` or an absolute path would resolve `target` outside the worktree root and `unlink()` would mutate the REAL tree, violating the documented safety property ("the real tree is never mutated", `_ablate`'s own docstring). As context, not the same defect: `_scores` (around `:419`) tests `if not any(f in exception_text for f in removed)` — bare substring containment with no separator anchor — so a removed `a/b.ttl` also matches an exception naming `a/b.ttl.bak` or `x/a/b.ttl`; this errs permissive (a false ablation signal), not unsafe, unlike the path-traversal exposure above | Pre-existing in every prior M19 loop, not this branch's introduction — no manifest criterion today declares a `..`-relative or absolute `prog:oracleArtifact` (the live manifest's 29 declared artifacts are all repo-relative, no `..` segment), so the exposure is latent, not live; the disjointness guard `_refuse_materialisation_collision` is the natural home for a `..`/absolute refusal, and adding it now would be scope creep on a fix-wave that ships prose and register rows only | A `sh:pattern` on `prog:oracleArtifact` in `tests/arc-shapes.ttl` refusing `..` segments and absolute paths, paired with a producer-side `..`/absolute check in `_refuse_materialisation_collision` (CLAUDE.md § Producer-side guards vs the membrane — belt AND suspenders, the membrane closes the graph, the guard fails fast at the call site). Separately, anchoring `_scores`' substring match on a path separator (or exact-segment comparison) closes the residual false-positive noted above |
| R127 (25/117 closed) | **`dec:rationale` has NO cardinality constraint while `dec:EventShape` caps `dec:condition` at 1, so the day an escalating decision carries a second rationale — e.g. the `de`/`fr` language pair CLAUDE.md § Serialization explicitly PERMITS — every document containing it refuses at document scope, with a message naming neither `dec:rationale` nor the language tag.** ⚠️ **THIS ROW IS COUPLED TO A SHIPPED ORACLE AND MUST NOT BE CLOSED ALONE.** | Measured 2026-08-25, `docs/superpowers/2026-08-25-holon-05-seam-6-refusal-vehicle.md`, and re-verified in this task from the tree: `vocab/queries/escalation-furnish.rq` binds `?req dec:condition ?why` from `?d dec:rationale ?why`; `vocab/shapes/dec-shapes.ttl:60-63` caps `dec:condition` at `sh:maxCount 1`; nothing caps `dec:rationale` anywhere. The lever is one added triple on a NON-SUPERSEDED escalated decision and it refuses with `legs == ('dec',)` — confirmed end-to-end on `apple-fy2026q3-statements` (`Weakened` → `Compromised`) and on the 1.1 s `false_transposed` fixture | **Deliberately NOT fixed, and the reason is the coupling, not cost** (spec §9, ruled 2026-08-25): it is the ONLY measured route into a document-scope refusal. Every tab-side lever refuses at the PAGE gate (`compile.py:1173`) before document validation is reached, and no compile can mint a second `dec:rationale` (`BandRecorder.record` writes exactly one; the four decision-URI namespaces are disjoint — measured by Task 4 and recorded in `test_membrane_health.py:426-429`). **THE FOUR COUPLED TESTS, BY NAME** — all in `tests/etkl/test_membrane_health.py`: `test_re_entering_the_seam_leaves_exactly_one_conformance_value` (`:182`, lever inline at `:215`), `test_re_entering_the_seam_leaves_exactly_one_health_value` (`:362`, lever at `:387`), `test_compromised_is_reachable_by_the_r127_lever_on_a_real_graph` (`:422`, lever at `:460`), `test_the_refusal_carries_the_graph` (`:552`, lever at `:578`). **THE CRITERION IS "APPLIES THE LEVER AND ASSERTS A REFUSAL", NOT "CITES R127"** — and the two sets differ, which is why the criterion is stated: R127 is cited on 10 docstring lines plus `src/iladub/etkl/document.py:1309` (11 sites), and those citations also sit in the `apple_report` fixture (`:51`) and the `_one_more_rationale` helper (`:77`), neither of which is a test. `test_intact_and_weakened_are_reachable_on_real_input` (`:401`) USES `apple_report` but never applies the lever and only reads the graph, so it is **not** coupled and is deliberately excluded. Each of the four wraps `_seal` in `pytest.raises` after adding the second rationale; remove the lever and each fails with DID NOT RAISE | The furnish collapses multiple rationales, **or** `dec:rationale` is capped, **or** `dec:EventShape` admits one condition per language — **and, IN THE SAME ACT, the four tests above are re-homed onto whatever lever replaces it.** Closing this row without that turns four shipped oracles red for an invisible reason: the failure reads DID NOT RAISE and nothing in the traceback names this row. **Do NOT delete the tests** — the invariants they pin (the mint replaces; the refusal carries the graph) are untouched by this row's closure |
| R130 (24/116 closed) | **`holon:05` §4.9's `(query, term)` vacuity registry ships its REVERSE arm only; its FORWARD arm — the population enumerator — is UNBUILT, and that is this residue.** SHIPPED (`tests/etkl/test_vacuity_registry.py::test_no_registered_query_term_has_gone_live`): the arm that fails the suite the day `membrane-health.rq`'s promotion clause goes live on the corpus, forcing DE-REGISTRATION rather than leaving the residue to a human's memory. It needs no population ENUMERATOR — like its shipped analogue `test_no_registered_shape_has_gone_live` (`:337`) its ROWS are hand-typed, so nothing has to enumerate the `.rq` files; it does still consume the corpus graphs (and the shape analogue additionally calls `shapes_graph()` -> `wired_shape_files()` (`:133-140`) to look its rows' shapes up), and an earlier wording of this row said "no population at all", which is measurably false — and its two rows name terms of a query MEASURED to run over the compiled document graph, so neither can be a category error. **NOT SHIPPED:** the forward arm, *"every idle query is registered"*, which needs a population the way `test_every_idle_shape_is_registered` (`:325`) needs `wired_shape_files` (`:133-140`). The consequence, stated rather than left to be discovered: a SECOND query that goes vacuous is caught by nothing, and `membrane-health.rq`'s clause is guarded only because a human typed its two terms into the dict. **NUMBERING, read before renumbering:** the loop's controller ruled this row `R128` on the ground that the register topped out at R126 and `R127` was a reserved label (10 committed citations for the uncapped `dec:rationale`). `R128` and `R129` are reserved the *same* way — spec §11 items 7 and 8 allocate them to *"`dec:supersedes` is constrained by nothing"* and *"a non-IRI `suggester_iri` crashes the membrane rather than refusing"*, cited in 6 committed places and raised by Task 7 — so this row takes **`R130`**, the first number the spec does not allocate. **The controller overturned its own `R128` instruction at review round 1 and this substitution now stands as the ruling.** The spec's closing line *"The next number after this loop is `R130`"* is superseded by this row: it is **`R131`** | 2026-08-25, Task 6 Step 1, at `f1a92f5`. **REPRODUCIBILITY, and it is partial — this row is [[R120]]'s class and says so rather than pretending otherwise.** The three commands below rebuild the population; the **164** and the **162** came from a sweep script that ran ONCE, on 2026-08-25, in a session-scoped scratchpad and **did not ship** (a committed census module was ruled scope growth for this loop). A maintainer verifying the `164 -> 3` closure criterion must re-derive it from the recipe here, not re-run a committed script. **(i) The corpus side** is the fixture body of `tests/etkl/test_vacuity_registry.py::corpus_graphs` run verbatim for all seven documents — `compile_document(path)` then `membrane.subclass_closure(rep.graph, compile_mod._FULL_ONT)` — giving 13079/6135/32141/4081/8823/11856/8720 triples and 61/47/67/84/68/59/68 `vocabulary_of` terms. **(ii) The query side** is `_TERM` (`:63`) + `_PREFIX_NS` (`:58-62`) over the COMMENT-STRIPPED file text, minus `vocabulary_of` (`:178-181`) unioned over the seven graphs; a term is reachable if present in ANY graph, mirroring `idle_shapes`' intersection at `:203-204`. **THE COMMANDS.** `ls vocab/queries/*.rq \| wc -l` -> **46** (45 before Task 3 added `membrane-health.rq`). `grep -rhoE '[A-Za-z0-9_.-]+\.rq' src/ \| sort -u \| wc -l` -> **33** distinct names. `grep -rnoE '[A-Za-z0-9_.-]+\.rq' src/` gives each name's sites, from which: `repeats-row.rq` names no file on disk ([[R34]]'s prospective derivation, prose-only at `src/iladub/etkl/ruledroles.py:28`) -> **32** exist, and exactly **3** are named only by `federate.py` (`federate-projection.rq`, `federate-projection-governed.rq`, `compile-f-grants.rq`), which runs over the grounded interior ∪ terms -> **population 29**. **WHAT "29" IS, precisely:** a TEXTUAL-MENTION population. The grep matches every `.rq` string anywhere in a `.py` file, comments and docstrings included, so a query merely *discussed* counts (`feed.py:13`, `rowgroups.py:5-16`) — which is exactly how the non-existent `repeats-row.rq` surfaced and had to be hand-excluded. The bias can only INFLATE the population and therefore only strengthens the conclusion below; it is recorded because the number is load-bearing, not because it changes the verdict. **THE RESULTS.** Over that population: **164 (query, term) unreachable pairs across 24 of the 29 queries** (worst: `unpivot-forward.rq` 12, `strip-aggregation-forward-sum.rq` and `unpivot-inverse.rq` 11 each, `header-row-role.rq` 10). Two are the registered pair. Without comment stripping the figure is **173**, which is why stripping is mandatory. **THE PREMISE, re-measured in every position:** `iladub:PromotionDecision` and `iladub:reviews` appear in NO subject, predicate or object of ANY of the seven graphs; `iladub:CandidateConcept` is present in 3 of 7 (apple, bfs, who-wfa), so the clause's other half IS live. **THE CATEGORY ERROR — 162 of the 164, and INDEPENDENTLY CORROBORATED at review round 1** by an enumeration of `interpret.run(` across `src/`: those queries never run over a compiled document graph but over transient `urn:iladub:evidence:` graphs built per band / per pair / per recipe (`boundary.boundary_evidence`, `classifygraph`, `headergraph`, `gridregion.grid_evidence`, `unitmarker.marker_evidence`, the `reshape`/`oracle` recipe graphs). Only **3** of the 29 execute against the graph `compile_document` returns: `membrane-health.rq` (`document.py:1324`), `escalation-furnish.rq` (`document.py:1229`), `adoption-candidate.rq` (`adoption.py:105`). **EVEN THAT NARROWED POPULATION OF 3 YIELDS 3 ROWS, NOT 2:** `escalation-furnish.rq` -> `risk:Breach` is a FALSE POSITIVE — `risk:Breach` IS in the apple, bfs and who-wfa graphs, as the object of `dec:constrainedBy`, a position `vocabulary_of` (predicates ∪ `rdf:type` objects) cannot see. Same class: `tab:Text` and `tab:Quantity` are in apple and ons and are likewise reported unreachable. **The two REGISTERED terms are immune to that blind spot** — one sits in `rdf:type`-object position, one is a predicate — which is why the shipped arm registers no false positive | The FORWARD arm only, and for two reasons, the second decisive. **(1)** 164 rows cannot each be given a MEASURED prose reason inside one task. **(2)** 162 of them MUST NOT be given one at all: registering them would record a category error as vacuity, the failure `tests/etkl/test_vacuity_registry.py`'s own docstring exists to prevent (*"Registering them would have recorded CONFORMANCE as VACUITY, which is the opposite of this guard's purpose"*) and which spec §2.8 warns of by name. Both escapes are out of scope, **and their sources are different — stated precisely because an earlier draft of this row over-attributed both to §4.9.** (a) Narrowing the population to the three compile-graph queries means hand-typing three filenames; the objection to that is the **module's own convention**, `wired_shape_files`' docstring (`:133-140`) — *"A list retyped here would pin this test's own copy rather than the membrane's"* — **not** anything §4.9 says, since §4.9 is silent on where the population comes from. (b) Fixing the `risk:Breach` false positive means widening `vocabulary_of`, and **that** half IS §4.9's, which lists the function among what is reused *unchanged*. **And the branch does not rest on §4.9's LITERAL fallback trigger**, which was measured NOT to hold: §4.9 triggers the fallback when the extension cannot avoid touching `shapes_graph`/`node_shapes`/`focus_nodes`/`body_terms`, and it can — none of the four is used by what shipped. The branch rests on the **substituted criterion the task brief states**: *can every row be given a measured prose reason within this task*. It cannot, for 164 rows of which 162 would be wrong to write. §4.9's rationale still applies verbatim: *"Rewriting the existing machinery is out of scope for a loop already minting three terms and a shape."* The REVERSE arm was ruled back IN at review round 1, on the measurement that it needs no population ENUMERATOR (its rows are hand-typed; it does consume the corpus graphs) — and it shipped | Two independent pieces, either order; together they let the FORWARD arm ship. **(a) Graph provenance per query, so the population can be READ rather than typed.** A query is comparable to the compiled graph only if it is executed against it, and nothing in the repo records that today. The cheapest honest form is a declaration beside each call site — or in each `.rq`'s `# GATE` header — naming the graph it runs over, plus a test that the declaration matches the call site; after which the enumerator is *the queries declared to run over the compile graph* and the pair count falls from **164 to 3** (re-derive both from the recipe in the Measured column; no committed script produces them). **(b) `vocabulary_of` widened to every IRI position** (or a second criterion beside it), removing the `risk:Breach` / `tab:Text` / `tab:Quantity` false positives and taking the narrowed count **3 to 2** — a change to make with a corpus run in hand, since it may also move the nine shipped SHACL rows, and that must be re-measured rather than reasoned about. With both, the forward arm ships beside the reverse arm already in the file, and §4.9's registry is whole. **Closing this row does NOT mean deleting the shipped arm** — that arm's own closure is the corpus growing a promotion, at which point it fires and its two rows are de-registered |
| R131 (25/120 closed) | **PARTIAL 2026-08-30 — HALF (a) IS DONE; THE ROW STAYS OPEN ON HALF (b), AND THAT IS THE ROW'S OWN INSTRUCTION** (*"the two halves are separable: raising the subclass is cheap … minting page health is the modelling change"*). **(a) DONE** by loop `the-membrane-returns-a-verdict`: the page-scope site now raises `membrane.MembraneRefusal`, not a bare `AssertionError`, so ONE `except` clause sees both scopes. `str(exc)` is byte-identical and every interceptor in the repo is isinstance-based, so the change is transparent. `graph` and `legs` travel with it. Oracles: `tests/etkl/test_page_scope_refusal.py` (3 tests). **FALSIFICATION:** reverting the raise → **2 failed, 1 passed** (the third pins the message, which is unchanged by design); restored → **3 passed**. **THE SEAM THE 2026-08-29 HANDOFF ORDERED MEASURED FIRST IS MEASURED, AND THE ANSWER IS NO.** *Can a tracked corpus document produce a page-scope refusal at all?* It cannot: wrapping `compile._validate` and `document._validate` separately (distinct bindings — `document.py` holds its own import-time reference — so the two scopes partition exactly, confirmed by a caller-frame histogram showing only the two call sites) over all 7 tracked documents gives **14 page-scope calls, 0 with `conforms=False`**, and **7 doc-scope calls, 0 refusing**. 14 rather than one per page because the raise site is guarded by `validate_shapes and (any tab:RecordTable or any tab:HierarchicalTable)`. **So the oracle INJECTS its refusal, and that is a weaker claim than a driven one** — it pins the TYPE and PAYLOAD of the raise, not that any real input reaches it; a regression making the page gate unreachable would not fail it. Said in the test's own docstring, not only here. **(b) NOT DONE:** page-scope membrane HEALTH is unminted; the graph these refusals carry has no health triple. `holon:06`'s. ORIGINAL ROW FOLLOWS. — **A PAGE-scope refusal PREEMPTS the document-scope one, so `etkl:Compromised` reports document-scope refusals ONLY.** A page-level violation aborts the whole document before `_seal` ever runs, and escapes as a bare `AssertionError` carrying no health signal at all — the document that was least clean is the one that reports nothing | `src/iladub/etkl/compile.py:1173` — `raise AssertionError(_refusal_message("asserted holon", legs, text))`, inside `compile_tables`, on the page graph; re-read 2026-08-25 at this task's HEAD. It is a bare `AssertionError`, **not** the `MembraneRefusal` subclass Task 2 introduced for the document seam, so a caller cannot even distinguish it by type. The asymmetry is stated in spec §4.5 rather than discovered | Spec §9 rules page-scope health OUT of `holon:05` explicitly: this loop does not give page-scope graphs health, does not mint at `compile.py:1173`, and does not touch the page-scope skip guard (§2.6). Closing it is not a bug fix but a modelling decision — **is a page graph a holon with its own membrane health?** — and that question is `holon:06`'s | The page-scope site either mints health of its own **or** raises `MembraneRefusal` too, and the decision above is taken and written down first. Note the two halves are separable: raising the subclass is cheap and would let one `except` clause see both scopes; minting page health is the modelling change |
| R132 (25/121 closed) | **Every compiled document shares ONE document IRI.** `_DOC` is a module constant, `compile_document` takes no `doc_uri`, and so the health subject is literally the same node for every document ever compiled — and it carries no link to the `…/doc/p{n}` page URIs under which all the content actually sits | `src/iladub/etkl/compile.py:22` — `_DOC = URIRef("https://example.org/etkl/doc")`. `src/iladub/etkl/document.py:1335-1336` — `def compile_document(pdf_path, validate_shapes=True, span_proposer=None, row_role_proposer=None)`, no `doc_uri` parameter. **BLAST RADIUS RE-MEASURED 2026-08-25 and the spec's figure does NOT reproduce:** spec §11 item 2 says *"5 files hardcode the literal"*; `git grep -l "https://example.org/etkl/doc" \| grep -v '^docs/'` returns **6** — `demo/etkl_1a_showcase.ipynb`, `scripts/measure_dec_membrane.py`, `src/iladub/etkl/compile.py`, `tests/etkl/test_adoption_gate.py`, `tests/etkl/test_holon.py`, `tests/etkl/test_unit_marker.py` (14 including `docs/`) | Ruled out of `holon:05` because it is a **signature change to `compile_document`** and Global Constraint 8 of this loop forbids one: the public signature does not change. It is also a URI-IDENTITY change, not a rename — two graphs merged today would silently unify their health values | `compile_document` threads a `doc_uri` (defaulted, so the signature stays compatible) and the health subject is minted from it. The blast radius is small and now enumerated, but the change must be taken as an identity decision with the merge semantics stated, not as a refactor |
| R134 (25/123 closed) | **The grounding portal has no health signal.** `etkl:MembraneHealth` now attaches to a compile-scope holon, and NOT to the portal — so the boundary the whole holonic model calls the governed crossing is the one boundary with no cleanliness report | `src/iladub/feed.py:642-643` — `if validate_shapes: conforms, report = _validate_grounding(g); assert conforms, f"grounded graph violates the promotion membrane:\n{report}"`. Two facts, re-measured 2026-08-25: it is a bare `assert`, which **`python -O` erases entirely**, so under `-O` the portal has neither health nor a guard; and `etkl:GroundingPortal` is instantiated **nowhere** — `git grep -n "GroundingPortal" -- '*.py' '*.rq'` returns exactly one hit, `tests/test_hga_alignment.py:44`, and that is a vocabulary-alignment table entry, not an instance | Spec §9: the portal guards a DIFFERENT graph behind a DIFFERENT boundary (§4.4), and a health value on it would mean something else — what is held at the grounding portal is a candidate concept awaiting a promotion decision, not a proposition inside a compiled document. Giving both the same term without saying how they differ would be worse than leaving one absent | **Closes with `holon:06`** — the Raw→portal→Clean traversal names the portal holon, and health follows the holon. Two things must land together: an instantiated `etkl:GroundingPortal`, and the `assert` at `feed.py:643` becoming a real membrane verdict that `-O` cannot erase |
| R136 (25/125 closed) | **A criterion can read `prog:met true` while its own `prog:source` points into the *"Planned work (not done yet)"* list, and NOTHING in the tree refuses it.** M10 asks only whether the `<path>:<line>` RESOLVES — does the path exist, is the line in range — and its own docstring says it *"must never grow into a claim about what the cited line says"* (`tests/test_arc_manifest.py:31-37`). So the pointer and the claim can contradict each other in the same file | Found by this task's OWN falsification round, 2026-08-25, and by measurement rather than by reading. With `holon:05` flipped to `prog:met true` and its `prog:source` re-pointed to `docs/holonic-interaction.md:157-161`, the doc edit was reverted (`git checkout docs/holonic-interaction.md`) so that `:157-161` once again read `## Planned work (not done yet)` followed by the un-moved bullet — and `./.venv/bin/python -m pytest -q tests/test_arc_manifest.py tests/test_doc_governance.py` returned **31 passed**. The line range still resolved, so M10 was satisfied by a pointer into the list of work that is NOT done | Not fixed here because the fix is a NEW ORACLE and this task is the loop's record, not a build task. It is also a **narrower** question than R105 (which closed as "a line pointer drifts as a file grows"): this one is not about drift at all — the pointer is exactly right and the CLAIM is wrong | Something joins a met criterion's `prog:source` to the heading it falls under and refuses a met criterion sourced from a "not done" section — a **strong** pointer check of the kind `test_etkl_criterion_sources_point_at_the_document_they_name` already performs for the `etkl` rung, which is the only rung with a second graph to join against. Note the general form is hard: `dec`, `holon`, `substrate` and `tab` cite prose, and heading-scope is the only structure available |
| R141 (25/130 closed) | **The ruled unit is enforced nowhere.** iladub's Contract now denominates the floor on work above the session baseline, but the code that gates, and the code that would falsify the ruling, both still run on total tokens | Ruled 2026-08-26 in `CLAUDE.md` § Loop & context hygiene and `docs/superpowers/2026-08-26-context-regime-ruling.md` §3; the status-line gauge was converted and verified the same day (fresh session reads `0k/50k` blue where the old gauge read `5%`; 183,435 tokens reads `137k/50k STOP` where the old read `18%`, blue, no warning — reproducing audit §2.2 exactly). But **plimslop is a separate repo and was not changed**: `plimslop/stop.py:58` still tests `session.tokens < LOWEST_FLOOR`, and `preflight` still compares the raw `--tokens` figure to the floor. `plimslop/measure.py:80` already computes `baseline = _total(first)` and every `turn` record already carries `tokens` and `baseline` as distinct fields — **the data exists and nothing subtracts it**. Separately, `reader.py` has views `curve | baseline | tiers` and **never reads `preflight` records** (`grep -n preflight plimslop/reader.py` → no hits), so the 54% override rate — the ruling's own falsifying instrument — is computed by no code in either repo | Deferred because it is a change to a repo this loop is not in, and because it has a **known correctness gap that must be settled first**: a compaction lowers `tokens` without undoing the reasoning already spent, so a naive `tokens − baseline` falsely resets the floor — precisely the failure the skill names (*"Compaction does not reset a floor"*). `measure.py` already tracks `compacted` and `cumulativeDroppedTokens`, so the fix is `working = tokens + dropped − baseline`, but that is a design decision with its own evidence burden, not a transcription. The converted gauge clamps at 0 and **documents the gap rather than hiding it**. **Closes when**: `stop.py` and `preflight` gate on working tokens with compaction accounted for, `reader.py` grows a `preflight` view that computes the override rate, and the rate has been re-read against the ruling's §5 prediction — *it falls materially below 54%*. **If it stays flat near 54%, the ruling is refuted by its own instrument** and §4's unverified premise (that baseline tokens cost multi-step reasoning less than working tokens) is the thing to attack **UPDATE 2026-08-26 — the code landed; the row stays open on its last clause.** plimslop `fb6b64e` (branch `r141-working-tokens`, design `docs/superpowers/specs/2026-08-26-r141-working-tokens-design.md`): `measure.Session.working` = `max(0, tokens + dropped − baseline)`; `stop.py`, `preflight` and `report.py` all gate on it; `stop.py:68`'s unsatisfiable-gate guard **deleted** (its premise is false once the baseline is subtracted — no baseline can consume the allowance), and the same false claim removed from `reader.py` and `report.py`. **The compaction gap this row was deferred on is closed by measurement, not assumption**: `cumulativeDroppedTokens == preTokens − postTokens` in 5 of 5 observed compactions, and **0 transcripts have ever compacted twice** — so `dropped` is now the SUM of per-boundary `(pre − post)`, which is correct whether or not the field accumulates, removing the untested premise instead of guarding it. `preflight` no longer takes the caller's word for the figure either: it recovers `baseline`/`tokens`/`dropped` from the latest `turn` record (152 of 152 live records join) and keeps `--tokens` as advisory `declared_tokens` — the `tokens` key deliberately **retains its old meaning** so both sides of the change stay like-for-like. Verified live: `--tokens 0` still logs `overridden` at 111,964 working. `reader override` computes the rate, its definition **recovered by reproduction** (39/72 = 54.2% and 35/67 = 52.2% at the audit's 681 lines, only under `overridden ÷ records at-or-over floor`). 107 → 134 tests, every change inverted and shown to fail. **WHAT REMAINS**: the closure clause *"the rate has been re-read against §5's prediction"* is **not satisfiable yet** — `observed (written after R141)` is **n=0**, and needs ~3 weeks of corpus. The counterfactual re-score is 40% (17/43) vs 51% (38/74) in the total unit; **that is old decisions re-scored, not behaviour change, and must not be reported as the prediction confirmed.** R140 is now unblocked. |
| R145 (27/134 closed) | **`test_every_authored_query_names_only_declared_terms` reads the whole tree, so `holon:05`'s oracle set now depends on far more than `holon:01`'s artifact.** Arm 1 of M19 refutes an edge only when EVERY one of the source's oracle tests passes, so a broad oracle WITHHOLDS refutations — it can admit a future edge that was never really tested, which is the one direction M19 is designed never to fail in | Measured 2026-08-28 by ablating each non-align ontology from the declaring graph and validating the corpus (`evidence_graph() + declaring_graph()` minus one file):<br>`dec.ttl → conforms=False`, `etkl-holons.ttl → False`, `etkl.ttl → False`, `iladub.ttl → False`, `risk.ttl → False`, `tab.ttl → False`, **`tab-datagrid.ttl → conforms=True`**.<br>So the oracle is sensitive to **6 of 7**, not to all seven — the plan's Step 3 says "any of the seven" and that is one file too many. `declaring_files()` is a glob, so the artifact dependency is the DIRECTORY, not a file | Found by this plan, not by the spec, and the fix is a change to the arc instrument rather than to the thing this loop built. `holon:05 → holon:01` is unaffected: it is the edge whose refutation the breadth correctly withdraws, and its falsification is measured both ways | Either a per-file parameterisation whose node ids let a criterion name the ONE query it depends on, or an M19 guard that refuses an oracle whose read-set is broader than its criterion's declared `prog:oracleArtifact` set. The second is the general fix and would catch the next broad oracle too |
| R146 (31/136 closed) | **Enumerated individuals are unreachable by ANY positional demand — 46 of them are already declared, and no rule this loop shipped could have demanded one.** | Spec §2.6 class 3, 2026-08-29. An enum member appears only as the object of an ordinary property or inside an `sh:in` list, so it occupies NODE ROLE ONLY and the vocabulary-role rule is structurally blind to it. Measured across the tracked `.ttl` corpus: **46 already-declared owned IRIs are enumerated individuals** (`GridAxiom` ×11, `CellDatatype` ×7, `Severity` ×4, `MembraneHealth` ×3, `RegionKind` ×3, …), every one node-role-only. Three more (`corpus:CompilesAbove`, `corpus:SemanticEscalation`, `corpus:Unadjudicated`, documented as terms at `tests/corpus-manifest.ttl:8-17`) were undeclared until this loop AUTHORED them into `vocab/internal/corpus.ttl` — by authorship, never by the membrane | Deliberately not patched. "No triple shows this term used as vocabulary, therefore it is not vocabulary" is inference by absence, which CLAUDE.md §8 forbids for derivations. The rule is a **sound lower bound** on what a membrane may demand, and lower-bound-ness is the correct epistemic shape for an open-world derivation — inventing a rule to reach these would trade that soundness for coverage | A demand licensed by the ENUMERATION itself: once `cor:Verdict owl:oneOf ( … )` is declared — and `vocab/internal/corpus.ttl` now declares exactly that — membership becomes POSITIVE evidence, and a derivation could emit `?member` from `?class owl:oneOf/rdf:rest*/rdf:first ?member` without inferring anything from an absence. **That is now buildable and was not before this loop.** The alternative close is a ruling that individuals are out of the membrane's scope, stated once rather than rediscovered |
| R147 (31/137 closed) | **`tab:product`: a dangling alignment OBJECT, reached by neither demand.** | Spec §2.6 class 1 / §9, 2026-08-29. `vocab/ontology/tab-fno-align.ttl:18` reads `tab:aggFnProduct rdfs:seeAlso tab:product .` — `tab:product` is declared by no ontology and occurs nowhere else in any tracked non-evidence file. D1 cannot reach it (it occupies no vocabulary role); D2 cannot reach it (D2 reaches SUBJECTS only, by design). The align family's node-role-only undeclared count of **10** reconciles as 9 subjects + this one object, and that arithmetic is stated so a reviewer does not read a contradiction against §4.1's 9 | Not deferred by oversight: **widening D2 to objects was considered and refused.** An align module's objects are, by construction, the EXTERNAL terms the module points at — that is what an alignment is — so a demand over objects would demand a declaration for `xpath-functions#sum`, which we must never author (CLAUDE.md § Source ownership). `tab:product` is the one owned term sitting in a position reserved for foreign ones, and no positional rule can tell it apart from its neighbours without a namespace test on the object, which is the tuned filter §8 forbids. Named here so its survival is a RECORDED LIMIT rather than an oversight | Either a ruling that an owned IRI in an align OBJECT position is itself the anomaly — in which case the demand is `owned(?o) ∧ align-module(?g)`, which is positional and does not test the object's name, and is probably right — or a decision about `tab:product` specifically: it exists because `product` has no XPath/F&O counterpart, so it is a real gap in the standard being papered over with an owned term, and declaring it in `vocab/ontology/tab.ttl` beside `tab:AggregationFunction` would close this row in one line |
| R148 (31/138 closed) | **The `.rq` half demands every owned IRI a query names, REGARDLESS OF ROLE — the asymmetry the `.ttl` half exists to avoid.** | Spec §4.9, re-measured 2026-08-29: the 48 authored queries name **173** distinct owned IRIs, and `tests/query_terms.py` emits all of them with no role attached. Harmless TODAY, and measured to be so: no `.rq` names an instance IRI, so the 9 `prog:` and 12 `docgov:` terms visible to that half are all genuine vocabulary, and deleting the namespace exemption did not make the `.rq` population demand a category error. Role IS partially recoverable from the SPARQL algebra — spec §4.9 measures **143 of the 173** getting a role from BGP positions, the remaining 30 being genuinely unknown-role (`BIND`/`FILTER`/property paths/`CONSTRUCT` templates). *That 143/30 split is quoted from the spec's measurement and was NOT re-run here* | It changes no verdict today and it is a second extractor's worth of work. This loop already carries two demands, three new vocabularies and two repairs; adding a third derivation to fix a hypothetical would have been the loop that shipped nothing | A second algebra walk emitting role alongside the term, disposed by the text scan the way `named_terms_by_algebra` is disposed by `named_terms_by_text` today — the inner independence must be preserved or the cross-check becomes theatre. **It becomes a DEFECT, not a residue, the moment an authored query names an instance IRI**, and nothing warns when that happens: a cheap partial close is a test asserting the `.rq` population names zero IRIs matching the arc's instance pattern, which would at least fire |
| R149 (31/139 closed) | **A register row's stated caveat does not bind the row's own numbers — `R143` warned about exactly the contamination its own headline carried.** | Spec §2.7, 2026-08-29. [[R143]]'s row states 209 undeclared owned terms and, in the same row, warns that *"an extractor walking `all_nodes()` without an `isinstance(n, URIRef)` guard reports the bare namespace IRI as an undeclared term."* It reproduces as **203 URIRefs plus 6 `sh:namespace` literals**. The warning was written for future counts and was falsified by the count it was written in — by its own author, in the same sentence | Nothing mechanical is proposed, and that restraint is deliberate. This is raised as an OBSERVATION about the register rather than a defect with a fix, because the obvious remedy — a lint that re-runs every row's stated measurement — is a large instrument for a failure mode measured exactly once | A loop that wants to close this should FIRST measure how often such a lint would have fired across all 141 rows. If the answer is "twice", the lint is not worth its maintenance and the honest close is a ruling that says so. Note the neighbouring [[R137]]: the register has no integrity check at all, and these two are the same shape — *the register records claims about itself that nothing checks* |
| R150 (31/140 closed) | **An owned term named ONLY inside an `sh:sparql` query string is invisible to every positional rule — it is characters in a literal, not a node in any graph.** | Found 2026-08-29 while authoring `vocab/internal/docgov.ttl`, and then measured directly by scanning literal text for `<https://w3id.org/iladub…>`: **10 owned IRIs occur inside `sh:sparql`/`sh:message` literals**. Nine are also real nodes elsewhere or are named by an `.rq`, so they are reached anyway. **One is not: `docgov:inWikiIndex`** — used by `dg:WikiIndexMembershipShape` (`vocab/shapes/doc-governance-shapes.ttl:146`) and emitted by `tests/docgov_extract.py:174`, a live governance term that no positional rule over RDF can see, because its only occurrence in tracked RDF is a run of characters inside a `sh:select` string. It was undeclared until this loop declared it as authored surplus in `vocab/internal/docgov.ttl` | The gap is REAL but its measured population is one term, and closing it means parsing SPARQL out of Turtle literals — i.e. running the `.rq` extractor over embedded query text, a third artifact family with its own escaping and prefix-scope problems. Declaring the one live term was the honest immediate act; building the extractor for it was not this loop's | Run `tests/query_terms.py`'s existing algebra walk over every `sh:select`/`sh:ask` literal in the tracked `.ttl` corpus and emit `etkl:namesTerm` from it — the extractor already exists and this is a new POPULATION for it, not new logic. The oracle is ready-made: `docgov:inWikiIndex` must be demanded, and a fixture shape whose `sh:select` names an undeclared term must be refused. Related to [[R148]]: both are *"a query's terms are read by a weaker rule than a graph's"* |
| R151 (31/141 closed) | **CORRECTED IN PLACE 2026-08-29 — the remedy this row stated is REFUTED; the row stays OPEN.** **`holon:02 → holon:01` is still unauthored, and [[R117]] — the row that would have licensed it — is now closed.** The prediction below was run before any edge was written, exactly as this row demanded, and it FAILED: `holon:02`'s oracle stays GREEN under ablation, so the edge must not be authored today | [[R117]]'s "what would close it" had TWO halves: build the oracle, **then re-author the edge** that arm 1 refuted on 2026-08-22. That loop delivered the first half and explicitly not the second (`bcea2b9`). The reading was sound and is recorded: `2026-08-22-arc-edges-authored.md` §5, §9.1 exists so the edge is re-authored rather than re-derived, and `vocab/ontology/iladub-hga-align.ttl` really does subclass five terms declared only in `vocab/ontology/etkl-holons.ttl`. **RE-MEASURED 2026-08-29 at `5bc6b00`**, by hand through `_ablate`'s exact mechanism (`git worktree add --detach <wt> HEAD`, `baml_client` materialised, `PYTHONPATH=<wt>/src`, cwd = the worktree) — M19 has no standalone driver, and `ablation_refusals` iterates only edges already present in the manifest, which this one is not. Control, nothing removed: `test_alignment_axioms_present PASSED`, `test_alignment_modules_only_point_outward PASSED`. After `rm vocab/ontology/etkl-holons.ttl`: **both PASSED again.** So `all(r == PASSED)` at `tests/test_arc_ablation.py:622` fires, and re-authoring the edge today would earn `M19: arm 1 refutes holon:02 prog:dependsOn holon:01` verbatim — the 2026-08-22 refutation reproduces unchanged. **THREE measured reasons, and the third is the one that matters:** **(i)** the membrane [[R117]]'s closure shipped is not in `holon:02`'s oracle set at all — `grep -n "artifact_declarations" tests/arc-manifest.ttl` has no match, and `holon:02`'s two `prog:oracleTest` values (`tests/arc-manifest.ttl:293-319`) are unchanged since 2026-08-20, so the ablation cannot see it. **(ii)** The membrane's CONTENT claim is nonetheless TRUE: emptying `etkl-holons.ttl` to zero bytes (parses, declares nothing) makes `tests/test_artifact_declarations.py::test_every_artifact_names_only_declared_terms` refuse with 19 results, including exactly the five terms this row names — `urn:iladub:artifact:vocab/ontology/iladub-hga-align.ttl names https://w3id.org/iladub/etkl#CleanDocumentHolon, which no owned vocabulary declares`; the same test on the restored tree is `1 passed`. **(iii)** But under a REAL ablation — the file *deleted*, not emptied — that same test fails as an **instrument error, not a refusal**: `tests/artifact_terms.py:86: ValueError: …/vocab/ontology/etkl-holons.ttl: Turtle parse failed: [Errno 2] No such file or directory`, because `artifact_files()` (`tests/artifact_terms.py:76-81`) enumerates `git ls-files -z '*.ttl'` — the **index**, which still lists a file deleted from the working tree — and `artifact_dataset()` (`:84-88`) raises on any parse failure rather than skipping. It therefore goes red in EVERY criterion's ablation worktree that removes any tracked `.ttl`, `dec:03`'s as readily as `holon:01`'s: **ablation-nonspecific by construction**, and a live instance of [[R145]] | Arc work, not membrane work. Bundling an edge re-authoring into a closure about declaration would have hidden it: a struck row reads as *done*, and half of R117's remedy would have disappeared into a strike. Raised as its own row precisely so the strike stays honest. **The 2026-08-29 correction changes nothing about the deferral and removes nothing from it** — the row exists so that a struck [[R117]] cannot swallow half its own remedy, and that is still exactly why it is open. What changed is that the remedy it named is now *known* to be wrong, which is the outcome its own falsifiable framing was written for | **SUPERSEDED 2026-08-29 — do NOT re-author the edge. Arm 1 refutes it today, measured above.** The original text read: *"Re-author the edge in `tests/arc-manifest.ttl` with its rationale, and let CI's two-sided ablation adjudicate it. **The refutation that killed it should now fail to reproduce**: deleting `etkl-holons.ttl` leaves `iladub-hga-align.ttl` subclassing five undeclared terms, which the membrane this loop shipped refuses — so `holon:02`'s oracle should now go red where before it stayed green."* It did not go red. **The corrected remedy, in order:** **(a)** Make the declaration membrane **ablation-legible** — `artifact_dataset()` must distinguish *"the artifact is absent from the working tree"* from *"the artifact is present and malformed"*, so that removing the one ablated file is the only thing that turns it red. Without (a) this test cannot adjudicate **any** `.ttl` dependency edge, this one included. **(b)** Add `tests/test_artifact_declarations.py::test_every_artifact_names_only_declared_terms` to `holon:02`'s `prog:oracleTest`. As the manifest stands (`tests/arc-manifest.ttl:293-319`) M16's A3 (ends share no `oracleTest`) and A6 (ends share no artifact FILE) are both satisfied by that addition — re-check if any other oracle moves first. **(c)** ONLY THEN re-run the prediction, and run it **two-sided**: the oracle must go RED when `etkl-holons.ttl` is removed **and stay GREEN** when an unrelated tracked `.ttl` is removed. Red for both means (a) is not done and the instrument is still measuring the tree rather than the edge. **And if it is green for the first, the edge is genuinely refuted and this row closes by RECORDING that** — an edge that does not exist is a real answer, not a failed loop. **A closure that skips (a) is [[R145]] wearing this row's number** |
| R153 (38/143 closed) | **The downward-citation instrument built for [[R139]] cannot see MARKDOWN — and one of R139's three measured instances was in `.md`.** The rule it enforces (a comment may not cite a line below itself in its own file) is medium-independent; the instrument is not. | MEASURED 2026-08-31 by the R139 census, recorded in full in that row and reproduced here only as its headline: over `docs/**` the same filter ladder scores **71-99% false positives**, and a third citation form found only in prose (*"at lines 12, 133"*) scores **0/44** — it almost always names lines of a compiled PDF, not of a source file. The live instance is the note added to `docs/superpowers/specs/2026-08-25-the-membrane-reports-its-health-design.md` §4.5 while closing [[R138]]: it listed the file's other stale citations by line, all BELOW the note, and inserting it shifted them (`:890`→`:912`, `:905`→`:927`). Caught by re-measuring AFTER, repaired with a `grep -n` that has no line number to invalidate. | Deferred because the failure is not a filter that needs tightening but a **referent domain the instrument does not have**: in this repo's markdown a bare `:NNN` overwhelmingly means *the source file named upstream in the passage*, and 793 of 822 such hits point at `compile.py`/`document.py` across heading boundaries no bounded lexical window can span. Shipping the source-comment form over `docs/**` would produce a lint 71-99% wrong, which is a lint that gets deleted. **This is a gap, not a chore** — nobody has proposed a mechanism that would work, and the honest state is that the convention (CLAUDE.md plan-rule 7) is the only guard markdown has. | Closes when either (a) an instrument resolves a markdown citation's referent well enough to beat the census's precision — the plausible shape is *document*-scoped rather than paragraph-scoped referent tracking, since a spec names its subject file once in a heading and then cites it for pages; or (b) the class is measured NOT to recur in `.md` over a stated window, and the row is refused with that measurement. **Do not close it by widening the source-comment lint's globs** — the census already refuted that, and the numbers are in [[R139]]. |
| R155 (41/145 closed) | **The residual division on WHO's header line is a WORD GAP, and closing it needs BOTH readings at once.** | Measured 2026-08-31 at `5743af3` + the [[R154]] fix, WHO page 0 band 2 line 0. Every interior boundary that line meets, with its clearances: `b=517.97 L-b=-0.107 R-b=-0.109` FLUSH, `b=522.65 +0.086/+0.084` FLUSH, `b=523.37 -0.634/-0.636` FLUSH, `b=528.77 -0.546/-0.548` FLUSH, `b=583.49 -3.645/-0.887` FLUSH, `b=588.23 -2.574/-2.576` FLUSH, `b=588.95 +2.809/+5.567` FLUSH, **`b=594.35 -2.591/+0.167` CLEAR** — the `n` of `in` ends 2.591pt short of the rule and the `k` of `kg)` begins 0.167pt past it. Both sides genuinely clear, so `_row_dividers` honours it and the carried top-level header nodes read `'Z-scores (weight in'` and `'kg)'` rather than one label | **Deferred because the predicate that closes [[R154]] provably cannot reach it.** `_row_dividers` distinguishes *cuts ink* from *falls in a gap*; it cannot distinguish *word gap* from *column gutter*, because at the character level those are the same picture — there is no character-local fact that separates them. Reaching it requires the WORD reading, and [[R154]]'s row records the coarse form of that swap breaking what the ruled reading gets RIGHT: `'-3 SD'` split into `'-3'` and `'SD'`. The [[R154]] fix leaves that line **byte-identical**, so any successor must hold that as a hard invariant, not an aspiration | A per-word reconciliation that consults `extract_words`' runs ONLY at boundaries `_row_dividers` has already honoured — the population is now tiny (WHO's whole header line has exactly one), which is the half of the problem [[R154]] made tractable. The oracle is two-sided and cheap: WHO line 0 must become one node, WHO line 1 must stay byte-identical, and no document may regress on the §3.3 battery. **Note the trap this row exists to prevent**: a fix that merges across word gaps globally re-creates exactly the failure [[R154]] avoided, since the twelve real data columns are separated by gaps that are geometrically indistinguishable from this one |
| R156 (41/146 closed) | **A kind judgement got stronger over a grid that conflates two side-by-side tables — but the EMITTED STRUCTURE is unchanged, and the only new triple is the type itself.** | Measured 2026-08-31 on the [[R154]] branch, `compile_tables('corpus/ag-trade/cbh-stem-2026-08-03.pdf', 0)`, baseline vs. fix. **(a) NARROWED BY MEASUREMENT — the first version of this row overstated it, and the graph diff is why.** Region 9 moves `UNSUPPORTED_TABLE / asserted` → `RECORD_TABLE / asserted`, but the table's structure is IDENTICAL either side: `13 EntryCell · 3 LabelCell · 16 hasCell · 3 hasHeaderNode · 3 hasLeafColumn · 9 hasLeafRow` in BOTH. The conflation this row was raised about — row `r2` pairing the left table's `'ALB 129,183 APW1/ASW9/AWW1 27,023 3,345 1,293 160,845'` with the right table's `'ESP 1 - 15 August'` — **is asserted identically at baseline**, so [[R154]] neither causes nor worsens it. The whole delta on `table9` is **one type triple** (`htable9 a tab:RecordTable`) plus **four cellText FIDELITY REPAIRS**: `'Stock at Port ( Main Storage Area) as at 29/07/2 026'` → `'Stock at Port (Main Storage Area) as at 29/07/2026'`, `'PORT MAINTENANCE SHU TDOWN DATES - 2026'` → `'...SHUTDOWN...'`, and two garbled prose notes (`'ass ume total capacity is allocat ed'`, `'distribut ion of the informati on contained withi n'`) made whole. **(b)** the same run: page-0 score `0.06984126984126984 → 0.05711086226203808`, asserted `66 → 51`, regions 1/3/5/7 reason `MERGE_AMBIGUOUS → REGION_TILING_FAILED` — every one of those regions is ESCALATED in both, so no false assertion rides on it | **Deferred because the measurement shows [[R154]] did not cause the thing this row was raised about.** The record structure over a two-table grid is PRE-EXISTING and byte-identical; what [[R154]] adds is one type triple and four repaired strings. Refusing [[R154]] would keep the garbled text and still leave the conflation. The remaining question is narrow and real: **may a band whose header row concatenates two tables' headers carry `tab:RecordTable` at all** — a kind-gate question, not a cell-extraction one. **(b) corrects a claim [[R154]]'s spec §3.3 made too broadly** — *"every escalation reason counter unchanged in every document"* holds at DOCUMENT scope, where `document.py` runs a pass-2 repair, and is FALSE at page scope; spec, closure row and handoff corrected in place | For (a): rule on the type triple alone. The discriminator needs no tolerance and is visible in the evidence — line 1 restarts with `'ALB'`, which is also a ROW KEY of the first table, and the right-hand table ends two rows before the left. For (b): decide whether the page-scope battery should be gated at all, given the document driver legitimately repairs it — `test_typing_equiv` gates page scope and is what caught this |
